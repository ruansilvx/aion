# apply

Execute the tasks.md checklist for an active OpenSpec change — writes/modifies exactly the Flutter Dart files it specifies, marking tasks off one at a time. Use for the /apply stage of the OpenSpec SDD workflow.

Full skill source: `aion-arch/.claude/skills/apply/`.
