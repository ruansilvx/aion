# Architecture convention baseline pointer

A new project's architecture-convention baseline references
`aion-arch/flutter-conventions.md` as pinned at baseline version `0.1.0`.
This bundled file is a pointer, not a copy — Aion's own architecture
conventions document lives in the `aion-arch` repository and is not
duplicated into every project's baseline bundle.
