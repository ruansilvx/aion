---
ticketId: AIO-2580
type: story
status: done
priority: none
parentId: 64786df9-02fa-4b5f-98a7-ca811e51409f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.455295
updatedAt: 2026-09-02T21:17:20.455295
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 0. Dependencies

- [x] **T00** — Add missing dependencies to `pubspec.yaml` and run `flutter pub get`.
  Required package additions:
  - `uuid: ^4.x` — UUID v4 generation for ticket and comment IDs.
  - `phosphor_flutter: ^2.x` — Phosphor icon set (Light weight). Used for all
    icons per design.md §1.9 glyph table. Import as `PhosphorIcons` and render
    with `PhosphorIcon(PhosphorIcons.xxx)` at 20–24px, color `textSecondary`.
  Font assets (already bundled — no package needed):
  - `assets/fonts/Manrope-VariableFont_wght.ttf` and
    `assets/fonts/JetBrainsMono-VariableFont_wght.ttf` are committed to the
    repo and registered in `pubspec.yaml` under `flutter.fonts` as families
    `'Manrope'` and `'JetBrainsMono'`. **Do NOT add `google_fonts`.**
    `AionText` constants reference `'Manrope'` and `'JetBrainsMono'` as plain
    string family names — Flutter resolves them from the bundled assets at
    runtime with zero network dependency.
  Verify both packages are present. Run `flutter pub get`.

---