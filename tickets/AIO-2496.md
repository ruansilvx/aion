---
ticketId: AIO-2496
type: story
status: done
priority: none
parentId: acd2b438-557e-4a1f-9e2b-4fae6ca86a5f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.441422
updatedAt: 2026-09-02T21:17:20.441422
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation layer — widgets

- [x] T8. In
      `lib/features/tickets/presentation/widgets/trashed_ticket_tile.dart`,
      add the age label to the existing type-chip/subtasks `Row` using
      `formatTrashedAge(ticket.deletedAt!)`, styled
      `AionText.time.copyWith(color: c.textMuted)`, per design.md.
      Update the class's dartdoc if the "deliberately simpler" framing
      needs a note about the new age label. Per design.md §1 Age label
      (§1.1 Placement & layout — change line 2 from `Row` to `Wrap`,
      `spacing: 9, runSpacing: 6`, so the age label reflows instead of
      clipping when crowded; §1.3 Copy/bucketing for the exact label
      text per elapsed-time bucket; §1.4 Overflow behavior).

- [x] T9. In `lib/features/tickets/presentation/screens/trash_screen.dart`:
      add a new `_PurgeOldAction` private widget (styled like the
      existing `_EmptyTrashAction`, distinct icon
      `PhosphorIcons.clockCounterClockwiseLight`, label
      `context.l10n.ticketTrashPurgeOldAction`, dartdoc); wire it into
      the header `Row` to the left of `_EmptyTrashAction`, reading
      `TrashLoaded.purgeEligibleCount` for its enabled state; its
      `onTap` calls the existing `_confirmPermanentDelete` helper with
      `ticketTrashPurgeOldConfirmTitle`/`ticketTrashPurgeOldConfirmMessage`
      and, on confirm, `context.read<TrashCubit>().purgeOldTrash()`.
      Per design.md §2 "Purge old" header action (§2.0 geometry, §2.1
      placement — on narrow/mobile widths, move both bulk-action
      buttons to their own right-aligned sub-row beneath the title
      block instead of the title row itself, §2.2–§2.6 state styling)
      and §3 "Purge old" confirm dialog (§3.1 — reuses the existing
      destructive-tone dialog shell unchanged; only new copy/icon
      choice, including the confirm button's white label text, which
      needs the existing `// white text — no token for white` comment
      per flutter-conventions.md's documented exception, not a new
      color token).