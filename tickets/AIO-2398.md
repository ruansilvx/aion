---
ticketId: AIO-2398
type: story
status: done
priority: none
parentId: 7161c960-0bf3-4230-a027-501ca12410ec
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.427767
updatedAt: 2026-09-02T21:17:20.427767
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Manual verification (desktop)

- [x] **T21.** Run the app on Windows desktop (per this project's
  desktop-only verification convention — agent/model calls and the
  full UI aren't exercised the same way on web). Confirm: `/workspace/tickets`
  opens to Board view by default; the new Sort trigger appears next to
  Filters and lists all six fields; selecting a field reorders both
  List and Board (switch views to confirm both reflect the same order);
  re-tapping the active field flips its direction and the list/board
  re-render in reverse order; typing a search query and confirming
  `Relevance` becomes selectable/its results order by match quality;
  clearing the query while `Relevance` is NOT the explicit choice falls
  back to `createdAt` descending, while an explicitly-chosen non-relevance
  sort survives the query being cleared; reopening the app (or
  navigating away and back) shows the persisted sort restored; the
  Trash screen's ticket order reflects the same persisted sort. No
  `setState`-after-dispose or overlay-related crashes in the new
  `TicketSortPopover`/`_SortTriggerButton`.