---
ticketId: AIO-1271
type: story
status: done
priority: none
parentId: c8ac9fc6-324b-4f39-bc5e-3388cde40f53
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.284206
updatedAt: 2026-09-02T21:17:20.284206
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 2. `CreateProjectCubit` / `CreateProjectState` changes

- [x] 2.1 Modify `aion/lib/features/projects/presentation/cubit/create_project_state.dart` —
      add `bool wasExistingGitRepo` to `CreateProjectSuccess`. Update its
      class dartdoc and `props`.
- [x] 2.2 Modify `aion/lib/features/projects/presentation/cubit/create_project_cubit.dart` —
      `submit` gains `bool appendGitignore = true`; `_initializeDesktopProject`
      branches on `GitRepositoryClient.isGitRepository(rootPath)`: skip
      `init()` when already a repo, call `GitignoreEditor.ensureIgnored`
      when `appendGitignore` is true in that branch; unchanged otherwise.
      `CreateProjectSuccess` now carries `wasExistingGitRepo`. Update every
      touched method's dartdoc to reflect the new branching (per
      project.md, `@param` for the new parameter).
- [x] 2.3 Extend `test/features/projects/presentation/cubit/create_project_cubit_test.dart`
      (`bloc_test`) — new cases: existing-repo directory + `appendGitignore:
      true` skips `init`, calls `GitignoreEditor`; existing-repo + `false`
      skips both `init` and the editor, still succeeds with
      `wasExistingGitRepo: true`; non-repo directory behaves exactly as
      today (`wasExistingGitRepo: false`, `init` called). Mock
      `GitRepositoryClient`/`GitignoreEditor` via mocktail.