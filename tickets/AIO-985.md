---
ticketId: AIO-985
type: story
status: done
priority: none
parentId: db91fdb7-f189-42f6-b315-f8c180b966a6
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.244038
updatedAt: 2026-09-02T21:17:20.244038
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation layer — widgets

- [x] **T18** — Modify `lib/design_system/molecules/
  backlinks_section.dart`: change `backlinks` parameter type from
  `List<Ticket>` to `List<BacklinkRef>`; render rows with `origin ==
  wikilink` visually distinguished from `origin == explicitLink` rows.
  Update dartdoc. Per design.md §2 (Row-origin model) and §3 (Backlinks
  row — wikilink origin variant: §3.0 shared geometry, §3.1 explicit
  unchanged, §3.2 the new origin tag, §3.3 states, §3.4 empty).
- [x] **T19** — Create `lib/design_system/molecules/
  wikilink_suggestion_list.dart`: `WikilinkSuggestionItem` (`{required
  String ticketId, required String title, String? breadcrumb}` — a
  small, feature-agnostic value type, not a `Ticket`, keeping
  `MarkdownEditor` domain-free) and `WikilinkSuggestionList` — generic
  `List<WikilinkSuggestionItem> items` + `ValueChanged<
  WikilinkSuggestionItem> onSelected`, no domain-entity constructor
  params (design-system-eligible from creation, per the promotion
  rule). Full dartdoc. File header. Per design.md §4
  (`WikilinkSuggestionList` — autocomplete overlay: §4.1 panel chrome,
  §4.2 suggestion row, §4.3 row states, §4.4 empty-query state, §4.5
  no-matches state).
- [x] **T20** — Modify `lib/design_system/molecules/
  markdown_editor.dart`: add optional `List<WikilinkSuggestionItem>
  Function(String query)? wikilinkSuggestions` and `Future<
  WikilinkSuggestionItem?> Function(String title)? onCreatePage`
  constructor parameters. `TextEditingController` listener detects an
  in-progress `[[<query>` span (just-typed `[[`, no closing `]]` yet on
  the current line); when `wikilinkSuggestions != null`, opens a
  `WikilinkSuggestionList` inside an `Overlay`/`LayerLink`/
  `CompositedTransformFollower` **anchored to the live text caret
  position** (a `LayerLink` pinned to the `[[` insertion point,
  flipping above the caret if the panel would overflow the viewport
  bottom — build this for real per design.md §4.1, not the
  field-anchored MVP simplification originally proposed),
  `Escape`/outside-tap-dismissible. On selection, replaces the
  in-progress span with **`[[<ticketId>]]`** (bare, no alias — not
  `[[<Title>]]`) and moves the cursor past the closing brackets. In the
  no-matches state, pressing Enter calls `onCreatePage(query)` (when
  supplied) and inserts its result the same way; `null`/no callback
  leaves Enter a no-op there. Update dartdoc to describe both new
  optional behaviors and their `null` no-op cases. Per design.md §4.1
  (panel chrome, caret anchor/overflow-flip behavior), §4.5
  (create-on-the-fly), and §1.4 (bracket-pair glyph).
- [x] **T21** — Modify the shared Markdown AST-to-widgets renderer
  (`MarkdownView` — confirm exact file, likely `design_system/molecules/
  markdown_view.dart` or `design_system/atoms/`): add optional `Ticket?
  Function(String target)? resolveWikilink`, `void Function(Ticket)?
  onWikilinkTap`, and `void Function(String title)?
  onCreateWikilinkTarget` parameters. Parse `[[Target]]`/
  `[[Target|Alias]]` sequences via `WikilinkExtractor.extractReferences`
  (reused directly, not a second ad hoc regex — `core/markdown/` has no
  Flutter dependency). For each match: if `resolveWikilink(match.target)`
  is non-`null`, render a tappable, visually distinct resolved span
  (calls `onWikilinkTap`) whose displayed text is `match.alias` when
  present, otherwise the resolved `Ticket.title` live; if `null`, render
  a plain, chip-backed unresolved span, tappable-to-create only when
  `onCreateWikilinkTarget != null` **and**
  `!WikilinkExtractor.looksLikeTicketId(match.target)` (an unresolved
  id means the target was trashed/deleted, not a new-page candidate).
  All three parameters default to `null`, preserving every other
  consumer's current behavior unchanged. Update dartdoc. Per design.md
  §5 (Inline wikilink span — resolved & tappable: §5.1 rendered text,
  §5.2 states) and §6 (Inline wikilink span — unresolved: §6.1 rendered
  text, §6.2 states, §6.3 contrast rationale).
- [x] **T22** — Modify `lib/features/pages/presentation/screens/
  page_detail_screen.dart`: in `initState`, call
  `PagesCubit.loadWikilinkCandidates()`, cache the result in local
  `State`; wire `MarkdownEditor.wikilinkSuggestions` to a
  case-insensitive substring filter (capped, e.g. 20) over the cached
  list mapped to `WikilinkSuggestionItem`; wire
  `MarkdownEditor.onCreatePage`/`MarkdownView.onCreateWikilinkTarget`
  to `PagesCubit.createPage(title: ...)`, mapping/navigating into the
  result the same way `PageCreateScreen` already does on successful
  creation; wire `MarkdownView.resolveWikilink` (id match first via
  `WikilinkExtractor.looksLikeTicketId`, else case-insensitive title
  match, against the cached candidate list) and `onWikilinkTap` (navigates
  via `ticketDetailRoute(ticket)`); update the `BacklinksSection` call
  site for its new `List<BacklinkRef>` parameter type. Per design.md §7
  (In-context placement).
- [x] **T23** — Modify `lib/features/pages/presentation/screens/
  page_create_screen.dart`: same `loadWikilinkCandidates`/
  `wikilinkSuggestions`/`onCreatePage`/`resolveWikilink` wiring as T22
  for its initial-content `MarkdownEditor`/preview, if it renders one
  (confirm against the current file — its known scope is title +
  optional parent + optional initial content field). Per design.md §7
  (In-context placement).
- [x] **T24** — Update `ticket_detail_screen.dart`'s documentation-mode
  `BacklinksSection` call site (`resource` tickets) for the new
  `List<BacklinkRef>` parameter type — a `resource` ticket's Backlinks
  section now surfaces wikilink-origin rows too (via T15's widened
  merge), with no new wiring needed here beyond the type change, since
  resources don't author wikilinks themselves (no `MarkdownEditor`
  changes apply to this screen). Per design.md §2 (Row-origin model)
  and §3 (Backlinks row — wikilink origin variant), same rendering T18
  adds. **Note (post-/verify correction):** the actual call site lives
  in `lib/features/tickets/presentation/widgets/
  ticket_metadata_section.dart` (the widget `ticket_detail_screen.dart`
  delegates its documentation-mode sections to), not in
  `ticket_detail_screen.dart` itself — that's the file that was
  correctly modified.