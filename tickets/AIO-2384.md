---
ticketId: AIO-2384
type: task
status: done
priority: none
parentId: dec54684-2454-46ac-aef3-d4589142d45e
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.427767
updatedAt: 2026-09-02T21:17:20.427767
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# **T10.** Modify `lib/features/tickets/presentation/cubit/tickets_cubit.dart`:

- Add optional constructor param `TicketListSortRepository? sortRepository`
(assigned to a new `late final TicketListSortRepository? _sortRepository`
field), documented in the constructor's existing dartdoc block per
the file's established pattern (mirror the `filterRepository`
paragraph, referencing `aion-arch/changes/ticket-sort-control-and-board-as-default-view`).
- Add `TicketListSort? _explicitSort`, `TicketListSort _lastSort`
(initialized via the new `_implicitSort(null)` static helper),
`TicketListSort get currentSort`, and
`static TicketListSort _implicitSort(String? query)` — per
design.md §3.1. Full dartdoc on each.
- Add `Future<void> setSort(TicketListSort sort)` and
`Future<void> loadPersistedSort()` — per design.md §3.2. Full
dartdoc on each, mirroring `_persistFilters`/`loadPersistedFilters`'s
existing dartdoc shape.
- In `searchTickets`, resolve and store `_lastSort = _explicitSort ??
_implicitSort(query);` alongside the existing `_lastQuery`/
`_lastStatuses`/etc. assignments (design.md §3.3).
- Pass `sort: _lastSort` to every one of the 8 existing
`_repository.searchTickets(...)` call sites: `searchTickets`,
`loadMoreTickets`, `createTicket`, `updateTicketStatus`,
`trashTicket`, `trashTickets`, `updateStatusForTickets`,
`updatePriorityForTickets`.
- Update dartdoc on `TicketsCubit`'s class-level comment and on
`searchTickets`/`loadMoreTickets` to mention sort resolution.