---
ticketId: AIO-544
type: task
status: done
priority: none
parentId: 12fb9363-262b-4c86-a85b-67cfe93e7b22
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.189498
updatedAt: 2026-09-02T21:17:20.189498
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T29. Existing `TicketsCubit`/`ChatCubit`/`AutomationSettingsCubit`

unit tests updated:
- New `MockGitRepositoryClient`/`MockGitHubCliClient`/
`MockFlutterVerifier` + a shared
`stubSuccessfulCodingExecutionInfra` helper in
`tickets_cubit_test.dart`; every existing build site that
exercises `_runCodingExecution` (`coding-execution trigger` and
`per-phase model routing` groups) now supplies the three new
mocks + `projectRootPath` — no real `git`/`gh`/`flutter`
subprocess in tests.
- `automation_settings_cubit_test.dart`'s `load` test (pre-existing,
not part of this change's original scope) hardcoded "both
`AutomationContext` values" — updated to stub/expect all three
now that `codingExecutionRetry` exists, since `AutomationSettingsCubit.load`
iterates `AutomationContext.values` generically.
- `provider_settings_cubit_test.dart`'s underlying
`provider_settings_cubit.dart` had a `switch (event)` over
`AgentEvent` with no `AgentToolUseEvent` case — not caught by
that test file itself, but was a `flutter analyze` compile error
(non-exhaustive switch); fixed as a no-op case alongside
`AgentTextEvent`/`AgentDoneEvent`.
- `chat_cubit_test.dart` needed no changes — `onToolUse` is a purely
additive optional parameter.