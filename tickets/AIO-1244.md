---
ticketId: AIO-1244
type: story
status: done
priority: none
parentId: 88f65bed-75f1-44af-a767-912a8a5c3fe3
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.273148
updatedAt: 2026-09-02T21:17:20.273148
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Screen wiring

- [x] 15. Modify `lib/features/tickets/presentation/screens/tickets_list_screen.dart`
      — remove `_statusFilter`/`_typeFilter`/`_priorityFilter` fields and
      the three `AppDropdown` widgets; add the Filters trigger button
      (count badge, `AppDropdown.isActive`-matching tint) wrapped in
      `TicketFilterPopover`, wired to `context.watch<TicketsCubit>()`'s
      selection getters and `context.read<TicketsCubit>()`'s toggle
      methods. (design.md §3.3, per design.md §1 Toolbar composition and
      §3 TicketFilterButton)
- [x] 16. Modify `lib/features/tickets/presentation/screens/tickets_list_screen.dart`
      — add the chip row (one `AppFilterChip` per selected value across
      all three fields, `onRemove` calling the matching toggle method;
      hidden when nothing is selected). (design.md §3.3, per design.md §5
      Chip row layout)
- [x] 17. Modify `lib/features/tickets/presentation/screens/tickets_list_screen.dart`
      — update `_hasActiveFilter` to read the cubit's selection getters;
      update `_runSearch()` to read the cubit's selection getters instead
      of the removed local fields when building its `searchTickets(...)`
      call. (design.md §3.3)
- [x] 18. Modify `lib/features/tickets/presentation/screens/tickets_list_screen.dart`
      — add `_initializeAndSearch()` (awaits
      `TicketsCubit.loadPersistedFilters()`, then calls `_runSearch()`);
      call it via `unawaited(...)` from `initState` in place of the
      current bare `_runSearch()` call. (design.md §3.3)