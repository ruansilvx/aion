---
ticketId: AIO-426
type: story
status: done
priority: none
parentId: 907cd7b5-8a45-4721-aa30-9160bb02bc86
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.178294
updatedAt: 2026-09-02T21:17:20.178294
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tasks: Bug ticket type

Ordered checklist. Every task touching a `.dart` file includes its
documentation (dartdoc + file header, where new) in the same task — not
a follow-up. Tasks marked **[design]** render UI whose exact layout is
set by the Claude Design pass (see `proposal.md`'s Design gate) —
`/design-sync` will annotate these with the relevant `design.md`
section reference before `/apply` may proceed.

- [x] T1. In `lib/features/tickets/domain/enums/ticket_type.dart`:
  add `TicketType.bug` with full dartdoc (per `design.md`'s Domain
  layer section), update `_rank` to return `2` for `task || bug`, leave
  `canParent`/`isAlwaysRoot` unchanged, and add the new
  `TicketTypeHierarchy.executableTypes` static const list and
  `isExecutable` getter, each fully dartdoc'd. Correct `TicketType.task`'s
  existing dartdoc per `specs/spec.md`'s MODIFIED entry.

- [x] T2. Add new file
  `lib/features/tickets/domain/enums/ticket_severity.dart`: the
  `TicketSeverity` enum (`critical | high | medium | low`) exactly as
  specified in `design.md`, with a file header and full dartdoc on the
  enum and every value.

- [x] T3. In `lib/features/tickets/domain/entities/ticket.dart`: add the
  four new nullable fields (`severity`, `stepsToReproduce`,
  `expectedBehavior`, `actualBehavior`) to the class, constructor,
  `props`, and `copyWith` (as `TicketFieldSetter`-typed params, same
  shape as `complexity`), each fully dartdoc'd per `design.md`.

- [x] T4. In `lib/features/tickets/data/models/ticket_model.dart`: add
  the four new nullable `TextColumn`s (`severity`, `steps_to_reproduce`,
  `expected_behavior`, `actual_behavior`) to `TicketsTable`, each
  dartdoc'd per `design.md`.

- [x] T5. In `lib/core/database/app_database.dart`: bump `schemaVersion`
  to `6`, add the `if (from < 6)` migration branch calling `addColumn`
  for all four new columns, and update the class dartdoc's version-
  history list with a Version 6 entry.

- [x] T6. In
  `lib/features/tickets/data/repositories/drift_ticket_repository.dart`:
  update `_toEntity` to map the four new columns to `Ticket` fields
  (using `_parseNullableEnum` for `severity`), and update `createTicket`/
  `updateTicket`'s `TicketsTableCompanion` construction to write all
  four fields back, exactly as specified in `design.md`.

- [x] T7. Run
  `flutter pub run build_runner build --delete-conflicting-outputs` in
  the app repo to regenerate `app_database.g.dart` and any other
  affected `*.g.dart` files for the new columns.

- [x] T8. In `lib/features/tickets/presentation/cubit/tickets_cubit.dart`:
  - Add `severity`, `stepsToReproduce`, `expectedBehavior`,
    `actualBehavior` optional params to `createTicket`, threaded into
    the constructed `Ticket`. Update the method's dartdoc.
  - Replace every `ticket.type == TicketType.task` /
    `task.type != TicketType.task` coding-execution comparison
    (`updateTicketStatus`, `changeTicketStatus`,
    `_interceptTaskExecutionTrigger`, `getTicketById`'s execution-state
    block) with `.isExecutable` / `!.isExecutable`, per `design.md`'s
    Cubit layer section. Update each method's dartdoc to say "Task or
    Bug" where it previously said "Task."
  - Replace every `types: const [TicketType.task]` (or `[nextRank]`
    where `nextRank == TicketType.task`) query used for design-review-
    gate or SDD-readiness purposes (`_nextSddStage`,
    `_sddStageAdvanceCheck`'s `proposed` and `designSync` branches,
    `_codingExecutionGateCheck`, `getTicketById`'s `needsDesignReview`
    computation, `advanceSddStage`'s stage-chat context generation) with
    `TicketTypeHierarchy.executableTypes` (conditionally, per
    `design.md`'s exact snippet for the `nextRank`-driven sites).
  - Update `_storyNeedsDesignReview`'s dartdoc per `specs/spec.md`'s
    MODIFIED entry.

- [x] T9. **[design]** In
  `lib/features/tickets/presentation/screens/create_ticket_screen.dart`:
  add `_selectedSeverity` state, the three new controllers/focus nodes
  (disposed alongside the existing ones), and the conditional Bug-fields
  `AnimatedSize` block per design.md §4 (CreateTicketScreen —
  Bug-conditional fields block: §4.1 placement/spacing, §4.2 severity
  selector incl. its required-field validation/error state, §4.3 the
  three multiline text fields). Add a new `SeverityBadge` widget
  (design.md §2) and the severity picker overlay (design.md §3) if not
  already added by T13/a shared location — see design.md §3 for the
  overlay's exact structure (`OverlayEntry` + `CompositedTransformFollower`).
  Wire `_submit` to pass the four new values into
  `TicketsCubit.createTicket`. No `Widget _xxx(...)` helper functions —
  extract a private widget class if the block needs its own state.

- [x] T10. **[design]** In
  `lib/features/tickets/presentation/widgets/ticket_metadata_section.dart`:
  add the `bug`-only "Bug details" section (severity + three
  inline-editable text fields, each committing via `updateTicket`/
  `copyWith`) per design.md §5 (Ticket detail screen — "Bug details"
  section: §5.1 placement/padding/divider, §5.2 severity row using the
  same severity picker overlay as T9, §5.3 the three inline-editable
  text blocks and their view/hover/editing states), and widen the
  Linked Tickets/Backlinks visibility gate from
  `ticket.type != TicketType.resource` to also allow `TicketType.bug`,
  exactly as specified in the original design.md architecture section
  (no dedicated component spec section — this is a visibility-condition
  change, not new UI).

- [x] T11. **[design]** In
  `lib/design_system/molecules/type_chip.dart`: add
  `TicketType.bug => l10n.ticketTypeBug` to `ticketTypeLabel` and
  `TicketType.bug => c.typeBug` to `TypeChip`'s color switch, per
  design.md §1.4 (Resolver extension) and §1.5 (TypeChip renderings —
  confirms `TypeChip` itself is unchanged, only the resolver switch
  gains a case).

- [x] T12. **[design]** In
  `lib/design_system/molecules/linked_tickets_section.dart`: add the
  matching `TicketType.bug => c.typeBug` case to its color switch, per
  design.md §1.4.

- [x] T13. **[design]** In `lib/design_system/tokens/aion_colors.dart`:
  add the new required `typeBug` field to `AionColors`, its constructor,
  and both theme palette instances, per design.md §1.1/§1.3 (`typeBug`:
  Arctic `Color(0xFF5E8C1E)`, Obsidian `Color(0xFF98D13C)`). Also add
  the new `AionSeverityColors` class, its `arcticSeverity`/
  `obsidianSeverity` const instances, and the new `severity` field on
  `AionColors` wiring them in, per design.md §2.2. Add the new
  `SeverityBadge` widget itself (`design_system/molecules/`, parallel to
  `PriorityBadge`) per design.md §2.1–§2.5 (two-axis differentiation
  from `PriorityBadge`, two size variants, unselected/placeholder
  variant, trigger states) — this is the shared component T9/T10 both
  use, so build it here rather than duplicating it in either screen
  file.

- [x] T14. In `lib/l10n/app_en.arb`: add `ticketTypeBug`,
  `ticketSeverityCritical`/`High`/`Medium`/`Low`, and the create-form /
  detail-section field labels the Claude Design pass specifies in
  `design.md`. Run `flutter gen-l10n` to regenerate `AppLocalizations`.

- [x] T15. Fix any other exhaustive `switch (TicketType ...)` the Dart
  compiler flags after T1 that isn't already covered by T9–T12 (run
  `flutter analyze` to find them — this is the actual discovery
  mechanism per `design.md`'s note, not a pre-enumerated list). `flutter
  analyze` came back clean — no other sites needed fixing.

- [x] T16. Add new test file
  `test/features/tickets/domain/ticket_type_hierarchy_test.dart`
  extension (or update it if `define-type-compatibility-matrix` already
  created it): every parent/child pair involving `bug`, asserted against
  `design.md`'s matrix table.

- [x] T17. Extend `test/features/tickets/cubit/tickets_cubit_test.dart`:
  a `bug` ticket moved to `inProgress` triggers the same gate/queue/
  worktree flow a `task` does; a governing Story with a UI-work-keyword-
  matching Bug sibling blocks exactly like a Task sibling would; a Bug
  with no governing Story executes unconditionally, mirroring the
  existing Task test.

- [x] T18. Extend the repository test covering `createTicket`/
  `updateTicket` (`test/features/tickets/data/...` — match whatever file
  already covers `complexity`'s round-trip): the four new fields
  persist and read back correctly.

- [x] T19. Run this grep check (per `flutter-conventions.md`'s Widget
  composition section) and confirm zero results:
  `grep -rn '^\s*Widget _[a-zA-Z0-9_]*(' lib/ --include=*.dart`. Found 8
  pre-existing hits, all unrelated to this change (7 in
  `design_system/molecules/markdown_view.dart`, 1 in
  `ticket_metadata_section.dart`'s SDD-stage-tracker code, untouched by
  this change) — flagged as a separate out-of-scope cleanup task rather
  than fixed here, since converting them is unrelated widget refactoring
  outside this change's scope.

- [x] T20. Run `flutter analyze` and `flutter test` in the app repo; fix
  any fallout before considering this change implementation-complete.
  Both clean: `flutter analyze` reports no issues; `flutter test` passes
  all 503 tests.