---
ticketId: AIO-1234
type: story
status: done
priority: none
parentId: 88f65bed-75f1-44af-a767-912a8a5c3fe3
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.273148
updatedAt: 2026-09-02T21:17:20.273148
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Cubit layer

- [x] 8. Modify `lib/features/tickets/presentation/cubit/tickets_cubit.dart`
      — rename `_lastStatus`/`_lastType`/`_lastPriority` to
      `_lastStatuses`/`_lastTypes`/`_lastPriorities` (`Set<T>`, default
      `const {}`); add `final TicketListFilterRepository? _filterRepository`
      constructor param (optional, documented alongside the existing
      optional-dependency block per this file's own convention).
      (design.md §2.1)
- [x] 9. Modify `lib/features/tickets/presentation/cubit/tickets_cubit.dart`
      — update `searchTickets`'s signature to `statuses`/`types`/
      `priorities` (`Set<T>`, default `const {}`) and every call site that
      reuses `_lastStatuses`/`_lastTypes`/`_lastPriorities`
      (`loadMoreTickets` and each mutation-triggered refresh method).
      Update dartdocs referencing the old nullable-param shape.
      (design.md §2.2)
- [x] 10. Modify `lib/features/tickets/presentation/cubit/tickets_cubit.dart`
      — add `selectedStatuses`/`selectedTypes`/`selectedPriorities`
      getters. Full dartdoc. (design.md §2.3)
- [x] 11. Modify `lib/features/tickets/presentation/cubit/tickets_cubit.dart`
      — add private `_toggleFilter<T>` helper plus
      `toggleStatusFilter`/`toggleTypeFilter`/`togglePriorityFilter`
      public methods: mutate the relevant set, persist via
      `_filterRepository`/`_projectId` (no-op if either is `null`),
      re-run `searchTickets` reusing `_lastQuery` and the other two
      dimensions. Full dartdoc on all four. (design.md §2.4)
- [x] 12. Modify `lib/features/tickets/presentation/cubit/tickets_cubit.dart`
      — add `loadPersistedFilters()`: reads persisted filters into
      `_lastStatuses`/`_lastTypes`/`_lastPriorities` without emitting a
      state; no-ops if `_filterRepository`/`_projectId` is `null`. Full
      dartdoc. (design.md §2.5)