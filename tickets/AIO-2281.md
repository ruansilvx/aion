---
ticketId: AIO-2281
type: story
status: done
priority: none
parentId: b79daef3-971b-4664-8c08-4bdec9db8147
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.413857
updatedAt: 2026-09-02T21:17:20.413857
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T18. New `test/features/tickets/domain/ticket_link_direction_test.dart`
      (or alongside existing domain test conventions if a different
      location is established) — `TicketLinkTypeRelative.inverse` for
      all five enum values; `relativeLinkType` for both
      viewing-ticket-is-source and viewing-ticket-is-target branches,
      all five stored types; `toCanonical` round-trips correctly against
      `relativeLinkType` (picking the displayed relative type back
      through `toCanonical` reproduces the row's original stored type).
- [x] T19. New or extended test coverage for
      `drift_ticket_link_repository.dart`/`ticket_link_dao.dart` (locate
      existing coverage first) — `getLinkById` returns the row or
      `null`; `deleteLink` removes exactly one row by id, no-ops for an
      unknown id; `updateLinkType` changes only `linkType`, leaves
      `sourceTicketId`/`targetTicketId` untouched. Mocktail-mocked
      dependencies per project.md's repository-test convention — no real
      drift instance unless persistence behavior specifically requires
      it.
- [x] T20. `test/features/tickets/cubit/tickets_cubit_test.dart` — add
      `bloc_test` cases for `createTicketLink`/`deleteTicketLink`/
      `updateTicketLinkType`: happy path emits refreshed
      `TicketDetailLoaded` with updated `linkedTickets`/`backlinks`;
      no-op when constructed without a `TicketLinkRepository`;
      `_refreshBlockedBoardState` fires only when the affected link is
      (or was) `blocks`/`blockedBy`, not for `relatesTo`/`duplicates`.
- [x] T21. New `test/design_system/molecules/linked_tickets_section_test.dart`
      — renders the relative-type glyph/label per row from
      `LinkedTicketRef.relativeType`; tapping the remove action (after
      confirming) calls `onRemove` with the row's `linkId`; selecting a
      new type in `_LinkTypeEditor` calls `onChangeType` with
      `(linkId, newRelativeType)`; a row whose `relativeType` is
      `duplicatedBy` renders no edit control.
- [x] T22. `test/features/tickets/presentation/widgets/ticket_link_picker_test.dart`
      — update any assertions that referenced the now-removed private
      `_linkTypeGlyph`/`_linkTypeLabel` functions directly; verify the
      picker's rendered glyph/label output is unchanged after T13's
      promotion to extension methods.
- [x] T23. Widget-test coverage for `ticket_metadata_section.dart`'s
      Linked Tickets section (locate the existing test file covering
      `TicketMetadataSection`/`TicketDetailScreen` first) — selecting a
      candidate via `TicketLinkPicker` now calls
      `TicketsCubit.createTicketLink` (not a direct repository call);
      tapping a row's remove/edit actions call
      `TicketsCubit.deleteTicketLink`/`.updateTicketLinkType` with the
      expected arguments.
- [x] T24. Manual interactive verification pass (per project.md's
      `AppDropdown`/`Overlay` guidance — `_LinkTypeEditor` is another
      `Overlay`-based selector) in real Chrome, per
      `CLAUDE.md`'s shared rules (close the tab/stop the dev server
      when done): create a link, confirm its glyph/label read correctly
      from both tickets' detail screens; edit its type and confirm both
      sides update; remove a `blocks`/`blockedBy` link and confirm the
      Board's `BlockedBadge` disappears for the previously-blocked
      ticket; confirm a `duplicatedBy`-relative row shows no edit
      control.