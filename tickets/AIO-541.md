---
ticketId: AIO-541
type: task
status: done
priority: none
parentId: c3cd15e3-16b0-40c1-aadd-383cb8c32cdd
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.189498
updatedAt: 2026-09-02T21:17:20.189498
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T27. `lib/l10n/app_en.arb` — added:

- `ticketDetailCodingExecutionFailedTitle` — "Verification failed —
PR not opened".
- `ticketDetailCodingExecutionRetryButton` — "Retry".
- `ticketDetailCodingExecutionStatusVerificationFailed` — the
status-dot row's text for the new failure state (not explicitly
listed in the original task text, but required by design.md §0's
state-resolution table — every other `CodingRunState` has one).
- Updated `ticketDetailCodingExecutionRunningHint`'s copy to drop
the now-inaccurate "opening a pull request" clause.
- Settings copy for the new "Coding-Execution Retry"
`_AutomationSection` instance.
Skipped `ticketDetailCodingExecutionToolUseHint` (the "Running
{tool}..." ICU string) — the live tool-use line's text is assembled
in `TicketsCubit`/`ChatCubit` from a dynamic tool name/summary, the
same category as the pre-existing (unlocalized) "Execution failed:
{message}" comment text `ChatCubit.runChatTurn` already persists;
kept consistent with that existing precedent rather than
localizing one dynamic string while its sibling stays raw. Ran
`flutter gen-l10n` after every `.arb` edit.