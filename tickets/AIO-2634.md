---
ticketId: AIO-2634
type: story
status: done
priority: none
parentId: 64786df9-02fa-4b5f-98a7-ca811e51409f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.455295
updatedAt: 2026-09-02T21:17:20.455295
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 8. Tests

- [x] **T19** — Create `test/features/tickets/cubit/tickets_cubit_test.dart`
  Use `bloc_test` + `mocktail`. Mock `TicketRepository`.
  Test cases:
  - `loadTickets` emits `[TicketsLoading, TicketsLoaded]` on success.
  - `loadTickets` emits `[TicketsLoading, TicketsError]` on exception.
  - `createTicket` emits `[TicketCreating, TicketCreated]` on success.
  - `createTicket` emits `[TicketCreating, TicketsError]` on exception.

- [x] **T20** — Create `test/features/tickets/data/repositories/drift_ticket_repository_test.dart`
  Use in-memory Drift DB (`NativeDatabase.memory()`).
  Test cases:
  - `createTicket` then `getAllTickets` returns the created ticket.
  - `getTicketById` returns correct ticket when found.
  - `getTicketById` returns null when not found.
  - Enum round-trip: `type`, `status`, and `priority` survive write/read.
  - `priority` defaults to `TicketPriority.none` when not supplied.
  - `estimate` and `time_spent` nullable fields survive write/read (null and
    non-null cases).
  - First ticket generated `ticketId` is `"AIO-1"` (default prefix).
  - Second ticket generated `ticketId` is `"AIO-2"` (sequence increments).
  - `ticketId` field survives entity mapping round-trip.

- [x] **T20b** — Create `test/features/tickets/data/repositories/drift_comment_repository_test.dart`
  Use in-memory Drift DB (`NativeDatabase.memory()`).
  Test cases:
  - `addComment` then `getCommentsForTicket` returns the comment.
  - Multiple comments returned in `createdAt` ascending order.
  - `authorType` round-trip: `human` and `ai` survive write/read.
  - `aiModel` field: null for human comments, non-null for AI comments —
    both survive write/read.
  - Human and AI comments on the same ticket are both returned together.

---