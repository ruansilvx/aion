---
ticketId: AIO-1138
type: story
status: done
priority: none
parentId: 99d34c95-7ca6-44ab-a2f0-e1276d33a8f3
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.261924
updatedAt: 2026-09-02T21:17:20.261924
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `TicketsCubit` — tool handlers

- [x] T13. Edit `presentation/cubit/tickets_cubit.dart` — add
      `_canBranch(Ticket chat)` (depth-cap check, design.md §6).
- [x] T14. Edit `presentation/cubit/tickets_cubit.dart` — add
      `_createBranchChat(Ticket chat, String title, String? description)`
      mirroring `_createStageChat`'s direct-repository-create shape
      (design.md §6).
- [x] T15. Edit `presentation/cubit/tickets_cubit.dart` — add
      `_closeBranch(Ticket branchChat, String parentId, String summary)`
      (status update + fold comment onto the parent, design.md §6).
- [x] T16. Edit `presentation/cubit/tickets_cubit.dart` — add
      `_pendingProposals` field and `_awaitProposalConfirmation` (design.md
      §6).
- [x] T17. Edit `presentation/cubit/tickets_cubit.dart` — add
      `_handleBranchToolCall`/`_handleCloseBranchToolCall`, each resolving
      `AutomationContext.chatBranching`'s confidence and switching on it
      per design.md §6.
- [x] T18. Edit `presentation/cubit/tickets_cubit.dart` — add public
      `confirmPendingToolProposal(String chatId)` /
      `rejectPendingToolProposal(String chatId)`, per design.md §6.
- [x] T19. Edit `presentation/cubit/tickets_cubit.dart` — wire
      `_toolsFor`/`onToolCall` into `_runStageChatTurn`,
      `_runCodingExecution`'s execution-chat turn, and `retryDesignSync`'s
      `ChatCubit.runChatTurn` calls (each passes the appropriate tool
      definition list and the matching `_handleBranchToolCall`/
      `_handleCloseBranchToolCall` bound to that call's own chat ticket).