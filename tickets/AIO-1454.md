---
ticketId: AIO-1454
type: story
status: done
priority: none
parentId: 16a56dcd-558b-4d27-9747-32e5764789f1
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.302519
updatedAt: 2026-09-02T21:17:20.302519
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 9. Board and detail-screen cancel affordances

- [x] T44. Edit `lib/features/tickets/presentation/screens/tickets_board_view.dart`
  (`BoardColumn`) — apply `clusterSiblingsAdjacently` to the column's
  ticket list when `ExecutionSchedulingReady.mode ==
  ExecutionSchedulingMode.hybrid` (read via `context.watch`), unchanged
  otherwise. Update widget dartdoc (design.md §9; per the Claude Design
  export's design.md §5 — ordering only, no new component/badge, primary
  sort preserved, siblings pulled adjacent to their earliest-sorted
  member).
- [x] T45. Continue editing `tickets_board_view.dart` (`_CardVisual`'s
  status badge) — add a small inline cancel affordance shown only
  alongside the `Running`/`Queued #N` states, calling
  `TicketsCubit.cancelCodingExecution(ticket)` with no confirmation
  dialog (design.md §9; per the Claude Design export's design.md §2.1
  `ExecutionCancelControl`, `boardBadge` placement — created in T26a).
- [x] T46. Edit the Task/Bug detail screen's execution-status section
  (`presentation/screens/ticket_detail_screen.dart` or its dedicated
  execution-section widget, per current file layout) — add a labeled
  cancel button calling the same `cancelCodingExecution`, alongside the
  existing `isExecuting`/`executionQueuePosition` display (design.md §9;
  per the Claude Design export's design.md §2.2 `ExecutionCancelControl`,
  `detailButton` placement — created in T26a).
- [x] T46a. **(added by /design-sync)** Create
  `lib/features/tickets/presentation/widgets/resume_runs_prompt.dart` —
  `ResumeRunsPrompt`, a one-time-per-launch inline banner pinned to the
  top of the Board view, listing interrupted coding-execution runs with
  Resume/Dismiss actions, per the Claude Design export's design.md §3.
  Shown only when `restoreExecutionQueue()` (T36)'s `gated` branch has
  surfaced pending entries to resume — this likely needs a small
  `TicketsState` addition (e.g. a `pendingResumePrompt` field on
  `TicketsLoaded`) to carry those entries from `restoreExecutionQueue()`
  to the Board widget; exact shape left for `/apply` to size against
  T36's actual implementation. Resume re-enqueues every surviving entry
  and calls `_tryStartNextQueuedExecutions()` (same as T36's `auto`
  path); Dismiss falls back to `manual`'s behavior (clear the snapshot,
  rely on the existing orphaned/stalled retry banner). File header +
  dartdoc.
- [x] T47. Edit `lib/core/routing/app_router.dart` / `WorkspaceShell`
  (wherever `TicketsCubit` is constructed per-project) — call
  `restoreExecutionQueue()` once, immediately after construction, gated
  on the same optional-dependency non-null check every other coding-
  execution-adjacent init already uses (design.md §5.3).