---
ticketId: AIO-132
type: task
status: done
priority: none
parentId: ae6f813e-3ee3-4ab4-a5f5-f2f183db1b99
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.142116
updatedAt: 2026-09-02T21:17:20.142116
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T13. `lib/features/providers/presentation/screens/settings_screen.dart`

— MODIFY:
- `_ProviderStatusCard`'s title/subline read from
`state.providerDisplayName` and a new small `switch` on the
resolved provider's `ProviderId` (`claudeAgentSdk` → today's
"Authenticated via your Claude plan"; `anthropicMessagesApi` →
"Configured via API key") instead of the hardcoded
`settingsProviderCardTitle`/`settingsProviderSubline` l10n strings.
- New "PROVIDERS" eyebrow section + `_AnthropicApiKeySection`
(`StatefulWidget`, owns a `TextEditingController`/`FocusNode`/local
`_revealed` bool), placed between `_ProviderStatusCard` and the
"MODELS" eyebrow: label + description, an `AppTextField`
(`obscureText: !_revealed`, `suffixIcon:` an eye/eye-slash toggle
`GestureDetector`) bound to `AnthropicProviderConfigCubit`, a Save
`AppButton` calling `saveApiKey`, a `ProviderConnectionBadge` +
Test Connection `AppButton` pair (disabled when `!hasApiKey`)
calling `testConnection`.
- `_ModelPhaseSection` — computes
`availableModels.map((m) => m.providerId).toSet()` per phase and
passes `itemRowLabel: <set>.length > 1 ? (m) =>
'${_providerPrefix(m.providerId)} · ${m.label}' : null` to its
`AppDropdown` (new private `_providerPrefix(ProviderId)` helper —
`'Claude Agent SDK'`/`'Anthropic API'`).
Per design.md's Component Spec §2 (`ProvidersSection` container/
identity/field/action-row geometry), §4 (`SaveKeyButton` — reuse
`AppButton`'s existing primary variant verbatim, including its real
shipped glow shadow, not the export's approximated alpha), §5
(connection cluster + the `ApiKeyState.none` "NO KEY" presentation —
**note:** this needs `ProviderConnectionBadge` to render a caller-
supplied label string while keeping the `unknown` state's exact
chrome; add an optional label-override param there rather than a new
badge state or a duplicate one-off widget), §6 (dynamic
`ProviderStatusCard` name/subline + truncation), and §7.5 (dropdown
worked example). Depends on T4, T8, T9, T10, T11, T12. Update dartdoc
to match every changed/added widget.