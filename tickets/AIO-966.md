---
ticketId: AIO-966
type: story
status: done
priority: none
parentId: db91fdb7-f189-42f6-b315-f8c180b966a6
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.244038
updatedAt: 2026-09-02T21:17:20.244038
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Domain layer

- [x] **T2** — Create `lib/features/tickets/domain/enums/
  backlink_origin.dart`: `BacklinkOrigin` enum (`explicitLink |
  wikilink`), dartdoc on the enum and both values. File header.
- [x] **T3** — Create `lib/features/tickets/domain/entities/
  backlink_ref.dart`: `BacklinkRef extends Equatable` (`{required Ticket
  ticket, required BacklinkOrigin origin}`). Full dartdoc. File header.
- [x] **T4** — Create `lib/features/tickets/domain/entities/
  page_wikilink.dart`: `PageWikilink extends Equatable` (`id`,
  `sourcePageId`, `targetPageId`, `createdAt`). Dartdoc notes
  `targetPageId` may reference a `page` or `resource` ticket;
  `sourcePageId` is always a `page`. File header.
- [x] **T5** — Create `lib/features/tickets/domain/repositories/
  page_wikilink_repository.dart`: `PageWikilinkRepository` (`abstract
  interface class`) — `getIncomingLinks`, `getOutgoingLinks`,
  `replaceOutgoingLinks`, `deleteLinksForTickets`, per design.md
  (dartdoc notes the widened `page`/`resource` target semantics). Full
  dartdoc. File header.
- [x] **T6** — Modify `PageRelations` (wherever it's currently declared
  under `core/contracts/page_ticket_provider.dart` or its own file):
  change `backlinks` field type from `List<LinkedTicketRef>` to
  `List<BacklinkRef>`. Update its dartdoc accordingly.
- [x] **T7** — Modify `core/contracts/page_ticket_provider.dart`: add
  `Future<List<Ticket>> getWikilinkCandidates()` to the
  `PageTicketProvider` interface — every live `page`/`resource` ticket
  (widened beyond `page`-only), no self/descendant exclusion (contrast
  explicitly with `getValidParentCandidatesForPage`). Full dartdoc.