---
ticketId: AIO-316
type: story
status: done
priority: none
parentId: 751e56c8-0458-4136-85c1-9b3202d8a6da
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.163211
updatedAt: 2026-09-02T21:17:20.163211
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 5. `BaselineUpgradeBanner` + `TicketsListScreen` wiring (design-gated — do not implement final visuals until `/design-sync` sets the gate to APPROVED)

- [x] 5.1 Create
      `aion/lib/features/tickets/presentation/widgets/baseline_upgrade_banner.dart` —
      `BaselineUpgradeBanner` per design.md §1 `BaselineUpgradeBanner —
      shell (common to both states)`, §1.1 `Offer state`, §1.2 `"Upgrade"
      button — states`, §1.3 `Inline version pill`, §1.4 `Dismiss × —
      states`, and §1.5 `Upgrading state` (two-state shape, offer /
      upgrading), reusing `codebase_analysis_banner.dart`'s
      `_BannerShell`/`_IconChip`/`_DismissButton` (hoist them into a small
      shared file, e.g. `presentation/widgets/banner_shell.dart`, both
      banners import — per design.md's note; do not copy-paste a second
      set). Non-Material; `AionColors`/`AionText`/`AionSpacing` only (use
      a raw `12`/`14`/`26` etc. literal exactly where design.md specifies
      one — its pixel values are already multiples of the existing scale
      even where no named token matches precisely). Full class + method
      dartdocs.
- [x] 5.2 Modify
      `aion/lib/features/tickets/presentation/screens/tickets_list_screen.dart` —
      new `_showBaselineUpgradeOffer` local bool, set from
      `ActiveProjectProvider.offerBaselineUpgrade` in `initState`
      alongside the existing `offerCodebaseAnalysis` check (calls
      `consumeBaselineUpgradeOffer()`), mounted as a second conditional
      `Padding` child at the same insertion point as
      `CodebaseAnalysisBanner`, per design.md §4.1 `TicketsListScreen
      (/workspace/tickets)` (renders first/above `CodebaseAnalysisBanner`
      when both are present, per design.md §3's stacking note, gap
      `AionSpacing.sp12`). Update the screen's class dartdoc to mention
      the new banner.
- [x] 5.3 Update `aion/lib/l10n/app_en.arb` — add keys for
      `BaselineUpgradeBanner`'s offer/upgrading copy (title, body with
      target-version interpolation, accept button label, dismiss
      semantics label, upgrading status text), following the existing
      `codebaseAnalysis*`/`baselineUpgrade*` naming convention.
- [x] 5.4 Extend
      `test/features/tickets/presentation/screens/tickets_list_screen_test.dart` —
      banner shown only when the mocked `ActiveProjectProvider` reports
      `offerBaselineUpgrade: true`; tapping "Upgrade" calls
      `acceptBaselineUpgrade()`; banner never reappears after
      `consumeBaselineUpgradeOffer` fires; both banners can render
      simultaneously without layout errors when both offer flags are
      true.