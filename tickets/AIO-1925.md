---
ticketId: AIO-1925
type: story
status: done
priority: none
parentId: 48acc8f2-b7f2-4931-88c4-3fe463cfdd30
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.370003
updatedAt: 2026-09-02T21:17:20.370003
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation — Epic/Story detail screen

- [x] 15. `lib/features/tickets/presentation/widgets/ticket_metadata_section.dart`
      — generalize `_GatedBanner`, `_ManualAdvanceButton`, and
      `_AutoAdvancedNote` to accept optional explicit
      `title`/`subLine`/`actionLabel` overrides (falling back to their
      existing stage-derived computation when omitted), per design.md
      §5.2 and design.md's "Component Spec" §1.1 Gated state, §1.2
      Manual state, §1.3 Auto state.
- [x] 16. Same file — add `verifyRetryReady` (`bool`, default `false`),
      `verifyRetryConfidence` (`AutomationConfidence?`), and
      `onRetryVerify` (`VoidCallback?`) params to `_SddStageSection`; add
      the new footer tier (rendered when `verifyRetryReady` is `true`,
      taking priority over the generic `canAdvance`/`_NotReadyHint`
      tiers) using task 15's generalized widgets with the new copy
      (title "Ready to retry verification", action "Retry verification")
      per design.md §5.2 and design.md's "Component Spec" §1 "Ready to
      retry verification" footer tier (§1.0 slot/trigger/visibility
      rule, §1.4 not-ready predecessor copy).
- [x] 17. Same file — wire the `_SddStageSection(...)` call site (`:728`)
      to pass `verifyRetryReady`/`verifyRetryConfidence` from
      `TicketDetailLoaded` and an `onRetryVerify` callback resolving the
      ticket's current Verifying-stage chat (via a small public wrapper
      on `TicketsCubit`, or an equivalent lookup — implementation detail)
      and calling `TicketsCubit.retryVerify`, per design.md's "Component
      Spec" §1.0 (visibility rule, mutual exclusivity with the advance
      tier).