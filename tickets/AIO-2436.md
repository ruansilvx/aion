---
ticketId: AIO-2436
type: story
status: done
priority: none
parentId: 60978800-50b4-47e4-a9fb-70c304ad0d3f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.432867
updatedAt: 2026-09-02T21:17:20.432867
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Routing & Trash screen

- [x] T27 — `lib/core/routing/app_router.dart`: add
  `GoRoute(path: '/tickets/trash', builder: ...)` wrapping `TrashScreen`
  in `BlocProvider<TrashCubit>(create: (context) =>
  TrashCubit(context.read<TicketRepository>())..load())`, registered
  **before** the existing `GoRoute(path: '/tickets/:id', ...)` in the
  `routes` list (go_router matches path segments in order — `:id` would
  otherwise greedily match the literal `trash` segment). Also wrap the
  existing `/tickets` route's `TicketsListScreen` with
  `BlocProvider<TicketSelectionCubit>(create: (_) =>
  TicketSelectionCubit())`, matching the file's existing
  `BlocProvider<CommentsCubit>` precedent on `/tickets/:id`. Update the
  file's top dartdoc to list the new route. Per design.md §6, §8.
- [x] T28 — Create `lib/features/tickets/presentation/screens/trash_screen.dart`:
  `TrashScreen` — header (`ticketTrashScreenTitle` title, back
  navigation to `/tickets`, "Empty trash" action per design.md §8) over
  a `BlocBuilder<TrashCubit, TrashState>` body (loading spinner, error +
  retry, `ticketTrashEmptyState` empty state, or a `ListView` of
  `TrashedTicketTile`). File header + full dartdoc. Per design.md §8.
  Visual spec: design.md's Specification §7 TrashScreen (§7.1–§7.4),
  §6.2 Confirm dialog — Permanent-delete tone (for the "Empty trash"
  action).
- [x] T29 — Create `lib/features/tickets/presentation/widgets/trashed_ticket_tile.dart`:
  `TrashedTicketTile` — id badge + title + type chip (no status/
  priority), non-navigating, trailing Restore (no confirm, calls
  `TrashCubit.restore` directly) and Permanently Delete
  (`showAppConfirmDialog`, destructive → `TrashCubit.permanentlyDelete`)
  icon actions. File header + full dartdoc. Per design.md §8. Visual
  spec: design.md's Specification §8 TrashedTicketTile (§8.1–§8.3),
  §6.2 Confirm dialog — Permanent-delete tone (for the per-row action).