---
ticketId: AIO-710
type: story
status: done
priority: none
parentId: 570fd526-ce2a-44a7-bcc1-427b56b92659
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.211308
updatedAt: 2026-09-02T21:17:20.211308
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T5. `test/core/agent/claude_agent_sdk_provider_test.dart` —
  MODIFY: add a unit test asserting `supportsSkillDiscovery` is `true`.
  Depends on T2.
- [x] T6. `test/core/agent/anthropic_messages_api_provider_test.dart`
  — MODIFY: add a unit test asserting `supportsSkillDiscovery` is
  `false`. Depends on T3.
- [x] T7. `test/features/tickets/cubit/tickets_cubit_test.dart` —
  MODIFY:
  - `buildProviderStack` — add
    `when(() => provider.supportsSkillDiscovery).thenReturn(true);`
    alongside its existing `normalizeErrorMessage`/`describeOverage`
    stubs, so every existing test using this shared fixture (including
    every current `delegatedSkill` test in the "Skill attachments
    (Phase 2)" group) keeps exercising a discovery-capable provider —
    matching `ClaudeAgentSdkProvider`'s real declared value — without
    each test needing its own stub.
  - New test in the `'Skill attachments (Phase 2)'` group (sibling to
    the existing `delegatedSkill`/`aionNativeTemplate` cases): a
    `delegatedSkill` attachment (`AutomationConfidence.auto`) whose
    resolved provider has
    `when(() => provider.supportsSkillDiscovery).thenReturn(false)`
    (a second `MockAgentProvider` — reuse the existing
    `buildProviderStack` helper on a fresh `MockAgentModelClient`, then
    override that provider's `supportsSkillDiscovery` stub and repoint
    `registry.providerById(ProviderId.claudeAgentSdk)` to it) — asserts
    `commentRepository.addComment` is called with content starting
    `'Skill attachment failed:'` and that `gitClient.createWorktree`/
    `agentClient.run` on the *original* discovery-capable client is
    never called with this attachment's prompt (the worktree the
    failing provider's own run would have used is still created/torn
    down per §3's "what does not change" — assert
    `gitClient.removeWorktree` is still called once, not that worktree
    creation is skipped). Depends on T4, and the `buildProviderStack`
    edit above.