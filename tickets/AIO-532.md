---
ticketId: AIO-532
type: story
status: done
priority: none
parentId: 2a157abe-81fc-4081-8a3c-341e6f32c4db
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.189498
updatedAt: 2026-09-02T21:17:20.189498
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation layer — UI (blocked on design gate)

- [x] T21. `features/tickets/presentation/screens/ticket_detail_screen.dart`
      — `TicketDetailLoaded` destructuring gains `executionFailureReason`/
      `executionCanRetry`; pass through to `_CodingExecutionSection`
      per design.md §0 (updated state resolution table).
- [x] T22. Same file — `_CodingExecutionSection`'s if/else chain gains the
      failure branch per design.md §0 (`CodingRunState.verificationFailed`
      row) and §5 (enum). Update its class dartdoc.
- [x] T23. Same file — new `_ExecutionActionBanner` widget (`_BannerTone`
      `success`/`failure`) replacing `_ExecutionReadyForReviewBanner`,
      per design.md §1 (container/glyph/text/error-region/button geometry
      and states) and §5 (enum); the existing ready-for-review call site
      becomes `tone: .success` with unchanged copy/behavior. Also added
      the two new `dangerTint`/`dangerBorderTint` derived helpers on
      `AionColorsHubTokens` (`design_system/tokens/aion_colors.dart`,
      design.md §4) that this widget's failure tone depends on. Dartdoc
      the new widget; removed the old one's now-superseded dartdoc.
- [x] T24. Same file — `_ExecutionRunningHint` gains the new
      `_ExecutionLiveToolLine` child, per design.md §2 (layout, pulse-dot
      motif, truncation). Data source deviates from design.md's literal
      `BlocBuilder<ChatCubit, ChatState>` prescription — see final apply
      report for why (`TicketDetailLoaded.executionLiveActivity`,
      pushed by `TicketsCubit._emitLiveExecutionActivity`, is used
      instead). Also simplified the verb/argument two-tone `TextSpan`
      split (design.md §2.1) to one plain string/style, since
      `ChatCubit`'s emitted status string isn't structured into separate
      parts — flagged in the apply report, not a blocking issue.
- [x] T25. `features/providers/presentation/screens/settings_screen.dart`
      — third `_AutomationSection` instance for
      `AutomationContext.codingExecutionRetry` per design.md §3 (copy +
      confirmed no new visual states — byte-identical geometry to the
      existing two instances).