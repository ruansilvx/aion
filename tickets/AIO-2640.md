---
ticketId: AIO-2640
type: story
status: done
priority: none
parentId: 64786df9-02fa-4b5f-98a7-ca811e51409f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.455295
updatedAt: 2026-09-02T21:17:20.455295
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 10. Verification

- [x] **T22** — Run `flutter analyze`. Zero errors.
- [x] **T23** — Run `flutter test`. All tests pass.
- [x] **T24** — Run `flutter run -d chrome`. Verify: list screen loads,
  create ticket flow works (ticket gets ID e.g. `AIO-1`), detail screen
  shows `ticketId` badge, no 404 on manual URL entry (localhost dev server
  handles this automatically; Firebase rewrite needed for deployed hosting).
- [ ] **T25** — Run `flutter run` on a native target (desktop or mobile).
  Verify same flows work on native.
- [x] **T26** — Material coupling and token discipline grep checks:
  - `grep -r "Theme.of(" lib/` → zero results.
  - `grep -r "MaterialApp" lib/` → zero results.
  - `grep -r "ThemeData" lib/` → zero results.
  - `grep -r "ColorScheme" lib/` → zero results.
  - `grep -r "Color(0x" lib/features/` → zero results (raw literals only
    permitted in `lib/core/theme/aion_colors.dart`).
  - `grep -r "InkWell\|ElevatedButton\|TextButton\|FloatingActionButton\|ListTile\|SnackBar\|ScaffoldMessenger\|Scaffold\|AppBar\|CircularProgressIndicator\|DropdownButton\|Card" lib/` → zero results.
  - `grep -r "PriorityBadge\|CommentTile" lib/` → results only in
    `lib/features/tickets/` subtree (no imports in `core/` or other features).
  All checks must pass before marking this task complete.

- [ ] **T27** — Run `dart analyze` with `prefer_documenting_undocumented_public_members`
  lint enabled in analysis_options.yaml → zero warnings.

- [ ] **T28** — Accessibility spot-check on desktop:
  - Tab through `TicketsListScreen`: `AppFab` and each list tile reachable.
  - Tab through `CreateTicketScreen`: title → type → priority → description
    → submit in order; Enter/Space activates focused element.
  - Flutter Accessibility Inspector: verify semantic labels on `AppFab`,
    `TicketListTile` (includes ticketId + title), and `AppTextField`s.
  - On `TicketDetailScreen`: verify `CommentTile` semantic labels include
    author type and content. Verify comment input `AppTextField` and Send
    `AppButton` are reachable via Tab.
  - Verify light and dark themes both render legibly (toggle system theme).
  - Verify `PriorityBadge` colors meet WCAG AA in both light and dark themes.