---
ticketId: AIO-241
type: story
status: done
priority: none
parentId: 6307a905-6e8b-466c-b07f-3c706e41ec62
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.156861
updatedAt: 2026-09-02T21:17:20.156861
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Create the three barrels

- [x] **T1** — Create `lib/core/design_system.dart`. File header:
  `// design_system.dart — Design-system barrel: theme tokens and ThemeScope (core layer).`
  Body: six `export` statements for `theme/aion_colors.dart`,
  `theme/aion_radius.dart`, `theme/aion_shadows.dart`,
  `theme/aion_text.dart`, `theme/aion_theme.dart`,
  `theme/theme_scope.dart` (see design.md for exact content). No
  existing files move or change.
- [x] **T2** — Create `lib/core/core.dart`. File header:
  `// core.dart — Barrel for core infrastructure: database, routing, localization, utils, widgets (core layer).`
  Body: 13 `export` statements covering `database/`, `localization/`,
  `routing/`, both `utils/*.dart`, all eight `widgets/*.dart`.
  Excludes `theme/` (that's T1's job).
- [x] **T3** — Create `lib/features/tickets/tickets.dart`. File header:
  `// tickets.dart — Public-surface barrel for the tickets feature (domain + presentation only; data layer intentionally excluded, see flutter-conventions.md).`
  Body: 20 `export` statements — every file under `domain/` (entities,
  enums, exceptions, repository interfaces) and every file under
  `presentation/` (cubit, state, screens, widgets). Nothing under
  `data/` is exported.