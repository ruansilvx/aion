---
ticketId: AIO-2392
type: story
status: done
priority: none
parentId: 7161c960-0bf3-4230-a027-501ca12410ec
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.427767
updatedAt: 2026-09-02T21:17:20.427767
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] **T16.** Create `test/features/tickets/data/repositories/shared_prefs_ticket_list_sort_repository_test.dart`
  — round-trip `get`/`setSort`, project-id key isolation between two
  project ids, default-`null` read when nothing's been saved yet.
  Mirrors `shared_prefs_ticket_list_filter_repository_test.dart`'s
  existing shape.
- [x] **T17.** Create `test/features/tickets/domain/utils/ticket_sort_comparator_test.dart`
  — every `TicketSortField` × both `TicketSortDirection` values sort
  correctly against a small fixture ticket list, plus the
  `relevance`-falls-back-to-createdAt-descending case.
- [x] **T18.** Modify `test/features/tickets/cubit/tickets_cubit_test.dart`
  — add coverage for `setSort` (persistence call when
  `sortRepository`/`projectId` are supplied via a mocked
  `TicketListSortRepository`, no-op persistence when either is `null`,
  mirroring the existing `toggleStatusFilter` test shape) and
  `loadPersistedSort` (populates `currentSort` from a fake repository
  without emitting a state, mirroring `loadPersistedFilters`'s test);
  add assertions that `searchTickets`/`loadMoreTickets`/every
  mutation-refresh method passes the expected resolved `sort` to the
  mocked `TicketRepository.searchTickets`.
- [x] **T19.** Modify `test/features/tickets/data/daos/ticket_dao_test.dart`
  — add cases for each `TicketSortField` × both directions against a
  real in-memory drift instance (both the no-query and the
  FTS-query-active branches), confirming: enum-ordinal ordering for
  priority/status/type, direct-column ordering for createdAt/updatedAt,
  `bm25` ordering preserved for `relevance` with a query active,
  `relevance` falling back to `createdAt` descending with no query, and
  the `createdAt DESC` tiebreaker breaking ties correctly for a
  non-createdAt field.
- [x] **T20.** Modify `test/features/tickets/cubit/trash_cubit_test.dart`
  — add coverage for `load()` applying the persisted sort to `roots`
  (via a mocked `TicketListSortRepository`), the
  `relevance`-persisted-falls-back-to-createdAt case, and the
  no-`sortRepository`/no-`projectId` fallback.