---
ticketId: AIO-2674
type: story
status: done
priority: none
parentId: 097cbcf9-4910-46b7-acb8-673604e809d9
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.459835
updatedAt: 2026-09-02T21:17:20.459835
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# UI — gated behind the design gate

- [x] T20. `presentation/screens/workflow_status_settings_screen.dart`
      — `StatusRow` (any scope) and `SddStageRenameRow` each gain
      `_AttachmentBadge` (shown when a target has a configured
      attachment) and `_AttachmentForm` (inline kind/name/confidence
      editor), per Component Spec §2 (`AttachmentBadge`, incl. the
      resting `+ Attach skill` affordance and per-confidence tint/glyph
      states), §3 (`AttachmentForm`: kind `SegmentedControl` §3.1,
      template-picker/skill-name/no-templates name field §3.2–§3.4,
      confidence `SegmentedControl` + actions §3.5, field error §3.6),
      and §4 (row-integration placement on both row types). Call the
      real `AionColorsHubTokens.neutralTint`/`.neutralBorderTint`
      methods directly rather than hand-typing §2.1's restated
      `0.15 : 0.09`/`0.28 : 0.18` literals — see proposal.md's Design
      gate block for the real values. Wire to `WorkflowConfigCubit
      .createAttachment`/`updateAttachment`/`deleteAttachment`.
- [x] T21. New file
      `presentation/screens/workflow_prompt_templates_screen.dart` —
      `WorkflowPromptTemplatesScreen`: list of `WorkflowPromptTemplate`s
      with create/edit/delete, per Component Spec §5 (§5.1 composition,
      §5.2 header, §5.3 `TemplateRow` incl. inline `{{variable}}`
      highlighting, §5.4 `AddTemplateControl`, §5.5 inline create/edit
      editor, §5.6 empty state). Full dartdoc + file header.
- [x] T22. `core/routing/app_router.dart` — register
      `WorkflowPromptTemplatesScreen`'s route
      (`/workspace/settings/workflow/templates`). Add a "manage
      templates" entry point from T20's `_AttachmentForm` and from
      `WorkflowStatusSettingsScreen`'s header.
- [x] T23. `presentation/screens/ticket_detail_screen.dart` — new
      widgets `_PendingSkillAttachmentBanner` (rendered when
      `TicketDetailLoaded.pendingSkillAttachment` is non-`null`;
      Confirm/Reject call `confirmPendingSkillAttachment`/
      `rejectPendingSkillAttachment`) per Component Spec §6 (§6.1
      typeChat-keyed tokens incl. the new `chatGlow` helper — see
      proposal.md's Design gate block, §6.2 default state, §6.3
      in-flight state) and `_RunAttachedSkillButton` (rendered when the
      ticket's current status/stage has a `manual`-confidence
      attachment; calls `runAttachedSkillManually`) per Component Spec
      §7 (§7.1 geometry, §7.2 states). Call the real
      `AionColorsHubTokens.primaryWash` method directly rather than
      hand-typing §7.2's restated `0.14 : 0.07` literal — see
      proposal.md's Design gate block for the real value.