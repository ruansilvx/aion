---
ticketId: AIO-1875
type: story
status: done
priority: none
parentId: 1f953fb4-cab6-47a7-bdfd-8e129b5d1cb0
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.363289
updatedAt: 2026-09-02T21:17:20.363289
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation layer — UI

- [x] T15. In
  `lib/features/tickets/presentation/screens/create_ticket_screen.dart`:
  add a Complexity `SelectionMenu<TicketComplexity>` field, mirroring the
  existing Priority field's structure and placement per `design.md`
  §1 Complexity picker (§1.1–§1.6 for the meter glyph, trigger, menu
  rows, and all states).

- [x] T16. In
  `lib/features/tickets/presentation/screens/ticket_detail_screen.dart`:
  - Add the Complexity `SelectionMenu<TicketComplexity>` to the
    ticket-meta row, mirroring the existing Priority block, per
    `design.md` §1 Complexity picker.
  - Add the SDD-stage section for `epic`/`story` tickets (current-stage
    display + auto/gated/manual-dependent control), reading
    `AutomationSettingsCubit`'s loaded value and
    `TicketDetailLoaded.canAdvanceSddStage`, per `design.md` §2 SDD-stage
    section (§2.1 tracker, §2.2 not-ready, §2.3 gated banner, §2.4
    manual button, §2.5 auto note, §2.6 state matrix).
  - Add the `_StreamingBubble` widget and swap `CommentsCubit` for
    `ChatCubit` when `ticket.type == TicketType.chat` (route-level
    provider swap in `app_router.dart`, screen-level `_sendComment`
    branch), per `design.md` §3 Streaming chat bubble.
  - Update `CommentTile` to render `CommentAuthorType.system` comments
    distinctly (`isSystem` branch), per `design.md` §4
    `CommentAuthorType.system` comment variant.
  - Update the class-level dartdoc describing what the screen renders.

- [x] T17. In
  `lib/features/tickets/presentation/widgets/ticket_overflow_menu.dart`:
  add the "Promote to Epic" action (shown only for `signal` tickets) and
  its existing-vs-new chooser, wiring `TicketLinkPicker` (candidates
  filtered to `TicketType.epic`) and
  `TicketsCubit.promoteSignalToEpic`, per `design.md` §5 "Promote to
  Epic" action + chooser (§5.1 menu item, §5.2–§5.3 inline chooser
  expansion, §5.4 `TicketLinkPicker` reuse). Update dartdoc.

- [x] T18. In
  `lib/features/providers/presentation/screens/settings_screen.dart`:
  add the "SDD Stage Automation" section
  (`SelectionMenu<AutomationConfidence>`, backed by
  `AutomationSettingsCubit`), per `design.md` §6 "SDD Stage Automation"
  setting (§6.1 label/description, §6.2 trigger, §6.3 menu rows). Update
  dartdoc.

- [x] T19. In `lib/design_system/molecules/linked_tickets_section.dart`:
  fix `_LinkRowState._typeColor` to add explicit `TicketType.signal`/
  `.release`/`.chat` arms per `design.md`. Update the widget-level
  comment noting all seven types now resolve explicitly.

- [x] T20. In `lib/core/routing/app_router.dart`: wire the new
  `AutomationSettingsCubit`/`ChatCubit` providers into the relevant
  routes (Settings route for the former; ticket-detail route for the
  latter, conditional on ticket type per T16); pass `AgentModelClient`/
  `CommentRepository` into `TicketsCubit`'s `create` call per T11.