---
ticketId: AIO-810
type: story
status: done
priority: none
parentId: b6289537-c4a4-4a03-997a-f046a69c31b8
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.222704
updatedAt: 2026-09-02T21:17:20.222704
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation layer — widgets/screens

- [x] T10. `lib/features/tickets/presentation/widgets/documentation_tree_item.dart`
  — ADD: `DocumentationTreeItem` — presentational row widget (type icon,
  title, depth indent, expand chevron for `page` rows), no business
  logic. Full dartdoc, file header comment. Non-Material constraint
  applies (no `ListTile`/`Card`/etc.). Per design.md §4 `DocumentationTreeItem`
  (§4.0–§4.6, including the flat search-result variant §4.5).
- [x] T11. `lib/features/tickets/presentation/widgets/documentation_linked_tickets_section.dart`
  — ADD: `DocumentationLinkedTicketsSection` — presentational list
  widget given `List<Ticket>` + tap callback. Full dartdoc, file header
  comment. Per design.md §8.3 Linked Tickets section.
- [x] T12. `lib/features/tickets/presentation/widgets/documentation_backlinks_section.dart`
  — ADD: `DocumentationBacklinksSection` — presentational list widget,
  same shape as T11. Full dartdoc, file header comment. Per design.md
  §8.4 Backlinks section.
- [x] T13. `lib/features/tickets/presentation/screens/documentation_screen.dart`
  — ADD: `DocumentationScreen` per design.md — header (title, search
  field, "+ New page"/"+ New resource" actions), tree/search-results
  body, empty state. Wraps content in `BlocProvider<DocumentationCubit>`
  consumption via `BlocBuilder`. Full dartdoc, file header comment.
  Non-Material constraint applies. Per design.md §3 `DocumentationScreen`
  (§3.1–§3.6), §6 `DocumentationSearchField`, and §7 `DocumentationEmptyState`.
- [x] T14. `lib/features/tickets/presentation/screens/ticket_detail_screen.dart`
  — MODIFY: render `DocumentationLinkedTicketsSection`,
  `DocumentationBacklinksSection`, and (for `page` type only) a
  "Sub-pages" section, all conditional on
  `ticket.type == TicketType.page || ticket.type == TicketType.resource`.
  Update file's existing dartdoc to mention the new conditional
  sections. Per design.md §8 Ticket-detail documentation-mode sections
  (§8.1 shared frame, §8.2 Sub-pages, §8.5 insertion order).
- [x] T15. `lib/features/tickets/presentation/screens/tickets_list_screen.dart`
  — MODIFY: add a "Documentation" header icon button (mirrors the
  existing Trash button) navigating to `/workspace/documentation`;
  exclude `TicketType.page`/`TicketType.resource` from the type-filter
  dropdown's `items`. Update relevant inline dartdoc. Per design.md §5
  Documentation entry-point icon button.
- [x] T16. `lib/features/tickets/presentation/screens/create_ticket_screen.dart`
  — MODIFY: add optional `initialType`/`initialParentId` constructor
  params (used when opened from `DocumentationScreen`'s "+ New
  page"/"+ New resource" actions), defaulting to current behavior when
  omitted. Update constructor dartdoc.