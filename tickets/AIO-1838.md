---
ticketId: AIO-1838
type: story
status: done
priority: none
parentId: b2a407ad-661f-4620-9517-d36eb3cab6da
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.360227
updatedAt: 2026-09-02T21:17:20.360227
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation layer — Cubit

- [x] T3. In `lib/features/tickets/presentation/cubit/tickets_cubit.dart`:
  change `_nextSddStage` from `SddStage? _nextSddStage(SddStage?
  current)` to `Future<SddStage?> _nextSddStage(Ticket ticket)` per
  `design.md` §2 — the `proposed` case now fetches the ticket's child
  Tasks and calls the new `_storyNeedsDesignReview` helper to decide
  between `SddStage.designBrief` and `SddStage.verifying`; every other
  case is a straight port of the existing switch arms. Add the new
  private `_storyNeedsDesignReview(List<Ticket> tasks)` helper with
  dartdoc, exactly as specified in `design.md` §2. Update both call
  sites (`advanceSddStage`, `_sddStageAdvanceCheck`) to `await
  _nextSddStage(ticket)`.

- [x] T4. In the same file: add `SddStage.designBrief`/
  `SddStage.designSync` cases to `_sddStageAdvanceCheck`'s switch, per
  `design.md` §3. Add the new private helpers `_linkedDesignPage(String
  storyId)` and `_designSyncApproved(String storyId)` with full dartdoc,
  exactly as specified in `design.md` §3.

- [x] T5. In the same file: add `SddStage.designBrief`/
  `SddStage.designSync` branches to `_spawnStageChat`, per `design.md`
  §4 — the `designBrief` branch creates the `TicketType.page` design
  ticket and links it via `_linkRepository?.createLink(...)` before
  falling through to the existing chat-creation sequence. Add
  `SddStage.designBrief => 'Design Brief'`/`SddStage.designSync =>
  'Design Sync'` arms to `_stagePresentName`. Update `_spawnStageChat`'s
  dartdoc to mention the two new branches.

- [x] T6. In the same file: add `dart:io` and `package:path` (as `p`)
  imports; add `SddStage.designBrief`/`SddStage.designSync` branches to
  `_assembleStageContext`, per `design.md` §5. Add the new private
  `_readTokenFilesForContext()` helper with full dartdoc, exactly as
  specified in `design.md` §5.

- [x] T7. In the same file: add the new public `retryDesignSync(Ticket
  designSyncChat)` method with full dartdoc, exactly as specified in
  `design.md` §6.

- [x] T8. In the same file: extend `getTicketById`'s existing `story`
  special-case path (where `canAdvanceSddStage`/`sddStageBlockReason`
  are already computed) to also compute `needsDesignReview` via
  `_storyNeedsDesignReview` against the story's current child Tasks
  (`null` if no Tasks exist yet), populating the new
  `TicketDetailLoaded.needsDesignReview` field from T2. Update
  `getTicketById`'s dartdoc to mention this.