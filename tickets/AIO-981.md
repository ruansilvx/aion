---
ticketId: AIO-981
type: task
status: done
priority: none
parentId: e421e51c-5b62-4e01-b579-4499107aaf63
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.244038
updatedAt: 2026-09-02T21:17:20.244038
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# **T14** — Modify `lib/features/tickets/data/services/

ticket_markdown_reconciler.dart` (or wherever the shared embedding-
regen-parity seam actually lives — confirm by reading the file before
editing): after a successful, non-`Unparseable` apply for a `page`
ticket, trigger the same reindex/cascade logic
`TicketsCubit._reindexAndCascadeWikilinks`/`_rewriteReferrerOrDefer`
perform (including the widened `page`/`resource` candidate set and
the `ActiveTicketViewRegistry` deferral) through whichever shared call
this file already makes for embedding-regen parity — do not duplicate
the extraction/resolution logic inline if a shared helper can be
reused instead; if no such shared helper currently exists (the two
paths independently duplicate embedding-regen triggering today), add
one (e.g. a service-level `reindexPageWikilinks(...)` in a small new
`data/services/page_wikilink_indexer.dart`, taking the same
dependencies `TicketsCubit`'s private methods do) and call it from
both `TicketsCubit` and this reconciler path, rather than leaving
T13's logic un-reusable here. Full dartdoc on anything new. File
header.