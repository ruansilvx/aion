---
ticketId: AIO-1893
type: story
status: done
priority: none
parentId: 28c4b699-fa11-44bb-a1b4-ad186f63a574
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.366837
updatedAt: 2026-09-02T21:17:20.366837
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tasks: sdd-ticket-foundation

Ordered checklist. Every task touching a `.dart` file includes its
documentation (dartdoc + file header, where new) in the same task — not
a follow-up. Design-gate status: PENDING (see proposal.md) — do not
start T5 until `/design-sync` has set final `typeSignal`/`typeRelease`
values in this change's `design.md`.

- [x] T1. In `lib/features/tickets/domain/enums/ticket_type.dart`:
  - Add `TicketType.signal` and `TicketType.release` enum values, each
    with full dartdoc, exactly as specified in `design.md`'s Domain
    layer section.
  - Update `TicketTypeHierarchy._rank`'s switch to include both new
    values in the existing unranked (`null`) arm.
  - Update `canParent` to add the `signal`/`release` special case (returns
    `child == TicketType.chat`), placed after the existing `page`
    special case.
  - Add the new `isAlwaysRoot` getter with full dartdoc, exactly as
    specified in `design.md`.

- [x] T2. In `lib/features/tickets/presentation/cubit/tickets_cubit.dart`:
  replace `updateTicketParent`'s `if (ticket.type == TicketType.epic)`
  check with `if (ticket.type.isAlwaysRoot)`. Update the method's
  dartdoc to name `epic`/`signal`/`release` as the rejected types instead
  of just `epic`.

- [x] T3. In
  `lib/features/tickets/presentation/screens/create_ticket_screen.dart`:
  change the parent-field visibility condition from
  `_selectedType == TicketType.epic` to `_selectedType.isAlwaysRoot`.
  Update the class-level dartdoc sentence describing when the parent
  field is hidden to name all three types. No new public symbols
  introduced, so no new dartdoc beyond that update.

- [x] T4. In `lib/design_system/molecules/type_chip.dart` per design.md §1.5 TicketType → color resolver, §2.3 New-type renderings:
  - Add `TicketType.signal => l10n.ticketTypeSignal` and
    `TicketType.release => l10n.ticketTypeRelease` arms to
    `ticketTypeLabel`'s switch.
  - Add `TicketType.signal => c.typeSignal` and
    `TicketType.release => c.typeRelease` arms to `TypeChip.build`'s
    color switch, placed before the existing `_ => c.typeTask`
    catch-all so they're matched explicitly rather than falling through.

- [x] T5. In `lib/design_system/tokens/aion_colors.dart` per design.md §1.3 AionColors additions: add
  `typeSignal`/`typeRelease` `Color` fields to `AionColors` (with dartdoc
  matching the existing `typeTask`/etc. pattern), add both to the
  constructor's required params, and add both to the `arctic`/
  `obsidian` const instantiations using the final values recorded in
  this change's `design.md` by `/design-sync`.

- [x] T6. In `lib/l10n/app_en.arb`: add `"ticketTypeSignal": "Signal"` and
  `"ticketTypeRelease": "Release"` keys alongside the existing
  `ticketType*` entries. Run `flutter gen-l10n` (or let the next
  `flutter analyze`/`flutter run` trigger it) so the generated
  `AppLocalizations` getters exist before T4's code references them.

- [x] T7. Extend
  `test/features/tickets/domain/ticket_type_hierarchy_test.dart` from
  its existing 6×6 `canParent` matrix to a full 8×8 matrix (64 cases,
  including same-type pairs), adding every `(signal|release, *)` and
  `(*, signal|release)` pair per `design.md`'s Domain layer section.

- [x] T8. Extend
  `test/features/tickets/cubit/tickets_cubit_test.dart`'s
  `updateTicketParent` group: a `signal` ticket and a `release` ticket
  each attempting a non-null reparent short-circuit before calling the
  repository (`verifyNever`), emit `TicketsError(reason:
  invalidParent)` then `TicketDetailLoaded`, mirroring the existing
  `epic` rejection test case.

- [x] T9. Run `flutter analyze` and `flutter test` in the app repo; fix
  any fallout before considering this change implementation-complete.