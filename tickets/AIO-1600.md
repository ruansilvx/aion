---
ticketId: AIO-1600
type: story
status: done
priority: none
parentId: 4fe449e0-2f8b-4802-a071-e65fb4468910
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.322679
updatedAt: 2026-09-02T21:17:20.322679
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `TicketsCubit` wiring

- [x] T11. `tickets_cubit.dart` — add optional constructor param `NotificationRepository? notificationRepository` and `late final NotificationRepository? _notificationRepository` field, following `_commentRepository`'s exact pattern. Update the constructor's existing dartdoc list of optional dependencies. See design.md §4.1.
- [x] T12. `tickets_cubit.dart` — add `final ValueNotifier<int> unreadNotificationCount = ValueNotifier(0);` field and `Future<void> loadUnreadNotificationCount()` method, both fully dartdoc'd. Confirm whether `TicketsCubit` already overrides `close()`; add (or extend) an override calling `unreadNotificationCount.dispose()` before `super.close()`. See design.md §4.2 and its Open questions note. (`close()` already existed — extended it.)
- [x] T13. `tickets_cubit.dart` — add the private `Future<void> _recordNotification({required String ticketId, required NotificationKind kind, required String message})` helper, fully dartdoc'd. See design.md §4.3. Resolved: `TicketsCubit` had no pre-existing non-widget `AppLocalizations` resolution; since English is the only generated locale, a new `_l10n` field constructs `AppLocalizationsEn()` directly (documented on that field).
- [x] T14. `tickets_cubit.dart` — call `_recordNotification` from `_runCodingExecution`'s three terminal branches (PR-opened, verification-failed, hard-`catch` infra failure), each with the correct `NotificationKind` and a message built from the new `notificationExecutionPrOpened`/`notificationExecutionVerificationFailed`/`notificationExecutionFailed` l10n keys (T20). See design.md §4.4.
- [x] T15. `tickets_cubit.dart` — in `_runStageChatTurn`, add a local `failed` flag set in the existing `catch`, and call `_recordNotification` from the `finally` block (after worktree cleanup and `_inFlightStageAdvanceIds` removal) with `NotificationKind.stageAdvanceCompleted`/`.stageAdvanceFailed` accordingly, using the new `notificationStageAdvanceCompleted`/`notificationStageAdvanceFailed` l10n keys (T20). See design.md §4.4.
- [x] T16. `tickets_cubit.dart` — add `getRecentNotifications({int limit = 20})`, `markNotificationRead(String id)`, `markAllNotificationsRead()`, each fully dartdoc'd. See design.md §4.5.
- [x] T17. `tickets_cubit.dart` — add `executionPrSubLine` (`String?`) to `TicketDetailLoaded` (new field, `copyWith` updated) and compute it inside `getTicketById`'s existing per-ticket computation block via `NotificationRepository.getMostRecentForTicket`, alongside `executionAwaitingReview`. See design.md §4.6.
- [x] T18. `lib/features/tickets/presentation/cubit/tickets_state.dart` — add `TicketDetailLoaded.executionPrSubLine` field (default `null`), update its dartdoc and `copyWith`.