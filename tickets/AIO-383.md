---
ticketId: AIO-383
type: story
status: done
priority: none
parentId: aec5e777-0cd8-45ff-b1d3-ffff29993d7f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.170179
updatedAt: 2026-09-02T21:17:20.170179
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T23. `test/features/tickets/cubit/tickets_cubit_test.dart` — new
      cases: `advanceSddStage` returns the chat id before any
      `agentClient.run` call resolves (i.e. it doesn't wait on the
      stubbed model call to complete — assert ordering via a
      manually-completed `Future`/`Completer` in the test double, not a
      timing-based assertion); a second `advanceSddStage` call for a
      ticket already in-flight returns `null` and does not call
      `repository.createTicket` again; `_runStageChatTurn`'s hard-failure
      path posts a `"Stage advance failed: "` comment and emits
      `TicketsError(reason: sddStageAdvanceFailed)`;
      `_inFlightStageAdvanceIds` is cleared on both the success and
      failure paths (assert via a subsequent `getTicketById`'s
      `isAdvancingStage` being `false`). Update existing `advanceSddStage`
      tests that previously asserted on the full-turn-awaited return
      value — assert against the new fast-resolving contract instead.
- [x] T24. Same file — new cases for `_refreshInFlightBoardState`: after
      `updateTicketStatus` moves a Task to `inProgress` while
      `TicketsLoaded` is the current state,
      `state.inFlightExecutionIds` contains that Task's id; a second
      Task moved to `inProgress` while the first is still running
      appears in `state.executionQueuePositions` at position `1`; both
      clear once `_runCodingExecution` completes (stub the agent/git
      clients to resolve quickly). Mirror the same three assertions for
      `advanceSddStage`/`inFlightAdvanceIds`. Assert
      `_refreshInFlightBoardState` is a no-op (no new emission) when the
      cubit's last state was `TicketDetailLoaded`, not `TicketsLoaded`.
- [x] T25. Same file — `_computeStageAdvanceFailure` cases: no stage
      chat yet → not failed; most recent comment `ai`-authored → not
      failed; most recent comment `"Stage advance failed: ..."` →
      failed, retriable; a stage chat with a non-`ai` most-recent
      comment and the ticket not in `_inFlightStageAdvanceIds`
      (simulating a post-restart orphan) → the fixed "ended without a
      clear result" message, retriable.
- [x] T26. `test/features/tickets/screens/tickets_board_view_test.dart`
      (or the existing widget-test file covering `TicketBoardCard`, if
      one exists under a different path — locate it first rather than
      assuming a new file) — a card for a Task whose id is in
      `TicketsLoaded.inFlightExecutionIds` renders the running badge; a
      queued Task renders its position; an Epic/Story in
      `inFlightAdvanceIds` renders the advancing badge; a card for a
      ticket in none of the three renders no badge (today's exact
      output).
- [x] T27. `test/core/routing/workspace_nav_shell_test.dart` (new file if
      none exists) — emitting `TicketsError(reason:
      codingExecutionBlocked)` (or any other non-null-reason value) from
      a provided `TicketsCubit` shows an `AppToast` with the matching
      `ticketsErrorMessage` text; emitting one with `reason: null` shows
      no toast from this listener.
- [x] T28. `test/features/tickets/screens/create_ticket_screen_test.dart`
      (locate existing file first) — confirms the narrowed listener
      (T21) still toasts a raw/unclassified `TicketsError` but no longer
      double-toasts a classified one (paired with T27's shell-level
      coverage, so the split doesn't leave a gap or a duplicate).