---
ticketId: AIO-860
type: task
status: done
priority: none
parentId: 0e493f41-ce84-4138-9357-160e2d626e56
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.226735
updatedAt: 2026-09-02T21:17:20.226735
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T21. `features/providers/presentation/screens/settings_screen.dart`

— new `_ExecutionContextCapSection` widget (plus a private
`_ExecutionContextCapHelperRow` for the three helper-line variants),
placed under the "MODELS" eyebrow below the Execution model row
(design.md §16), implementing the pasted Claude Design export per
design.md "Execution Context Cap: Settings Component Spec" §1
(placement/ordering) and §2 (`ExecutionContextCapField` structure,
states, helper-row variants). Use `needsRepairTint`/
`needsRepairBorderTint` (existing `AionColorsHubTokens` helpers)
for the clamp-chip fill/border, not the export's proposed
`warningTint`/`warningBorderTint` — see the design gate note in
proposal.md. `AppTextField` (`design_system/atoms/app_text_field.dart`)
gained optional `keyboardType`/`inputFormatters` passthrough params
(both default `null`, every existing call site unaffected) since
the atom didn't previously expose them and the digits-only numeric
entry this field needs depends on them. Dartdoc the new widget
classes and the atom's two new fields.