---
ticketId: AIO-935
type: story
status: done
priority: none
parentId: a673aaf3-ea52-4157-b776-66bd3bfdb025
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.240839
updatedAt: 2026-09-02T21:17:20.240839
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tasks: Idea, Known Gap, and Open Question ticket types

Ordered checklist. Every task touching a `.dart` file includes its
documentation (dartdoc + file header, where new) in the same task — not
a follow-up. Tasks marked **[design]** render UI whose exact layout is
set by the Claude Design pass (see `proposal.md`'s Design gate) —
`/design-sync` will annotate these with the relevant `design.md`
section reference before `/apply` may proceed.

- [x] T1. In `lib/features/tickets/domain/enums/ticket_type.dart`:
  replace `TicketType.signal` with `TicketType.idea`, `TicketType.knownGap`,
  `TicketType.openQuestion`, each fully dartdoc'd per `design.md` §1.1.
  Update `TicketTypeHierarchy._rank` (three `null` arms replacing the one
  `signal` arm), `canParent` (the `this == TicketType.signal ||
  this == TicketType.release` branch widens to also match `idea`/
  `knownGap`/`openQuestion`, same `child == TicketType.chat` body), and
  `isAlwaysRoot` (all three new values included). Update the class-level
  dartdoc references that name `signal` explicitly.

- [x] T2. Add new file
  `lib/features/tickets/domain/entities/gap_or_question_ref.dart`: the
  `GapOrQuestionRef` record typedef (`ticket`, `raisedOn`, `linkId`) per
  `design.md` §1.2, with a file header and full dartdoc, mirroring
  `linked_ticket_ref.dart`'s existing style.

- [x] T3. In `lib/features/tickets/presentation/cubit/tickets_state.dart`:
  add `gapsAndOpenQuestions` (`List<GapOrQuestionRef>`, default `const
  []`) to `TicketDetailLoaded`'s constructor, fields, dartdoc, and
  `props`, per `design.md` §3.5.

- [x] T4. In `lib/core/database/app_database.dart`: bump `schemaVersion`
  to `11`, add the `if (from < 11)` migration branch running
  `UPDATE tickets SET type = 'idea' WHERE type = 'signal'` per
  `design.md` §2.1, and update the class dartdoc's version-history list
  with a Version 11 entry describing the data-only migration.

- [x] T5. In `lib/features/tickets/data/models/ticket_model.dart`:
  update `TicketsTable.suggestedType`'s dartdoc — "meaningful only for
  `TicketType.signal` tickets" → "meaningful only for `TicketType.idea`
  tickets" (text-only, no column/schema change).

- [x] T6. In `lib/features/tickets/presentation/cubit/tickets_cubit.dart`:
  - Rename `promoteSignal` → `promoteIdea` per `design.md` §3.1: same
    signature/body, guard checks against `TicketType.idea`, dartdoc
    updated to say "idea"/"Ideas" throughout.
  - Add `createGapOrQuestion` per `design.md` §3.2, fully dartdoc'd.
  - Add `reclassifyIdea` per `design.md` §3.3, fully dartdoc'd.
  - Extend `loadDocumentRelations` per `design.md` §3.4: build
    `subtreeIds` via the existing `_descendantIds` helper, query
    `_linkRepository.getLinksByTypes([TicketLinkType.relatesTo])` once,
    populate `gapsAndOpenQuestions`, and include it in the emitted
    `TicketDetailLoaded`. Update the method's dartdoc to describe the
    new aggregation.
  - `runCodebaseSummarization`: both the run-record ticket and every
    finding ticket now use `TicketType.idea`; update the method's
    dartdoc's `signal` references to `idea` and its `promoteSignal`
    reference to `promoteIdea`.

- [x] T7. In
  `lib/features/tickets/presentation/widgets/ticket_overflow_menu.dart`
  per `design.md` §4.4 and Component Spec §4 (Overflow-menu reclassify
  rows + target picker):
  - Every `TicketType.signal` comparison and `promoteSignal` call
    becomes `TicketType.idea`/`promoteIdea`.
  - Add "Change to Known Gap"/"Change to Open Question" rows to
    `_RootMenu`, gated on `ticketType == TicketType.idea` alongside the
    existing promote rows. Wire selection to a searchable existing-
    ticket picker (reusing the `TicketLinkPicker`-as-picker mechanic the
    promote flow already uses, but with no "create new" option) calling
    `TicketsCubit.reclassifyIdea`.
  - Update `_RootMenu`'s row-0/autofocus conditional (`ticketType !=
    TicketType.signal` → `!= TicketType.idea`).

- [x] T8. In
  `lib/features/tickets/presentation/screens/create_ticket_screen.dart`:
  widen the type-dropdown filter to `.where((type) => type !=
  TicketType.page && type != TicketType.knownGap && type !=
  TicketType.openQuestion)` per `design.md` §4.5.

- [x] T9. In
  `lib/features/tickets/presentation/widgets/ticket_filter_popover.dart`:
  update the exhaustive type→label switch (`TicketType.signal =>
  c.typeSignal` arm replaced by `idea`/`knownGap`/`openQuestion` arms)
  and the Type-group's hardcoded exclusion list to carry `signal`'s
  existing exclusion forward onto `idea`/`knownGap`/`openQuestion` per
  `design.md` §4.6 and Component Spec §1.6 (`TicketType` resolver +
  labels).

- [x] T10. In `lib/design_system/molecules/type_chip.dart`: update both
  exhaustive `TicketType` switches (label and color) per `design.md`
  §4.7 and Component Spec §1.6 (`TicketType` resolver + labels).

- [x] T11. In `lib/design_system/molecules/linked_tickets_section.dart`:
  update `_typeColor`'s exhaustive switch per `design.md` §4.7 and
  Component Spec §1.6 (`TicketType` resolver + labels).

- [x] T12. **[design]** In
  `lib/features/tickets/presentation/widgets/ticket_metadata_section.dart`
  per `design.md` §4.1 and §4.3, and Component Spec §2
  (`GapsAndOpenQuestionsSection`) and §5 ("Raised on" indicator):
  - Widen the Documentation-mode exclusion gate from `chat`/`signal`/
    `release` to `chat`/`idea`/`knownGap`/`openQuestion`/`release`.
  - Wire `GapsAndOpenQuestionsSection` (T14) into the Documentation-mode
    block for every non-excluded type, reading
    `state.gapsAndOpenQuestions`, its "+ Add" trigger opening the
    creation popover (T15) with `targetTicketId: ticket.id`.
  - Add the "Raised on" read-only indicator for `knownGap`/
    `openQuestion` tickets' own detail screen (their single outgoing
    `relatesTo` link's target), tap-to-navigate.

- [x] T13. **[design]** In
  `lib/features/pages/presentation/screens/page_detail_screen.dart` per
  `design.md` §4.2 and Component Spec §2 (`GapsAndOpenQuestionsSection`):
  wire `GapsAndOpenQuestionsSection` (T14) after the
  existing `LinkedTicketsSection`/`BacklinksSection` pair, same
  `state.gapsAndOpenQuestions` source, same creation-popover wiring.

- [x] T14. **[design]** Add new file
  `lib/design_system/molecules/gaps_and_open_questions_section.dart`:
  `GapsAndOpenQuestionsSection` per `design.md` §6.1 and Component
  Spec §2 (full — §2.1–§2.6: container, header, row, rollup, empty
  state, states) — header label/count-pill/trailing-slot, empty state,
  per-`GapOrQuestionRef` row (type-color dot, ticket key + title,
  tap-to-navigate, "on `<raisedOn.title>`" annotation when rolled up
  from a descendant), full dartdoc and file header.

- [x] T15. **[design]** Add new file for the gap/open-question creation
  popover (exact filename/class name set by the Claude Design pass —
  default `lib/design_system/molecules/raise_gap_or_question_picker.dart`)
  per `design.md` §6.2 and Component Spec §3 (`RaiseGapOrQuestionPicker`
  — full §3.1–§3.3.5; the Claude Design pass confirmed the class name
  and filename match the default guessed here): `Overlay`-anchored,
  type-choice menu → compact title/description form, calling
  `TicketsCubit.createGapOrQuestion`, full dartdoc and file header.

- [x] T16. **[design]** In
  `lib/design_system/tokens/aion_colors.dart` per `design.md` §7 and
  Component Spec §1 (New color tokens — §1.1–§1.4 for exact hex
  values): rename `typeSignal` → `typeIdea` and its `signalFill`/
  `signalBorderTint`/`signalIconTint`/`signalChipTint` tint-method
  family → `ideaFill`/`ideaBorderTint`/`ideaIconTint`/`ideaChipTint`
  (same formulas, both theme instances); add new required tokens
  `typeKnownGap`/`typeOpenQuestion` (both themes) with values from the
  Design Gate export (Arctic `#2F9E3F`/`#C6397D`, Obsidian
  `#54C85C`/`#EE72AE`).

- [x] T17. In
  `lib/features/tickets/presentation/widgets/codebase_analysis_banner.dart`:
  update every `typeSignal`/`signalFill`/`signalBorderTint`/
  `signalIconTint`/`signalChipTint` reference to the T16 renames, per
  Component Spec §1.3 (confirms the rename is value-preserving —
  `CodebaseAnalysisBanner`'s accent is visually unaffected).

- [x] T18. In
  `lib/features/tickets/presentation/cubit/inbox_cubit.dart` per
  `design.md` §5:
  - `_brainDumpPrompt`: `SIGNAL:` marker → `IDEA:`.
  - `_parseBrainDumpBlocks`: `signalMatch`/`^SIGNAL:` → `ideaMatch`/
    `^IDEA:`.
  - `_materializeBrainDumpSignals` → `_materializeBrainDumpIdeas`,
    creates `TicketType.idea`.
  - `_assembleWhatNextContext`: remove the `pages`/`knownGaps`/
    `_extractKnownGapsSection` markdown-scanning path entirely; add a
    direct query for `TicketType.knownGap`/`TicketType.openQuestion`
    tickets (each resolved against its own `relatesTo` target for
    context) as priority tier 2; rename the `openSignals` block to query
    `TicketType.idea` as priority tier 3. Delete
    `_extractKnownGapsSection` once nothing calls it.

- [x] T19. In `lib/l10n/app_en.arb` per `design.md` §8: rename
  `ticketTypeSignal` → `ticketTypeIdea`, `codebaseAnalysisOfferBodySignal`
  → `codebaseAnalysisOfferBodyIdea`, update `codebaseAnalysisDoneTitle`'s
  "signal ticket(s)" copy to "idea ticket(s)". Add `ticketTypeKnownGap`,
  `ticketTypeOpenQuestion`, `documentationGapsAndQuestionsLabel`,
  `documentationGapsAndQuestionsEmpty`, the rolled-up-row "raised on
  {title}" pattern string, `ticketOverflowReclassifyToKnownGap`,
  `ticketOverflowReclassifyToOpenQuestion`, and the creation popover's
  menu-row/form copy.

- [x] T20. Update
  `test/features/tickets/domain/ticket_type_hierarchy_test.dart`:
  replace every `signal`-keyed case with `idea`/`knownGap`/
  `openQuestion` across `canParent`'s full matrix and `isAlwaysRoot`.

- [x] T21. Extend the `tickets_cubit` test suite: `promoteIdea`
  regression (same coverage the old `promoteSignal` tests had, renamed);
  `createGapOrQuestion`'s three rejection paths (wrong type, missing
  target, nonexistent target) plus its happy path; `reclassifyIdea`'s
  equivalent validation set (including "current type isn't idea") plus
  its happy path (type mutated, link created, id/createdAt preserved);
  `loadDocumentRelations`'s recursive aggregation (a gap raised on a
  grandchild Task surfaces on the Epic three levels up with the correct
  `raisedOn`).

- [x] T22. Update the `inbox_cubit` test suite: brain-dump block parsing
  against the renamed `IDEA:` marker; `_assembleWhatNextContext`'s new
  gap/question-tickets-first assembly (remove markdown-scanning test
  cases, since `_extractKnownGapsSection` no longer exists; add cases
  for the new direct `knownGap`/`openQuestion` query).

- [x] T23. Run
  `flutter pub run build_runner build --delete-conflicting-outputs` in
  the app repo — no new drift columns are added by this change, but
  regenerate defensively in case any generated code references the
  removed `TicketType.signal` enum value by name.

- [x] T24. Run the non-Material/widget-composition grep checks per
  `flutter-conventions.md` and confirm no new violations were
  introduced by T14/T15's new widgets.

- [x] T25. Run `flutter analyze` and `flutter test` in the app repo; fix
  any fallout before considering this change implementation-complete.