---
ticketId: AIO-2426
type: story
status: done
priority: none
parentId: 60978800-50b4-47e4-a9fb-70c304ad0d3f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.432867
updatedAt: 2026-09-02T21:17:20.432867
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation — list & board screens

- [x] T19 — `lib/features/tickets/presentation/screens/tickets_list_screen.dart`:
  add a "Select" toggle icon button to the header row next to
  `_ViewModeToggle` (same `_ViewModeIcon`-shaped styling), calling
  `context.read<TicketSelectionCubit>().enter()`, hidden when the
  rendered ticket list is empty. Add a Trash-entry icon button
  (neutral, not danger-colored) navigating to `context.go('/tickets/trash')`.
  Per design.md §7. Visual spec: design.md's Specification §2 Select-mode
  toggle button (header), §3 Trash-entry icon button (header).
- [x] T20 — Same file: replace the `Positioned` `AppFab` with
  `TicketSelectionBar` (new widget, T21) when
  `context.watch<TicketSelectionCubit>().state.isActive` is `true`.
  Wire Cancel to `context.read<TicketSelectionCubit>().clear()`,
  select-all to `context.read<TicketSelectionCubit>().selectAll(
  currentTicketIds)` (the currently visible/filtered list), and Delete
  to: `previewTrashCount(selectedIds.toList())` →
  `showAppConfirmDialog` (title `ticketBulkTrashConfirmTitle` on the
  original selection count, message `ticketTrashConfirmMessage` on the
  preview total, `isDestructive: true`) → if confirmed,
  `context.read<TicketsCubit>().trashTickets(selectedIds.toList())`.
  Per design.md §7. Visual spec: design.md's Specification §4
  TicketSelectionBar, §6.1 Confirm dialog — Reversible tone (use the
  existing `AppConfirmDialog` shell's own width/shadow/scrim per that
  section's design-sync note — do not introduce new dialog dimensions).
- [x] T21 — Create `lib/features/tickets/presentation/widgets/ticket_selection_bar.dart`:
  `TicketSelectionBar` — selected count (`ticketSelectionCountLabel`,
  ICU plural), select-all/deselect-all toggle
  (`ticketSelectionSelectAllAction`/`ticketSelectionDeselectAllAction`),
  Cancel, destructive Delete `AppButton` (`ticketSelectionDeleteAction`,
  disabled when `selectedIds` is empty). File header + full dartdoc.
  Per design.md §7. Visual spec: design.md's Specification §4
  TicketSelectionBar (§4.1–§4.5 for each sub-control's states).
- [x] T22 — Same file (`tickets_list_screen.dart`): `BlocBuilder
  <TicketsCubit, TicketsState>` switch — rename the `TicketDeleting()`
  arm to `TicketTrashing()` and add a `TicketsBatchTrashing()` arm
  (both `Center(child: AppSpinner())`); add
  `TicketsBatchTrashed(:final tickets)` to the list-rendering
  OR-pattern. In the existing `BlocListener<TicketsCubit,
  TicketsState>`: remove the `hasChildren` branch entirely; add a
  branch for `TicketsBatchTrashed(:final trashedCount)` that shows
  `AppToast.show(context, context.l10n.ticketBulkTrashSummaryToast(
  trashedCount))` then calls
  `context.read<TicketSelectionCubit>().clear()`. Per design.md §7.
- [x] T23 — Same file: `TicketOverflowMenu`'s delete call sites in this
  file (none directly — `TicketOverflowMenu` itself is updated in T25)
  — skip; `TicketListTile` — read `isSelectionActive`/`isSelected` via
  `context.select` on `TicketSelectionCubit`. When active: render a
  leading `AppCheckbox(value: isSelected)` before the id badge,
  override the row's `onTap`/`ActivateIntent` to call
  `context.read<TicketSelectionCubit>().toggle(ticket.id)` instead of
  navigating, and omit `TicketOverflowMenu` from the trailing slot.
  Update the class dartdoc. Per design.md §7 (selection-mode rendering,
  carried over unchanged from the original bulk-select design). Visual
  spec: design.md's Specification §5 Ticket row/card — Select-mode
  variant, §5.2 List row.
- [x] T24 — `lib/features/tickets/presentation/screens/tickets_board_view.dart`:
  `TicketBoardCard` — read `isSelectionActive` via `context.select` on
  `TicketSelectionCubit`. When active, skip the `Draggable`/
  `LongPressDraggable` wrapper and render the plain `card` widget with
  `onTap`/`ActivateIntent` overridden to
  `context.read<TicketSelectionCubit>().toggle(ticket.id)`.
  `_CardVisual` — same selection awareness as `TicketListTile`: a
  leading `AppCheckbox` in the card's top row, replacing
  `TicketOverflowMenu`/`MoveToStatusMenu` (neither rendered) while
  active. Update both classes' dartdocs. Per design.md §7. Visual spec:
  design.md's Specification §5.3 Board card — Select mode.