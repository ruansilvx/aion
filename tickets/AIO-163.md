---
ticketId: AIO-163
type: story
status: done
priority: none
parentId: 240c0794-e9e6-4ce1-bf87-4ebdf249668b
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.146722
updatedAt: 2026-09-02T21:17:20.146722
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests — `TicketsCubit`

- [x] T11. Edit `test/features/tickets/cubit/tickets_cubit_ticket_crud_test.dart`
      — remove the `'log_time dispatches to _handleLogTimeToolCall'`
      test and the whole `'log_time — no AutomationConfidence gate'`
      group. Update the file header comment and the "ticket a
      `create_ticket`/`add_link`/`log_time` tool call is..." doc
      comment near the top to drop `log_time`. Leave
      `create_ticket`/`add_link` coverage untouched.
- [x] T12. Edit `test/features/tickets/cubit/tickets_cubit_test.dart` —
      update the `_toolsFor`/`agentClient.run` assertion at the
      `_runCodingExecution resolves ModelPhase.execution` test (the one
      asserting `containsAll(['create_ticket', 'add_link', 'log_time'])`)
      to expect only `['create_ticket', 'add_link']`. Also add
      `ticketRepository`/`addTimeSpent` stubbing wherever this file's
      existing `TicketsCubit` fixture drives a real `runChatTurn` call
      (via the mocked `agentClient.run`), so the new time-logging call
      doesn't throw against an unstubbed mock in previously-passing
      tests.