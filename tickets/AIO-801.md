---
ticketId: AIO-801
type: story
status: done
priority: none
parentId: b6289537-c4a4-4a03-997a-f046a69c31b8
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.222704
updatedAt: 2026-09-02T21:17:20.222704
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data layer

- [x] T3. `lib/features/tickets/data/daos/ticket_dao.dart` — MODIFY: add
  `getTicketsByParent`/`getAllTicketsByType` query implementations
  (type-filtered via `.where((t) => t.type.isIn(...))`,
  `deletedAt.isNull()`, and for `getTicketsByParent` also
  `parentId.equals(parentId)` or `parentId.isNull()` when `parentId` is
  null). Dartdoc per method.
- [x] T4. `lib/features/tickets/data/repositories/drift_ticket_repository.dart`
  — MODIFY: implement the two new `TicketRepository` methods by
  delegating to `TicketDao`, mapping `TicketData` rows to `Ticket`
  entities via the existing model mapper. Dartdoc per method.
- [x] T5. `lib/features/tickets/data/services/ticket_document_search_service.dart`
  — ADD: `TicketDocumentSearchService` class per design.md — constructor
  taking `EmbeddingProvider` and `TicketRepository`, `search(String
  query, {int limit = 20})` returning cosine-ranked `List<Ticket>` scoped
  to `page`/`resource`, private `_cosineSimilarity` helper. Full class
  and method dartdoc, file header comment.
- [x] T6. `lib/features/tickets/data/services/document_parent_migration_service.dart`
  — ADD: `DocumentParentMigrationService` class per design.md —
  constructor taking `TicketRepository`, `TicketLinkRepository`, and a
  `SharedPreferences` instance; `migrateIfNeeded()` converts
  resource/page `parentId` → `epic`/`story`/`task` relationships into
  `relatesTo` `TicketLink`s and clears `parentId`, gated by a persisted
  `hasMigratedDocumentParentsToLinks` flag. Full dartdoc, file header
  comment.