---
ticketId: AIO-565
type: story
status: done
priority: none
parentId: f6538d5c-9308-4317-b18d-a288d5f56cfc
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.193569
updatedAt: 2026-09-02T21:17:20.193569
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `WorkflowConfigCubit`

- [x] T14. New file `presentation/cubit/workflow_config_state.dart` —
      `WorkflowConfigState` sealed class family:
      `WorkflowConfigInitial`, `WorkflowConfigLoaded` (`statuses`,
      `designStagesEnabled`, `stageDisplayNameOverrides`),
      `WorkflowConfigError` (`message`, `previous`) (design.md §4.1). Full
      dartdoc on the class and every field per project.md's
      Cubit-state-class documentation minimum.
- [x] T15. New file `presentation/cubit/workflow_config_cubit.dart` —
      `WorkflowConfigCubit(WorkflowStatusRepository, SddStageConfigRepository)`:
      `load()`, `createStatus`/`updateStatus`/`deleteStatus`/
      `reorderStatuses`, `setDesignStagesEnabled`,
      `setStageDisplayNameOverride` (design.md §4). Implements the two
      Cubit-level invariants — status-name uniqueness within scope, and
      every `WorkflowStatusRole` always resolving to exactly one
      shared-base status (reject the create/update/delete that would
      break either) — plus the delete-blocked-while-in-use check. Full
      dartdoc, each method explaining its rejection conditions and what
      state it emits on success/failure.