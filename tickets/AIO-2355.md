---
ticketId: AIO-2355
type: story
status: done
priority: none
parentId: cf463d53-a402-4803-af23-5ef7bb630573
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.424721
updatedAt: 2026-09-02T21:17:20.424721
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation — screen

- [x] T8. Modify `aion/lib/features/tickets/presentation/screens/tickets_list_screen.dart`
      per design.md §1 List-footer loading state, §2 List-footer error/retry state,
      §3 Board-mode "Load more" button, §4 List pagination behavior, §5 Board pagination behavior:
      - Add `ScrollController _scrollController`, created in `initState`
        (`addListener(_handleScroll)`), disposed in `dispose`
        (`removeListener` + `dispose()`).
      - Add private `_handleScroll()` per design.md §5 (400px lookahead,
        calls `context.read<TicketsCubit>().loadMoreTickets()`).
      - Extend `_currentTickets(TicketsState)` with `TicketsLoadingMore`
        and `TicketsLoadMoreFailed` cases.
      - Add private `_hasMore(TicketsState)` helper per design.md §5.
      - In `_buildTicketsBody`'s list-mode branch: attach `controller:
        _scrollController` to the `ListView.separated`; extend
        `itemCount` by 1 when `state is TicketsLoadingMore ||
        state is TicketsLoadMoreFailed`; render the new
        `_TicketListFooter` widget (see T9) at that extra index.
      - Add the board-mode "Load more" button: a `Positioned` at
        `left: 18, bottom: 24`, visible when `_viewMode ==
        _TicketViewMode.board && _hasMore(state) && !selection.isActive`
        (mirrors the existing `AppFab`/`TicketSelectionBar` visibility
        branch structure), calling
        `context.read<TicketsCubit>().loadMoreTickets()` on tap. Use
        existing design tokens only (`AionColors`/`AionText`/`AionRadius`/
        `AionShadows`, no raw values) and `context.l10n` for its label —
        add the new label string to `lib/l10n/app_en.arb` per
        flutter-conventions.md's Localization rules.
      - Full dartdoc on every new field/method/widget; update the class
        header dartdoc if its stated behavior changes.
- [x] T9. Add private `_TicketListFooter` widget per design.md §1
      List-footer loading state and §2 List-footer error/retry state,
      in the same file (`tickets_list_screen.dart`, alongside
      `TicketListTile`/`PriorityBadge`/etc.) — renders a centered `AppSpinner` when
      `isLoadingMore`, or a tappable retry row (icon + `context.l10n`
      text, `Semantics(button: true)`, `FocusableActionDetector` with an
      `ActivateIntent` handler per this file's existing interactive-widget
      pattern) when the last load-more attempt failed. Add its retry
      label string to `lib/l10n/app_en.arb`. Full dartdoc.
- [x] T10. Run `flutter gen-l10n` after T8/T9's `.arb` additions to
      regenerate `AppLocalizations` (gitignored, not committed).