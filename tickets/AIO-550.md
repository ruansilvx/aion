---
ticketId: AIO-550
type: story
status: done
priority: none
parentId: f6538d5c-9308-4317-b18d-a288d5f56cfc
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.193569
updatedAt: 2026-09-02T21:17:20.193569
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Domain — new types

- [x] T1. New file `domain/enums/workflow_status_role.dart` —
      `WorkflowStatusRole` enum (`executionTrigger | reviewReady | done`),
      dartdoc per design.md §1.1 explaining exactly which existing
      `TicketsCubit` gate/trigger each value replaces.
- [x] T2. New file `domain/entities/workflow_status.dart` — `WorkflowStatus`
      entity (`Equatable`): `id`, `name`, `displayName`, `ticketType`
      (`TicketType?`), `sortOrder`, `role` (`WorkflowStatusRole?`). Const
      constructor, `props`, full dartdoc per design.md §1.2 including the
      name-uniqueness-within-scope contract and the
      role-restricted-to-shared-base-only constraint.
- [x] T3. New file `domain/entities/default_workflow_statuses.dart` — the
      `defaultWorkflowStatuses` const list reproducing today's exact
      hardcoded behavior (design.md §1.3's table). Dartdoc explaining
      this is what a brand-new project (and every pre-existing project on
      schema-15 upgrade) seeds.
- [x] T4. `domain/entities/ticket.dart` — change `status`'s type from
      `TicketStatus` to `String`; rewrite its dartdoc to describe it as a
      project-defined status name resolved against
      `WorkflowStatusRepository`/`WorkflowConfigCubit` (design.md §1.4).
      Update `copyWith`'s `status` parameter type to match.
- [x] T5. Delete `domain/enums/ticket_status.dart`.
- [x] T6. New file `domain/repositories/sdd_stage_config_repository.dart`
      — `SddStageConfigRepository` interface exactly as design.md §1.5
      specifies (`getDesignStagesEnabled`/`setDesignStagesEnabled`,
      `getDisplayNameOverride`/`setDisplayNameOverride`). Full dartdoc,
      citing `AutomationSettingsRepository` as the precedent this mirrors.
- [x] T7. New file `domain/repositories/workflow_status_repository.dart`
      — `WorkflowStatusRepository` interface exactly as design.md §1.6
      specifies (`getAll`/`create`/`update`/`delete`/`reorder`/
      `seedDefaultsIfEmpty`). Dartdoc explaining this is a dumb
      persistence layer — no validation, which lives in
      `WorkflowConfigCubit`.