---
ticketId: AIO-847
type: story
status: done
priority: none
parentId: de51291e-d1f0-4162-ae92-8785a5faf823
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.226735
updatedAt: 2026-09-02T21:17:20.226735
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `TicketsCubit` — reuse + handoff

- [x] T11. `features/tickets/presentation/cubit/tickets_cubit.dart` —
      constructor gains `ExecutionContextCapRepository?
      executionContextCapRepository` (design.md §7), following the
      existing optional-dependency pattern exactly (stored field,
      appended to the class-level constructor dartdoc's running list).
- [x] T12. Same file — new shared `_mostRecentExecutionChat(String
      taskId)` helper (design.md §8); `_executionSucceededWithPr` and
      `_computeExecutionFailure` rewritten to call it instead of each
      duplicating the query/filter/sort inline. Dartdoc the new helper;
      update both callers' dartdocs to reference it.
- [x] T13. Same file — new `_resolveExecutionChat`, `_createExecutionChat`,
      `_executionChatTitle`, `_executionChatCount`,
      `_executionChatOverCap`, `_effectiveExecutionContextCap`, and the
      `_handoffThresholdRatio = 0.9` constant (design.md §9). Dartdoc
      each.
- [x] T14. Same file — new `_handoffExecutionChat`,
      `_assembleChatTranscript`, `_assembleHandoffContext` (design.md
      §10). Dartdoc each.
- [x] T15. Same file — `_assembleExecutionContext` gains the optional
      `handoffSummary` parameter and its prepended block (design.md
      §11). Update its dartdoc.
- [x] T16. Same file — `_runCodingExecution`'s opening
      chat-acquisition block replaced with a `_resolveExecutionChat`
      call; every `persistedChat` reference in the method renamed to
      `chat`; the `_assembleExecutionContext` call site passes
      `handoffSummary` (design.md §12). Rewrite the method's
      class-level dartdoc to describe find-or-create-or-handoff instead
      of unconditional creation.
- [x] T17. Same file — `retryCodingExecution`'s dartdoc updated per
      design.md §13 (no code change).