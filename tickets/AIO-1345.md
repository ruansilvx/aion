---
ticketId: AIO-1345
type: task
status: done
priority: none
parentId: a3712bc7-2bf9-41e0-a148-2446cee9def7
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.290783
updatedAt: 2026-09-02T21:17:20.290783
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T3. In `aion/lib/design_system/molecules/selection_menu.dart`:

replace the per-item `GestureDetector` wrapper
(`selection_menu.dart:154-174`) with `OverlayMenuItem` per
`design.md` §3 — `onTap` calls `widget.onSelected(item)` then
`_removeOverlay()` exactly as today, `semanticsLabel:
widget.itemLabel(item)`, `child` unchanged (the existing
`builder != null ? builder(...) : Text(...)` ternary, inside its
existing `Padding`). Update the class dartdoc to note per-item
rows are now keyboard-focusable/activatable, not just the
trigger.
Per design.md's Claude Design export §1 and §5 "State summary"
(the "Neutral" row) — a `SelectionMenu` item is a plain,
non-accented row.