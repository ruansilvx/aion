---
ticketId: AIO-1342
type: story
status: done
priority: none
parentId: 91227490-6eb5-4901-ad45-64e339e65906
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.290783
updatedAt: 2026-09-02T21:17:20.290783
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `TicketOverflowMenu` integration

- [x] T2. In `aion/lib/features/tickets/presentation/widgets/ticket_overflow_menu.dart`:
      - `_MenuActionRow` — replace its `Semantics`/`GestureDetector`
        wrapper with `OverlayMenuItem` per `design.md` §2, keeping its
        existing `Padding`/`Row` content as `child` unchanged.
      - `_PromoteRootRow` — replace its `Semantics`/`GestureDetector`/
        `DecoratedBox` wrapper with `OverlayMenuItem` per `design.md`
        §2, passing `restingColor: suggested ? c.accentTint(accent,
        t.isDark) : null` and deleting the row's own now-redundant
        `DecoratedBox`/ternary. Keep its existing `Padding`/`Row`
        content (including the conditional `_SuggestedPill`) as `child`
        unchanged.
      - `_PromoteChooser`'s "Create new epic/bug" row — replace its
        `Semantics`/`GestureDetector` wrapper with `OverlayMenuItem`
        per `design.md` §2. Leave the "Link to existing" row and
        `_ChooserHeader`'s back-caret button untouched.
      - Update the class-level dartdoc (currently states rows render
        "distinct default/hover/keyboard-focused/pressed/open fills"
        for the trigger only) to note that the action-list rows
        themselves are now keyboard-focusable/activatable via
        `OverlayMenuItem` too.
      Per design.md's Claude Design export §2 "Context: plain row"
      (`_MenuActionRow`, `_PromoteChooser`'s "Create new" row) and §3
      "Context: tinted suggested row" (`_PromoteRootRow`) — supersedes
      this task's own `restingColor: c.accentTint(...)` sketch above;
      use the export's `accent`/`restingTinted` params instead.