---
ticketId: AIO-2754
type: story
status: done
priority: none
parentId: 5f7e47fe-ba4d-401d-a316-d51b2d681a8d
estimate: null
timeSpent: null
createdAt: 2026-09-09T01:24:54.150
updatedAt: 2026-09-09T01:24:54.150
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tasks: scope-exploring-stage-instructions

- [x] 1. In `aion/lib/features/tickets/presentation/cubit/tickets_cubit.dart`,
      add a new `if (stage == SddStage.exploring) { ... }` branch as the
      **first** branch of the `if`/`else if` chain inside
      `_assembleStageContext` (~line 7070, currently starting with
      `if (stage == SddStage.verifying || stage == SddStage.archived)`),
      appending the read-only-investigation instructional text specified in
      `design.md`'s "Change" section verbatim (or equivalent wording — match
      its meaning exactly: no file edits/creates/deletes, no implementation
      plan, no code, no request for repo write access, end with a written
      exploration summary). Convert the existing leading `if` into an
      `else if` so the chain reads `exploring → (verifying||archived) →
      designBrief → designSync → proposed`. Do not change any other
      branch's text or behavior.
- [x] 2. Update `_assembleStageContext`'s own dartdoc (the comment block
      immediately above its signature, ~lines 7038-7053 currently) to
      mention the new `exploring` branch, in the same enumerated style the
      dartdoc already uses for `verifying`/`designBrief`/`designSync`/
      `proposed` — this is documentation for the method this task's own
      edit changes, not a follow-up.
- [x] 3. In `aion/test/features/tickets/cubit/tickets_cubit_test.dart`, add
      a test asserting the new instruction is actually posted: extend or
      add alongside the existing `null → exploring` "persists the next
      stage and spawns a chat ticket once the precondition is met" test
      (~line 5887) — capture the comment posted via
      `commentRepository.addComment(captureAny())` and assert its
      `content` contains distinguishing substrings from the new
      instruction (e.g. mentions of not editing/creating/deleting files
      and not asking for write access). Follow the file's existing
      `blocTest`/`captureAny`/`verify` conventions exactly (see the
      `DESIGN GATE`/`VERIFY GATE` content-assertion tests elsewhere in the
      same file for the pattern).
- [x] 4. Run `flutter analyze` and `flutter test test/features/tickets/`
      (or the full suite if faster to reason about) from `aion/` and
      confirm everything passes clean before this change moves to
      `/verify`.