---
ticketId: AIO-1539
type: story
status: done
priority: none
parentId: d029505c-0048-4441-a320-7f5b53a1d2db
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.314728
updatedAt: 2026-09-02T21:17:20.314728
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Verify-report follow-up

- [x] T9. `lib/core/routing/workspace_nav_shell.dart` — MODIFY: add the
  Brand header zone to `_Sidebar`, per `design.md`'s Claude Design visual
  spec §1 ("Vertical arrangement" zone 1) — a 26px hexagon emblem plus
  the `"AION"` wordmark (`AionText.caption`, `textPrimary`),
  `EdgeInsets.fromLTRB(8, 2, 8, 20)`, `Row` with `gap: 10`, sitting above
  the nav items. **Deviates from design.md's literal
  `CustomPaint`/`ClipPath` polygon instruction**: `ProjectSwitcherMenu`
  (checked directly, not from memory) renders its hexagon glyph via
  `PhosphorIcon(PhosphorIcons.hexagonLight, ...)`, not a hand-painted
  polygon — no polygon-drawing code exists anywhere else in the app.
  Uses `PhosphorIcons.hexagonFill` (not `hexagonLight`) at
  `AionColors.primary`, matching `project.md`'s explicit icon convention
  ("Faction motifs (filled, primary color) reserved for ... section
  headers — never inline controls") — a sidebar brand header is exactly
  a section-header context, not an inline control. New private widget +
  dartdoc. Resolves verify-report.md's first WARNING.
- [x] T10. Manual interactive pass on a native Windows desktop build
  (`flutter run -d windows`), narrowing the actual OS window below 900px
  to exercise `_CompactShell`/`_BottomTabBar` live — the web/Chrome pass
  in T8 could not do this because `resize_window` had no effect on that
  session's browser window. **Verified live** (native `aion.exe`,
  resized via Win32 `MoveWindow`, screenshotted via
  `System.Drawing.Graphics.CopyFromScreen`, clicked via `mouse_event` —
  no browser involved): at 700×900px, the bottom tab bar renders three
  cells (Tickets active by default, Documentation, secondary-actions "U"
  trigger), no overflow/clipping; tapping Documentation switches the
  active highlight correctly and navigates; tapping the secondary-actions
  trigger opens the popover **upward** from the bar with both "Switch
  Project"/"Trash" rows visible and the trigger's menu-open ring showing;
  tapping "Switch Project" correctly navigates to `/hub` (previously
  entirely unverified — confirmed working); reopening the project and
  widening the same live window back to 1400×900 switches back to the
  sidebar with no restart needed, and the new T9 brand header (hexagon +
  "AION" wordmark) renders correctly at the sidebar's top. Native app and
  `flutter run` process stopped at the end of the pass. Resolves
  verify-report.md's second WARNING — no bugs found in the previously
  untested path this time (unlike T8's sidebar pass, which did surface
  one).