---
ticketId: AIO-2079
type: story
status: done
priority: none
parentId: 38afbd1e-ebd9-4b10-8625-1299ab8b368f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.388088
updatedAt: 2026-09-02T21:17:20.388088
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Agent-call plumbing

- [x] T1. In `lib/core/contracts/agent_model_client.dart`: add
  `toolsEnabled` (`bool`, default `false`) and `workingDirectory`
  (`String?`, default `null`) to `AgentRequest`'s constructor/fields/
  `props`, each with a dartdoc explaining default-preserving behavior for
  every existing caller, per `design.md` §1.1.

- [x] T2. In `lib/core/agent/claude_agent_sdk_client.dart`: pass
  `workingDirectory: request.workingDirectory` to `Process.start`; add
  `'toolsEnabled': request.toolsEnabled` to the JSON payload written to
  the bridge process's stdin. Update `run`'s dartdoc to mention cwd/tool
  passthrough, per `design.md` §1.2.

- [x] T3. In `agent_bridge/index.mjs`: destructure `toolsEnabled` from the
  parsed request; change the `query()` options so `allowedTools: []` is
  only included when `toolsEnabled` is falsy (spread-conditional, per
  `design.md` §1.3), and update the file's header comment to describe
  the now-conditional tool access.