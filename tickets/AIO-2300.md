---
ticketId: AIO-2300
type: story
status: done
priority: none
parentId: 6ba422fe-cb66-485e-8b21-c82bee0d4f6b
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.417994
updatedAt: 2026-09-02T21:17:20.417994
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation — call sites

- [x] T5. Modify `aion/lib/features/tickets/presentation/screens/ticket_detail_screen.dart`
      — remove the now-moved `TicketParentPicker`/`_NoParentRow`/
      `_CandidateRow` classes (T2 relocated them); add
      `import 'package:aion/features/tickets/presentation/widgets/ticket_parent_picker.dart';`.
      Update the usage site (was line 256) to:
      `ticket.type == TicketType.epic ? const SizedBox.shrink() : TicketParentPicker(ticketType: ticket.type, currentParentId: ticket.parentId, candidatesLoader: () => context.read<TicketsCubit>().getValidParentCandidates(ticket), onParentSelected: (parentId) => context.read<TicketsCubit>().updateTicketParent(ticket, parentId))`
      per design.md "Changes §6. TicketDetailScreen call site update" and
      spec.md MODIFIED. This site should use the existing inline
      `"+ PARENT"` trigger variant per Ticket Parent Spec §1 — see T2's
      flagged variant-parameter note.
- [x] T6. Modify `aion/lib/features/tickets/presentation/screens/create_ticket_screen.dart`:
      - Add `import 'package:aion/features/tickets/presentation/widgets/ticket_parent_picker.dart';`.
      - Add `String? _selectedParentId;` state field.
      - In the `AppDropdown<TicketType>` `onChanged`, when the new value
        is `TicketType.epic`, also clear `_selectedParentId` to null.
      - Add a new form row after the type dropdown (before priority):
        label `context.l10n.createTicketParentLabel`, then
        `_selectedType == TicketType.epic ? const SizedBox.shrink() : TicketParentPicker(ticketType: _selectedType, currentParentId: _selectedParentId, candidatesLoader: () => context.read<TicketsCubit>().getAllTickets(), onParentSelected: (id) => setState(() => _selectedParentId = id))`.
      - Update `_submit()` to pass `parentId: _selectedParentId` into
        `createTicket(...)`.
      - Update the class-level dartdoc to mention the new parent field.
      per design.md "Changes §5. CreateTicketScreen" and Component Spec
      §1 "CreateTicketScreen — form layout context" (field order: Title
      → Type → Parent → Priority → Description; body padding
      `EdgeInsets.fromLTRB(22, 2, 22, 14)`; `AionSpacing.sp16` gap
      between fields; wrap the Type→Parent visibility toggle in an
      `AnimatedSize(duration: 150ms, curve: Curves.easeOut)` per §1.2 so
      switching to/from Epic doesn't jump the form) and §2 "Parent field
      trigger (form-field variant)" for the field's own look (empty/
      filled/hover/focused/pressed/disabled/error states, §2.1-§2.3) —
      this is the new trigger variant flagged in T2's note; the "Optional"
      marker reuses the existing `commonOptionalMarker` l10n string
      (`app_en.arb:7`), no new string needed for it.