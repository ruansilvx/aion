---
ticketId: AIO-2386
type: story
status: done
priority: none
parentId: 7161c960-0bf3-4230-a027-501ca12410ec
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.427767
updatedAt: 2026-09-02T21:17:20.427767
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation layer — widgets

- [x] **T12.** Create `lib/features/tickets/presentation/widgets/ticket_sort_popover.dart`
  — `TicketSortPopover` widget, per design.md §5.1: `LayerLink`/
  `CompositedTransformFollower`/`OverlayEntry` mechanics mirroring
  `TicketFilterPopover`'s structure, one row per `TicketSortField`
  (including the active one), same-field-tap-flips-direction /
  different-field-tap-switches-with-default-direction handling (§5.1's
  `_handleTap`/`_defaultDirectionFor`), `Relevance` row disabled when
  `!hasActiveQuery`. Full dartdoc on the widget class and every public
  member; file header. Visuals per the Claude Design spec appended to
  design.md: §2 `TicketSortPopover` — the open overlay panel (container/
  positioning: §2.1; dismissal: §2.2; structure: §2.3; `_SortRow` layout
  + all six states — default/hover/focused/pressed/disabled — §2.4/§2.4.1;
  direction glyph: §2.5; leading accent swatches: §2.6) and §1 Sort model
  & behavior (tap semantics: §1.4; per-field default direction: §1.5).
- [x] **T13.** Modify `lib/features/tickets/presentation/screens/tickets_list_screen.dart`:
  - Change `_TicketViewMode _viewMode = _TicketViewMode.list;` to
    `_TicketViewMode.board` (design.md §5.4), per the Claude Design
    spec's §1.7 Board becomes the default view. Update the class-level
    comment/`_TicketViewMode` dartdoc that currently states "the screen
    always opens in List mode."
  - Add `_isSortPopoverOpen`/`_isSortTriggerFocused` state fields to
    `_TicketsListScreenState` (or the relevant section widget — see
    below), mirroring `_isPopoverOpen`/`_isTriggerFocused`.
  - Update `_initializeAndSearch` to also `await
    context.read<TicketsCubit>().loadPersistedSort();` alongside the
    existing `loadPersistedFilters()` call, before `_runSearch()`
    (design.md §5.3).
  - Rename `_TicketFilterSection` → `_TicketFilterAndSortSection`
    (update its one call site in `build`); add a new
    `_SortTriggerButton` widget (sibling of `_FilterTriggerButton`,
    same idle/active/open/focused visual language, label showing the
    active field per design.md §6's `ticketsListSortTriggerLabel`/
    `ticketsListSortTriggerActiveLabel`) and wrap it plus the existing
    Filters trigger in a `Row`, with the new `TicketSortPopover` (T12)
    wired per design.md §5.3. Visuals per the Claude Design spec
    appended to design.md: §3 `_SortTriggerButton` — the "Sort" trigger
    (dimensions/layout: §3.1; idle sub-state's four visual states:
    §3.2; active sub-state's four visual states: §3.3; idle↔active
    transition: §3.4) and §4 Header row layout (composition: §4.1;
    trigger row spacing/alignment with the existing Filters trigger and
    `ViewModeToggle`: §4.2).
  - Full dartdoc on every new/renamed widget and field.
- [x] **T14.** Modify `lib/l10n/app_en.arb` — add the ten new keys from
  design.md §6 (`ticketsListSortTriggerLabel`,
  `ticketsListSortTriggerActiveLabel`, the six
  `ticketsListSortField*` row labels, `ticketsListSortTriggerSemantics`,
  `ticketsListSortDirectionAscendingSemantics`/
  `...DescendingSemantics`), each with an ARB `@`-description
  metadata entry matching this file's existing convention (see
  `ticketsListFilterTriggerLabel`'s neighboring `@ticketsListFilterTriggerLabel`
  block for the expected shape).