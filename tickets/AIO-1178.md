---
ticketId: AIO-1178
type: story
status: done
priority: none
parentId: 506e4086-a4c6-495c-ba3a-0c64f488a6d3
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.268053
updatedAt: 2026-09-02T21:17:20.268053
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 2. Domain layer — `features/projects/domain/`

- [x] 2.1 `domain/entities/project.dart` — `Project` entity (`id`,
      `name`, `storageKey`, `rootPath` nullable, `baselineVersion`,
      `createdAt`, `lastOpenedAt`), `Equatable`. File header + dartdoc on
      class and every field per Documentation Standards.
- [x] 2.2 `domain/entities/baseline_manifest.dart` — `BaselineManifest`
      entity (`version`, `assets: List<BaselineAsset>`). Header + dartdoc.
- [x] 2.3 `domain/entities/baseline_asset.dart` — `BaselineAsset` entity
      (`key`, `kind: BaselineAssetKind`, `bundledPath`) and the
      `BaselineAssetKind` enum (`skill | modelConfig |
      architectureConvention`, one-line dartdoc per value). Header +
      dartdoc.
- [x] 2.4 `domain/entities/project_override.dart` — `ProjectOverride`
      entity (`projectId`, `assetKey`, `overridePath`). Header + dartdoc.
- [x] 2.5 `domain/entities/resolved_config_item.dart` —
      `ResolvedConfigItem` entity (`key`, `source: ConfigSource`,
      `contentPath`) and the `ConfigSource` enum (`baseline | override`,
      one-line dartdoc per value). Header + dartdoc.
- [x] 2.6 `domain/repositories/project_repository.dart` —
      `ProjectRepository` abstract interface per design.md §3
      (`getAllProjects`, `getProject`, `createProject`,
      `updateLastOpened`, `removeProject`). Header + dartdoc per method
      including `@throws` notes where a disk/DB failure is possible.
- [x] 2.7 `domain/repositories/baseline_repository.dart` —
      `BaselineRepository` abstract interface per design.md §3
      (`getAvailableBaselineVersions`, `getManifest`, `readOverrides`).
      Header + dartdoc.