---
ticketId: AIO-2310
type: story
status: done
priority: none
parentId: 6ba422fe-cb66-485e-8b21-c82bee0d4f6b
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.417994
updatedAt: 2026-09-02T21:17:20.417994
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Verify fixes

- [x] T11. **(WARNING fix)** `aion-arch/changes/ticket-parent-selection-ux/specs/spec.md`'s
      MODIFIED bullet for `TicketDetailScreen`'s `TicketParentPicker`
      usage site claimed the epic-gate (`ticket.type == TicketType.epic
      ? SizedBox.shrink() : ...`) was newly added by this change. False —
      the gate was already present in the codebase before `/apply`, and
      `aion-arch/specs/tickets.md`'s existing current-state description
      already documents it. Corrected the bullet to say only the
      constructor's parameter names changed at that call site.
- [x] T12. **(WARNING fix)** `TicketParentPicker`'s form-field trigger
      (`aion/lib/features/tickets/presentation/widgets/ticket_parent_picker.dart`)
      was missing the Disabled and Error states specified in design.md's
      Component Spec §2.3. Added `isDisabled`/`errorText` constructor
      params: `isDisabled` renders 50% opacity + blocks
      opening/interaction (`IgnorePointer`) and is wired from
      `CreateTicketScreen` to `_isSubmitting`; `errorText` renders a
      danger-colored border/ring plus a helper `Text` line below the
      control when non-null (no current caller sets it — create-time
      `parentId` still isn't validated by the Cubit — but the rendering
      path now exists per spec). Dartdoc added on both new fields.