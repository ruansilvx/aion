---
ticketId: AIO-2433
type: story
status: done
priority: none
parentId: 60978800-50b4-47e4-a9fb-70c304ad0d3f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.432867
updatedAt: 2026-09-02T21:17:20.432867
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation — overflow menu (single-ticket delete)

- [x] T25 — `lib/features/tickets/presentation/widgets/ticket_overflow_menu.dart`:
  update `_onDeletePressed` to call
  `context.read<TicketsCubit>().previewTrashCount([widget.ticket.id])`
  before `showAppConfirmDialog`, pass `context.l10n.ticketTrashConfirmMessage(total)`
  as the dialog's `message`, and call `TicketsCubit.trashTicket`
  instead of the removed `deleteTicket` on confirm. Update the class
  dartdoc to describe the trash (not permanent-delete) semantics. Per
  design.md §7. Visual spec: design.md's Specification §6.1 Confirm
  dialog — Reversible tone (existing `AppConfirmDialog` shell, new
  `tone` parameter per §11 Implementation notes — do not fork a second
  dialog widget).
- [x] T26 — `lib/features/tickets/presentation/screens/ticket_detail_screen.dart`:
  update the existing `BlocListener` branch from `state is TicketDeleted`
  to `state is TicketTrashed`; remove the `hasChildren` toast branch.
  Per design.md §5, §7.