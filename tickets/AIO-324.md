---
ticketId: AIO-324
type: task
status: done
priority: none
parentId: 73cfe1bf-7e4b-4077-a695-3cee86886d4e
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.163211
updatedAt: 2026-09-02T21:17:20.163211
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 6.3 Modify

`aion/lib/features/providers/presentation/screens/settings_screen.dart` —
new `_BaselineUpgradeSection` private widget per design.md §2
`Settings "BASELINE" section` (§2.1 `Section layout`, §2.2 `Action —
upgrade available`, §2.3 `Action — upgrading`, §2.4 `Action —
already on latest`), inserted per §4.2 `SettingsScreen` (immediately
after "OVERRIDES", same raw `22`-px inter-section gap the screen
already uses between its existing sections — there is no
`AionSpacing.sp22` token; design.md's own reference to one is a
naming slip design-sync flagged, not a new token to add), mirroring
`_OverridesSummarySection`'s shape (`BlocBuilder<
BaselineUpgradeCubit, BaselineUpgradeState>`, current-version text,
an `AppButton` calling `context.read<BaselineUpgradeCubit>()
.upgrade()`, §2.4's quiet no-button message when `isUpToDate` rather
than a disabled button). Update the screen-level dartdoc/comment
enumerating its sections.