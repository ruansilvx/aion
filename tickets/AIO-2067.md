---
ticketId: AIO-2067
type: story
status: done
priority: none
parentId: 47a2b682-2c1b-45d1-a8f3-d29a6f0ff6a1
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.383375
updatedAt: 2026-09-02T21:17:20.383375
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T35. Unit tests for `TicketMarkdownSerializer` (round-trip
      determinism, `ParsedOk`/`ParsedPartial`/`Unparseable` branches)
      in `aion/test/core/markdown/ticket_markdown_serializer_test.dart`.
      9 tests, all passing (`flutter test` run and verified).
- [x] T36. Unit tests for `TicketMarkdownReconciler` (all three parse
      branches, active-view blocking vs. background-apply paths,
      embedding-regen trigger) in
      `aion/test/features/tickets/data/services/ticket_markdown_reconciler_test.dart`,
      mocking `TicketRepository`/`EmbeddingProvider` via `mocktail`,
      real `ActiveTicketViewRegistry`/file I/O against a temp
      directory. 7 tests, all passing.
- [x] T37. Unit tests for `TicketGitProjector` (writes expected file
      content, skips commit when `hasChanges` is false) in
      `aion/test/features/tickets/data/services/ticket_git_projector_test.dart`,
      mocking `GitRepositoryClient`. 3 tests, all passing.
- [x] T38. Unit tests for `TicketDbReconstructionService` (imports
      valid files, updates existing rows, skips and reports
      unparseable ones, triggers bulk embedding backfill) in
      `aion/test/features/tickets/data/services/ticket_db_reconstruction_service_test.dart`.
      5 tests, all passing.
- [x] T39. Unit tests for `TicketRepairCubit` (state transitions across
      `reformat`/`restoreFromLastKnownGood`, success/failure/thrown-error
      paths) in `aion/test/features/tickets/cubit/ticket_repair_cubit_test.dart`
      (corrected path — cubit tests live under `test/features/tickets/cubit/`,
      not `presentation/cubit/`, per the existing convention) using
      `bloc_test`. 5 tests, all passing.
- [x] T40. Unit tests for the `TicketsCubit` modifications from T25
      (embedding trigger fires on title/description change and only
      then; git projector invoked with the correct event label per
      trigger; both no-op cleanly when omitted) in the existing
      `aion/test/features/tickets/cubit/tickets_cubit_test.dart`
      (corrected path, same as T39), mocking `EmbeddingProvider` and
      `TicketGitProjector`. **Found and fixed a real regression while
      writing these**: T25's new unconditional `getTicketById` calls in
      `createTicket`/`updateTicketStatus`/`trashTicket`/`trashTickets`
      broke 5 pre-existing tests that didn't stub it (mocktail throws
      `MissingStubError` on an unstubbed call) — added the missing
      stubs to those 5 tests rather than leaving them broken. Full
      suite: 42 tests, all passing.

**Full-suite regression check** (not a listed task, done after T40 to be
sure nothing else broke): ran `flutter test` across the entire app.
Found and fixed two more pre-existing tests broken by this change:
- `bundled_baseline_repository_test.dart`'s "returns the single bundled
  version" hardcoded `['0.1.0']` — updated to expect both versions now
  that `_bundledVersions` has two entries.
- `drift_ticket_repository_test.dart`'s migration test builds a
  hand-crafted "v1 shape" by dropping columns `onCreate` (which always
  builds at the *current* schema) added — it already dropped
  `deleted_at` but never accounted for the new `sync_status` column,
  causing a duplicate-column error on the simulated upgrade. Fixing it
  surfaced a real ordering constraint: `sync_status` can't be dropped
  before the test's `createTicket` call, since (unlike `deleted_at`)
  `DriftTicketRepository.createTicket` now sets it explicitly — dropped
  it immediately after the insert instead.

Final count: 252 tests, all passing.