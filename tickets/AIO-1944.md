---
ticketId: AIO-1944
type: story
status: done
priority: none
parentId: 691cefc0-3a4f-451f-9dd4-bbfbdb237632
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.373554
updatedAt: 2026-09-02T21:17:20.373554
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data (`lib/features/tickets/data/`)

- [x] `models/transition_precondition_graphs_table.dart` — drift
      `TransitionPreconditionGraphsTable` (`sddStage` unique,
      `rootNodeId` nullable), row type
      `TransitionPreconditionGraphData`.
- [x] `models/transition_precondition_nodes_table.dart` — drift
      `TransitionPreconditionNodesTable` (`id` primary key, `fieldId`,
      `matchedBranchKind`/`matchedBranchNodeId`,
      `unmatchedBranchKind`/`unmatchedBranchNodeId`), row type
      `TransitionPreconditionNodeData`.
- [x] `daos/transition_precondition_dao.dart` —
      `TransitionPreconditionDao`: CRUD for both tables plus
      `seedDefaultsIfEmpty` (idempotent — inserts the 5
      precondition-bearing stages' graphs + nodes reproducing
      `design.md` §3 exactly).
- [x] `repositories/drift_transition_precondition_repository.dart` —
      `DriftTransitionPreconditionRepository implements
      TransitionPreconditionRepository`, mapping row data ↔ domain
      entities.
- [x] `app_database.dart` — register
      `TransitionPreconditionGraphsTable`/
      `TransitionPreconditionNodesTable` in `@DriftDatabase(tables:
      [...])`, bump `schemaVersion` 18 → 19, add the additive migration
      step, wire `TransitionPreconditionRepository.seedDefaultsIfEmpty()`
      into the schema-19 upgrade path and fresh-install seeding.