---
ticketId: AIO-2477
type: story
status: done
priority: none
parentId: dc003a29-7b47-47fa-b112-813e28c0baf0
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.438116
updatedAt: 2026-09-02T21:17:20.438116
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T17. `test/features/tickets/cubit/tickets_cubit_test.dart` —
      `createTicket`/`updateTicket` trigger `TicketTokenPredictor.suggest`;
      `TicketTokenPredictor.suggest`'s full guard chain (no provider, wrong
      type, no embedding, already-executed) each no-op correctly; a
      populated candidate set persists the correct min/max via a fake
      `TicketRepository.applyTokenPrediction` call; zero comparable
      candidates leaves fields untouched (no write). `_executionTokenTotals`
      seeding (`loadTickets`/`getTicketById`) and incrementing
      (`_runCodingExecution`'s turn-completion points) are each covered
      with a fake `AgentModelClient` emitting a scripted `AgentDoneEvent`.
- [x] T18. `test/features/tickets/data/repositories/drift_ticket_repository_test.dart`
      (or sibling file matching existing test organization) —
      `getExecutionTokenTotals` covers: multiple chats per task summed
      together; a task with zero execution chats absent from the result
      map; `inputTokens`/`outputTokens` both null on a comment contributing
      `0`; a multi-id batch query returning entries only for ids with
      data. `applyTokenPrediction` persists both fields together and
      never disturbs any other `Ticket` field.
- [x] T19. `test/features/tickets/domain/entities/ticket_test.dart` (or
      sibling covering `Ticket.copyWith`) — a `copyWith` call with no
      `predictedExecutionTokensLow`/`High`-related parameter (none
      exists) leaves both fields unchanged on the copy, confirming the
      deliberate-exclusion contract T1 documents actually holds — the
      same regression shape `ticket-copywith-drops-deletedat` flagged for
      `deletedAt`.
- [x] T20. Widget tests for `TokenCountLabel.range`/`.total` (formatting
      edge cases: `<1000`, `~1.2K`, `~999K`, `~1.0M`) and
      `_cardTokenLabel`'s precedence rule (running total present → shown
      over prediction; neither present → renders nothing; non-task/bug
      ticket → always `null`) — file location matches existing
      `tickets_board_view`/widget test conventions.