---
ticketId: AIO-1492
type: story
status: done
priority: none
parentId: 899cd133-eb63-421a-a874-93812dd98f45
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.311179
updatedAt: 2026-09-02T21:17:20.311179
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Domain layer

- [x] T1. Create `lib/features/providers/domain/enums/model_phase.dart`:
  new `ModelPhase` enum (`frontier | capable | execution`), each value
  with a one-line dartdoc, plus an enum-level dartdoc and file header
  comment, per `design.md` §1.1.

- [x] T2. In `lib/features/tickets/domain/enums/sdd_stage.dart`: add the
  import for `ModelPhase` and the new `SddStageModelPhase` extension
  (`modelPhase` getter, switch per `design.md` §1.2), with its own
  dartdoc explaining the mapping and citing
  `per-phase-tier-based-model-routing.md`.

- [x] T3. Create `lib/features/providers/domain/repositories/model_routing_repository.dart`:
  new `ModelRoutingRepository` abstract interface
  (`getModelForPhase`/`setModelForPhase`), class-level dartdoc explaining
  it replaces `AgentSettingsRepository`, per `design.md` §1.3.

- [x] T4. Create `lib/features/providers/data/repositories/shared_prefs_model_routing_repository.dart`:
  new `SharedPrefsModelRoutingRepository` implementing T3's interface —
  per-phase keys plus the legacy-key fallback read, per `design.md` §1.4.
  Class-level dartdoc documents the fallback behavior and the removed
  repository it supersedes.

- [x] T5. Delete `lib/features/providers/domain/repositories/agent_settings_repository.dart`
  and `lib/features/providers/data/repositories/shared_prefs_agent_settings_repository.dart`
  — fully superseded by T3/T4, no remaining references after T9–T15.