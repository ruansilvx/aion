---
ticketId: AIO-393
type: story
status: done
priority: none
parentId: 3e095e14-a7e9-489e-891b-6a291c68e280
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.175274
updatedAt: 2026-09-02T21:17:20.175274
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data layer

- [x] T1. `features/tickets/data/daos/ticket_link_dao.dart` —
      `getLinksByTypes(List<TicketLinkType> types)` (design.md §1.1): a
      single query joining `ticketLinksTable` against `ticketsTable`
      twice (source/target, both `deletedAt.isNull()`), filtered by
      `linkType.isIn(types.map((t) => t.name))`, no per-ticket-id
      filter. Dartdoc it, cross-referencing `deleteLinksForTickets`'s
      similarly whole-table-scoped shape.
- [x] T2. `features/tickets/domain/repositories/ticket_link_repository.dart`
      — add `Future<List<TicketLinkData>> getLinksByTypes(List<TicketLinkType>
      types);` to the interface (design.md §1.2). Dartdoc it.
- [x] T3. `features/tickets/data/repositories/drift_ticket_link_repository.dart`
      — implement `getLinksByTypes`, delegating straight to
      `_db.ticketLinkDao.getLinksByTypes(types)` (same one-line shape as
      the class's existing methods). Dartdoc it.