---
ticketId: AIO-1845
type: story
status: done
priority: none
parentId: b2a407ad-661f-4620-9517-d36eb3cab6da
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.360227
updatedAt: 2026-09-02T21:17:20.360227
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation layer — UI (blocked on design gate)

- [x] T9. In
  `lib/features/tickets/presentation/screens/ticket_detail_screen.dart`:
  change `_SddStageSection`'s `_stages` from a fixed 4-element list to
  the computed variable-length getter specified in `design.md` §8
  (needs a new `needsDesignReview` parameter threaded from
  `TicketDetailLoaded.needsDesignReview`). Add
  `SddStage.designBrief`/`SddStage.designSync` arms to `_stageLabel`/
  `_stageStatusLabel`, backed by the new l10n keys from T13. Per
  design.md §1 (variable-length tracker, §1.1–§1.4) and §1.3 (stage
  display names).

- [x] T10. In the same file: add the new `_RetryValidationButton` widget
  (styled per `design.md` §8/§10 — plain button matching
  `_ManualAdvanceButton`'s existing visual treatment, per whatever
  `design.md`'s design-gate section records once `/design-sync` runs),
  with full dartdoc. Wire it onto a `TicketType.chat` ticket's detail
  screen, shown only when the conditions in `design.md` §8 are met
  (title prefix, parent `sddStage`, most recent AI reply content);
  tapping it calls `TicketsCubit.retryDesignSync(ticket)`. Per
  design.md §4 (Retry validation button, §4.1–§4.3).

- [x] T11. In the same file: give the existing gated "ready to advance"
  banner widget (`_GatedBanner`) a stage-aware leading glyph — a
  `typePage`-colored pencil/page-edit glyph when the target stage is
  `SddStage.designBrief`/`SddStage.designSync`, unchanged `primary`
  sparkle glyph otherwise — plus the per-stage title/sub-line copy from
  the table in design.md §3.3 ("Ready to advance to Design Brief" /
  "Ready to advance to Design Sync" / "Ready to advance to
  Verification", each with its own precondition-met sub-line). Update
  `_GatedBanner`'s dartdoc. Per design.md §3 (§3.1–§3.3) — flagged as
  missing a task during `/design-sync` (see proposal.md's Design gate
  section).

- [x] T12. In the same file: add a new `_LinkedDesignPageChip` widget,
  shown on a Story's detail screen directly under the meta row only when
  `TicketDetailLoaded.needsDesignReview` is `true` and a linked design
  Page exists — tapping it navigates to that Page's `TicketDetailScreen`.
  Requires extending `TicketDetailLoaded` with a new `linkedDesignPage`
  (`Ticket?`) field, populated by `getTicketById`'s existing `story`
  special-case path via the same lookup `TicketsCubit._linkedDesignPage`
  already does internally for the `designBrief`/`designSync`
  precondition checks (T4) — expose it to the UI rather than
  re-implementing the lookup in the widget layer. Update
  `TicketDetailLoaded`'s and `getTicketById`'s dartdoc. Per design.md §5
  (§5.1–§5.2) — flagged as missing a task during `/design-sync` (see
  proposal.md's Design gate section).