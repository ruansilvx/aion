---
ticketId: AIO-2029
type: story
status: done
priority: none
parentId: 47a2b682-2c1b-45d1-a8f3-d29a6f0ff6a1
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.383375
updatedAt: 2026-09-02T21:17:20.383375
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data layer — drift schema

- [x] T5. Modify `lib/features/tickets/data/models/ticket_model.dart` —
      add `syncStatus` `TextColumn` (default `'synced'`) to
      `TicketsTable` — matches the existing `type`/`status`/`priority`
      convention of storing enum names as text, not `IntColumn`
      (design.md suggested `intEnum`; deviated to match codebase
      convention). Document the column per existing column doc style.
- [x] T6. Modify `lib/core/database/app_database.dart` — bump
      `schemaVersion` from `3` to `4`; add the `syncStatus` column via
      `m.addColumn` in `migration`'s `onUpgrade` for `from < 4`.
- [x] T7. Run `flutter pub run build_runner build --delete-conflicting-outputs`
      to regenerate drift code for the schema change.
- [x] T8. Modify `lib/features/tickets/data/repositories/drift_ticket_repository.dart`
      — persist/read `syncStatus` and `embedding`; implement
      `updateSyncStatus`/`updateEmbedding`. `ticket_dao.dart` needed no
      changes — its existing generic `updateFields(id, companion)`
      already covers arbitrary-column updates.