---
ticketId: AIO-2152
type: task
status: done
priority: none
parentId: 6b50516f-d60d-43a5-bfb1-d52c7896e02e
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.394206
updatedAt: 2026-09-02T21:17:20.394206
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T6 — `lib/features/tickets/presentation/screens/tickets_list_screen.dart`:

add an import for `ticket_overflow_menu.dart`. In `TicketListTile`'s
top `Row`, add `TicketOverflowMenu(ticket: ticket)` after the
`PriorityBadge` slot (with an `AionSpacing.sp8` gap). In
`_TicketsListScreenState.build`, add a `TicketDeleting()` arm to the
`BlocBuilder`'s state switch (spinner, same as `TicketsLoading()`),
and wrap the existing `BlocBuilder<TicketsCubit, TicketsState>` in a
`BlocListener<TicketsCubit, TicketsState>` that shows
`AppToast.show(context, context.l10n.ticketDeleteBlockedByChildren(state.childCount ?? 0))`
when `state is TicketsError && state.reason == TicketsErrorReason.hasChildren`.
Add the `app_toast.dart` import. Update `TicketListTile`'s class
dartdoc to mention the new overflow trigger. Per design.md §1
Overflow trigger — list row (`TicketListTile`).