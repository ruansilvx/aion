---
ticketId: AIO-1999
type: story
status: done
priority: none
parentId: 1a337933-81b8-45cd-b2ab-8dc40524b949
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.379628
updatedAt: 2026-09-02T21:17:20.379628
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# (untitled section)

# Tasks: spec-ticket-type

Ordered checklist. Every task touching a `.dart` file includes its
documentation (dartdoc + file header, where new) in the same task — not
a follow-up. Design-gate status: APPROVED (see proposal.md) — visual
tasks (T4–T7, T14, T15) carry `design.md` section references from
`/design-sync`.

- [x] T1. In `lib/features/tickets/domain/enums/ticket_type.dart`:
  - Add `TicketType.spec` enum value with full dartdoc, exactly as
    specified in `design.md`'s Domain layer section, appended after
    `bug`.
  - Update `TicketTypeHierarchy._rank`'s switch to include `spec` in the
    existing unranked (`null`) arm.
  - Update `canParent` to add `spec` to the existing `idea`/`knownGap`/
    `openQuestion`/`release` special case (returns `child ==
    TicketType.chat`).
  - Update `isAlwaysRoot` to add `spec` to the existing disjunction.
  - Update this file's own class-level dartoc references that
    enumerate the unranked/always-root types to also name `spec`.

- [x] T2. In `lib/core/automation/automation_context.dart`: add
  `AutomationContext.specAutoLink` with full dartdoc, exactly as
  specified in `design.md`, appended after `ticketLinking`.

- [x] T3. In
  `lib/features/tickets/presentation/cubit/tickets_state.dart`: add
  `TicketsErrorReason.specWriteFailed` with a dartdoc sentence matching
  the existing `sddStageAdvanceFailed` entry's shape. Add the matching
  localized-message arm wherever `ticketsErrorMessage` switches on
  `TicketsErrorReason` (same file or its resolver, per existing
  pattern).

- [x] T4. per design.md §1.1–§1.3 `typeSpec` accent color. In `lib/design_system/tokens/aion_colors.dart`: add a
  `typeSpec` `Color` field to `AionColors` (dartdoc matching the
  existing `typeSignal`/`typeRelease` pattern), add it to the
  constructor's required params, and add both `arctic`/`obsidian`
  const values — pick values visually distinct from the seven existing
  type colors (spot-check against `typeSignal`/`typeStory`'s known
  close-hue pair in Obsidian, per `sdd-ticket-foundation`'s design-gate
  note, and avoid repeating that mistake).

- [x] T5. per design.md §1.4–§1.5 Resolver extension, TypeChip renderings. In `lib/design_system/molecules/type_chip.dart`: add
  `TicketType.spec => l10n.ticketTypeSpec` to `ticketTypeLabel`'s
  switch, and `TicketType.spec => c.typeSpec` to `TypeChip.build`'s
  color switch, placed before the existing `_ => c.typeTask` catch-all.

- [x] T6. per design.md §1.4 Resolver extension. In `lib/design_system/molecules/linked_tickets_section.dart`:
  add a `TicketType.spec => c.typeSpec` arm to `_typeColor`'s switch,
  placed before its catch-all — proactively, so `spec` never falls
  through the way `signal`/`release`/`chat` initially did (see
  `sdd-ticket-execution`'s bug-fix note).

- [x] T7. per design.md §1.4 Resolver extension (`typeLabelFor` → `"SPEC"`). In `lib/l10n/app_en.arb`: add a `ticketTypeSpec` key/value
  ("Spec"), matching the existing `ticketTypeSignal`/`ticketTypeRelease`
  entries' shape.

- [x] T8. In
  `test/features/tickets/domain/ticket_type_hierarchy_test.dart`:
  extend the expectation matrix from 9×9 to 10×10, adding `spec`'s row
  (parents only `chat`) and column (accepted only as a child of `epic`/
  `story`/`task`/`bug`/`resource`/`page`/`chat`/`idea`/`knownGap`/
  `openQuestion`/`release`/`spec` per each existing type's own rule —
  i.e. wherever `chat` is currently accepted, `spec`'s row mirrors it;
  `spec` itself only ever appears as a child under the existing
  `chat`-only rule shared with `idea`/`knownGap`/`openQuestion`/
  `release`, so most of its column is `false`).

- [x] T9. In
  `lib/features/tickets/presentation/cubit/tickets_cubit.dart`:
  - Add the private `_createEpicSpec(Ticket epic)` method exactly as
    specified in `design.md`'s "Auto-creation at Epic archival"
    section — assembles context via the existing
    `_assembleStageContext`-equivalent shape (epic title/description +
    direct Story children's titles/statuses), resolves the model via
    `_resolveModelAndProvider(ModelPhase.capable)`, creates the
    `TicketType.spec` ticket, links it to `epic` via
    `TicketLinkRepository.createLink(linkType:
    TicketLinkType.relatesTo)`, catches any failure into
    `TicketsError(reason: TicketsErrorReason.specWriteFailed)`. Full
    dartdoc per design.md.
  - In `advanceSddStage`'s `nextStage == SddStage.archived` branch,
    add the `if (ticket.type == TicketType.epic) { unawaited
    (_createEpicSpec(refreshed)); }` call before the existing `emit`,
    exactly as specified in `design.md`. Update `advanceSddStage`'s own
    dartdoc to describe this new side effect and the Epic-only guard.

- [x] T10. In the same file: add the private
  `_maybeAutoLinkToSpec(Ticket ticket)` method exactly as specified in
  `design.md`'s "Auto-linking to the nearest relevant spec" section —
  reuses `domain/utils/embedding_similarity.dart`'s `cosineSimilarity`
  against every live `TicketType.spec` ticket (0.75 threshold, single
  best match), gated by
  `AutomationSettingsRepository.getConfidence(AutomationContext
  .specAutoLink)`. Full dartdoc per design.md, including the
  `auto`/`gated`/`manual` behavior split.

- [x] T11. In the same file: wire `_maybeAutoLinkToSpec`'s two call
  sites exactly as specified in `design.md`:
  - `createGapOrQuestion` — `unawaited(_maybeAutoLinkToSpec(created))`
    after the existing mandatory target link succeeds.
  - `updateTicketStatus` — `unawaited(_maybeAutoLinkToSpec(refreshed))`
    when the new status has the `done` role and `ticket.type ==
    TicketType.bug`, after the status write is confirmed. Update both
    methods' dartdoc to mention this new side effect.

- [x] T12. In
  `lib/features/tickets/presentation/cubit/tickets_state.dart` (or
  wherever `TicketDetailLoaded` is defined): add the
  `_pendingSpecLinkSuggestion`-equivalent field needed for the `gated`
  confirm-banner path described in `design.md`, mirroring
  `TicketDetailLoaded`'s existing `sddStageFailureReason`-style optional
  field pattern (nullable, included in `copyWith`). Full dartdoc.

- [x] T13. In
  `lib/features/pages/presentation/cubit/pages_cubit.dart`: widen
  every `ticket.type == TicketType.page` branch that decides whether a
  ticket is this cubit's to render/manage to `ticket.type ==
  TicketType.page || ticket.type == TicketType.spec`, per `design.md`'s
  "PagesCubit/PageDetailScreen" section. Update affected methods'
  dartdoc to mention `spec` alongside `page`.

- [x] T14. per design.md §2 `SpecOriginBadge`, §3 `PageDetailScreen`
  integration. In
  `lib/features/pages/presentation/screens/page_detail_screen.dart`:
  same widening as T13 for any `ticket.type == TicketType.page`
  branches controlling rendering/navigation eligibility. Add the new
  `SpecOriginBadge` widget (design.md §2) as the first child of the
  scroll body, spec-only, per §3.2's placement/spacing and §3.3's
  absence rule for non-spec tickets. Update the class-level dartdoc to
  describe `spec` support. Confirm (no code change expected beyond the
  badge, per design.md) that the existing `MarkdownEditor` usage and
  relation-loading need no further `spec`-specific branching.

- [x] T15. per design.md §4.1 New entry, §4.4 Downstream form behavior.
  In
  `lib/features/tickets/presentation/screens/create_ticket_screen.dart`:
  add `spec` to the type dropdown's included types (the existing
  exclusion list currently reads `type != TicketType.page && type !=
  TicketType.knownGap && type != TicketType.openQuestion` — leave that
  list excluding those three, `spec` simply isn't added to any
  exclusion), positioned after `Page`/before `Idea` per §4.1. Per
  §4.4, selecting `Spec` must show the same field set as `Page` (Title,
  Type, optional Parent, Markdown content) — no Priority/Complexity/
  Severity/Estimate fields. Update the class-level dartdoc listing
  directly-selectable types to include `spec`.

- [x] T16. Confirm (no code change expected) that
  `lib/features/tickets/presentation/screens/documentation_screen.dart`'s
  existing `ticket.type == TicketType.page` check at its Documentation-
  listing filter is left untouched, per `design.md`'s "DocumentationScreen"
  section — `spec` tickets are deliberately not listed there. If any
  other type-based filter in that file would incidentally start
  including `spec` tickets as an unintended side effect of T1's enum
  addition (e.g. a generic "every non-`page`/`resource` type" catch-all
  rather than an explicit allowlist), narrow it defensively so `spec`
  stays excluded there too.

- [x] T17. Add/update unit tests:
  - `TicketTypeHierarchy` coverage from T8.
  - `TicketsCubit._createEpicSpec`/`advanceSddStage`'s Epic-only guard —
    a test asserting a Story reaching `archived` does *not* create a
    `spec` ticket, and an Epic reaching `archived` does, titled/linked
    as specified.
  - `TicketsCubit._maybeAutoLinkToSpec` — one test per
    `AutomationConfidence` value (`auto` creates the link immediately,
    `gated` surfaces the pending-suggestion state without creating a
    link, `manual` does neither proactively), plus a below-threshold
    case asserting no link/error.
  - `createGapOrQuestion`/`updateTicketStatus` call-site wiring —
    confirm `_maybeAutoLinkToSpec` fires (mockable/verifiable via the
    existing `EmbeddingProvider`/`TicketLinkRepository` mocktail
    fakes) only under the documented trigger conditions (bug reaching a
    done-role status; not for `task`).