---
ticketId: AIO-865
type: story
status: done
priority: none
parentId: de51291e-d1f0-4162-ae92-8785a5faf823
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.226735
updatedAt: 2026-09-02T21:17:20.226735
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T25. `test/features/tickets/cubit/tickets_cubit_test.dart` —
      update the existing `retryCodingExecution` test (currently asserts
      `repository.createTicket` is called once per retry) to reflect
      reuse: a retry against a Task with an existing, under-cap
      execution chat calls `repository.createTicket` **zero** times and
      posts its context/turns to the existing chat's id. Add new cases:
      a first-ever trigger still creates one chat; a chat at/over
      `_handoffThresholdRatio` of its cap triggers `_handoffExecutionChat`
      (one summary `agentClient.run` call, one new `createTicket`, one
      `linkRepository.createLink` with `TicketLinkType.relatesTo`); a
      summary-turn hard failure falls back to reusing the old chat with
      no new ticket created; `_executionSucceededWithPr`/
      `_computeExecutionFailure` still resolve correctly against a
      `"(continued)"` chat. Also fixed three pre-existing tests in the
      same file whose fixtures had implicitly (and, before this change,
      harmlessly) modeled "an execution chat already exists" for the
      Task being moved to `inProgress`: `_resolveExecutionChat` now
      actually consults that fixture pre-run, so "runs the coding-
      execution chat on an approved Task..." needed a stateful chat-
      lookup stub (empty until `createTicket` is called, matching real
      create-then-findable ordering), "queues a second Task FIFO..."
      needed an explicit empty-chat-list stub for both Tasks (previously
      unneeded since no pre-run lookup existed), and "_runCodingExecution
      resolves ModelPhase.execution" needed its expected
      `getModelForPhase` call count bumped 2 → 3 (the new cap check
      resolves the model once more, before the implement/verify turns).
- [x] T26. `test/features/tickets/cubit/chat_cubit_test.dart` —
      `runChatTurn` persists `inputTokens`/`outputTokens` from a terminal
      `AgentDoneEvent` that carries them, and persists `null`/`null` when
      the terminal event is `AgentErrorEvent` (no `done` ever seen).
- [x] T27. `test/features/tickets/data/repositories/
      drift_comment_repository_test.dart` — round-trips a comment with
      non-null `inputTokens`/`outputTokens` through `addComment` +
      `getCommentsForTicket`.
- [x] T28. No dedicated unit test for `ClaudeAgentSdkClient._parseLine`
      exists today (that class has no test file at all — desktop-only,
      spawns a real Node process, verified live rather than unit-tested
      per its own dartdocs). T26's `chat_cubit_test.dart` cases are this
      change's actual coverage for usage flowing through
      `AgentDoneEvent`; T3's `_parseLine` change itself is covered only
      by the same live-verification approach the rest of that class
      already relies on — call this out explicitly in the `/apply`
      report rather than inventing a test file this codebase doesn't
      otherwise have.
- [x] T29. `test/features/providers/presentation/cubit/
      execution_context_cap_cubit_test.dart` — new file:
      `load` reads through to `ExecutionContextCapReady`; `setOverride`
      persists and clamps (rejects/clamps a value at or above the
      model's real limit, clears on `null`/`<= 0`).