---
ticketId: AIO-746
type: story
status: done
priority: none
parentId: 86186491-b695-4612-bf6a-b11a762a4c54
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.218399
updatedAt: 2026-09-02T21:17:20.218399
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 1. Create `design_system/tokens/` (content identical to `core/theme/`, header comment only)

- [x] Create `lib/design_system/tokens/aion_colors.dart` from
      `lib/core/theme/aion_colors.dart`. Update header comment to
      `// design_system/tokens/aion_colors.dart — ... (design-system layer).`.
      Internal imports (none — this file has no local cross-imports) unchanged.
- [x] Create `lib/design_system/tokens/aion_radius.dart` from
      `lib/core/theme/aion_radius.dart`. Update header comment path/layer.
- [x] Create `lib/design_system/tokens/aion_shadows.dart` from
      `lib/core/theme/aion_shadows.dart`. Update header comment path/layer.
      Keep its existing direct import of `aion_colors.dart` (same-directory
      relative import, unaffected by the move). Implemented as a
      `package:aion/design_system/tokens/aion_colors.dart` absolute import,
      matching this codebase's established style of never using relative
      imports (confirmed across every existing cross-import in `core/theme/`
      and `core/widgets/`) — same self-import-exception intent as design.md,
      different syntax than tasks.md's literal `../` suggestion.
- [x] Create `lib/design_system/tokens/aion_text.dart` from
      `lib/core/theme/aion_text.dart`. Update header comment path/layer.
- [x] Create `lib/design_system/tokens/aion_theme.dart` from
      `lib/core/theme/aion_theme.dart`. Update header comment path/layer.
      Keep its existing direct import of `aion_colors.dart` (absolute
      `package:` import, see note above).
- [x] Create `lib/design_system/tokens/theme_scope.dart` from
      `lib/core/theme/theme_scope.dart`. Update header comment path/layer.
      Keep its existing direct import of `aion_theme.dart` (absolute
      `package:` import, see note above).