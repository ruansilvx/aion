---
ticketId: AIO-1962
type: story
status: done
priority: none
parentId: 691cefc0-3a4f-451f-9dd4-bbfbdb237632
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.373554
updatedAt: 2026-09-02T21:17:20.373554
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# UI

- [x] `lib/features/providers/presentation/widgets/transition_node_form.dart`
      — `TransitionNodeForm`: field picker (`SelectionMenu` over
      `transitionFieldsFor(stage)`), matched/unmatched branch picker
      (2-option outcome chip pair — `allowed`/`blocked` — or "continue
      to another field"). Shared by both panes. Per design.md §0
      Shared vocabulary and §3 `TransitionNodeForm` (§3.1 field picker,
      §3.2 no parameter field — intentionally absent, §3.3–§3.4
      matched/unmatched branch pickers, §3.5 actions). The `blocked`
      reason text field design.md §3.3 originally specified is
      **intentionally not built** — reconciled 2026-09-01 post-`/verify`
      in design.md §0.4 in favor of proposal.md's auto-derived-reason
      architecture, which is what shipped.
- [x] `lib/features/providers/presentation/widgets/transition_outline_list.dart`
      — `TransitionOutlineList` (design.md names this pane
      `PreconditionOutlineList` — reconcile the class name with this
      file's naming before/during `/apply`, don't ship both): one row
      per `TransitionNode` (field name, no parameter chip), children
      indented beneath their parent branch (nested outline, no depth
      cap — though design.md §2.3 caps authoring depth at 6, shallower
      than the sibling engine's 8), 2-value outcome badge on terminal
      branches, inline `TransitionNodeForm` expand-in-place per row.
      Per design.md §2 (§2.1 list row, §2.2 terminal outcome badge,
      §2.3 "+ Add field check" affordance).
- [x] `lib/features/providers/presentation/screens/sddstage_precondition_editor_screen.dart`
      — `SddStagePreconditionEditorScreen`: `Row` of two `Expanded`
      panes with a 1px divider (layout scaffold from
      `DecisionGraphEditorScreen`/`MarkdownEditor._Wide`),
      `GraphCanvas<TransitionNode>` (default/left, genuinely reused
      generic primitive, configured per design.md §1
      `PreconditionGraphCanvas` — §1.2's parameter-free field-check
      node, §1.3's 2-value terminal, §1.7 empty-graph state) +
      `TransitionOutlineList` (right), both bound to the same
      `TransitionPreconditionConfigLoaded` state. Per design.md §4
      (§4.1 header, §4.2 panes, §4.3 divider). **`/verify` (2026-09-01)
      found §4.1's stage-position chip/dirty indicator and all of §4.4
      (narrow-width behavior) missing — see "Verify follow-ups"
      below.**
- [x] `lib/features/tickets/presentation/screens/workflow_status_settings_screen.dart`
      — extend each precondition-bearing stage's `_SddStageRenameRow`
      with a "Configure precondition" affordance that pushes
      `SddStagePreconditionEditorScreen(stage: ...)`. `null`-start and
      `archived` rows are unaffected. Per design.md §5.1 (compact
      variant), §5.3 states. **`/verify` (2026-09-01) found §5.2 (the
      labeled variant + live field-check count) was shipped as a
      documented simplification, always rendering §5.1 — see "Verify
      follow-ups" below.**