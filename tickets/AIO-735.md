---
ticketId: AIO-735
type: story
status: done
priority: none
parentId: b029f19e-5e2c-4d10-8f45-4a018c21d937
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.214950
updatedAt: 2026-09-02T21:17:20.214950
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T11. `test/core/build/dependency_cache_service_test.dart` (new) — `seed` no-ops when the cache dir doesn't exist; `seed` copies a populated cache dir's contents correctly (nested directories included); `writeBack` replaces stale cache contents rather than merging with them; `cacheDirFor` produces the expected path shape.
- [x] T12. `test/features/tickets/cubit/tickets_cubit_test.dart` (existing) — `_runCodingExecution` against a Node.js-marker fixture project calls `npm install` and seeds/writes-back the cache (mock `Process.run`/`DependencyCacheService` per T1's resolved injection approach); a non-Node.js fixture project skips the dependency step entirely; a failed `npm install` doesn't abort the run.
  - Mocked at `DependencyCacheService.installDependencies` (see T1/T3's `installDependencies` extraction note) — `Process.run` itself is never reached in these tests.
- [x] T13. `tickets_cubit_test.dart` — `_nextEligibleForHybrid` treats two Tasks under different Stories of the same Epic as conflicting (was: not conflicting pre-change); direct same-parent siblings still conflict (regression guard for the pre-existing behavior); two Tasks under unrelated Epics don't conflict.
- [x] T14. `test/features/tickets/domain/utils/sibling_cluster_test.dart` (existing or new) — `clusterSiblingsAdjacently` groups by the new `topmostAncestorId` map correctly, including the "different Story, same Epic" case; a ticket absent from the map (or mapped to its own id) still isn't grouped with anything, preserving today's `null`-parentId behavior.
- [x] T15. Widget/golden test for `BoardColumn` under Hybrid mode confirming the same-Epic-different-Story clustering renders adjacently (extend whatever existing test already covers `BoardColumn`'s Hybrid-mode clustering, if one exists from `parallel-work`'s own apply pass).
  - Extended `tickets_board_view_test.dart`'s existing "rapid board reordering (parallel-work tasks.md T50 regression)" group: added `topmostAncestorId` maps to its pre-existing `TicketsLoaded` fixtures (now required since `BoardColumn` reads the map from cubit state instead of computing it inline), plus a new dedicated test for the different-Story-same-Epic adjacency case.