---
ticketId: AIO-2099
type: task
status: done
priority: none
parentId: 5dd1ff3a-8cf0-4732-9344-42a6b506191c
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.388088
updatedAt: 2026-09-02T21:17:20.388088
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T16. In

`lib/features/tickets/presentation/screens/ticket_detail_screen.dart`:
add the coding-execution section container (eyebrow + run-status line,
per design.md's Component Spec §0) plus the `_ExecutionRunningHint`
(per design.md's Component Spec §1), `_ExecutionQueueHint` (§2), and
`_ExecutionReadyForReviewBanner` (§3) widgets (the first two mirroring
`_NotReadyHint`'s informational-row shape; the banner mirroring
`_GatedBanner`'s confirm-action shape but re-keyed to `success` per
§3.1–§3.4, with its action button calling
`context.read<TicketsCubit>().changeTicketStatus(ticket,
TicketStatus.inReview)`), each with full dartdoc. Wire the
mutually-exclusive rendering (`isExecuting` → `executionQueuePosition
!= null` → `executionAwaitingReview`) into a `TicketType.task` ticket's
detail layout, in the same position `_SddStageSection` occupies for
epic/story tickets, per design.md's Component Spec §0.