---
ticketId: AIO-1516
type: story
status: done
priority: none
parentId: 899cd133-eb63-421a-a874-93812dd98f45
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.311179
updatedAt: 2026-09-02T21:17:20.311179
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T19. Delete `test/features/providers/data/repositories/shared_prefs_agent_settings_repository_test.dart`.
  Create `test/features/providers/data/repositories/shared_prefs_model_routing_repository_test.dart`:
  cover per-phase persistence, the legacy-key fallback, and the
  `AgentModel.sonnet` ultimate default.

- [x] T20. Create `test/features/providers/presentation/cubit/model_routing_cubit_test.dart`,
  mirroring `automation_settings_cubit_test.dart`'s structure: `load()`
  populates all three phases; `selectModel` persists and updates only
  the targeted phase's entry.

- [x] T21. Update `test/features/providers/presentation/cubit/provider_settings_cubit_test.dart`:
  replace the `AgentSettingsRepository` fake/mock with a
  `ModelRoutingRepository` one; update `load()`/`testConnection()`
  assertions to key off `ModelPhase.frontier`; remove `selectModel` test
  cases (method no longer exists).

- [x] T22. Create `test/features/tickets/domain/enums/sdd_stage_model_phase_test.dart`:
  one assertion per `SddStage` value confirming `modelPhase` matches the
  confirmed split (T2).

- [x] T23. Update `test/features/tickets/cubit/tickets_cubit_test.dart`:
  every existing construction site that previously asserted
  `model == AgentModel.sonnet` for a stage-chat/coding-execution
  `runChatTurn` call now either (a) still expects `AgentModel.sonnet`
  when the cubit is constructed without a `ModelRoutingRepository`
  (falls back per T9), or (b) is extended with a fake
  `ModelRoutingRepository` and a new assertion that `_spawnStageChat`/
  `retryDesignSync`/`_runCodingExecution` each request the correct
  `ModelPhase` (`frontier` for exploring/proposed/verifying, `capable`
  for designBrief/designSync/archived, `execution` for coding
  execution).

- [x] T24. Update `test/features/tickets/cubit/chat_cubit_test.dart`:
  update every `ChatCubit(...)` construction site for the two new
  required constructor params (fake `TicketRepository`/
  `ModelRoutingRepository`); update `sendMessage` call sites to drop the
  removed `model` argument; add new test cases covering `_phaseForChat`'s
  three branches (epic/story parent by stage, task parent, defensive
  no-parent fallback).