---
ticketId: AIO-893
type: story
status: done
priority: none
parentId: 34f5d83d-805c-4bbe-9e27-6ebda07f31da
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.230479
updatedAt: 2026-09-02T21:17:20.230479
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] **T16.** Create `test/features/tickets/domain/utils/ticket_rollup_calculator_test.dart`
  — unit tests for `computeRollups`: a childless ticket produces no
  entry; a single-level parent with 2 children sums correctly; a
  3-level chain recurses correctly (grandparent's rollup reflects
  grandchild values through the parent's own rollup); a subtree with
  every value `null` produces `null`, not `0`; a mixed subtree (some
  null, some set) sums only the set values; a parent's own value plus
  children's values both contribute (Jira-style addition); a
  cycle-guard case (malformed input) doesn't hang.
- [x] **T17.** Modify `test/features/tickets/cubit/tickets_cubit_test.dart`
  — add `bloc_test` cases: `updateTicket` changing `estimate`/
  `timeSpent` triggers a rollup recompute that reaches
  `_repository.updateRollup` for every ancestor up to the root;
  `updateTicket` changing an unrelated field (e.g. `priority` only)
  does *not* trigger a recompute; `updateTicketParent` recomputes both
  the old and new parent chains in one operation; `trashTicket`/
  `trashTickets` recompute the (now-former) parent's rollup, excluding
  the trashed subtree from the new total; an ancestor whose computed
  rollup didn't actually change is not re-written/re-projected (mock
  verify `updateRollup`/`projectBatch` call counts). Mock
  `TicketGitProjector.projectBatch` alongside the existing `project`
  mock. **Added post-design-sync (amended 2026-08-11):** add a case for
  `getRollupCounts` — returns the correct `estimateCount`/
  `timeSpentCount` for a multi-level subtree, `0`/`0` for a childless
  ticket, and performs no repository writes (query-only).
- [x] **T18.** Modify `test/features/tickets/cubit/trash_cubit_test.dart`
  — add a `bloc_test` case: `restore` triggers a rollup recompute of
  the restored ticket's parent chain, re-including the restored
  subtree's contribution.
- [x] **T19.** Modify `test/features/tickets/data/repositories/drift_ticket_repository_test.dart`
  — add a test for `updateRollup`: writes only the two rollup columns,
  leaves `updatedAt` untouched.
  Modify `test/features/tickets/data/daos/ticket_dao_test.dart` if
  `updateFields` needs a new case for the two columns (likely covered
  already by its existing generic-companion tests — verify, add only
  if a gap is found).
- [x] **T20.** Modify `test/features/tickets/data/services/ticket_git_projector_test.dart`
  — add tests for `projectBatch`: writes N files and makes exactly one
  commit; no-ops on an empty list; skips the commit (but still writes
  files) when `hasChanges` is false, matching `project`'s existing
  guard.
- [x] **T21.** Modify `test/core/markdown/ticket_markdown_serializer_test.dart`
  — extend the round-trip test(s) to cover `estimateRollup`/
  `timeSpentRollup` serializing and parsing correctly (including both
  `null` and set-value cases).
- [x] **T22.** Modify `test/features/tickets/domain/entities/ticket_test.dart`
  — add the two new fields to the existing `Equatable`/`copyWith`
  coverage; explicitly assert `copyWith` has no way to set either
  field (i.e. confirm the constructor signature omits them — a
  compile-time guarantee, but add a doc-comment-level test note if
  there's no meaningful runtime assertion to make).