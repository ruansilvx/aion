---
ticketId: AIO-2458
type: story
status: done
priority: none
parentId: dc003a29-7b47-47fa-b112-813e28c0baf0
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.438116
updatedAt: 2026-09-02T21:17:20.438116
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data — schema + repository

- [x] T2. `data/models/ticket_model.dart` — add
      `predictedExecutionTokensLow`/`predictedExecutionTokensHigh`
      nullable `IntColumn`s (`predicted_execution_tokens_low`/
      `predicted_execution_tokens_high`) (design.md §1.2). Dartdoc both,
      cross-referencing `TicketTokenPredictor`.
- [x] T3. `core/database/app_database.dart` — bump `schemaVersion` 13 →
      14; add a `MigrationStrategy.onUpgrade` step (`from == 13`) adding
      both new columns via `ADD COLUMN`, following the existing
      `estimateRollup`/`timeSpentRollup` migration's exact pattern
      (design.md §1.2). No backfill.
- [x] T4. `data/repositories/drift_ticket_repository.dart` (+ its
      interface, `domain/repositories/ticket_repository.dart`) — add
      `getExecutionTokenTotals(List<String> taskIds)`: one grouped query
      joining chat-type `TicketModel` rows (`parentId IN taskIds`, `title
      LIKE 'Coding Execution — %'`) to `TicketCommentModel` on `ticketId`,
      grouped by `parentId`, summing `COALESCE(inputTokens, 0) +
      COALESCE(outputTokens, 0)`; returns only keys with a non-zero total
      (design.md §1.3). Dartdoc on both the interface method and its
      implementation, explaining the one-query-not-per-id contract.
- [x] T5. Same two files — add `applyTokenPrediction(String id, {required
      int low, required int high})`: a dedicated write updating only
      `predictedExecutionTokensLow`/`High`, never routed through
      `updateTicket`/`copyWith` (mirrors `applyEstimationSuggestion`'s
      existing shape) (design.md §1.3). Dartdoc both.