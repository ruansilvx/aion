---
ticketId: AIO-586
type: story
status: done
priority: none
parentId: f6538d5c-9308-4317-b18d-a288d5f56cfc
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.193569
updatedAt: 2026-09-02T21:17:20.193569
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T30. `test/features/tickets/cubit/workflow_config_cubit_test.dart`
      — role-uniqueness rejection on create/update/delete (each path);
      name-uniqueness-within-scope rejection; reorder persists correctly;
      delete-while-in-use rejection; `designStagesEnabled`/display-name-
      override round-trip (design.md §7).
- [x] T31. `test/features/tickets/data/repositories/drift_workflow_status_repository_test.dart`
      (or sibling matching existing test organization) — CRUD;
      `seedDefaultsIfEmpty`'s idempotency (calling twice never duplicates
      rows); `seedDefaultsIfEmpty` on an already-populated table is a
      no-op.
- [x] T32. `test/features/tickets/cubit/tickets_cubit_test.dart` — every
      migrated gate-check call site from T17–T22, re-verified against a
      `WorkflowStatus` fixture where role assignments differ from the
      default preset (e.g. a project that renamed the `executionTrigger`
      status to `"working"`), confirming each gate keys off role, not
      literal name. `_storyNeedsDesignReview`'s new short-circuit covered
      both `designStagesEnabled == true` (existing heuristic unchanged)
      and `== false` (always `false`).
- [x] T33. `test/core/database/app_database_test.dart` (or sibling
      covering schema migrations) — an in-memory `AppDatabase` opened at
      schema 14, upgraded to 15, ends with `defaultWorkflowStatuses`
      present and every pre-existing `tickets.status` value still valid
      against it. A fresh `onCreate` install also ends with
      `defaultWorkflowStatuses` seeded.
- [x] T34. Widget tests for `WorkflowStatusSettingsScreen` (add/rename/
      reorder/delete a status; role reassignment; the
      `designStagesEnabled` toggle and display-name override fields) —
      file location matches existing screen-test conventions. Blocked
      until T24's design-gated implementation lands.