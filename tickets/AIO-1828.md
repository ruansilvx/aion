---
ticketId: AIO-1828
type: story
status: done
priority: none
parentId: 7dfe8f34-74f6-4e29-bac4-e187745f731e
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.357704
updatedAt: 2026-09-02T21:17:20.357704
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Verify follow-ups

- [x] T8. Stage the current app icon (Flutter's default template icon —
      no real Aion branding exists yet on any platform, see the
      captured idea `aion-has-no-real-app-icon`) into a canonical
      cross-platform location, `aion/assets/icons/aion_icon.png`, and
      repoint `build_appimage.sh`'s `--icon-file` at it instead of
      reaching into the macOS-specific asset catalog directly.
      Addresses `/verify`'s WARNING 1.
- [x] T9. Scope `prepare-release/SKILL.md`'s last-release-tag lookup to
      `v*.*.*`-shaped tags (`git tag --list 'v*.*.*' --sort=-v:refname`)
      instead of "the newest tag regardless of shape." Addresses
      `/verify`'s SUGGESTION 1.
- [x] T10. Escape `.` characters in the version string before using it
      in `release.yml`'s changelog-extraction `awk` regex, so a version
      like `0.2.0` can't match unintended text via the unescaped-dot
      wildcard. Addresses `/verify`'s SUGGESTION 2.

Note: `/verify`'s WARNING 2 (the pipeline is entirely untested — no tag
has ever been pushed) isn't a code fix — it's inherent to a CI workflow
that hasn't run yet. It stays an open risk until a real `vX.Y.Z` tag
push exercises it end to end; not something `/apply` can close out.

Note: `aion-arch/specs/release.md` (the new current-state spec file) is
**not** a task here — per this project's existing convention,
`/archive` creates/merges it from `specs/spec.md` automatically; no
change's `tasks.md` ever creates its own current-state spec file
directly.