---
ticketId: AIO-304
type: story
status: done
priority: none
parentId: 751e56c8-0458-4136-85c1-9b3202d8a6da
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.163211
updatedAt: 2026-09-02T21:17:20.163211
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 2. `ProjectRepository` / `DriftProjectRepository` version mutation

- [x] 2.1 Modify
      `aion/lib/features/projects/domain/repositories/project_repository.dart` —
      add `Future<void> updateBaselineVersion(String id, String version)`,
      documented like the existing `updateLastOpened` (one-line summary +
      `@throws` if `id` doesn't exist).
- [x] 2.2 Modify
      `aion/lib/features/projects/data/repositories/drift_project_repository.dart` —
      implement `updateBaselineVersion`, mirroring `updateLastOpened`'s
      exact shape (single-column `ProjectsTableCompanion` write). Update
      class dartdoc if it enumerates supported operations.
- [x] 2.3 Modify
      `aion/lib/features/projects/domain/entities/project.dart` —
      `baselineVersion`'s doc comment no longer says "immutable through
      this change's UI"; update it to reference
      `ActiveProjectCubit.acceptBaselineUpgrade` as the legitimate
      post-creation write path (per design.md's Data model changes).
- [x] 2.4 Extend
      `test/features/projects/data/repositories/drift_project_repository_test.dart` —
      new case: `updateBaselineVersion` updates only that column, leaves
      every other field untouched, and throws for an unknown id (mirror
      `updateLastOpened`'s existing test shape).