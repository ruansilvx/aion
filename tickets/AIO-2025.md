---
ticketId: AIO-2025
type: story
status: done
priority: none
parentId: 47a2b682-2c1b-45d1-a8f3-d29a6f0ff6a1
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.383375
updatedAt: 2026-09-02T21:17:20.383375
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Domain layer — tickets feature

- [x] T2. Create `lib/features/tickets/domain/enums/ticket_sync_status.dart`
      — `TicketSyncStatus` enum (`synced`, `pendingReconcile`,
      `needsRepair`), each value documented per project.md's enum
      documentation minimum. File header comment included.
- [x] T3. Modify `lib/features/tickets/domain/entities/ticket.dart` —
      add non-nullable `syncStatus` field (default `TicketSyncStatus.synced`
      at construction sites that don't specify it), update `embedding`
      field's dartdoc to describe the new generation trigger instead of
      "population deferred."
- [x] T4. Modify `lib/features/tickets/domain/repositories/ticket_repository.dart`
      — add `Future<void> updateSyncStatus(String ticketId, TicketSyncStatus status)`
      with dartdoc explaining it's independent of `updateTicket` and
      must not perturb `updatedAt`. Also adds
      `Future<void> updateEmbedding(String ticketId, Uint8List embedding)`,
      not called out as a separate line in this checklist but required
      by T25's trigger — `updateTicket` explicitly excludes `embedding`
      per its existing contract, so a dedicated method (matching the
      existing `updateTicketStatus`/`updateTicketParent` pattern) is
      needed rather than routing through `updateTicket` as design.md's
      prose loosely suggested.