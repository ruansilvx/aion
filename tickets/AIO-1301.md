---
ticketId: AIO-1301
type: story
status: done
priority: none
parentId: f10d07c8-78ca-4146-996d-e58c802a1683
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.287759
updatedAt: 2026-09-02T21:17:20.287759
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 1. Domain additions

- [x] 1.1 Create `aion/lib/features/tickets/domain/enums/inbox_purpose.dart` —
      `InboxPurpose` enum (`brainDump`, `whatNextGuidance`,
      `releasePlanning`, `qa`), each value documented per project.md's
      enum-value minimum (what that purpose does, one line).
- [x] 1.2 Modify `aion/lib/features/tickets/domain/entities/ticket.dart` —
      add `TicketType? suggestedType` and `InboxPurpose? inboxPurpose`
      fields, `props`, and `copyWith` support (following the existing
      `TicketFieldSetter<T>` pattern used for `severity`/
      `stepsToReproduce`/etc.). Document both fields per project.md's
      "inline `///` if the field name alone is ambiguous" rule —
      state explicitly which ticket type each is meaningful for and
      that it's `null` everywhere else.
- [x] 1.3 Modify `aion/lib/features/tickets/data/models/ticket_model.dart` —
      add `suggested_type`/`inbox_purpose` nullable `TextColumn`s to
      `TicketsTable` (matching `complexity`/`sddStage`'s existing plain-
      text-enum-name storage convention, not a drift enum converter);
      extend the row↔entity conversion with
      `TicketType.values.byName(...)`/`InboxPurpose.values.byName(...)`
      parse-or-null pairs, mirroring `sddStage`'s existing conversion
      logic exactly. Document the new columns.
- [x] 1.4 Modify `aion/lib/core/database/app_database.dart` —
      `schemaVersion` → `8`; `migration.onUpgrade` gains
      `if (from < 8) { await m.addColumn(ticketsTable,
      ticketsTable.suggestedType); await m.addColumn(ticketsTable,
      ticketsTable.inboxPurpose); }`, following the exact shape of the
      existing version-6/7 branches. Update the class dartdoc's
      version-history list with a "Version 8 adds
      `TicketsTable.suggestedType`/`inboxPurpose`" line.
- [x] 1.5 Run `flutter pub run build_runner build
      --delete-conflicting-outputs` to regenerate drift/json codegen
      for the new columns/fields. (Not committed — `*.g.dart` stays
      gitignored per project.md.)
- [x] 1.6 Extend `test/features/tickets/domain/entities/ticket_test.dart`
      (or create if it doesn't yet exist covering `copyWith`/`props`) —
      new fields included in equality/copy tests. Extend
      `test/core/database/app_database_test.dart` (or equivalent
      migration test file) with a version-7-to-8 upgrade case asserting
      both new columns exist and default to `null` on existing rows.
      Extend `test/features/tickets/data/models/ticket_model_test.dart`
      — round-trip conversion for both new fields, `null` and
      populated.