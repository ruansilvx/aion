---
ticketId: AIO-1414
type: story
status: done
priority: none
parentId: 16a56dcd-558b-4d27-9747-32e5764789f1
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.302519
updatedAt: 2026-09-02T21:17:20.302519
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 4. Persistence — Drift table and repository

- [x] T10. Create `lib/features/tickets/data/models/execution_queue_table.dart`
  — `ExecutionQueueTable extends Table` → `ExecutionQueueEntryData`
  (`id`, `task_id`, `in_flight`, `queue_position`), file header + column
  dartdocs (design.md §7).
- [x] T11. Create `lib/features/tickets/data/daos/execution_queue_dao.dart`
  — `ExecutionQueueDao` (`@DriftAccessor`), `getSnapshot()` (ordered by
  `queue_position`, nulls first), `replaceSnapshot(...)` wrapped in a
  single `transaction` (delete-all + bulk insert), file header + dartdoc
  (design.md §7).
- [x] T12. Edit `lib/core/database/app_database.dart` — register
  `ExecutionQueueTable`/`ExecutionQueueDao`; bump schema version 12 → 13;
  add `if (from < 13)` migration creating the table (no backfill). Update
  the class's existing schema-version dartdoc/comment (design.md §7).
- [x] T13. Run `flutter pub run build_runner build
  --delete-conflicting-outputs` to regenerate `*.g.dart` for the new
  table/DAO and the `AppDatabase` change (not committed).
- [x] T14. Create `lib/features/tickets/domain/repositories/execution_queue_repository.dart`
  — `ExecutionQueueRepository` interface (`getSnapshot`,
  `replaceSnapshot`), dartdoc (design.md §5.3).
- [x] T15. Create `lib/features/tickets/data/repositories/drift_execution_queue_repository.dart`
  — `DriftExecutionQueueRepository` implementation (row↔entity mapping
  private method, per the Repository interface + implementation split
  convention), dartdoc (design.md §5.3).
- [x] T16. Create `lib/features/providers/domain/repositories/execution_scheduling_repository.dart`
  — `ExecutionSchedulingRepository` interface (`getMode`/`setMode`,
  `getConcurrencyCeiling`/`setConcurrencyCeiling`), dartdoc noting the
  `strictFifo`/`2` defaults (design.md §6).
- [x] T17. Create `lib/features/providers/data/repositories/shared_prefs_execution_scheduling_repository.dart`
  — `SharedPrefsExecutionSchedulingRepository` implementation, keys
  `execution_scheduling.mode`/`execution_scheduling.concurrency_ceiling`,
  dartdoc (design.md §6).
- [x] T18. Edit `lib/core/automation/automation_settings_repository.dart`
  (interface + `SharedPrefsAutomationSettingsRepository`) — wire the new
  `codingExecutionResume` context to key
  `automation_settings.coding_execution_resume_automation`, defaulting
  to `gated` (design.md §1). Update dartdoc listing the five contexts.
- [x] T19. Write/extend repository unit tests for T15/T17/T18 (mocktail,
  no real Drift instance except where persistence behavior itself is
  under test, per project.md's Testing conventions).