---
ticketId: AIO-2411
type: story
status: done
priority: none
parentId: 60978800-50b4-47e4-a9fb-70c304ad0d3f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.432867
updatedAt: 2026-09-02T21:17:20.432867
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data

- [x] T7 — `lib/features/tickets/data/daos/ticket_dao.dart`: remove
  `countChildTickets` and `deleteTicketRow`. Add `getDescendantIds`,
  `getAncestorIds` (both with cycle-guard via a visited set),
  `softDeleteByIds`, `restoreByIds`, `deleteTicketRows`,
  `getTrashedTickets`, each with dartdoc. Add a `deleted_at IS NULL`
  condition to `getAllTickets()` and to both branches of
  `searchTickets(...)` (the plain-filter branch and the FTS
  `customSelect` branch). Per design.md §4.
- [x] T8 — `lib/features/tickets/data/daos/comment_dao.dart`: replace
  `deleteCommentsForTicket(String)` with
  `deleteCommentsForTickets(List<String>)` (batch `isIn`), dartdoc
  updated. Per design.md §4.
- [x] T9 — `lib/features/tickets/data/daos/ticket_link_dao.dart`:
  replace `deleteLinksForTicket(String)` with
  `deleteLinksForTickets(List<String>)` (batch), dartdoc updated. Per
  design.md §4.
- [x] T10 — Run `flutter pub run build_runner build
  --delete-conflicting-outputs` to regenerate the DAO `.g.dart` files
  for T7–T9.
- [x] T11 — `lib/features/tickets/data/repositories/drift_ticket_repository.dart`:
  remove `deleteTicket`. Implement `trashTicket`, `trashTickets`,
  `restoreTicket`, `permanentlyDeleteTicket`, `emptyTrash`,
  `getTrashedTickets` exactly as specified in design.md §4. Add
  `deletedAt` to `_toEntity`'s mapping. Add a `deleted_at IS NULL`
  filter is not needed here (already applied at the DAO layer) — do not
  duplicate it. Dartdoc on every new/changed method.