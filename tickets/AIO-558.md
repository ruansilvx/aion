---
ticketId: AIO-558
type: story
status: done
priority: none
parentId: f6538d5c-9308-4317-b18d-a288d5f56cfc
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.193569
updatedAt: 2026-09-02T21:17:20.193569
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data — schema + DAOs + repositories

- [x] T8. New file `data/models/workflow_status_table.dart` —
      `WorkflowStatusesTable` drift table exactly as design.md §2.1
      specifies (`id`, `name`, `display_name`, `ticket_type` nullable,
      `sort_order`, `role` nullable; no FK constraints). Dartdoc per
      `ExecutionQueueTable`'s existing style.
- [x] T9. `core/database/app_database.dart` — add `WorkflowStatusesTable`
      to `@DriftDatabase(tables: [...])`; bump `schemaVersion` 14 → 15;
      add the `from < 15` `onUpgrade` branch (`m.createTable(...)` +
      `WorkflowStatusDao.seedDefaultsIfEmpty()`) and call the same seed
      from `onCreate` (design.md §2.2). Extend the class dartdoc's
      version-history list with a "Version 15 adds..." sentence, following
      the existing style for versions 2–14.
- [x] T10. New file `data/daos/workflow_status_dao.dart` —
      `WorkflowStatusDao` following `ExecutionQueueDao`'s exact shape:
      `getAll`, `insertOne`/`updateOne`/`deleteOne`, `reorder(List<String>
      idsInSortOrder)` (transactional, mirrors
      `ExecutionQueueDao.replaceSnapshot`'s transaction pattern), and
      `seedDefaultsIfEmpty` (checks the table is empty before
      batch-inserting `defaultWorkflowStatuses` — idempotent, safe to call
      from both `onCreate` and every `onUpgrade`). Full dartdoc.
- [x] T11. New file `data/repositories/drift_workflow_status_repository.dart`
      — `DriftWorkflowStatusRepository implements WorkflowStatusRepository`,
      delegating to `WorkflowStatusDao`, mapping `WorkflowStatusData` ↔
      `WorkflowStatus` (design.md §2.3). Full dartdoc.
- [x] T12. New file
      `data/repositories/shared_prefs_sdd_stage_config_repository.dart` —
      `SharedPrefsSddStageConfigRepository implements
      SddStageConfigRepository`, mirroring
      `SharedPrefsAutomationSettingsRepository`'s exact key-naming/
      serialization pattern (design.md §2.5). Full dartdoc.
- [x] T13. `domain/repositories/ticket_repository.dart` +
      `data/repositories/drift_ticket_repository.dart` —
      `updateTicketStatus`'s `TicketStatus status` parameter becomes
      `String status` on both the interface and implementation; remove
      the `TicketStatus.values.firstWhere(...)` deserialization round-trip
      at the implementation's read path (design.md §2.4) — the stored
      string now passes straight through into `Ticket.status`.