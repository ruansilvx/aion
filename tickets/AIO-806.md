---
ticketId: AIO-806
type: story
status: done
priority: none
parentId: b6289537-c4a4-4a03-997a-f046a69c31b8
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.222704
updatedAt: 2026-09-02T21:17:20.222704
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation layer — cubits

- [x] T7. `lib/features/tickets/presentation/cubit/documentation_state.dart`
  — ADD: `DocumentationState` sealed class + `DocumentationInitial`,
  `DocumentationLoading`, `DocumentationLoaded` (root docs, cached
  children map, optional search results), `DocumentationError`. Dartdoc
  per state describing the UI condition it represents.
- [x] T8. `lib/features/tickets/presentation/cubit/documentation_cubit.dart`
  — ADD: `DocumentationCubit` — `load()`, `loadChildren(String pageId)`,
  `search(String query)` (debounced), `clearSearch()`, per design.md.
  Constructor takes `TicketRepository` and `TicketDocumentSearchService`.
  Full class/method dartdoc, file header comment.
- [x] T9. **CORRECTED TARGET** — `ticket_detail_cubit.dart` does not
  exist; `TicketDetailScreen` is actually backed by the shared
  `TicketsCubit`/`TicketsState` (root-scoped, no per-screen detail
  cubit). Implemented in
  `lib/features/tickets/presentation/cubit/tickets_cubit.dart` (new
  `loadDocumentRelations(String ticketId)` method, plus an optional
  `TicketLinkRepository? linkRepository` constructor param, wired at
  `lib/core/routing/app_router.dart`'s `TicketsCubit` construction site)
  and `lib/features/tickets/presentation/cubit/tickets_state.dart`
  (`TicketDetailLoaded` gains `childDocs`/`linkedTickets`/`backlinks`
  fields, default empty). Same grouping behavior as originally specified:
  direct children via `getTicketsByParent` for `page` tickets;
  `getLinksForTicket` results grouped into `linkedTickets` (other side
  in epic/story/task/chat) vs `backlinks` (other side in page/resource).