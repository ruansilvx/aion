---
ticketId: AIO-1535
type: story
status: done
priority: none
parentId: d029505c-0048-4441-a320-7f5b53a1d2db
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.314728
updatedAt: 2026-09-02T21:17:20.314728
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Verification

- [x] T6. `flutter analyze` — must be clean, no issues.
- [x] T7. `flutter test` — full suite must pass (no Cubit/repository
  logic is added by this change, so no new automated tests are expected
  beyond what T1–T5 touch incidentally; confirm nothing existing broke).
- [x] T8. Manual interactive pass in real Chrome (per project.md's
  Testing section — not the sandboxed preview browser). **Verified live:**
  (a) wide-window (1400px/1600px) sidebar shows Tickets/Documentation,
  active item highlighted correctly on `/workspace/tickets`,
  `/workspace/tickets/new`, `/workspace/tickets/:id`,
  `/workspace/tickets/trash` (all Tickets-active), and
  `/workspace/documentation`, `/workspace/pages/new`,
  `/workspace/pages/:id` (all Documentation-active); (c) the
  secondary-actions trigger opens the popover upward with the ring
  showing correctly, "Trash" navigates to `/workspace/tickets/trash`;
  (d) navigating Tickets → Documentation → Tickets works with no dead
  end at any point (previously impossible — this is the bug the change
  fixes); (e) `TicketsListScreen`'s header renders cleanly with no
  leftover buttons/avatar, `DocumentationScreen`'s header likewise. No
  new console errors introduced (the two ONNX-embedding exceptions
  present are the same pre-existing, unrelated browser-environment
  issue documented in `documentation-section`'s tasks.md T26).
  **Not verified live:** (b) the `<= 900px` compact/bottom-tab-bar
  layout, and "Switch Project"'s navigation to `/hub` — this session's
  `resize_window` control had no effect on the real Chrome window
  (confirmed via `window.innerWidth` staying fixed at 2174px across
  multiple resize attempts, a window-un-maximize attempt, and a fresh
  tab), an environment limitation rather than a code issue. The compact
  layout was instead verified via careful code review: it reuses the
  exact same `_NavItem`/`_SecondaryActionsTrigger` widgets already
  confirmed working live, differing only in container arrangement
  (`Row` of `Expanded` cells, which — unlike the sidebar's `Column` —
  already gives tight width constraints with no `crossAxisAlignment`
  fix needed). Recommend a follow-up manual check on an actual narrow
  window/device before considering this fully closed. Chrome tab
  closed and dev server stopped at the end of the pass.