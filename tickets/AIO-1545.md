---
ticketId: AIO-1545
type: story
status: done
priority: none
parentId: de935bb2-df1a-4878-b173-bb56983b4864
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.318264
updatedAt: 2026-09-02T21:17:20.318264
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `core/contracts/` — new abstraction types

- [x] T1. `lib/core/contracts/provider_id.dart` — ADD: `enum ProviderId
  { claudeAgentSdk }`. Per design.md §1. Full dartdoc, file header.
- [x] T2. `lib/core/contracts/tool_access_tier.dart` — ADD: `enum
  ToolAccessTier { noTools, readOnly, full }`. Per design.md §1. Full
  dartdoc, file header.
- [x] T3. `lib/core/contracts/agent_model_descriptor.dart` — ADD:
  `AgentModelDescriptor` (`Equatable`; `providerId`, `modelId`, `label`,
  `contextWindowTokens`) — replaces `AgentModel`'s role as a
  provider-scoped model value object. Per design.md §1. Full dartdoc,
  file header.
- [x] T4. `lib/core/contracts/consumption_signal.dart` — ADD: sealed
  `ConsumptionSignal` (`message`), `UsageWindowConsumption`,
  `CostConsumption` (`amountUsd` nullable). Per design.md §1. Full
  dartdoc, file header.
- [x] T5. `lib/core/contracts/agent_provider.dart` — ADD:
  `AgentProvider` abstract interface (`id`, `displayName`,
  `supportedToolAccessTiers`, `availableModels`, `client`,
  `describeOverage(String) → ConsumptionSignal`,
  `normalizeErrorMessage(String) → String`). Depends on T1–T4 and the
  existing `agent_model_client.dart`. Per design.md §1. Full dartdoc,
  file header.
- [x] T6. `lib/core/contracts/provider_registry.dart` — ADD:
  `ProviderRegistry` abstract interface (`availableProviders`,
  `providerById(ProviderId) → AgentProvider`, throws `StateError` if
  unregistered). Depends on T1, T5. Per design.md §1. Full dartdoc, file
  header.