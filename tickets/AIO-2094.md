---
ticketId: AIO-2094
type: task
status: done
priority: none
parentId: 30a503e4-3053-4230-86e6-56ece16eb2bf
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.388088
updatedAt: 2026-09-02T21:17:20.388088
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T12. In

`lib/features/tickets/presentation/cubit/tickets_cubit.dart`: **bugfix,
do this before T13.** Per `design.md` §3, change
`_sddStageAdvanceCheck`'s `case SddStage.proposed:` (story branch only —
the epic branch, checking child Stories archived, is unchanged) so it
requires only "Tasks exist" (not done) when the Story needs design
review (`_storyNeedsDesignReview`), while still requiring "Tasks done"
when it doesn't (the skip-design branch, unaffected — it never had a
pre-code stage to protect). Change its `case SddStage.designSync:` to
additionally require every child Task to be `TicketStatus.done`
alongside the existing `_designSyncApproved` check, before allowing the
advance to `verifying`. Update both cases' inline comments to explain
why (a UI-touching Story could never reach `designBrief` under the old
precondition, since reaching it required Tasks already done). No enum
or field changes — `SddStageBlockReason.awaitingDesignApproval`'s
existing hint text already covers both failure modes.