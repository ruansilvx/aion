---
ticketId: AIO-1403
type: task
status: done
priority: none
parentId: ffc75440-4aa2-471a-a14a-dd48c40afcbc
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.302519
updatedAt: 2026-09-02T21:17:20.302519
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T1a. **(added by /design-sync)** Edit

`aion/lib/design_system/tokens/aion_colors.dart` (`AionColorsHubTokens`
extension) — add `cancelFocusRing(bool isDark)` and `dangerText(bool
isDark)` derived helpers per the Claude Design export's design.md §7.1
(`cancelFocusRing` = `danger.withValues(alpha: isDark ? 0.30 : 0.16)`,
a `danger`-keyed parallel of the existing `focusRing`; `dangerText` =
`Color.lerp(danger, white/black, 0.22/0.35)`, derived exactly like the
existing `warningText`). Dartdoc on each, following the existing
`focusRing`/`warningText` pattern. No new `AionColors` fields, no
per-theme `arctic`/`obsidian` const changes — both are pure formulas
over existing base colors. Sequenced early since T26a/T27/T40/T45/T46
below depend on them.