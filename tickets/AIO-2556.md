---
ticketId: AIO-2556
type: story
status: done
priority: none
parentId: 5fe97f9a-b8b1-4f9c-b8c2-ea8e89548d64
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.450420
updatedAt: 2026-09-02T21:17:20.450420
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 3. Workflow-status UI wiring

- [x] T4. `aion/lib/features/tickets/presentation/cubit/
      workflow_config_state.dart`: add `sharedBaseStatuses` and
      `effectiveStatusesForType(TicketType)` getters to
      `WorkflowConfigLoaded`, and the two top-level resolver functions
      `resolveSharedStatusOrder(WorkflowConfigState)`/
      `resolveStatusOrderForType(WorkflowConfigState, TicketType)` —
      exact shapes in `design.md` §3b/§3c. Import
      `default_workflow_statuses.dart` and `ticket_type.dart` here (this
      becomes the one file that still imports the former). Dartdoc on
      both getters and both functions; this file's existing header
      comment needs no change. Per design.md §2 Status-dot color rule.
- [x] T5. `aion/lib/core/routing/app_router.dart`: add a
      `BlocProvider<WorkflowConfigCubit>` inside
      `_WorkspaceShellState.build`, alongside the existing
      `BlocProvider<TicketsCubit>`, constructed from the
      `WorkflowStatusRepository`/`SddStageConfigRepository`/
      `TicketRepository`/`WorkflowSkillAttachmentRepository`/
      `WorkflowPromptTemplateRepository` already provided in the same
      `MultiRepositoryProvider`, `..load()`ed immediately. Leave the two
      existing per-route `WorkflowConfigCubit` constructions
      (`/workspace/settings/workflow`,
      `/workspace/settings/workflow/templates`) untouched. Add a doc
      comment on the new provider explaining why it's additive, not a
      replacement (see `design.md` §3a).
- [x] T6. `aion/lib/features/tickets/presentation/screens/
      tickets_board_view.dart`: remove the module-level
      `_defaultStatusOrder` constant and its
      `default_workflow_statuses.dart` import. Replace its one read (the
      board's own column list) with
      `resolveSharedStatusOrder(context.watch<WorkflowConfigCubit>().state)`.
      Update `_MoveToStatusMenuState._showOverlay` the same way, via its
      own `context.watch<WorkflowConfigCubit>()` call. Update this
      file's doc comments that currently describe the fixed-default-set
      limitation (the top-of-file note referencing "a follow-up change
      threads `WorkflowConfigCubit` through it" — this is that change).
      Per design.md §2 Status-dot color rule, §3 BoardColumn, §4
      MoveToStatusMenu.
- [x] T7. `aion/lib/features/tickets/presentation/screens/
      tickets_list_screen.dart`: at the `TicketFilterPopover`/
      `TicketColumnsPopover` construction sites (lines 1122/1158),
      derive `resolveSharedStatusOrder(context.watch<
      WorkflowConfigCubit>().state)` locally. Per design.md §5
      TicketColumnsPopover, §6 TicketFilterPopover.
- [x] T8. `aion/lib/features/tickets/presentation/widgets/
      ticket_columns_popover.dart`: remove the module-level
      `_defaultStatusOrder` constant and its `default_workflow_
      statuses.dart` import. Call `resolveSharedStatusOrder(context.
      watch<WorkflowConfigCubit>().state)` inside `build`, alongside its
      existing `ticketStatusLabel(context, ...)` calls. Update the class
      doc comment's "6 `TicketStatus` values" / known-limitation note.
      Per design.md §2 Status-dot color rule, §5 TicketColumnsPopover.
- [x] T9. `aion/lib/features/tickets/presentation/widgets/
      ticket_filter_popover.dart`: same treatment as T8. Per design.md
      §2 Status-dot color rule, §6 TicketFilterPopover.
- [x] T10. `aion/lib/features/tickets/presentation/widgets/
      ticket_selection_bar.dart`: convert `BulkStatusMenu`'s `static
      final List<String> order` to a `build`-time local via
      `resolveSharedStatusOrder(context.watch<WorkflowConfigCubit>()
      .state)`. Update the field's former doc comment (moves from a
      field doc to inline context at its new call site). Per design.md
      §2 Status-dot color rule, §7 BulkStatusMenu.
- [x] T11. `aion/lib/features/tickets/presentation/widgets/
      ticket_metadata_section.dart`: replace the status `SelectionMenu`'s
      `items: [for (final s in defaultWorkflowStatuses) s.name]` with
      `resolveStatusOrderForType(context.watch<WorkflowConfigCubit>()
      .state, ticket.type)`. Remove the `default_workflow_statuses.dart`
      import if nothing else in this file still uses it. Per design.md
      §2 Status-dot color rule, §8 Ticket detail status picker.
- [x] T12. Update the three existing test harnesses that construct a
      `TicketsBoardView`/`TicketsListScreen`/`ticket_metadata_section.dart`
      widget tree, so T5–T11's new `context.watch<WorkflowConfigCubit>()`
      calls don't throw `ProviderNotFoundException` against them:
      `test/features/tickets/presentation/screens/
      tickets_board_view_test.dart`'s shared `wrapBoard`/inline builder
      (~line 59) and its second inline builder (~line 387),
      `test/features/tickets/presentation/screens/
      tickets_list_screen_test.dart`'s shared `_wrap` (~line 70), and
      `test/features/tickets/presentation/widgets/
      ticket_metadata_section_test.dart`'s shared `_wrap` (~line 74) —
      each gains a `BlocProvider<WorkflowConfigCubit>.value(value:
      <a loaded fake/mock>)` alongside its existing `TicketsCubit`
      provider. Each harness is a single shared function reused by every
      test case in its file, so this is three edits, not a
      per-test-case sweep. Use a `WorkflowConfigLoaded` fixture built
      from `defaultWorkflowStatuses` unless a specific test case needs a
      custom/reconfigured status set to exercise T6–T11's live-status
      behavior.
- [x] T13. Run `flutter analyze` and the existing Cubit/widget test
      suite; fix any break from the above before moving on. No new unit
      tests are required for T4's getters/resolvers specifically if
      existing `WorkflowConfigCubit`/`workflow_config_state` tests
      already cover `WorkflowConfigLoaded` construction — extend them
      with a case for each new getter (a mixed base+extension status
      list; assert `sharedBaseStatuses` excludes extensions and
      `effectiveStatusesForType` includes only the matching type's) if
      no such coverage exists yet.