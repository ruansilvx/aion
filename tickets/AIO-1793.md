---
ticketId: AIO-1793
type: story
status: done
priority: none
parentId: 4e35f8b2-61bc-412f-9537-808e0bb40b91
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.353397
updatedAt: 2026-09-02T21:17:20.353397
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# UI

- [x] **T8** — `aion/lib/features/tickets/presentation/widgets/release_summary_section.dart`
  (new file): `ReleaseSummarySection` widget per `design.md` §5.1 —
  reuses the `design_system` `LinkedTicketsSection` molecule for the
  linked-work list, adds a `Prepare Release` `AppButton` (disabled with
  a tooltip when there's nothing linked). File header, widget class
  dartdoc, dartdoc on every public constructor parameter. Visual spec:
  design.md's Claude Design export, §1 "ReleaseSummarySection" (§1.1–1.4
  — distinct from this doc's own earlier architecture §5.1 above the
  export's `---` divider; the export owns pixel-level states).

- [x] **T9** — `aion/lib/features/tickets/presentation/screens/ticket_detail_screen.dart`:
  wire in `ReleaseSummarySection` for `ticket.type == TicketType.release`
  per `design.md` §5.2 — the one new type-conditional branch this task
  adds; every other type's existing rendering is untouched. Update the
  screen class's own dartdoc to note the `release`-type branch. Visual
  spec: design.md's Claude Design export §1.1 "Container & layout" for
  the render slot (renders where `epic`/`story` render `_SddStageSection`
  today).

- [x] **T10** — `aion/lib/features/tickets/presentation/screens/release_draft_screen.dart`
  (new file): `ReleaseDraftScreen` per `design.md` §5.3 — editable
  changelog/version `AppTextField`s (version field replaced by a "no
  version file detected" notice when `draft.detectedVersionFile` is
  `null`), Cancel/`Confirm & Tag` actions, `Confirm & Tag` gated by
  `AppConfirmDialog` before calling `TicketsCubit.confirmRelease`. On
  failure, stays on screen with an error banner and the same editable
  draft rather than popping — matches T7's retry rationale. File
  header, widget class dartdoc, dartdoc on every public constructor
  parameter. Visual spec: design.md's Claude Design export §3
  "ReleaseDraftScreen" (§3.1–3.7, incl. the derived-tag-preview pill
  §3.6 and the "no version file" footer-relabeling behavior defined in
  §6), plus §4 "Tag/branch confirmation dialog" for the `Confirm & Tag`
  gate's exact content, and §5 "Push-failure error banner" for the
  on-failure state (§5 supersedes this task's plain "error banner"
  wording with a fully specified component — the banner stays mounted
  with the draft, is not a toast/dialog, and shows a "committed and
  tagged locally" sub-line when the local write succeeded but the push
  failed) and §6 "No version file detected notice" for the two
  unsupported-stack projects (`go.mod`/`requirements.txt`) — in that
  case `Confirm & Tag` relabels to "Save changelog" and skips §4
  entirely.

- [x] **T11** — `aion/lib/features/tickets/presentation/screens/inbox_screen.dart`:
  add the fifth "Cut a release" launcher card per `design.md` §5.4 —
  fires immediately (matches the existing "What's next" pattern, not
  the expand-in-place brain-dump/Q&A pattern), calls
  `TicketsCubit.autoCreateReleaseTicket()`, navigates to the new
  ticket's detail route on success, shows an inline "nothing to
  release" message on `null`. Update the screen class's own dartdoc to
  list five purposes/actions instead of four. Note: this card is
  **not** a new `InboxPurpose` value — see `design.md` §5.4 for why.
  Visual spec: design.md's Claude Design export §2 "Inbox 'Cut a
  release' launcher card" — note its layout call: on the wide 2×2 grid
  the fifth card renders **full-width** in its own third row (§2.1),
  not half-width, and it's placed after "Plan a release" rather than
  adjacent to it (deliberate mis-tap avoidance, §2.1). Its in-flight
  state (§2.4) and the "nothing to release" outcome are also specified
  here as §7 "'Nothing to release' inline message" (§7.1–7.3) — a
  dismissible notice below the launcher grid, not a toast/dialog.

- [x] **T12** — `aion/lib/features/tickets/presentation/screens/tickets_board_view.dart`:
  add the `TicketsErrorReason.releasePreparationFailed` case to the
  existing reason→toast `switch` (alongside `specWriteFailed`). Visual
  spec: design.md's Claude Design export §1.3.5 for this toast's exact
  copy ("Couldn't draft the release", `warning-circle` icon in
  `danger`) — the parallel "Couldn't scan for unreleased work" toast
  from §2.4 (auto mode's own failure path) is a separate, local
  `AppToast` call inside T11's Inbox screen work, not a
  `TicketsErrorReason` case, since `autoCreateReleaseTicket` returning
  `null` for "nothing to release" is a distinct, non-error outcome from
  an actual scan failure per §2.4's own three-outcome breakdown.