---
ticketId: AIO-577
type: story
status: done
priority: none
parentId: f6538d5c-9308-4317-b18d-a288d5f56cfc
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.193569
updatedAt: 2026-09-02T21:17:20.193569
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# UI — gated behind the design gate (T24–T26)

- [x] T24. New file
      `presentation/screens/workflow_status_settings_screen.dart` —
      `WorkflowStatusSettingsScreen`: `_TicketStatusesSection` (per-type
      selector, reorderable `WorkflowStatus` list with name/displayName/
      role editing, add-status control) + `_SddStagesSection`
      (`designStagesEnabled` toggle, 5 display-name override fields), per
      Component Spec §2 (screen layout/header), §3 (section headers), §4
      (`ScopeSelector`), §5 (`StatusRow` incl. drag/inherited/error
      states), §6 (`AddStatusControl` incl. hand-rolled reorder, §6.4),
      §7 (validation/error states), §8 (`SddToggleRow` +new `ToggleSwitch`
      atom), §9 (`SddStageRenameRow`), §10 (`RoleDropdown`), §11 (token
      usage — no new `AionColors`/`AionRadius`/`AionSpacing` members). Use
      `AionColorsHubTokens.errorRing`/`.neutralBorderTint`/`.neutralTint`
      by calling the real methods, not the doc's restated literals (see
      proposal.md's Design gate block for the three numeric discrepancies).
      Built from `design_system/` atoms/molecules (`AppButton`,
      `AppTextField`) plus the new `ToggleSwitch`/`RoleDropdown`/
      `StatusRow`/`ScopeSelector`/`AddStatusControl` atoms this spec
      defines. Full dartdoc + file header.
- [x] T25. `core/routing/app_router.dart` — register
      `WorkflowStatusSettingsScreen`'s route.
      `presentation/screens/workspace_nav_shell.dart` (or wherever the
      secondary-actions popover is defined) — add an entry alongside the
      existing provider `SettingsScreen` link.
- [x] T26. `tickets_board_view.dart`, `ticket_filter_popover.dart`,
      `ticket_columns_popover.dart`, `ticket_metadata_section.dart`,
      `ticket_sort_comparator.dart` — replace every `TicketStatus.values`
      iteration/comparator with a read of `WorkflowConfigCubit`'s current
      `statuses` (filtered/merged by the relevant `ticketType`, sorted by
      `sortOrder`), per design.md §5.2's file list. Thread
      `WorkflowConfigCubit`'s state into each widget the same way
      `TicketsLoaded`/`TicketDetailLoaded` are already threaded.