---
ticketId: AIO-2491
type: story
status: done
priority: none
parentId: acd2b438-557e-4a1f-9e2b-4fae6ca86a5f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.441422
updatedAt: 2026-09-02T21:17:20.441422
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation layer — state & cubit

- [x] T5. In `lib/features/tickets/presentation/cubit/trash_state.dart`,
      add `purgeEligibleCount` as `TrashLoaded`'s third positional
      field. Update its class dartdoc and `props`.

- [x] T6. In `lib/features/tickets/presentation/cubit/trash_cubit.dart`:
      add `static const Duration purgeAgeThreshold = Duration(days:
      30);` with a dartdoc explaining it's fixed/non-configurable;
      update `load()` to compute `purgeEligibleCount` from the `all`
      list it already fetches and pass it into the emitted
      `TrashLoaded`; add `purgeOldTrash()` (dartdoc, emit-try-catch
      shape matching `emptyTrash()`) that calls
      `TicketRepository.purgeTrashOlderThan(purgeAgeThreshold)` then
      reloads.