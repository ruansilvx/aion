---
ticketId: AIO-926
type: story
status: done
priority: none
parentId: 0745d23f-c178-4df7-a57b-fde9df1feaf2
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.236688
updatedAt: 2026-09-02T21:17:20.236688
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T5. Added round-trip cases to the existing
      `aion/test/core/markdown/ticket_markdown_serializer_test.dart`:
      a ticket with `deletedAt: null` serializes to `deletedAt: null`
      and parses back to `null`; a ticket with a real `deletedAt`
      serializes to its ISO-8601 string and parses back to the
      identical `DateTime`; a frontmatter block with no `deletedAt` key
      at all still parses as `ParsedOk` with a `null` value for that
      field (backward compatibility with already-committed ticket
      files). 7/7 in this file passing.
      **Found a real, separate, pre-existing bug while writing this —
      not fixed here, out of scope**: `Ticket.copyWith` has no
      `deletedAt` parameter at all, and its returned `Ticket(...)`
      never passes `deletedAt:` either — so `deletedAt` silently resets
      to `null` on *every* `copyWith` call, regardless of the
      original's value. Confirmed harmless in current practice only
      because `DriftTicketRepository.updateTicket` (the only write path
      that persists a whole `copyWith`'d `Ticket`) explicitly excludes
      `deletedAt` from what it writes to the DB — so the incorrect
      in-memory value it produces is never actually persisted today.
      Still a real correctness gap in `Ticket.copyWith` itself
      (`lib/features/tickets/domain/entities/ticket.dart`) that a
      future caller could trip over; unrelated to this change's own
      scope (a domain-entity fix, not a markdown-serialization one), so
      flagged rather than fixed here. The test above builds a `Ticket`
      directly instead of via `copyWith` to work around it.
- [x] T6. Added a case to the existing
      `aion/test/features/tickets/data/services/ticket_git_projector_test.dart`.
      **Corrected from this task's original wording**: the existing
      tests in this file mock `GitRepositoryClient.hasChanges` directly
      (a stubbed boolean, disconnected from any real file diff), so
      they can't exercise the actual defect — that bug lived entirely
      in whether `deletedAt` was serialized, not in
      `TicketGitProjector`'s own logic, which is unchanged by this
      task. Added a new group instead, using a *real*
      `GitRepositoryClient` against a real temp git repo (`git init`,
      real `git log` inspection) — a ticket is projected 'created',
      then 'trashed' (deletedAt set), then 'restored' (deletedAt
      cleared), then 'restored' again unchanged, asserting the real
      commit count is 1, 2, 3, 3 — proving trashing/restoring now
      produce real commits while an actually-unchanged re-projection
      still correctly no-ops. 4/4 in this file passing.
- [x] T7. Added cases to the existing
      `aion/test/features/tickets/data/services/ticket_db_reconstruction_service_test.dart`
      (added an optional `deletedAt` param to the file's local `ticket()`
      test-fixture helper to support them): a new ticket file with
      `deletedAt` set reconstructs to a `Ticket` with that same
      `deletedAt` (import case); reconstructing an already-existing row
      from a file with `deletedAt` present overwrites the row's
      previous value (update case). 8/8 in this file passing. Full
      suite run afterward (`flutter analyze` + `flutter test`): clean,
      721/721 passing, no regressions.