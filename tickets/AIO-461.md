---
ticketId: AIO-461
type: story
status: done
priority: none
parentId: 088b2ab7-494c-4459-a671-01ae0fb60e0a
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.181925
updatedAt: 2026-09-02T21:17:20.181925
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Ticket-feature widgets

- [x] 8. Modify `lib/features/tickets/presentation/widgets/ticket_selection_bar.dart`
      — add `required ValueChanged<TicketStatus> onChangeStatus` and
      `required ValueChanged<TicketPriority> onChangePriority` constructor
      params. Full dartdoc on both, per `onDelete`'s existing shape.
      (design.md §7; per design.md "Bulk Status & Priority Edit" export §1)
- [x] 9. Modify `lib/features/tickets/presentation/widgets/ticket_selection_bar.dart`
      — add `BulkOverlayRow`, the shared row widget used by both new
      overlays: `GestureDetector` → `Focus` → `AnimatedContainer` (100ms
      `easeOut`) → `Row` (leading indicator, `SizedBox(width: 10)`,
      `Expanded` label). Default/hover/focused/pressed fill states exactly
      per spec (no disabled/error state — every value is always
      selectable). Full dartdoc. (per design.md "Bulk Status & Priority
      Edit" export §4)
- [x] 10. Modify `lib/features/tickets/presentation/widgets/ticket_selection_bar.dart`
      — add `BulkStatusMenu`: an `Overlay`/`OverlayEntry`/
      `CompositedTransformFollower` overlay (opens upward by default,
      flips downward if it would clip; full-screen transparent barrier
      dismisses on outside tap; `Escape` dismisses without applying),
      listing all six `TicketStatus` values via `BulkOverlayRow` in fixed
      order (Backlog · To Do · In Progress · In Review · Done · Cancelled)
      with the status-dot leading indicator per value, first row
      (Backlog) autofocused on open. Selecting a row calls the widget's
      `onSelected(status)` and closes the overlay. Full dartdoc.
      (per design.md "Bulk Status & Priority Edit" export §2)
- [x] 11. Modify `lib/features/tickets/presentation/widgets/ticket_selection_bar.dart`
      — add `BulkPriorityMenu`: same overlay mechanics, panel chrome, and
      auto-focus behavior as `BulkStatusMenu` (§10), listing all five
      `TicketPriority` values via `BulkOverlayRow` in fixed order
      (Critical · High · Medium · Low · None) with the priority-accent
      square indicator per value (hollow square for None). Selecting a
      row calls the widget's `onSelected(priority)` and closes the
      overlay. Full dartdoc. (per design.md "Bulk Status & Priority Edit"
      export §3)
- [x] 12. Modify `lib/features/tickets/presentation/widgets/ticket_selection_bar.dart`
      — in `build()`, insert the Status and Priority triggers between the
      Select-all/Deselect-all `AppButton` and the destructive Delete
      `AppButton`: default/hover/focused/pressed/disabled (0 selected)
      states exactly per spec, each wrapped in a `LayerLink` and opening
      its `BulkStatusMenu`/`BulkPriorityMenu` on tap, wired to
      `onChangeStatus`/`onChangePriority`. Add the `LayoutBuilder`-driven
      compact breakpoint (icon-only 34×34 triggers, no text label, count
      label compacts to bare number) for narrow widths. Import
      `ticketStatusLabel`/`ticketPriorityLabel` from
      `tickets_board_view.dart` for row labels. (per design.md "Bulk
      Status & Priority Edit" export §1.5, §1.6, §1.7b)