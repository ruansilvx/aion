---
ticketId: AIO-416
type: story
status: done
priority: none
parentId: 3e095e14-a7e9-489e-891b-6a291c68e280
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.175274
updatedAt: 2026-09-02T21:17:20.175274
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T18. `test/features/tickets/data/repositories/drift_ticket_repository_test.dart`
      (where `DriftTicketLinkRepository`/`TicketLinkDao` are already
      exercised, per the existing `getLinksForTicket excludes trashed`
      group) — new cases for `getLinksByTypes`: returns only rows
      matching the requested types across multiple tickets; excludes a
      row whose other side is trashed (mirrors the existing
      `getLinksForTicket excludes trashed` group); returns `[]` when no
      rows match.
- [x] T19. `test/features/tickets/cubit/tickets_cubit_test.dart` — new
      cases: `_computeBlockedTicketIds`/`blockedTicketIds` via
      `searchTickets` — a ticket with a `blockedBy` link to a non-`done`
      ticket appears in `blockedTicketIds`; the same link with the
      blocker `done` does not; a `blocks` link (reversed direction)
      resolves the *target* as blocked; `updateTicketStatus` moving a
      blocker to `done` while `TicketsLoaded` is current removes the
      blockee from `blockedTicketIds` on the next emission.
- [x] T20. Same file — decomposition cases: `_parseDecomposition` on a
      reply with a valid `## Decomposition` block returns the expected
      ordered pairs, including one with a `(blockedBy: ...)` suffix;
      returns `[]` for a reply with no block; returns `[]` for a block
      whose lines don't match the expected prefix.
      `_materializeDecomposition` creates one child ticket per parsed
      line under the parent; creates a `blockedBy` link for a resolvable
      `blockedByTitle`; still creates the child ticket (but no link) for
      an unresolvable `blockedByTitle`; is a no-op when
      `_linkRepository` is unset or the reply has no block. A
      `SddStage.proposed` turn's `_runStageChatTurn` calls
      `_materializeDecomposition` exactly once per successful turn, not
      for any other stage.
- [x] T21. `test/features/tickets/presentation/screens/tickets_board_view_test.dart`
      — a card whose ticket id is in `TicketsLoaded.blockedTicketIds`
      renders `_BlockedBadge`; a card whose id is absent renders neither
      it nor any change to today's existing badge layout; a card that
      is both blocked and has an execution-state badge (design.md §5.1's
      badge-order rule) renders both, in the specified order.
- [x] T22. New or existing widget-test file for `TicketLinkPicker`
      (locate any existing coverage first, e.g. under
      `test/features/tickets/presentation/widgets/`) — selecting a
      non-default `TicketLinkType` from the new `SelectionMenu` before
      tapping a candidate row calls `onSelected` with that type, not the
      default `relatesTo`; the picker offers exactly the types passed
      via `linkTypeOptions`, nothing else.
- [x] T23. Widget-test coverage for `ticket_metadata_section.dart`'s
      widened gate (locate the existing test file covering
      `TicketMetadataSection`/`TicketDetailScreen`'s Linked Tickets
      section first) — an `epic`/`story`/`task` ticket now renders the
      Linked Tickets section (previously rendered nothing for these
      types); selecting `blocks`/`blockedBy` from the picker calls
      `createLink` with that exact type, not `relatesTo`; `resource`'s
      existing single-tap `relatesTo` behavior is unchanged (no picker
      step introduced for it).