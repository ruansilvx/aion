---
ticketId: AIO-672
type: story
status: done
priority: none
parentId: 99ba9627-f06d-4c58-a59d-fee28572235d
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.203944
updatedAt: 2026-09-02T21:17:20.203944
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `GraphCanvas` pane (`lib/features/providers/presentation/screens/decision_graph_editor_screen.dart`)

- [x] `layout()`'s `isError` check: replace the bare
      `decisionConditionSpecById(node.conditionId) == null` term with
      `!isRecognizedConditionId(node.conditionId, automationContext)`.
      Error-chrome treatment per design.md (Component Spec) §4.1
      (error/incomplete rule node row).
- [x] Node chrome title (`spec?.displayName ?? node.conditionId`, two
      occurrences) → `decisionNodeTitle(node)`. Eyebrow/title text and
      styling per design.md (Component Spec) §4.1 Canvas node card
      (extends DG §1.2).
- [x] Node chrome parameter chip (`spec == null ? null :
      conditionParameterSummary(spec, node.conditionParams)`) →
      `decisionNodeSummary(node)`. Chip fill/border/padding per design.md
      (Component Spec) §4.1 — the bordered-chip variant is the one visual
      difference from a preset node's chip.
- [x] `onCreateChainedChild` closure: add a
      `conditionId == ruleBuilderConditionId` branch seeding
      `conditionParams` via `defaultRuleConditionParams(automationContext)`
      instead of `defaultConditionParams(spec)`.