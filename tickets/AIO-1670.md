---
ticketId: AIO-1670
type: story
status: done
priority: none
parentId: 6906ad43-8260-4640-9991-36b0b552c062
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.332612
updatedAt: 2026-09-02T21:17:20.332612
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 5. Generic skill baseline (`assets/baseline/0.3.0/`)

- [x] Create `assets/baseline/0.3.0/manifest.json` per design.md §1 —
      same 13 skill keys + `config/model-config`, all `bundledPath`s
      re-pointed to `0.3.0/`, `conventions/flutter-conventions` renamed
      to `conventions/architecture-conventions`.
- [x] Create the 12 **descriptive** skill content files under
      `assets/baseline/0.3.0/skills/`: `apply.md`, `archive.md`,
      `brainstorm.md`, `capture.md`, `clean-up.md`, `design-brief.md`,
      `design-sync.md`, `explore.md`, `handoff.md`, `propose.md`,
      `what-next.md`, `ticket-lint.md` — each a short (15–30 line),
      self-contained, stack-agnostic description of that stage's purpose
      in the generic `explore → propose → apply → verify → archive`
      cycle, per design.md §1. No references to Flutter/Dart, no
      pointers into `aion-arch`.
- [x] Create `assets/baseline/0.3.0/skills/verify.md` — **operative**
      instructions (not descriptive), per design.md §1's exact template:
      review the diff against the task, run whatever build/lint/test
      commands are appropriate (checking `Project conventions` for a
      suggested one), and end with exactly one of `"VERIFICATION:
      PASSED"` / `"VERIFICATION: FAILED — <reason>"`. This file is fed
      directly into a real coding-execution verify turn (§6) — get the
      exact terminal-line wording right, since §6's parsing depends on
      it verbatim.
- [x] Create `assets/baseline/0.3.0/architecture_convention.md` — the
      generic placeholder content per design.md §1.
- [x] Copy `assets/baseline/0.2.0/model_config.json` to
      `assets/baseline/0.3.0/model_config.json` unchanged (already
      stack-agnostic).
- [x] Update `lib/features/projects/data/repositories/
      bundled_baseline_repository.dart`'s `_bundledVersions` to include
      `'0.3.0'` (appended last).
- [x] Update `pubspec.yaml`'s `flutter.assets` list to add
      `assets/baseline/0.3.0/` and `assets/baseline/0.3.0/skills/`.
- [x] Update `test/features/projects/data/repositories/
      bundled_baseline_repository_test.dart` if it asserts an exact
      `_bundledVersions` list or count. It did (`getAvailableBaselineVersions`
      test) — updated to include `'0.3.0'`.