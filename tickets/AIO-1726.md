---
ticketId: AIO-1726
type: story
status: done
priority: none
parentId: 9b27d880-86fa-4105-be02-07f20851da13
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.337236
updatedAt: 2026-09-02T21:17:20.337236
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T20. `test/features/providers/presentation/cubit/provider_settings_cubit_test.dart`
  — `bloc_test`-based coverage of `ProviderSettingsCubit`: `load()`
  emits `Loading` → `Ready(connected)` on a successful test and →
  `Ready(disconnected)` on a failed one (mock `AgentModelClient` via
  `mocktail`); `testConnection()`/`selectModel()` no-op while a test is
  already in flight; `selectModel()` persists via the mocked
  `AgentSettingsRepository` and re-tests against the newly selected
  model.
- [x] T21. `test/features/providers/data/repositories/shared_prefs_agent_settings_repository_test.dart`
  — round-trip test: `setSelectedModel` then `getSelectedModel` returns
  the same `AgentModel`; unset key defaults to `AgentModel.sonnet`.
  Real logic (enum↔string mapping + default), not a trivial pass-through
  — not skipped per project.md's Testing conventions.