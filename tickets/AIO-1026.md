---
ticketId: AIO-1026
type: story
status: done
priority: none
parentId: 44553dea-eff8-46e5-9633-fe0f2ac06575
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.247955
updatedAt: 2026-09-02T21:17:20.247955
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Per-file literal retrofit

- [x] **T13** — `aion/lib/core/widgets/app_header.dart`: replace the
      `'Back'` semantic label with `context.l10n.commonBack`.
- [x] **T14** — `aion/lib/core/widgets/app_spinner.dart`: replace the
      `'Loading'` semantic label with `context.l10n.commonLoading`.
- [x] **T15** — `aion/lib/core/widgets/app_dropdown.dart` and
      `aion/lib/core/widgets/app_text_field.dart`: replace the
      hardcoded `' *'` required-field marker with
      `context.l10n.commonRequiredMarker`.
- [x] **T16** — `aion/lib/features/tickets/presentation/screens/tickets_list_screen.dart`:
      replace all identified literals (empty state, eyebrow, title,
      search hint, retry button, create-ticket semantic label/FAB text,
      list/board view-switch semantic labels) with `context.l10n.*`
      lookups, using the shared `commonNewTicket` key from T3.
- [x] **T17** — `aion/lib/features/tickets/presentation/screens/tickets_board_view.dart`:
      replace the empty-column text and the ticket-move semantic label
      (ICU `{ticketId}` placeholder) with `context.l10n.*` lookups.
- [x] **T18** — `aion/lib/features/tickets/presentation/screens/ticket_detail_screen.dart`:
      replace all field captions, placeholders, semantic labels, the
      created-date label, and the comments-count label with
      `context.l10n.*` lookups; replace the ad hoc `_formatDate()`
      helper with `intl`'s `DateFormat` (e.g. `DateFormat.yMMMd()`).
- [x] **T19** — `aion/lib/features/tickets/presentation/screens/create_ticket_screen.dart`:
      replace all field labels/hints and the submit button with
      `context.l10n.*` lookups, using the shared `commonNewTicket` key
      (deduping the previously-separate "Create ticket" literal).
- [x] **T20** — `aion/lib/features/tickets/presentation/widgets/inline_editable_field.dart`:
      replace the `'Cancel'`/`'Save'` button labels with
      `context.l10n.commonCancel` / `context.l10n.commonSave`.