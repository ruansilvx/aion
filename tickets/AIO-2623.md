---
ticketId: AIO-2623
type: story
status: done
priority: none
parentId: 64786df9-02fa-4b5f-98a7-ca811e51409f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.455295
updatedAt: 2026-09-02T21:17:20.455295
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 5. Presentation — BLoC

- [x] **T13** — Create `lib/features/tickets/presentation/cubit/tickets_state.dart`
  Define sealed `TicketsState` with all states from spec.md including
  `TicketDetailLoaded`. All extend `Equatable`.

- [x] **T13b** — Create `lib/features/tickets/presentation/cubit/comments_state.dart`
  Define sealed `CommentsState` with states: `CommentsInitial`, `CommentsLoading`,
  `CommentsLoaded` (holds `List<TicketComment>`), `CommentsError` (holds
  `String message`), `CommentAdding` (holds current list),
  `CommentAdded` (holds updated list). All extend `Equatable`.

- [x] **T14** — Create `lib/features/tickets/presentation/cubit/tickets_cubit.dart`
  `TicketsCubit extends Cubit<TicketsState>`.
  Methods: `loadTickets()`, `createTicket({type, title, description,
  priority = TicketPriority.none, parentId})`, `getTicketById(String id)`.
  Inject `TicketRepository` via constructor.

- [x] **T14b** — Create `lib/features/tickets/presentation/cubit/comments_cubit.dart`
  `CommentsCubit extends Cubit<CommentsState>`.
  Methods: `loadComments(String ticketId)`, `addComment({ticketId, content,
  authorType = CommentAuthorType.human, aiModel})`.
  Inject `CommentRepository` via constructor.
  Comments bypass the watcher gate — this cubit writes directly to the
  repository with no review queue. (See watcher gate bypass invariant in
  spec.md and design.md.)

---