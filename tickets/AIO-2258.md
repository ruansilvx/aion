---
ticketId: AIO-2258
type: story
status: done
priority: none
parentId: b79daef3-971b-4664-8c08-4bdec9db8147
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.413857
updatedAt: 2026-09-02T21:17:20.413857
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Domain layer

- [x] T1. `features/tickets/domain/enums/ticket_link_type.dart` — add
      the `TicketLinkTypeRelative` extension (`inverse` getter, design.md
      §2) plus two more extension methods, `glyph` (`IconData`) and
      `label(BuildContext)` (`String`), promoted verbatim from
      `ticket_link_picker.dart`'s private `_linkTypeGlyph`/
      `_linkTypeLabel` top-level functions (design.md §3's
      cross-layer note — `phosphoricons_flutter`'s `IconData`/
      `PhosphorIcons` are a package dependency, not a Flutter-framework
      one, so this stays consistent with the domain layer's
      no-Flutter-import rule apart from that one package). `glyph`/
      `label` must return exactly the per-relation values in design.md
      §1.2–§1.3 (directional vs. associative tone, glyph name, and
      copy — including the new `duplicatedBy` "Duplicated by" case,
      which today's `_linkTypeLabel` returns `''` for). Dartdoc all
      three.
- [x] T2. `features/tickets/domain/entities/linked_ticket_ref.dart` —
      new file: the `LinkedTicketRef` record typedef (`({Ticket ticket,
      TicketLinkType relativeType, String linkId})`, design.md §3).
      Dartdoc it and add the file header.
- [x] T3. `features/tickets/domain/utils/ticket_link_direction.dart` —
      new file: `relativeLinkType(TicketLinkData row, String
      viewingTicketId)` and `toCanonical(TicketLinkType
      relativeSelection, TicketLinkData row, String viewingTicketId)`
      (design.md §2 and §7). Dartdoc both in full, including the
      direction-resolution rule (mirror the rigor of
      `_computeBlockedTicketIds`'s existing dartdoc in
      `tickets_cubit.dart`). Add the file header.
- [x] T4. `features/tickets/domain/repositories/ticket_link_repository.dart`
      — add `getLinkById(String linkId)`, `deleteLink(String linkId)`,
      `updateLinkType(String linkId, TicketLinkType newType)` to the
      interface (design.md §5). Dartdoc all three.