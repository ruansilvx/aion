---
ticketId: AIO-820
type: story
status: done
priority: none
parentId: b6289537-c4a4-4a03-997a-f046a69c31b8
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.222704
updatedAt: 2026-09-02T21:17:20.222704
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T18. **CORRECTED TARGET** — the actual existing test file is
  `test/features/tickets/domain/ticket_type_hierarchy_test.dart` (not
  `domain/enums/ticket_type_test.dart`, which doesn't exist). Updated its
  full 6x6 expectation matrix for the new `canParent` rules (page→page,
  page→resource now true; epic/story/task→resource/page now false;
  epic/story/task→chat still true; resource/chat still fully leaf).
- [x] T19. `test/features/tickets/data/services/ticket_document_search_service_test.dart`
  — ADD: unit tests for `TicketDocumentSearchService.search` — cosine
  ranking correctness with mocked `EmbeddingProvider`/`TicketRepository`
  (mocktail), missing-embedding exclusion, limit enforcement.
- [x] T20. `test/features/tickets/data/services/document_parent_migration_service_test.dart`
  — ADD: unit tests for `migrateIfNeeded` — converts eligible
  parent/child pairs into links and clears parentId, no-ops on second
  call (flag gating), leaves unrelated tickets (including page-under-page)
  untouched, mocked repositories/prefs via mocktail.
- [x] T21. **CORRECTED PATH** — `test/features/tickets/cubit/documentation_cubit_test.dart`
  (matches this repo's actual test-folder convention, `test/features/tickets/cubit/`,
  not `presentation/cubit/`). ADD: `bloc_test` coverage for `load`,
  `loadChildren` (lazy-fetch, collapse, no-op during search),
  `search`/`clearSearch`, error states, mocked `TicketRepository`/
  `TicketDocumentSearchService`.
- [x] T22. **CORRECTED TARGET** — per T9's correction, implemented as new
  `loadDocumentRelations` coverage added to
  `test/features/tickets/cubit/tickets_cubit_test.dart` (`ticket_detail_cubit_test.dart`
  doesn't exist). Covers: page ticket populates
  childDocs/linkedTickets/backlinks; resource ticket never fetches
  childDocs; non-page/resource ticket no-ops; no-ops gracefully with no
  `TicketLinkRepository` provided.
- [x] T23. `test/features/tickets/data/daos/ticket_dao_test.dart` —
  MODIFIED: added coverage for `getTicketsByParent`/`getAllTicketsByType`
  (real in-memory drift instance, per existing DAO test convention for
  persistence-behavior tests) — root-level scoping, one-level children,
  all-types-any-depth scan, soft-delete exclusion.