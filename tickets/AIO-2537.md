---
ticketId: AIO-2537
type: task
status: done
priority: none
parentId: 29b6f9ef-568f-4f3c-b097-dd80900cbaea
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.447101
updatedAt: 2026-09-02T21:17:20.447101
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 12. Modify `lib/features/tickets/presentation/screens/trash_screen.dart`

— add private handlers `_bulkRestore(BuildContext, Set<String>)`
(no confirmation; calls `TrashCubit.restoreTickets(ids)`; on
`true`, shows `AppToast.show` with
`ticketTrashBulkRestoreSummaryToast(ids.length)` and calls
`TicketSelectionCubit.clear()`) and
`_bulkPermanentlyDelete(BuildContext, Set<String>,
Map<String, int> descendantCounts)` (computes the aggregate total
— `ids.length` plus the sum of `descendantCounts[id] ?? 0` for
each selected id — opens `_confirmPermanentDelete` with
`ticketTrashPermanentDeleteConfirmTitle` and the new
`ticketTrashBulkPermanentDeleteConfirmMessage(total)`; on confirm,
calls `TrashCubit.permanentlyDeleteTickets(ids)`; on `true`, shows
`AppToast.show` with `ticketTrashBulkPermanentDeleteSummaryToast
(total)` and calls `TicketSelectionCubit.clear()`). Wire both to
`TrashSelectionBar`'s `onRestore`/`onDeleteForever`. (design.md
§7.3; per design.md "Aion — Trash Screen Multi-Select" export §4
Bulk Delete-forever confirm dialog — content only, reuses the
existing `showAppConfirmDialog` shell, aggregate count copy stated
per §4's exact wording/pluralization)