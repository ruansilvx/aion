---
ticketId: AIO-2352
type: story
status: done
priority: none
parentId: cf463d53-a402-4803-af23-5ef7bb630573
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.424721
updatedAt: 2026-09-02T21:17:20.424721
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation — cubit/state

- [x] T6. Modify `aion/lib/features/tickets/presentation/cubit/tickets_state.dart`
      — add `hasMore: bool` (constructor param + field + `props` entry) to
      `TicketsLoaded`, `TicketCreated`, `TicketStatusUpdated`,
      `TicketsBatchTrashed` (keeps its existing `trashedCount`). Add two
      new states: `TicketsLoadingMore` (carries `tickets`) and
      `TicketsLoadMoreFailed` (carries `tickets` + `hasMore`). Full dartdoc
      on every new/changed member. See design.md §4.
- [x] T7. Modify `aion/lib/features/tickets/presentation/cubit/tickets_cubit.dart`:
      - Add `import 'dart:math' show max;`.
      - Add `static const _pageSize = 50;` and the remembered-filter
        fields (`_lastQuery`, `_lastStatus`, `_lastType`, `_lastPriority`,
        `_searchGeneration`).
      - Add the private `_listSnapshot(TicketsState)` helper (record
        return type, not a public/domain type).
      - Update `searchTickets`: bump `_searchGeneration`, cache the
        filters, pass `limit: _pageSize` to the repository, emit
        `TicketsLoaded(page.tickets, hasMore: page.hasMore)`, discard the
        result if superseded (generation check after the `await`). Extend
        the `hasVisibleList` switch to include `TicketsLoadingMore`/
        `TicketsLoadMoreFailed`.
      - Add `loadMoreTickets()` per design.md §3 exactly (snapshot guard,
        `TicketsLoadingMore` emit, offset = current tickets length,
        generation check, `TicketsLoadMoreFailed` on error preserving
        existing tickets).
      - Update `createTicket`, `updateTicketStatus`, `trashTicket`,
        `trashTickets`: bump `_searchGeneration` at the start of each;
        extend each method's `currentTickets`-extraction `switch` to cover
        `TicketsLoadingMore`/`TicketsLoadMoreFailed`; replace their
        `_repository.getAllTickets()` refresh call with
        `_repository.searchTickets(query: _lastQuery, status: _lastStatus,
        type: _lastType, priority: _lastPriority, limit: max(_pageSize,
        currentTickets.length))`; emit with the new `hasMore` field.
      - Update every changed method's dartdoc to describe the new
        emit sequence.
      - Do NOT change `getValidParentCandidates`/`updateTicketParent`'s
        `_repository.getAllTickets()` calls — out of scope, see
        proposal.md Non-goals.
      See design.md §3.