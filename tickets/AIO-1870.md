---
ticketId: AIO-1870
type: story
status: done
priority: none
parentId: 1f953fb4-cab6-47a7-bdfd-8e129b5d1cb0
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.363289
updatedAt: 2026-09-02T21:17:20.363289
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation layer — Cubit

- [x] T11. In `lib/features/tickets/presentation/cubit/tickets_cubit.dart`:
  - Add `advanceSddStage(Ticket ticket)` and its private
    `_spawnStageChat` helper, per `design.md`'s full precondition/spawn
    logic (constructor grows an `AgentModelClient` dependency and a
    `CommentRepository` dependency needed for `_spawnStageChat` to post
    the system-authored context comment and the eventual AI reply —
    wire both through the existing `BlocProvider<TicketsCubit>` `create`
    call in `app_router.dart`).
  - Add `promoteSignalToEpic(Ticket signal, {String? existingEpicId})`,
    per `design.md`.
  - Extend `getTicketById` to compute `canAdvanceSddStage` for `epic`/
    `story` tickets (fetch direct children via the existing
    `getTicketsByParent`, evaluate the current stage's precondition).
  - Add a new `TicketsErrorReason.sddStagePreconditionNotMet` value in
    `tickets_state.dart` and use it in `advanceSddStage`'s rejection
    path, mirroring `_emitInvalidParent`.
  - Update all touched methods' dartdoc.

- [x] T12. In `lib/features/tickets/presentation/cubit/tickets_state.dart`:
  add `canAdvanceSddStage` (`bool`, default `false`) to
  `TicketDetailLoaded`, with dartdoc; add
  `TicketsErrorReason.sddStagePreconditionNotMet` per T11.

- [x] T13. Create
  `lib/features/tickets/presentation/cubit/chat_state.dart`
  (`ChatInitial`, `ChatLoaded(List<TicketComment> comments, {String?
  streamingText})`, `ChatError(String message)`) and
  `lib/features/tickets/presentation/cubit/chat_cubit.dart`
  (`ChatCubit` — `loadMessages`, `sendMessage`), per `design.md`. Extract
  the shared "call `AgentModelClient.run`, accumulate `AgentTextEvent`s,
  persist final `ai` comment on `AgentDoneEvent`" logic into a method
  both `ChatCubit.sendMessage` and `TicketsCubit._spawnStageChat` call,
  per `design.md`'s note that both paths must not drift apart. Full
  dartdoc and file headers.

- [x] T14. Create
  `lib/features/providers/presentation/cubit/automation_settings_cubit.dart`
  (+ its state file) — loads/persists the global `AutomationConfidence`
  via `AutomationSettingsRepository`, mirroring
  `ProviderSettingsCubit`'s load/select shape. Full dartdoc and file
  header.