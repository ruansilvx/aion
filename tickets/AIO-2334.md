---
ticketId: AIO-2334
type: story
status: done
priority: none
parentId: 66202bd2-e332-4cd2-997e-70fc948a0cad
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.421185
updatedAt: 2026-09-02T21:17:20.421185
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T10. In `aion/test/features/tickets/data/repositories/drift_ticket_repository_test.dart`,
      add the `searchTickets` cases listed in `design.md`'s "Test plan"
      (text-only, structured-filter-only, combined, all-null parity with
      `getAllTickets`, and the FTS5-special-character-doesn't-throw case).
- [x] T11. In the same file, add the `onUpgrade` migration test described
      in `design.md`'s "Test plan" — hand-rolled schema-1 setup via raw
      SQL, `user_version` pragma set to `1`, reopened as `AppDatabase`,
      asserting pre-existing rows are searchable afterward.
- [x] T12. In `aion/test/features/tickets/cubit/tickets_cubit_test.dart`,
      replace the two `loadTickets` `blocTest` cases with equivalent
      `searchTickets()` (no-args) cases against a mocked
      `TicketRepository.searchTickets`, and add the two new cases from
      `design.md` (no intervening `TicketsLoading` when a list is already
      visible; `TicketsLoading` does still emit from `TicketsInitial`).