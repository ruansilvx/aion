---
ticketId: AIO-1478
type: story
status: done
priority: none
parentId: 2119e85e-5884-4c6b-848f-3075e40e9cb8
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.306054
updatedAt: 2026-09-02T21:17:20.306054
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation — Widget

- [x] T8. Add `TicketParentPicker` (`StatefulWidget`) to
      `aion/lib/features/tickets/presentation/screens/ticket_detail_screen.dart`,
      following `TicketOverflowMenu`'s exact `Overlay`/`LayerLink`/
      `CompositedTransformFollower`/`mounted`-guard mechanics (same file,
      same pattern — do not build on `SelectionMenu<T>`, see `design.md`
      for why). Include: trigger (current parent label or
      "+ PARENT" placeholder), search `AppTextField`, "No parent" row,
      scrollable filtered candidate list, `AppSpinner` while
      `getValidParentCandidates` is in flight. Full dartdoc on the class
      and any public members. Per design.md §1 TicketParentPicker trigger
      and §2 TicketParentPicker overlay panel.

- [x] T9. Wire `TicketParentPicker` into `TicketDetailScreen`'s build
      method, placed below the type/status `Row` and above the
      estimate/timeSpent `Row` (per `design.md`'s placement note). Per
      design.md §1.1 Placement.

- [x] T10. Add the `invalidParent` toast branch to `TicketDetailScreen`'s
      existing `BlocListener` (alongside the current `hasChildren`
      branch), calling `AppToast.show(context, context.l10n.ticketInvalidParentError)`.
      Per design.md §3 Validation & error toast.