---
ticketId: AIO-2085
type: story
status: done
priority: none
parentId: 38afbd1e-ebd9-4b10-8625-1299ab8b368f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.388088
updatedAt: 2026-09-02T21:17:20.388088
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Automation generalization

- [x] T5. Create `lib/core/automation/automation_context.dart`: new
  `AutomationContext` enum (`sddStage`, `codingExecution`), each value
  with a one-line dartdoc, plus the file header comment and a class-level
  dartdoc per `design.md` §8.

- [x] T6. In `lib/core/automation/automation_settings_repository.dart`:
  replace `getSddStageAutomation()`/`setSddStageAutomation(...)` with
  `getConfidence(AutomationContext context)`/`setConfidence(AutomationContext
  context, AutomationConfidence confidence)`, updating the interface's
  class-level dartdoc to describe the now-generalized, per-context
  storage (no longer "one value today... deferred until a second
  consumer exists" — that consumer now exists), per `design.md` §8.

- [x] T7. In
  `lib/core/automation/shared_prefs_automation_settings_repository.dart`:
  implement the generalized interface from T6 — a `switch` (or lookup
  map) from `AutomationContext` to its `shared_preferences` key string.
  `AutomationContext.sddStage` MUST keep the existing
  `automation_settings.sdd_stage_automation` key unchanged (preserves any
  already-saved user preference); add a new
  `automation_settings.coding_execution_automation` key for
  `AutomationContext.codingExecution`. Both default to
  `AutomationConfidence.gated` when unset. Update the class dartdoc, per
  `design.md` §8.

- [x] T8. In
  `lib/features/providers/presentation/cubit/automation_settings_state.dart`:
  change `AutomationSettingsReady` from a single `confidence` field to
  `Map<AutomationContext, AutomationConfidence> confidenceByContext`,
  updating constructor/fields/`props`/dartdoc, per `design.md` §8.

- [x] T9. In
  `lib/features/providers/presentation/cubit/automation_settings_cubit.dart`:
  change `load()` to fetch `getConfidence` for both `AutomationContext`
  values (e.g. via `Future.wait`) and emit the resulting map; change
  `selectConfidence` to `selectConfidence(AutomationContext context,
  AutomationConfidence confidence)`, persisting via `setConfidence` and
  re-emitting the updated map (copying the previous map, replacing just
  the changed key). Update the class dartdoc, per `design.md` §8.

- [x] T10. In
  `lib/features/providers/presentation/screens/settings_screen.dart`:
  generalize `_AutomationSection` to take `required AutomationContext
  context`, `required String label`, `required String description`
  (reading `state.confidenceByContext[context]` instead of
  `state.confidence`, calling `selectConfidence(context, v)` in its
  `onSelected`). Render it twice in the Settings body: the existing SDD
  Stage Automation copy (`AutomationContext.sddStage`) unchanged, plus a
  new instance for `AutomationContext.codingExecution` using the new
  l10n keys from T18. Update the widget's dartdoc, per `design.md` §8
  (architecture) and its Component Spec §4 (grouping under one shared
  "AUTOMATION" eyebrow with 20px spacing between instances, no divider —
  §4.1; trigger/menu-row visuals carry over unchanged, only per-instance
  copy differs — §4.2–§4.4).