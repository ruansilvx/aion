---
ticketId: AIO-2658
type: story
status: done
priority: none
parentId: 097cbcf9-4910-46b7-acb8-673604e809d9
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.459835
updatedAt: 2026-09-02T21:17:20.459835
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data — schema + DAOs + repositories

- [x] T7. New file
      `data/models/workflow_skill_attachment_table.dart` —
      `WorkflowSkillAttachmentsTable` drift table exactly as design.md
      §2.1 specifies (`id`, `workflow_status_id` nullable, `sdd_stage`
      nullable, `kind`, `template_id` nullable, `skill_name` nullable,
      `confidence`; no FK constraints). Dartdoc per
      `WorkflowStatusesTable`'s existing style.
- [x] T8. New file `data/models/workflow_prompt_template_table.dart` —
      `WorkflowPromptTemplatesTable` drift table exactly as design.md
      §2.2 specifies (`id`, `name`, `body`). Same dartdoc convention.
- [x] T9. `core/database/app_database.dart` — add both new tables to
      `@DriftDatabase(tables: [...])`; bump `schemaVersion` 15 → 16; add
      the `from < 16` `onUpgrade` branch (`m.createTable(...)` ×2, no
      seed call — design.md §2.3). Extend the class dartdoc's
      version-history list with a "Version 16 adds..." sentence,
      following the existing style for versions 2–15.
- [x] T10. New files `data/daos/workflow_skill_attachment_dao.dart` and
      `data/daos/workflow_prompt_template_dao.dart` — each following
      `WorkflowStatusDao`'s exact shape (`DatabaseAccessor<AppDatabase>`
      + `@DriftAccessor`, plain CRUD, no seeding logic). Full dartdoc on
      both.
- [x] T11. New files
      `data/repositories/drift_workflow_skill_attachment_repository.dart`
      and
      `data/repositories/drift_workflow_prompt_template_repository.dart`
      — implement their domain interfaces by delegating to their T10
      DAO, mapping the `*Data` row type ↔ domain entity (`kind`/
      `sddStage`/`confidence` string ↔ enum mapping lives here, per
      design.md §2.4). Full dartdoc on both.