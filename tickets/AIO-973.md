---
ticketId: AIO-973
type: story
status: done
priority: none
parentId: db91fdb7-f189-42f6-b315-f8c180b966a6
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.244038
updatedAt: 2026-09-02T21:17:20.244038
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data layer

- [x] **T8** — Create `lib/features/tickets/data/models/
  page_wikilink_model.dart`: `PageWikilinksTable extends Table` with
  `@DataClassName('PageWikilinkData')` — `id`, `sourcePageId` (named
  `source_page_id`), `targetPageId` (named `target_page_id`), `createdAt`
  (named `created_at`). Every column dartdoc'd (note `targetPageId` may
  be a `page` or `resource` id). No FK constraints. File header.
- [x] **T9** — Create `lib/features/tickets/data/daos/
  page_wikilink_dao.dart`: `PageWikilinkDao`
  (`@DriftAccessor(tables: [PageWikilinksTable])`) — `getIncomingLinks`,
  `getOutgoingLinks`, `replaceOutgoingLinks` (wrapped in
  `transaction<void>(...)`: delete existing outgoing rows for
  `sourcePageId`, then batch-insert one row per `targetPageIds` member),
  `deleteLinksForTickets` (batch, source-OR-target match, mirroring
  `TicketLinkDao.deleteLinksForTickets`'s two-query-and-dedupe shape if
  needed, or a single `OR`-clause delete if Drift's query builder
  supports it directly). Full dartdoc. File header.
- [x] **T10** — Create `lib/features/tickets/data/repositories/
  drift_page_wikilink_repository.dart`: `DriftPageWikilinkRepository
  implements PageWikilinkRepository` — maps `PageWikilinkData` ↔
  `PageWikilink` via a private `_toEntity`, delegates every method to
  `PageWikilinkDao`. Full dartdoc. File header.
- [x] **T11** — Modify `lib/core/database/app_database.dart`: register
  `PageWikilinksTable` + `PageWikilinkDao` in `@DriftDatabase`; bump
  schema version 11 → 12; `onCreate` creates `page_wikilinks`;
  `onUpgrade` (`if (from < 12)`) creates the table, then runs the
  one-time computed backfill described in design.md: select every live
  `page` ticket, build a candidate set of every live `page`/`resource`
  ticket (id ↔ ticketId ↔ title), run `WikilinkExtractor
  .extractReferences` over each page's `description`, resolve each
  match's `target` (id match first if `looksLikeTicketId`, else
  case-insensitive title match), bulk-insert the resolved rows. Update
  the file's migration-history dartdoc comment to record version 12's
  change, matching the style already used for versions 3–11.
- [x] **T12** — Run `flutter pub run build_runner build
  --delete-conflicting-outputs` to regenerate `page_wikilink_dao.g.dart`
  and `app_database.g.dart` (not committed, per existing convention).