---
ticketId: AIO-718
type: task
status: done
priority: none
parentId: ab183a76-f43e-4d01-9f06-d282f3abb46f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.211308
updatedAt: 2026-09-02T21:17:20.211308
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T10. `test/features/tickets/cubit/tickets_cubit_test.dart` —

MODIFY: in the `'SddStage entry'` group, add a new test sibling to
`'a configured attachment uses its own prompt instead of
_assembleStageContext, and fires per its own confidence'`: a
`delegatedSkill` `SkillAttachment` on `SddStage.exploring` whose
resolved provider has `supportsSkillDiscovery: false` (same
repoint-`registry.providerById` pattern T7's non-discovering-provider
test uses) — asserts `commentRepository.addComment` is called with
content starting `'Stage advance failed:'` and `agentClient.run` is
never called with this attachment's `/kickoff-skill` prompt. Unlike
T7's test, `gitClient.createWorktree`/`.removeWorktree` are asserted
*never* called at all here — T9's check sits before
`_runStageChatTurn`'s own worktree setup (not after, like
`_fireSkillAttachment`'s), so a failing check skips worktree creation
entirely rather than creating then tearing one down. Depends on T9.