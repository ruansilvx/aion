---
ticketId: AIO-1186
type: story
status: done
priority: none
parentId: 506e4086-a4c6-495c-ba3a-0c64f488a6d3
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.268053
updatedAt: 2026-09-02T21:17:20.268053
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 3. Core contracts & registry database — `core/`

- [x] 3.1 `core/contracts/active_project_provider.dart` —
      `ActiveProjectProvider` abstract interface (`activeProject`,
      `activeProjectStream`) per design.md §4. Header + dartdoc.
- [x] 3.2 `core/database/registry_database.dart` — drift `RegistryDatabase`
      with a single `projects` table mirroring `Project`'s fields. This is
      the one drift DB that is not per-project (design.md §3, §7). Header
      + dartdoc on the table and the database class.
- [x] 3.3 Run
      `flutter pub run build_runner build --delete-conflicting-outputs`
      to generate `registry_database.g.dart` for the new table (not
      committed, per Codegen convention).
- [x] 3.4 Modify `core/database/app_database.dart`'s `_openConnection()`
      split (native vs. WASM) to take a `Project` and address the DB by
      `storageKey`/`rootPath` per design.md §7, instead of one fixed
      global location. Update the class/file dartdoc to describe the new
      per-project addressing.