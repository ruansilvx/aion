---
ticketId: AIO-353
type: story
status: done
priority: none
parentId: aec5e777-0cd8-45ff-b1d3-ffff29993d7f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.170179
updatedAt: 2026-09-02T21:17:20.170179
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# State — `TicketsState` (`tickets_state.dart`)

- [x] T1. `TicketDetailLoaded` gains `isAdvancingStage` (`bool`, default
      `false`), `sddStageFailureReason` (`String?`, default `null`),
      `sddStageCanRetry` (`bool`, default `false`) — constructor params,
      fields, `props` entries (design.md §2). Dartdoc each field.
- [x] T2. `TicketsLoaded` gains `inFlightExecutionIds` (`Set<String>`,
      default `const {}`), `executionQueuePositions` (`Map<String,
      int>`, default `const {}`), `inFlightAdvanceIds` (`Set<String>`,
      default `const {}`) — constructor params, fields, `props` entries
      (design.md §1.4). Dartdoc each field, cross-referencing
      `TicketsCubit._refreshInFlightBoardState`.
- [x] T3. `TicketsErrorReason` gains `sddStageAdvanceFailed` (design.md
      §1.7). Dartdoc per the enum's existing per-value style.