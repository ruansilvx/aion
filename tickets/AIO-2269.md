---
ticketId: AIO-2269
type: story
status: done
priority: none
parentId: b79daef3-971b-4664-8c08-4bdec9db8147
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.413857
updatedAt: 2026-09-02T21:17:20.413857
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Pages cross-feature contract

- [x] T9. `core/contracts/page_ticket_provider.dart` —
      `PageRelations.linkedTickets`/`.backlinks` change type from
      `List<Ticket>` to `List<LinkedTicketRef>`; add `deleteLink(String
      pageId, String linkId)` and `updateLinkType(String pageId, String
      linkId, TicketLinkType newCanonicalType)` to the
      `PageTicketProvider` interface (design.md §3, mirroring
      `TicketsCubit`'s new methods' signatures). Dartdoc the changed
      field docs and the two new interface methods.
- [x] T10. `features/tickets/data/page_ticket_provider_impl.dart` —
      `loadPageRelations` computes `relativeType`/`linkId` per row via
      T3's `relativeLinkType`, matching `loadDocumentRelations`'s
      resolution (same convention this file's class doc already states:
      reads go straight to the repository, no `TicketsCubit`
      round-trip); implement `deleteLink`/`updateLinkType` by delegating
      to `_ticketsCubit.deleteTicketLink`/`.updateTicketLinkType`,
      matching `updatePage`'s existing delegate-to-`TicketsCubit`
      pattern. Dartdoc the changed/new methods.
- [x] T11. `features/pages/presentation/cubit/pages_cubit.dart` — new
      `deleteLink(String pageId, String linkId)` and `updateLinkType(String
      pageId, String linkId, TicketLinkType newCanonicalType)` methods,
      mirroring `updatePage`'s shape: delegate to `_provider`, reload
      relations via `_provider.loadPageRelations`, emit a refreshed
      `PageDetailLoaded`, catch into `PagesError`. Dartdoc both.