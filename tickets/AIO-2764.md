---
ticketId: AIO-2764
type: story
status: done
priority: none
parentId: d4780d82-4803-448b-8549-52d112db0edb
estimate: null
timeSpent: null
createdAt: 2026-09-09T13:51:33.977
updatedAt: 2026-09-09T13:51:33.977
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 2. `DriftTicketRepository` — return the cascade; seed `deletedAt` on insert

- [x] `lib/features/tickets/data/repositories/drift_ticket_repository.dart`:
      - `trashTicket`: keep its existing-id check (throws `StateError`
        if missing), then `return trashTickets([id]);`.
      - `trashTickets`: return `affected.toList()` instead of
        `affected.length`; return `const []` (not `0`) on the empty
        case.
      - `restoreTicket`: return `toRestore.toList()` after
        `restoreByIds` — no other logic change.
      - `_buildInsertCompanion` (shared by `createTicket`/
        `importTicket`): add
        `deletedAt: Value(ticket.deletedAt?.millisecondsSinceEpoch)`.
      - `updateTicket`'s companion is **not** touched — confirm it still
        has no `deletedAt` field; add a one-line comment there (if none
        exists) cross-referencing design.md's "Why not fix this at the
        `updateTicket` call site instead" so a future reader doesn't
        "fix" this by adding it.
      - Update the three changed methods' dartdoc to describe the new
        return value (mirror design.md's proposed text). File header
        comment already exists — leave as-is unless it's gone stale.