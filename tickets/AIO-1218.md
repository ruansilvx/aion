---
ticketId: AIO-1218
type: story
status: done
priority: none
parentId: 506e4086-a4c6-495c-ba3a-0c64f488a6d3
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.268053
updatedAt: 2026-09-02T21:17:20.268053
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 9. Manual verification (desktop + web, per Testing convention)

- [x] 9.1 `flutter analyze` clean.
- [x] 9.2 Interactive pass in real Chrome (per project.md's Testing
      section — not the sandboxed preview browser): first run shows Hub
      empty state, create a project, confirm it opens into the workspace,
      switch back to Hub, confirm a second project can be created and
      switching between the two shows isolated ticket data. Close the
      Chrome tab and stop the `web-server` process afterward.
- [x] 9.3 Interactive pass on a native desktop target (`flutter run -d
      windows` or equivalent): repeat 9.2's flow, additionally verifying
      the chosen directory picker works and `.aion/manifest.json` +
      an initialized empty git repo appear at the chosen `rootPath`.
      Completed manually by the user: created a project at
      `C:\Users\ruans\Desktop\test`, confirmed `.aion/manifest.json`
      (`{"baselineVersion": "0.1.0"}`), `.aion/data/app.db`, and an
      initialized (uncommitted, `master` branch) git repo all appear at
      the chosen `rootPath`, matching `CreateProjectCubit
      ._initializeDesktopProject`. This pass also surfaced a real bug —
      `NewProjectScreen`'s directory-picker `Row` threw "BoxConstraints
      forces an infinite height" on desktop (`CrossAxisAlignment.stretch`
      inside a `SingleChildScrollView`'s unbounded height) — fixed by
      wrapping it in `IntrinsicHeight`; verified via a scratch widget
      test reproducing and then clearing the exception, `flutter
      analyze`, and the full `test/features/projects` suite.