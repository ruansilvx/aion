---
ticketId: AIO-2542
type: story
status: done
priority: none
parentId: da9b2da2-d14f-4f16-b657-f8b2ac0d5e8e
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.447101
updatedAt: 2026-09-02T21:17:20.447101
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] 15. Add tests to `test/features/tickets/data/repositories/drift_ticket_repository_test.dart`
      (new cases inside or alongside the existing `permanentlyDeleteTicket
      / emptyTrash` group) — `permanentlyDeleteTickets`: deletes a
      childless ticket cleanly; deletes a multi-level subtree spanning
      several independently-selected roots in one call; a descendant
      already covered by another selected root's cascade isn't
      double-counted in the returned total; non-existent ids are
      silently skipped; comments/ticket_links/page_wikilinks cascade-
      delete alongside the ticket rows; returns `0` and performs no
      writes on an empty or all-nonexistent `ids` list. (design.md §10)
- [x] 16. Update `test/features/tickets/cubit/trash_cubit_test.dart` —
      `restoreTickets`: calls `TicketParentTrashService.restore` once
      per id (not a batched repository call), reloads to `TrashLoaded`
      on success and returns `true`; a repository throw emits
      `TrashError` and returns `false`. `permanentlyDeleteTickets`:
      calls `TicketRepository.permanentlyDeleteTickets` once with the
      full id list, reloads to `TrashLoaded` on success and returns
      `true`; a repository throw emits `TrashError` and returns `false`.
      (design.md §10)
- [x] 17. No widget tests for `TrashSelectionBar` or the modified
      `TrashedTicketTile`/`TrashScreen` — same rationale already
      documented in `tickets.md`'s Testing section for this
      selection-bar/`Overlay` widget family (see the archived
      `bulk-status-and-priority-edit-for-ticket-selection` change's
      identical call). Confirmed via the manual verification pass
      (task 18) instead. (design.md §10)