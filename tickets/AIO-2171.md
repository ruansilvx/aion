---
ticketId: AIO-2171
type: story
status: done
priority: none
parentId: 844bbf33-6b64-4c8c-804b-ecb50e358341
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.396743
updatedAt: 2026-09-02T21:17:20.396743
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation — state & cubit

- [x] T8 — `lib/features/tickets/presentation/cubit/tickets_state.dart`:
  add `TicketDeleting`, `TicketDeleted` state classes (dartdoc each); add
  `TicketsErrorReason.hasChildren`; add optional `childCount` field to
  `TicketsError`, set only when `reason == hasChildren` — update its
  dartdoc accordingly.
- [x] T9 — `lib/features/tickets/presentation/cubit/tickets_cubit.dart`:
  add `deleteTicket(String id)` — emits `TicketDeleting` then
  `TicketDeleted` on success; catches `TicketHasChildrenException`
  first (→ classified `TicketsError`), then generic `catch` (→
  unclassified `TicketsError`). Dartdoc on the method.