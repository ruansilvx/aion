---
ticketId: AIO-727
type: story
status: done
priority: none
parentId: b029f19e-5e2c-4d10-8f45-4a018c21d937
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.214950
updatedAt: 2026-09-02T21:17:20.214950
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Ancestor-based sibling conflict

- [x] T4. `tickets_cubit.dart` — add the private `Future<List<String>> _ancestorIds(String ticketId)` helper, fully dartdoc'd. See design.md §2.1.
- [x] T5. `tickets_cubit.dart` — update `_nextEligibleForHybrid` to build ancestor-inclusive comparison sets (via T4) instead of comparing `parentId` directly, per design.md §2.2's exact shape. Update its existing dartdoc.
- [x] T6. `tickets_cubit.dart` — add `Future<Map<String, String>> topmostAncestorIds(List<String> ticketIds)`, fully dartdoc'd, reusing `_ancestorIds`. See design.md §2.3.
- [x] T7. `lib/features/tickets/presentation/cubit/tickets_state.dart` — add `TicketsLoaded.topmostAncestorId` field (`Map<String, String>`, default `{}`), update its dartdoc and any `copyWith`.
  - `TicketsLoaded` has no `copyWith` (its only mutation path is a fresh constructor call at each emission site) — nothing to update there.
- [x] T8. `tickets_cubit.dart` — recompute `topmostAncestorId` (via T6) and re-emit `TicketsLoaded` whenever it changes, gated to `ExecutionSchedulingMode.hybrid` only (confirm/resolve design.md's open question on detecting a mode-switch-into-Hybrid signal before wiring this — may need a new small cross-cubit listener; check for existing precedent before inventing one). Follows `_refreshInFlightBoardState()`'s existing recompute-and-re-emit pattern as closely as possible.
  - Resolved without a new cross-cubit listener. `TicketsLoaded.blockedTicketIds` already has the identical lifecycle need (async, persisted-data-derived) and its established precedent is: recomputed fresh in `searchTickets`/`loadMoreTickets`, merely carried forward by `_refreshInFlightBoardState`/`_refreshBlockedBoardState`. `topmostAncestorId` follows the same shape via a new `_topmostAncestorIdIfHybrid` helper (gated on `_effectiveSchedulingMode()`, returns `{}` outside Hybrid so non-Hybrid sessions pay zero ancestor-walk cost). The mode-switch-into-Hybrid case resolves correctly because `TicketsListScreen` re-`searchTickets`es on every navigation back to the Board — the same navigation that already gives `BoardColumn`'s existing `context.select` on a freshly-created `ExecutionSchedulingCubit` the current persisted mode. No `Project`/`ExecutionSchedulingRepository` change, no `app_router.dart` wiring beyond T3's own `DependencyCacheService` addition.
- [x] T9. `lib/features/tickets/domain/utils/sibling_cluster.dart` — change `clusterSiblingsAdjacently`'s signature to accept `Map<String, String> topmostAncestorId` and group by that key instead of direct `parentId` equality. Update its dartdoc in full (per design.md §2.4's exact replacement text). Function stays pure/synchronous — no new imports beyond what it already has.
- [x] T10. `lib/features/tickets/presentation/screens/tickets_board_view.dart` — update `BoardColumn`'s `clusterSiblingsAdjacently(tickets)` call site to `clusterSiblingsAdjacently(tickets, ticketsLoaded.topmostAncestorId)` (or the equivalent state-access path per T7's exact field placement). No other change to `BoardColumn`.
  - Read via a second `context.select((TicketsCubit cubit) => ...)` in `BoardColumn.build`, mirroring the existing `isHybrid` select right above it — `TicketsLoaded.topmostAncestorId` if the cubit's current state is `TicketsLoaded`, else `{}`.