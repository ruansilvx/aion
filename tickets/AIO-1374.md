---
ticketId: AIO-1374
type: story
status: done
priority: none
parentId: 23efcc81-1953-4e50-82ac-8b5b26a27398
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.293989
updatedAt: 2026-09-02T21:17:20.293989
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Routing & navigation updates

- [x] T21. Create `core/routing/ticket_navigation.dart`:
      `ticketDetailRoute(Ticket ticket)` — returns `/workspace/pages/:id`
      for `TicketType.page`, `/workspace/tickets/:id` otherwise. File
      header + dartdoc.

- [x] T22. Modify `core/routing/app_router.dart`: add
      `GoRoute(path: '/workspace/pages/new', ...)` and
      `GoRoute(path: '/workspace/pages/:id', ...)` (in that order, per the
      file's existing literal-before-`:id` convention), wiring
      `PageCreateScreen`/`PageDetailScreen` and providing
      `PageTicketProvider`/`PagesCubit` via `RepositoryProvider`/
      `BlocProvider` alongside the existing `TicketsCubit` wiring in
      `WorkspaceShell.build` (`PageTicketProviderImpl` constructed with
      `context.read<TicketsCubit>()`, `context.read<TicketRepository>()`,
      `context.read<TicketLinkRepository>()`).

- [x] T23. Modify `features/tickets/presentation/screens/create_ticket_screen.dart`:
      remove `TicketType.page` from the type dropdown's options.

- [x] T24. Modify `features/tickets/presentation/screens/documentation_screen.dart`
      (and `DocumentationTreeItem` if it lives in a separate file — grep to
      confirm): change the "+ New page" action's target from
      `/workspace/tickets/new` (`CreateTicketRouteExtra(initialType: page)`)
      to `/workspace/pages/new`, and change page-node tap navigation to use
      T21's `ticketDetailRoute` resolver (or navigate directly to
      `/workspace/pages/:id`). "+ New resource" and resource-node taps are
      unchanged.

- [x] T25. Grep the codebase (`grep -rn "/workspace/tickets/\${" lib/` and
      similar) for every remaining call site that navigates to a ticket
      generically by id (e.g. other linked-tickets/backlinks taps, search
      results), and route each through T21's `ticketDetailRoute(ticket)`
      resolver instead of a hardcoded `/workspace/tickets/${ticket.id}`.