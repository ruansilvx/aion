---
ticketId: AIO-2526
type: story
status: done
priority: none
parentId: da9b2da2-d14f-4f16-b657-f8b2ac0d5e8e
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.447101
updatedAt: 2026-09-02T21:17:20.447101
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation layer — cubit

- [x] 4. Modify `lib/features/tickets/presentation/cubit/trash_cubit.dart`
      — add `Future<bool> restoreTickets(List<String> ids)`, placed
      after `permanentlyDelete` and before `emptyTrash`: loops
      `_parentTrashService.restore(id)` once per id in `ids`, then calls
      `load()`; returns `true` on success, or emits `TrashError` and
      returns `false` on a caught exception. Full dartdoc explaining why
      this loops the existing single-ticket path instead of a new batch
      repository method (cross-reference proposal.md's "Implementation
      split" section). (design.md §4)
- [x] 5. Modify `lib/features/tickets/presentation/cubit/trash_cubit.dart`
      — add `Future<bool> permanentlyDeleteTickets(List<String> ids)`,
      placed immediately after task 4's `restoreTickets`: calls
      `_repository.permanentlyDeleteTickets(ids)` then `load()`; returns
      `true` on success, or emits `TrashError` and returns `false` on a
      caught exception. Full dartdoc. (design.md §4)