---
ticketId: AIO-2378
type: story
status: done
priority: none
parentId: 7161c960-0bf3-4230-a027-501ca12410ec
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.427767
updatedAt: 2026-09-02T21:17:20.427767
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data layer

- [x] **T6.** Modify `lib/features/tickets/domain/repositories/ticket_repository.dart`
  — add a required `TicketListSort sort` parameter to
  `searchTickets`; update its dartdoc per design.md §2.1 (relevance +
  no-query fallback behavior). Import `ticket_list_sort.dart`.
- [x] **T7.** Modify `lib/features/tickets/data/repositories/drift_ticket_repository.dart`
  — thread the new `sort` parameter through `searchTickets` to
  `TicketDao.searchTickets` (thin passthrough, matching every other
  parameter). Update the method's dartdoc reference to ordering.
- [x] **T8.** Modify `lib/features/tickets/data/daos/ticket_dao.dart` —
  add `sort` to `searchTickets`'s signature; add the private
  `_enumOrdinalCaseSql`/`_directColumnSql` helpers (design.md §2.2),
  importing `ticketFieldEnumValues` from T5's
  `ticket_sort_comparator.dart` rather than re-declaring the ordinal
  mapping. Rewrite the no-query branch's `orderBy` to use the resolved
  `OrderingTerm` (+ `createdAt DESC` tiebreaker, omitted when
  `sort.field == createdAt`) instead of the hardcoded
  `OrderingTerm.desc(t.createdAt)`. Rewrite the FTS branch's `ORDER BY`
  to be conditional on `sort.field == relevance` (keep the existing
  `bm25(tickets_fts) ASC` clause) vs. every other field (the
  case-or-column SQL + `tickets.created_at DESC` tiebreaker), per
  design.md §2.2. Update dartdoc on `searchTickets` to describe the new
  parameter and both branches' resolution.
- [x] **T9.** Create `lib/features/tickets/data/repositories/shared_prefs_ticket_list_sort_repository.dart`
  — `SharedPrefsTicketListSortRepository implements TicketListSortRepository`,
  per design.md §1.3's exact key scheme and logic. File header + full
  dartdoc.