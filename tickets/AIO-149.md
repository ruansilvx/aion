---
ticketId: AIO-149
type: story
status: done
priority: none
parentId: 240c0794-e9e6-4ce1-bf87-4ebdf249668b
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.146722
updatedAt: 2026-09-02T21:17:20.146722
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `ChatCubit` — measurement mechanism

- [x] T1. Edit `lib/features/tickets/presentation/cubit/chat_cubit.dart`
      — add `import 'dart:math' show max;`. Add a new required
      `TicketRepository ticketRepository` parameter to `runChatTurn`,
      capture `startedAt = DateTime.now()` at the method's start, and
      restructure its body from three return points (an early
      `ChatTurnCancelled` return plus a trailing
      success/failure-conditional return) into one `result` variable
      assigned per-branch with a single trailing `return`, per
      design.md §2. Every existing branch's persisted-comment/returned-
      `ChatTurnResult` behavior must stay byte-identical — only the
      control-flow shape changes.
- [x] T2. Same file — add the new private static
      `_logElapsedTime(TicketRepository ticketRepository, String
      chatTicketId, DateTime startedAt)` helper per design.md §2.1:
      resolves `chatTicketId`'s parent via `getTicketById`, no-ops if
      `null`, otherwise computes `max(1, (elapsedSeconds / 60).ceil())`
      and calls `ticketRepository.addTimeSpent(parentId, minutes)`.
      Call it as the last step of `runChatTurn`, right before the
      single trailing `return result;` T1 introduced. Full dartdoc per
      design.md §2.1. Update `runChatTurn`'s own dartdoc to describe
      the new parameter and the automatic time-logging behavior it now
      performs on every terminal outcome.
- [x] T3. Same file — update `sendMessage`'s existing `runChatTurn` call
      to pass `ticketRepository: _ticketRepository` (the field it
      already holds).