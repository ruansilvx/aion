---
ticketId: AIO-1425
type: story
status: done
priority: none
parentId: 16a56dcd-558b-4d27-9747-32e5764789f1
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.302519
updatedAt: 2026-09-02T21:17:20.302519
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 5. `ChatCubit` — turn-result shape and stop button plumbing

- [x] T20. Create `lib/features/tickets/domain/entities/chat_turn_result.dart`
  (or co-locate with `ChatCubit` per existing convention) — sealed
  `ChatTurnResult` (`ChatTurnSuccess`/`ChatTurnFailure`/
  `ChatTurnCancelled(String accumulatedText)`), file header + dartdoc on
  each variant explaining who persists what (design.md §3).
- [x] T21. Edit `lib/features/tickets/presentation/cubit/chat_state.dart`
  — `ChatLoaded` gains `activeRunId` (`String?`, default `null`) + props
  entry, dartdoc update (design.md §3).
- [x] T22. Edit `lib/features/tickets/presentation/cubit/chat_cubit.dart`
  — change `runChatTurn`'s return type from `Future<bool>` to
  `Future<ChatTurnResult>`; on `AgentCancelledEvent`, return
  `ChatTurnCancelled(accumulatedText)` without persisting a comment (the
  success/failure comment-persistence behavior is otherwise unchanged);
  update dartdoc describing the new return contract (design.md §3).
- [x] T23. Continue editing `chat_cubit.dart` — `sendMessage` generates a
  `runId` (`const Uuid().v4()`) per turn, threads it through the
  `AgentRequest`, stores it on `ChatLoaded.activeRunId`; on
  `ChatTurnCancelled`, persists `accumulatedText` as an `ai` comment if
  non-empty, reloads the thread, clears `streamingText`/`currentToolUse`/
  `activeRunId`. Update dartdoc (design.md §3).
- [x] T24. Continue editing `chat_cubit.dart` — add
  `Future<void> cancelReply(String chatTicketId)`, resolving the same
  provider `sendMessage` used and calling `client.cancel(runId)` off
  `state.activeRunId`; no-op if `state` isn't `ChatLoaded` or
  `activeRunId` is `null`. Dartdoc (design.md §3).
- [x] T25. Edit `TicketsCubit._spawnStageChat` and
  `TicketsCubit._runCodingExecution`'s two call sites that predate this
  change (`_spawnStageChat` only — `_runCodingExecution`'s own rewrite is
  T27+) to switch on `ChatTurnResult` instead of a bool, preserving
  today's exact `true`/`false`-equivalent behavior for
  `ChatTurnSuccess`/`ChatTurnFailure`; `ChatTurnCancelled` is unreachable
  for `_spawnStageChat` in practice (no stop-button UI wired to SDD-stage
  chats this slice) but handled defensively.
- [x] T26. Write/extend `bloc_test` coverage for T22–T24 (design.md §11).