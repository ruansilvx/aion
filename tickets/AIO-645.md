---
ticketId: AIO-645
type: story
status: done
priority: none
parentId: 15c9f95f-13db-4f3b-a33f-96d1071c6c8c
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.201434
updatedAt: 2026-09-02T21:17:20.201434
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# UI

- [x] `DecisionNodeForm` (`lib/features/providers/presentation/widgets/decision_node_form.dart`):
      condition picker's `items` gains `agentJudgmentConditionSpec(automationContext)`
      (when non-null) after the rule-builder entry, labeled via
      `context.l10n.decisionGraphAgentJudgmentLabel` (mirroring the
      existing `decisionGraphRuleBuilderLabel` override at line ~933).
      When `_condition?.id == agentJudgmentConditionId`, render a
      multi-line `AppTextField` bound to `conditionParams['prompt']` in
      place of both the typed-parameter loop and the rule-builder trio —
      a third branch alongside the existing `condition.id ==
      ruleBuilderConditionId` checks (lines 493, 546, 601, 668 in the
      current file). `_save()`: build `conditionParams` from the prompt
      field's text when this kind is selected. Per design.md §1
      Condition Picker — "Ask the Agent" Entry and §2 Prompt Input
      Field (`AgentPromptField`), including §2.4's empty-value/
      incomplete handling and §0's identity rationale.
- [x] `DecisionOutlineList` (`lib/features/providers/presentation/widgets/decision_outline_list.dart`):
      add `isAgentJudgment` alongside the existing `isRuleBuilder` check
      (line ~294); render the `ASK ·` eyebrow and the same
      bordered-chip pattern populated from `decisionNodeSummary`. Chained-
      child creation (`_createChainedChild`) gains the matching
      `agentJudgmentConditionId` seeding branch. Per design.md §4 `ASK ·`
      — Outline Row.
- [x] `GraphCanvas` node cards, via `decision_graph_editor_screen.dart`'s
      node-content builder (the `isRuleBuilder`/`eyebrowText` logic
      around lines 607–614): same third branch, same `ASK ·` eyebrow/chip.
      `onCreateChainedChild` closure gains the matching seeding branch.
      Per design.md §3 `ASK ·` — Canvas Node Card.
- [x] Cost/latency info affordance (`AgentCostHint`) on both the canvas
      card and outline row, per design.md §5 Cost / Latency Info
      Affordance (`AgentCostHint`) — an `OverlayEntry` +
      `CompositedTransformTarget`/`Follower` tooltip (never Material's
      `Tooltip`), never surfaced on an error/incomplete outline row
      (§4.3).