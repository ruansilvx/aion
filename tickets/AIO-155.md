---
ticketId: AIO-155
type: story
status: done
priority: none
parentId: 240c0794-e9e6-4ce1-bf87-4ebdf249668b
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.146722
updatedAt: 2026-09-02T21:17:20.146722
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Removing `log_time`

- [x] T5. Edit `lib/features/tickets/presentation/cubit/ticket_crud_tool_definitions.dart`
      — delete the `logTimeToolDefinition` const and its dartdoc.
      Update the file header comment (currently lists
      `create_ticket/add_link/log_time`) to drop `log_time`.
      `createTicketToolDefinition`/`addLinkToolDefinition` are
      untouched.
- [x] T6. Edit `tickets_cubit.dart` — remove `logTimeToolDefinition`
      from `_toolsFor`'s returned list (back to the branch-or-close-
      branch tool plus `create_ticket`/`add_link`). Update its dartdoc,
      which currently names all three appended tools.
- [x] T7. Edit `tickets_cubit.dart` — remove the `'log_time' /*
      logTimeToolDefinition.name */ => _handleLogTimeToolCall(...)`
      case from `_onToolCallFor`'s switch; `branch_ticket` remains the
      `_` default case. Update its dartdoc (currently reads "not
      `close_branch`/`create_ticket`/`add_link`/`log_time`" — drop
      `log_time`).
- [x] T8. Edit `tickets_cubit.dart` — delete `_handleLogTimeToolCall`
      in its entirety, including its dartdoc.