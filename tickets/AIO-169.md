---
ticketId: AIO-169
type: story
status: done
priority: none
parentId: 240c0794-e9e6-4ce1-bf87-4ebdf249668b
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.146722
updatedAt: 2026-09-02T21:17:20.146722
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Post-apply corrections (found during /verify)

Neither of these was in the original task list — both were required for
T13/T14 to actually pass, and are now reflected in design.md/spec.md.

- **`inbox_cubit.dart`'s 4 `runChatTurn` call sites** (`startBrainDump`,
  `startWhatNextGuidance`, `startReleasePlanning`, `startQa`) weren't
  enumerated in design.md §4's original call-site table — `/apply`
  found them via a `flutter analyze` failure (`ticketRepository` is
  required) and threaded `ticketRepository: _ticketRepository` through
  each, mirroring T4's fix for `tickets_cubit.dart`. Since every Inbox
  chat is parentless by construction, `_logElapsedTime` no-ops at all 4
  — correct behavior, just previously undocumented. design.md §2/§4 and
  specs/spec.md's ADDED section now describe this accurately.
- **Two test files outside T9–T12's named scope** needed matching
  stubbing fixes once the new time-logging call started firing on every
  `runChatTurn`: `tickets_cubit_codebase_analysis_test.dart` (its
  `_runFullSummarization` coverage drives a real `runChatTurn`) and one
  `trashTicket` test in `tickets_cubit_test.dart` (its own blanket
  `getTicketById(any())` default, added to that file's shared `setUp`
  for T12, incidentally broke a test that was coincidentally relying on
  an *unrelated* unstubbed-mock error rather than the failure path it
  claimed to test — fixed by stubbing that one test's actual intended
  failure point).