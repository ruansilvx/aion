---
ticketId: AIO-857
type: story
status: done
priority: none
parentId: de51291e-d1f0-4162-ae92-8785a5faf823
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.226735
updatedAt: 2026-09-02T21:17:20.226735
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Settings — Execution Context Cap

- [x] T19. `features/providers/presentation/cubit/
      execution_context_cap_state.dart` — new file,
      `ExecutionContextCapState`/`ExecutionContextCapLoading`/
      `ExecutionContextCapReady` (design.md §15). File header + dartdocs.
- [x] T20. `features/providers/presentation/cubit/
      execution_context_cap_cubit.dart` — new file,
      `ExecutionContextCapCubit` with `load`/`setOverride` (design.md
      §15). File header + dartdocs.
- [x] T21. `features/providers/presentation/screens/settings_screen.dart`
      — new `_ExecutionContextCapSection` widget (plus a private
      `_ExecutionContextCapHelperRow` for the three helper-line variants),
      placed under the "MODELS" eyebrow below the Execution model row
      (design.md §16), implementing the pasted Claude Design export per
      design.md "Execution Context Cap: Settings Component Spec" §1
      (placement/ordering) and §2 (`ExecutionContextCapField` structure,
      states, helper-row variants). Use `needsRepairTint`/
      `needsRepairBorderTint` (existing `AionColorsHubTokens` helpers)
      for the clamp-chip fill/border, not the export's proposed
      `warningTint`/`warningBorderTint` — see the design gate note in
      proposal.md. `AppTextField` (`design_system/atoms/app_text_field.dart`)
      gained optional `keyboardType`/`inputFormatters` passthrough params
      (both default `null`, every existing call site unaffected) since
      the atom didn't previously expose them and the digits-only numeric
      entry this field needs depends on them. Dartdoc the new widget
      classes and the atom's two new fields.
- [x] T22. `lib/l10n/app_en.arb` — add
      `settingsExecutionContextCapLabel`/
      `settingsExecutionContextCapDescription`/
      `settingsExecutionContextCapEffective` (+ its placeholder metadata)
      (design.md §17). Also added
      `settingsExecutionContextCapOverrideTag`/
      `settingsExecutionContextCapClampWarning` (+ placeholder metadata)
      beyond design.md §17's literal list — the export's "OVERRIDE" pill
      and clamp-warning copy are user-visible strings design.md left as
      inline literals, but every other string on this screen (including
      status-pill labels like "Connected"/"Disconnected" in
      `ProviderConnectionBadge`) goes through `context.l10n`; hardcoding
      just these two would be the one inconsistent spot on the page.