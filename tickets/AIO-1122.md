---
ticketId: AIO-1122
type: story
status: done
priority: none
parentId: 99d34c95-7ca6-44ab-a2f0-e1276d33a8f3
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.261924
updatedAt: 2026-09-02T21:17:20.261924
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `ClaudeAgentSdkClient` / bridge

- [x] T3. Edit `agent_bridge/index.mjs` — accept `tools` on the request
      line; when non-empty, register each as a custom in-process tool via
      the Agent SDK's own custom-tool mechanism passed through `query()`'s
      `options`; each handler emits `{"type":"tool_call_request",
      toolCallId, name, arguments}` and awaits a matching reply line on a
      stdin reader kept open for the process's lifetime (replacing the
      current single-line-then-close pattern for the reply path — the
      initial request read stays single-line). Update the file's header
      comment block (the NDJSON shape list) to include the two new line
      types. Per design.md §3.
- [x] T4. Edit `core/agent/claude_agent_sdk_client.dart` — pass
      `request.tools` in the initial stdin line; keep stdin open instead
      of closing it after the first write; handle the new
      `'tool_call_request'` stdout line specially in the listener (emit
      `AgentToolCallEvent`, `await request.onToolCall!(...)`, write the
      reply line to stdin, continue listening) rather than through
      `_parseLine`'s normal return-an-`AgentEvent` path. Update class and
      method dartdoc per design.md §3. Full dartdoc on any new private
      helper extracted for the reply-handling logic.