---
ticketId: AIO-2760
type: spec
status: done
priority: none
parentId: null
estimate: null
timeSpent: null
createdAt: 2026-09-09T01:24:54.150
updatedAt: 2026-09-09T01:24:54.150
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Spec delta: scope-exploring-stage-instructions

**Reconciled 2026-09-09:** cross-checked against the current `aion-arch/specs/tickets.md` text — every distinctive symbol/behavior this delta names is present verbatim (expected: this change was archived 2026-09-08, only one day before this backfill ran, and `/archive` already merges deltas into `specs/*.md` at archive time). No content changes needed.

Target file: `aion-arch/specs/tickets.md`

## MODIFIED — Stage-advance orchestration (`advanceSddStage`'s stage-chat context assembly)

**Current text** (as it stands on `main`, the paragraph describing what
`_assembleStageContext` appends per stage):

> Otherwise — for every stage short of `archived` — creates that stage's
> `chat`-type child ticket (title `"<Stage> — <parent.title>"`, parented
> under `ticket`; for `designBrief` specifically, first creates a
> `TicketType.page` design ticket titled `"Design — <parent.title>"` and
> links it to `parent` via `TicketLinkRepository.createLink` — the human
> pastes their Claude Design export as that Page's content, mirroring how
> Documentation pages are edited) and posts a `CommentAuthorType.system`
> comment assembling plain-text context (parent title/description; a `##
> Related tickets` section from `TicketContextEnricher` when it has
> content to contribute, see [Related-tickets context
> enrichment](#related-tickets-context-enrichment); plus, for
> `verifying`/`archived`, direct children's titles/statuses (plus, for
> `verifying` specifically, a request to end the reply with `VERIFY GATE:
> APPROVED`/`PENDING` and, on `PENDING`, optionally a fenced `## Fixes
> Needed` block — one `"- Task: <title>"`/`"- Bug: <title>"` line per fix,
> the same optional `" (blockedBy: <title of an earlier line>)"` suffix
> format `## Decomposition` below uses; `archived`'s own request text —
> Epic spec synthesis — is unaffected); for `proposed`, a request to
> decompose `parent` into child Stories (epic parent) or Tasks (story
> parent), ending the reply with a fenced `## Decomposition` block — one
> `"- Story: <title>"`/`"- Task: <title>"` line per child, optionally
> suffixed `" (blockedBy: <title of an earlier line>)"`; for
> `designBrief`, the existing design-token file contents read off disk —
> `aion_colors.dart`/`aion_text.dart`/`aion_radius.dart`, via
> `_readTokenFilesForContext`, empty if no project root is resolved —
> plus a request to produce a ready-to-paste Claude Design prompt; for
> `designSync`, the linked design Page's pasted content plus the same
> token-file contents, plus a request to flag Material-widget leakage or
> off-token colors and end the reply with a `DESIGN GATE: APPROVED`/
> `PENDING` line) — the `_createStageChat` half, `await`ed.

**New text:**

> Otherwise — for every stage short of `archived` — creates that stage's
> `chat`-type child ticket (title `"<Stage> — <parent.title>"`, parented
> under `ticket`; for `designBrief` specifically, first creates a
> `TicketType.page` design ticket titled `"Design — <parent.title>"` and
> links it to `parent` via `TicketLinkRepository.createLink` — the human
> pastes their Claude Design export as that Page's content, mirroring how
> Documentation pages are edited) and posts a `CommentAuthorType.system`
> comment assembling plain-text context (parent title/description; a `##
> Related tickets` section from `TicketContextEnricher` when it has
> content to contribute, see [Related-tickets context
> enrichment](#related-tickets-context-enrichment); plus, for
> `exploring` specifically, an explicit read-only-investigation scope —
> mirroring `aion-arch/.claude/skills/explore/SKILL.md`'s own rule — that
> tells the model not to edit, create, or delete any project files, not
> to draft a file-by-file implementation plan or write code, and not to
> ask for write access to any repository, closing with a request for a
> written exploration summary instead of a proposal; for
> `verifying`/`archived`, direct children's titles/statuses (plus, for
> `verifying` specifically, a request to end the reply with `VERIFY GATE:
> APPROVED`/`PENDING` and, on `PENDING`, optionally a fenced `## Fixes
> Needed` block — one `"- Task: <title>"`/`"- Bug: <title>"` line per fix,
> the same optional `" (blockedBy: <title of an earlier line>)"` suffix
> format `## Decomposition` below uses; `archived`'s own request text —
> Epic spec synthesis — is unaffected); for `proposed`, a request to
> decompose `parent` into child Stories (epic parent) or Tasks (story
> parent), ending the reply with a fenced `## Decomposition` block — one
> `"- Story: <title>"`/`"- Task: <title>"` line per child, optionally
> suffixed `" (blockedBy: <title of an earlier line>)"`; for
> `designBrief`, the existing design-token file contents read off disk —
> `aion_colors.dart`/`aion_text.dart`/`aion_radius.dart`, via
> `_readTokenFilesForContext`, empty if no project root is resolved —
> plus a request to produce a ready-to-paste Claude Design prompt; for
> `designSync`, the linked design Page's pasted content plus the same
> token-file contents, plus a request to flag Material-widget leakage or
> off-token colors and end the reply with a `DESIGN GATE: APPROVED`/
> `PENDING` line) — the `_createStageChat` half, `await`ed. Note that
> `exploring` is the one stage whose stage-specific text is scoping
> instruction rather than an output-format request — every other stage's
> addition tells the model what shape to end its reply in; `exploring`'s
> addition instead constrains what the turn may do at all, closing the
> gap where a ticket entering `exploring` previously got no stage-specific
> instruction whatsoever (only the shared title/description/related-
> tickets preamble every stage gets), unlike `aion-arch`'s own `/explore`
> skill which has always been explicitly read-only.

No "Known gaps" entry is added or removed — the gap this change closes
was never separately documented as a `specs/tickets.md` known gap (it
was tracked in `aion-arch/ideas/decommission-aion-arch-cli-workflow.md`'s
`next_action` instead), and the gap is fully closed, not narrowed or
deferred, so nothing outstanding is left to document there.