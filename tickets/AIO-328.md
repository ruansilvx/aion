---
ticketId: AIO-328
type: story
status: done
priority: none
parentId: 751e56c8-0458-4136-85c1-9b3202d8a6da
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.163211
updatedAt: 2026-09-02T21:17:20.163211
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 7. Final pass

- [x] 7.1 `flutter analyze` clean across every file touched above.
- [x] 7.2 Full `flutter test` run — every new/extended test from tasks
      1.5, 2.4, 3.4, 5.4, 6.6 passes.
- [x] 7.3 Manual interactive pass on a **native desktop build**
      (`flutter run -d windows`), not web — per this project's existing
      convention (the equivalent `new-project-onboarding` pass, and this
      project's own testing guidance) of verifying desktop-only,
      filesystem-touching flows against a real running build rather than
      Flutter Web, which cannot reach `rootPath`-gated code at all.
      Using a scratch project pinned to an older bundled version (e.g.
      temporarily editing its `.aion/manifest.json`/registry row to
      `0.2.0`, or exercising the flow before a future baseline version
      ships and calling out that the pass instead confirms the
      no-op/guard paths against the already-latest `0.3.0` pin): confirm
      the upgrade banner appears once on open, declining hides it but it
      reappears on the next open, accepting bumps the version shown on
      `ProjectCard`/Settings, rewrites `.aion/manifest.json`, and leaves
      existing override files intact and still effective; confirm the
      Settings "Upgrade" action works independently of the banner having
      been shown; confirm a project already on the latest version shows
      no banner and Settings' section reads "up to date." Stop the
      `flutter run` process afterward (confirm no lingering `dart`/
      `flutter` processes) and remove any scratch project created for
      this pass.