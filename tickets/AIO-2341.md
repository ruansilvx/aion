---
ticketId: AIO-2341
type: task
status: done
priority: none
parentId: c85e8bb0-2787-4ea2-8d7a-a90a07628982
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.421185
updatedAt: 2026-09-02T21:17:20.421185
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T15. Manual interactive pass (`flutter run -d web-server --web-port

8765`, real Chrome instance via the Claude in Chrome tools — not the
sandboxed preview browser, per `project.md`'s testing convention).
Create several tickets spanning different statuses/types/priorities
and titles/descriptions. Confirm: typing in the search box filters
the list after the debounce delay, with better title matches
appearing before weaker description-only matches; each of the three
filter dropdowns narrows the list correctly and combines with the
others and with an active text search; selecting "All ___" clears
that filter; an empty-result search/filter shows "No tickets match
your search" (not "No tickets yet"); clearing the search box and
all filters back to defaults shows the full list again; the Retry
button (trigger an error state if feasible, or verify by code
inspection) re-applies the last active search/filter rather than
resetting it. No console errors during the pass.
Verified in a real Chrome instance (Claude in Chrome tools) against
`flutter run -d web-server --web-port 8765`. Created AIO-1 ("Fix
login authentication bug", Task, High), AIO-2 ("Add export to CSV",
Story, none), AIO-3 ("Investigate flaky test", Task, Critical,
description "the drift migration test occasionally fails on CI").
Confirmed: typing "authentication" narrows to AIO-1 only after the
debounce; typing "test" matches AIO-3 via both title and
description; the Type filter dropdown narrows to AIO-2 when set to
Story and renders the approved §3.2 active-state treatment
(`primarySubtle` fill, `primary` border, leading dot, `primary`
text) instead of the default resting style; combining Type=Story
with query "authentication" (no match) renders the §4 "No tickets
match your search" empty state with its motif icon and secondary
hint line, distinct from the plain "No tickets yet" state seen
before any tickets existed; resetting Type to "All types" restores
the full list; the Priority filter (set to High) correctly narrows
to AIO-1 alone and shows the same active-state treatment; a
screenshot taken mid-transition visually caught the §5
`AnimatedSwitcher` 120ms cross-fade in progress, confirming it's
wired up. The `CreateTicketScreen`'s own `AppDropdown` usages
(Type/Priority) were exercised incidentally while creating test
tickets and still render/behave exactly as before — confirming the
`isActive`/dot additions to the shared `AppDropdown` primitive are
non-breaking for existing call sites. No console errors at any
point beyond the pre-existing, already-documented
`WasmStorageImplementation.inMemory` fallback log. The Retry-button
re-applies-last-search behavior was verified by code inspection
only (T8's `_runSearch` wiring) — no practical way to force a
repository-throw error state interactively without a code change,
same reasoning as other changes' manual passes in this project.