---
ticketId: AIO-2465
type: story
status: done
priority: none
parentId: dc003a29-7b47-47fa-b112-813e28c0baf0
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.438116
updatedAt: 2026-09-02T21:17:20.438116
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `TicketsCubit` — wiring

- [x] T7. `presentation/cubit/tickets_cubit.dart` — new field
      `_executionTokenTotals` (`Map<String, int>`) and a
      `TicketTokenPredictor` instance (`_tokenPredictor`), constructed
      alongside `_estimationSuggester` in the constructor body (design.md
      §2, §3). Dartdoc `_executionTokenTotals` per design.md §3's exact
      wording (cache semantics, reconstructability, why
      `_refreshInFlightBoardState` must stay untouched by this).
- [x] T8. Same file — `createTicket`/`updateTicket` each also call
      `_tokenPredictor.suggest(ticket)` `unawaited`, alongside the
      existing `_estimationSuggester.suggest(ticket)` call.
- [x] T9. Same file — `loadTickets`/`loadMoreTickets`/`getTicketById`
      each batch-seed `_executionTokenTotals` via
      `getExecutionTokenTotals` for any not-yet-cached ticket id(s) in
      the page/ticket just loaded, using compare-and-keep-max against any
      existing in-memory value (design.md §3).
- [x] T10. Same file — `_runCodingExecution`'s implement- and verify-turn
      completion points each update `_executionTokenTotals[task.id]` by
      `(event.inputTokens ?? 0) + (event.outputTokens ?? 0)` immediately
      after that turn's `ai` comment is persisted, before the existing
      `_refreshInFlightBoardState()`/`_refreshTaskDetailIfShowing()`
      calls at those same points (design.md §3).
- [x] T11. `presentation/cubit/tickets_state.dart` — `TicketsLoaded` gains
      `executionTokenTotals` (`Map<String, int>`, default `const {}`) —
      constructor param, field, `props` entry. `TicketDetailLoaded` gains
      `executionTokenTotal` (`int?`, default `null`) — constructor param,
      field, `props` entry (design.md §3). Dartdoc both per design.md
      §3's wording.
- [x] T12. `presentation/cubit/tickets_cubit.dart` — `_refreshInFlightBoardState`
      now also passes `executionTokenTotals: Map.unmodifiable(_executionTokenTotals)`;
      `getTicketById`'s `TicketDetailLoaded` construction now also passes
      `executionTokenTotal: _executionTokenTotals[ticket.id]`.