---
ticketId: AIO-1560
type: story
status: done
priority: none
parentId: de935bb2-df1a-4878-b173-bb56983b4864
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.318264
updatedAt: 2026-09-02T21:17:20.318264
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `features/providers/presentation/` — cubits and Settings UI

- [x] T13. `lib/features/providers/presentation/cubit/model_routing_state.dart`
  — MODIFY: `ModelRoutingReady` gains `availableModels`
  (`List<AgentModelDescriptor>`); `modelByPhase` values become
  `AgentModelDescriptor`. Depends on T3. Update dartdoc to match.
- [x] T14. `lib/features/providers/presentation/cubit/model_routing_cubit.dart`
  — MODIFY: constructor takes a `ProviderRegistry` in addition to
  `ModelRoutingRepository`; `load()` also computes `availableModels` per
  T13, filtering `registry.availableProviders` by
  `ModelPhase.requiredToolAccessTier` (T10) against each provider's
  `supportedToolAccessTiers`, unioning the surviving providers'
  `availableModels`; `selectModel` takes an `AgentModelDescriptor`. Per
  design.md §3. Depends on T6, T10, T12, T13. Update dartdoc to match.
- [x] T15. `lib/features/providers/presentation/cubit/provider_settings_cubit.dart`
  — MODIFY: constructor takes a `ProviderRegistry` instead of an
  `AgentModelClient`; `load()`/`testConnection()`/`_runConnectionTest`
  resolve the Frontier tier's `AgentModelDescriptor` then its
  `AgentProvider` via the registry, run the ping through
  `provider.client`, and pass a `disconnected` result's message through
  `provider.normalizeErrorMessage` before storing it as
  `statusMessage`. Per design.md §7. Depends on T3, T6, T11. Update
  dartdoc to match.
- [x] T16. `lib/features/providers/presentation/cubit/execution_context_cap_cubit.dart`
  — MODIFY: reads `.contextWindowTokens` off the `AgentModelDescriptor`
  `ModelRoutingRepository.getModelForPhase` now returns (mechanical type
  follow-through, no behavior change). Depends on T11. Update dartdoc
  reference from `AgentModel` to `AgentModelDescriptor`.
- [x] T17. `lib/features/providers/presentation/screens/settings_screen.dart`
  — MODIFY: `_ModelPhaseSection`'s `AppDropdown<AgentModel>` becomes
  `AppDropdown<AgentModelDescriptor>`, `items:` sourced from
  `ModelRoutingReady.availableModels` (T13) instead of
  `AgentModel.values`. No other visual change. Depends on T13, T14.
  Update dartdoc references from `AgentModel` to `AgentModelDescriptor`.
  Per design.md §1 Confirmation (data-only, no visual change) — the
  full existing `AppDropdown` reference spec being preserved
  pixel-identical is §3 (component spec) and §4 (field states).
- [x] T18. `lib/features/providers/providers.dart` — MODIFY: remove
  `export 'domain/enums/agent_model.dart';` (file deleted in T9).
  Depends on T9.