---
ticketId: AIO-2330
type: story
status: done
priority: none
parentId: 66202bd2-e332-4cd2-997e-70fc948a0cad
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.421185
updatedAt: 2026-09-02T21:17:20.421185
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation — Widget

- [x] T8. In `aion/lib/features/tickets/presentation/screens/tickets_list_screen.dart`,
      per design.md §§1–5 (Toolbar composition, Search field, Filter
      dropdown trigger, "No results" empty state, Active-search feedback):
      - Replace the decorative search `Row` (lines 164-193 as read during
        `/explore`/`/propose` — confirm current line numbers before
        editing) with a real `AppTextField` bound to a new
        `_searchController`, per design.md §2 Search field (composition,
        states, leading-icon color per state).
      - Add `_statusFilter`/`_typeFilter`/`_priorityFilter` fields and a
        new filter row of three `AppDropdown` instances (status/type/
        priority, each with a `null` = "All ___" item), per design.md §1
        Toolbar composition (vertical stack, spacing) and §3 Filter
        dropdown trigger (default vs. active-selection visual treatment,
        no caption labels, equal-width `Expanded` cells).
      - Add the debounce on the search field's `onChanged`, and immediate
        `_runSearch()` calls on each dropdown's `onChanged`, per design.md
        §2.4/§5 — **use `250ms`**, the value specified in the approved
        design export (§5's "Active-search feedback"), not the `~300ms`
        placeholder from this change's original architecture draft.
      - Add `_runSearch()`, calling `context.read<TicketsCubit>().searchTickets(...)`
        with all four current local filter values.
      - Update `initState` and the error-state Retry button's `onPressed`
        to call `_runSearch()` instead of `loadTickets()`.
      - Per design.md §4 "No results" empty state: when `hasActiveFilter`
        is true and results are empty, render the motif (58×58 rounded
        square, `AionRadius.xl`, `c.primary.withOpacity(a)` fill, 27px
        search glyph) + primary line + secondary line exactly as
        specified — this supersedes the plain single-line text originally
        sketched in this change's architecture draft.
      - Per design.md §5: wrap the list body in the specified
        `AnimatedSwitcher` (120ms opacity cross-fade, no skeleton/shimmer)
        so result-set changes cross-fade instead of swapping instantly.
      - Update `_buildTicketsBody`'s empty-state branch to show
        `ticketsListNoResultsState` when `hasActiveFilter` is true (per
        `design.md`), otherwise the existing `ticketsListEmptyState`.
      - Add `dispose()` cancelling the debounce `Timer` and disposing
        `_searchController` (guard against a pre-existing `dispose()` —
        add to it if one exists, don't create a second override).
      - Update the class-level dartdoc to mention the now-functional
        search/filter row.