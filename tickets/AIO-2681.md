---
ticketId: AIO-2681
type: story
status: done
priority: none
parentId: 097cbcf9-4910-46b7-acb8-673604e809d9
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.459835
updatedAt: 2026-09-02T21:17:20.459835
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T25. `test/features/tickets/domain/render_workflow_prompt_template_test.dart`
      — every supported variable substitutes correctly; an unrecognized
      placeholder passes through unchanged; a template with no
      placeholders round-trips as-is.
- [x] T26. `test/features/tickets/cubit/workflow_config_cubit_test.dart`
      (extends the existing file) — attachment create/update/delete
      including the one-per-target rejection; template create/update
      rejecting a name collision; template delete rejecting while a
      live attachment references it.
- [x] T27. `test/features/tickets/data/repositories/drift_workflow_skill_attachment_repository_test.dart`
      and a sibling for `drift_workflow_prompt_template_repository_test.dart`
      — CRUD round-trip; `onChanged` fires on every write (attachment
      repository only).
- [x] T28. `test/features/tickets/cubit/tickets_cubit_test.dart`
      (extends the existing file) — a `WorkflowStatus` with an `auto`
      attachment fires on entry for both `SkillAttachmentKind` values,
      asserting the constructed `AgentRequest.prompt`/`toolsEnabled`/
      `workingDirectory` per design.md §3.2's table against a fake
      `AgentModelClient`; a `gated` attachment surfaces
      `pendingSkillAttachment` and only fires after
      `confirmPendingSkillAttachment`, never fires after
      `rejectPendingSkillAttachment`; a `manual` attachment never fires
      automatically and does fire via `runAttachedSkillManually`; an
      `SddStage` with a configured attachment uses `_promptFor` instead
      of `_assembleStageContext`; an `SddStage` with none behaves
      identically to Phase 1's existing stage-advance tests
      (regression coverage).
- [x] T29. `test/core/database/app_database_test.dart` (extends the
      existing file) — an in-memory `AppDatabase` opened at schema 15,
      upgraded to 16, ends with both new tables present and empty. A
      fresh `onCreate` install also ends with both tables present and
      empty.
- [x] T30. Widget tests for T20's `_AttachmentForm`/`_AttachmentBadge`
      and T21's `WorkflowPromptTemplatesScreen` (create/edit/delete an
      attachment; create/edit/delete a template) — file location
      matches existing screen-test conventions. Blocked until T20–T21's
      design-gated implementation lands.