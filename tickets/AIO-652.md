---
ticketId: AIO-652
type: story
status: done
priority: none
parentId: 15c9f95f-13db-4f3b-a33f-96d1071c6c8c
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.201434
updatedAt: 2026-09-02T21:17:20.201434
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] `test/core/automation/decision_graph_evaluator_test.dart` — update
      every existing call to `await evaluateDecisionGraph(...)`. Add
      cases: an `agentJudgment` node with a fake `askAgentJudgment`
      returning `true` (matched), `false` (unmatched), `null` (unmatched);
      `session == null` (unmatched, `askAgentJudgment` never invoked —
      assert the fake wasn't called); `askAgentJudgment == null`
      (unmatched); missing/non-`String` `conditionParams['prompt']`
      (unmatched, never invokes the fake).
- [x] `test/core/automation/decision_field_catalog_test.dart` — add
      cases: `agentJudgmentConditionSpec` returns `null` for a non-eligible
      context (e.g. `sddStage`) and a real spec for the 3 eligible ones;
      `isRecognizedConditionId`/`decisionNodeTitle`/`decisionNodeSummary`
      covering an `agentJudgment` node (including a missing/empty
      `prompt` — must not throw).
- [x] New `test/core/agent/agent_session_handle_test.dart` — trivial
      `Equatable` coverage (only if this project's convention tests
      trivial value types at all; skip if `AgentModelClient`'s existing
      value types like `AgentRequest` have no dedicated equality test of
      their own — match existing convention rather than inventing one).
- [x] `test/features/tickets/presentation/cubit/tickets_cubit_test.dart`
      (or wherever `_handleCreateTicketToolCall`/`_handleAddLinkToolCall`/
      `_handleBranchToolCall` are currently covered) — update existing
      `onToolCall` invocations for the new 4th argument. Add cases:
      `_askAgentJudgment` via a mocked `ProviderRegistry` returning a
      provider with `supportsSessionResume == false` (unmatched, no
      `client.run` call attempted — verify via mocktail `verifyNever`),
      a mocked `client.run` stream yielding `AgentTextEvent('Yes')` →
      `AgentDoneEvent()` (matched), `AgentTextEvent('No')` (unmatched),
      `AgentErrorEvent(...)` (unmatched), `AgentCancelledEvent()`
      (unmatched), ambiguous text (unmatched), and `_providerRegistry ==
      null` (unmatched, no lookup attempted).
- [x] `test/features/providers/presentation/screens/decision_graph_editor_screen_test.dart` —
      the "Ask the agent" entry appears in the picker for
      `ticketCreation`/`ticketLinking`/`chatBranching` and is absent for
      every other context; an `agentJudgment` node renders its `ASK ·`
      eyebrow and prompt-text chip and does *not* show error/incomplete
      chrome.
- [x] `flutter analyze` clean. No `@JsonSerializable`/drift surface is
      touched by this change, so the `build_runner` codegen step doesn't
      apply here — only the l10n regeneration from the Localization task
      above.