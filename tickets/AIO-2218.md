---
ticketId: AIO-2218
type: story
status: done
priority: none
parentId: b0b4f362-a113-4f0b-b8cb-169366663b51
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.402977
updatedAt: 2026-09-02T21:17:20.402977
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 8. Tests

- [x] **T12** — Create `test/features/tickets/domain/entities/ticket_test.dart`
  Cases: `copyWith` replaces `title`/`priority`/`type`/`status`/
  `updatedAt` independently when passed a bare value; `copyWith`
  replaces `description`/`estimate`/`timeSpent` when passed `() =>
  <value>`; `copyWith` explicitly clears `description`/`estimate`/
  `timeSpent` to `null` via `() => null`; omitted fields are unchanged
  from the original ticket.

- [x] **T13** — Modify `test/features/tickets/cubit/tickets_cubit_test.dart`
  Add cases: `updateTicket` emits `[TicketDetailLoaded(refreshed)]` on
  success (mock repository's `updateTicket` and `getTicketById`);
  `updateTicket` emits `[TicketsError(...)]` when
  `_repository.updateTicket` throws.

- [x] **T14** — Modify `test/features/tickets/data/repositories/drift_ticket_repository_test.dart`
  Add cases (in-memory Drift DB): `updateTicket` persists
  `title`/`description`/`priority`/`type`/`estimate`/`timeSpent`;
  `updateTicket` leaves `status`/`parentId`/`embedding`/`createdAt`
  untouched; `updateTicket` sets `updatedAt` to a timestamp `>=` the
  original. Also confirm the existing `updateTicketStatus` test case(s)
  still pass unchanged after the `TicketDao.updateStatus` → `update`
  rename (regression check for T03/T04) — adjust only if a test
  references the old DAO method name directly.

---