---
ticketId: AIO-2123
type: story
status: done
priority: none
parentId: 39272bcc-1770-4302-a37a-d9b025c08dcf
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.391110
updatedAt: 2026-09-02T21:17:20.391110
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `TicketsCubit` — tool handlers

- [x] T10. Edit `presentation/cubit/tickets_cubit.dart` — add
      `_handleCreateTicketToolCall(Ticket chat, Map<String, dynamic>
      arguments)`: validates `title`/`type` (declining with a reason
      string on invalid input, same defensive style as
      `_handleBranchToolCall`), resolves
      `AutomationContext.ticketCreation`'s confidence, and switches
      `manual`/`auto`/`gated` exactly like `_handleBranchToolCall`, per
      design.md §3.3.
- [x] T11. Edit `presentation/cubit/tickets_cubit.dart` — add
      `_handleAddLinkToolCall(Ticket chat, Map<String, dynamic>
      arguments)`: resolves `targetTicketId` via T2's
      `getTicketByTicketId` (declining if not found), validates
      `linkType`, resolves `AutomationContext.ticketLinking`'s
      confidence, and switches `manual`/`auto`/`gated`, calling
      `TicketLinkRepository.createLink` with `sourceTicketId:
      chat.parentId!` on acceptance, per design.md §3.3.
- [x] T12. Edit `presentation/cubit/tickets_cubit.dart` — add
      `_handleLogTimeToolCall(Ticket chat, Map<String, dynamic>
      arguments)`: validates `minutes` (declining non-positive/missing
      values), and calls T2's `addTimeSpent(chat.parentId!, minutes)`
      unconditionally — no `AutomationConfidence` branch, per design.md
      §3.3.