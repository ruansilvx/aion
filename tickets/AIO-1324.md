---
ticketId: AIO-1324
type: task
status: done
priority: none
parentId: 0502325a-ae03-463f-8a80-ab919328a6ea
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.287759
updatedAt: 2026-09-02T21:17:20.287759
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 5.3 Extended `test/core/routing/workspace_nav_shell_test.dart`

— `/workspace/inbox` resolves the Inbox nav item as active; tapping
each of the three destinations navigates correctly in both wide
and compact layouts. All passing. Also created
`test/core/routing/app_router_test.dart` — exercises the real
`appRouter` singleton (not a stand-in `GoRouter`) with every
cross-feature dependency `WorkspaceShell` needs mocked
(`AgentModelClient`/`ModelRoutingRepository`/`EmbeddingProvider`/
`AutomationSettingsRepository`/`ExecutionContextCapRepository`/
`BaselineRepository`/`ProjectRepository`) except `TicketRepository`,
which is backed by a real, temp-directory-addressed `AppDatabase` —
proving `/workspace/inbox` resolves to `InboxScreen` with a
genuinely working, drift-backed `InboxCubit`, both for the
empty-history case and for a ticket seeded directly via
`DriftTicketRepository` against the same project root. Required
`tester.runAsync` (drift's isolate-backed native connection needs
real, not fake-zone, async time to resolve) and a mocked
`path_provider` method channel (`drift_flutter`'s `driftDatabase`
unconditionally calls `getTemporaryDirectory` once per process to
point `sqlite3.tempDirectory` somewhere writable, regardless of
the `databasePath` override).