---
ticketId: AIO-1796
type: task
status: done
priority: none
parentId: 3f137d7b-c1de-45bf-83de-5e9a55264daa
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.353397
updatedAt: 2026-09-02T21:17:20.353397
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# **T10** — `aion/lib/features/tickets/presentation/screens/release_draft_screen.dart`

(new file): `ReleaseDraftScreen` per `design.md` §5.3 — editable
changelog/version `AppTextField`s (version field replaced by a "no
version file detected" notice when `draft.detectedVersionFile` is
`null`), Cancel/`Confirm & Tag` actions, `Confirm & Tag` gated by
`AppConfirmDialog` before calling `TicketsCubit.confirmRelease`. On
failure, stays on screen with an error banner and the same editable
draft rather than popping — matches T7's retry rationale. File
header, widget class dartdoc, dartdoc on every public constructor
parameter. Visual spec: design.md's Claude Design export §3
"ReleaseDraftScreen" (§3.1–3.7, incl. the derived-tag-preview pill
§3.6 and the "no version file" footer-relabeling behavior defined in
§6), plus §4 "Tag/branch confirmation dialog" for the `Confirm & Tag`
gate's exact content, and §5 "Push-failure error banner" for the
on-failure state (§5 supersedes this task's plain "error banner"
wording with a fully specified component — the banner stays mounted
with the draft, is not a toast/dialog, and shows a "committed and
tagged locally" sub-line when the local write succeeded but the push
failed) and §6 "No version file detected notice" for the two
unsupported-stack projects (`go.mod`/`requirements.txt`) — in that
case `Confirm & Tag` relabels to "Save changelog" and skips §4
entirely.