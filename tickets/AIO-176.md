---
ticketId: AIO-176
type: story
status: done
priority: none
parentId: 53222491-b760-477b-84ce-37a495596f35
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.149234
updatedAt: 2026-09-02T21:17:20.149234
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Manual verification

- [x] T3 — Live pass against the `flutter-web` dev server, driven from a
  real Chrome (Claude in Chrome). Verified: created a ticket, trashed it
  via `TicketOverflowMenu` (confirm dialog copy unchanged — "This ticket
  will move to Trash..."), confirmed it appeared on `/tickets/trash`
  ("Trashed today", `purgeEligibleCount == 0` so "Purge old" correctly
  stayed disabled) — sanity-checking that none of the existing manual
  trash machinery regressed. Reloaded the app (full page load, so
  `main()`/`_purgeOldTrashOnLaunch` re-runs from scratch): app booted
  cleanly straight back to the Tickets list with zero new console errors
  introduced by this change, and first paint was not visibly delayed
  (DDC's one-time compile dominates load time in this dev-server
  environment regardless of this change; the purge call itself is
  unawaited and runs after `runApp` is already in flight). Repeated
  across three separate full reloads — consistent, no crashes, no
  regressions.
  **Not verified live:** the actual "ticket older than 30 days gets
  removed on relaunch" end-to-end behavior. This Chrome automation
  context logs `Using WasmStorageImplementation.inMemory due to missing
  browser features: {MissingBrowserFeature.workerError}` on every load
  — the same drift-wasm non-persistent-storage fallback the archived
  `ticket-trash` change's own manual pass hit — so a reload always wipes
  all data (including any trashed ticket) rather than preserving it
  across the relaunch a real backdate-and-reload test needs. The purge's
  actual age-cutoff/cascade correctness is unaffected by this limitation
  and remains fully covered by the existing, unchanged
  `drift_ticket_repository_test.dart` `purgeTrashOlderThan` test group;
  what this pass adds on top is confirmation that the new launch-time
  trigger itself is wired correctly and doesn't destabilize app startup.