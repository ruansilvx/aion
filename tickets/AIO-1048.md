---
ticketId: AIO-1048
type: story
status: done
priority: none
parentId: 74b75c28-a4e0-4ed8-b481-86371570957d
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.250969
updatedAt: 2026-09-02T21:17:20.250969
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 3. Presentation — Cubit

- [x] **T06** — Modify `lib/features/tickets/presentation/cubit/tickets_state.dart`
  Add `TicketStatusUpdating` and `TicketStatusUpdated` states (each
  carrying `List<Ticket> tickets`), extending `TicketsState`, per
  `design.md`. Dartdoc each per the project's Cubit-state convention
  (describe what UI condition it represents and what data it carries).

- [x] **T07** — Modify `lib/features/tickets/presentation/cubit/tickets_cubit.dart`
  Add `updateTicketStatus(String id, TicketStatus status)` per
  `design.md`'s emit-try-catch shape: pull the current list from state
  (extend the existing `switch` to also cover `TicketStatusUpdating`/
  `TicketStatusUpdated`), build the optimistic list via
  `Ticket.copyWith`, emit `TicketStatusUpdating`, call
  `_repository.updateTicketStatus`, re-fetch via `getAllTickets()`, emit
  `TicketStatusUpdated` (or `TicketsError` on exception). Dartdoc states
  what it emits and when, per the existing method docs in this file.

---