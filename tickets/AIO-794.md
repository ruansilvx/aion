---
ticketId: AIO-794
type: task
status: done
priority: none
parentId: a2acc5f1-cde5-4113-a278-35fd097e7514
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.218399
updatedAt: 2026-09-02T21:17:20.218399
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Manual interactive pass in real Chrome (Claude in Chrome tools,

`flutter run -d web-server --web-port 8765`, per design.md
"Testing"): `TicketsListScreen` rendered (header, search field,
three `AppDropdown` filters, `AppFab`); created a ticket through
`CreateTicketScreen` (`AppTextField`, two `AppDropdown`s — opened
and selected "High" from one — full-width `AppButton` submit);
`TicketDetailScreen` rendered with the new ticket; clicked the
title `InlineEditableField<String>` into edit mode and committed
a change; triggered the estimate `InlineEditableField<int?>`'s
`FormatException`/revert path with invalid input; opened the
priority badge's `SelectionMenu`; opened `TicketOverflowMenu` →
"Delete ticket" → `showAppConfirmDialog` rendered correctly (icon
disc, title, message, Cancel/Delete `AppButton`s) and Cancel
dismissed it cleanly. Every design-system tier (tokens/atoms/
molecules) and the promoted `InlineEditableField` confirmed
working post-move. Not separately screenshotted: `AppToast`'s
visible pop (its 3-second auto-dismiss window was missed between
screenshots) and `TicketsBoardView`/`TrashScreen` — lower risk,
same import pattern as the screens already confirmed, and
`flutter analyze` already proves nothing in those files is
broken at the import level.