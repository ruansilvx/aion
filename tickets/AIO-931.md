---
ticketId: AIO-931
type: task
status: done
priority: none
parentId: b919be9a-069b-405d-aabd-6aae46c24918
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.236688
updatedAt: 2026-09-02T21:17:20.236688
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T8. Repeated `trash-restore-git-projection`'s T4 method (a

throwaway `flutter test` script — real `AppDatabase`/
`DriftTicketRepository`, real `GitRepositoryClient` against a
real temp git repo; deleted after the run, never committed).
**Adapted from the original wording**: this branch was cut from
`main`, which doesn't yet have `trash-restore-git-projection`'s
unmerged `TrashCubit` git-projector wiring, so the script called
`repository.trashTicket`/`.restoreTicket` directly followed by
`projector.project(..., 'trashed'/'restored')` inline instead of
going through `TrashCubit.restore` — testing this change's own
fix (the serializer) independently of that sibling branch's
unmerged wiring, which is out of this task's scope to depend on.
**Result, real `git log --oneline` output**:
```
2877e2d ticket: AIO-1 restored
65012f8 ticket: AIO-1 trashed
2054068 ticket: AIO-1 created
```
All three events produced real commits — confirmed via the
script's assertions and by inspecting the printed log/file output
directly. The final `AIO-1.md` correctly shows `deletedAt: null`
after the restore. GUI automation was not attempted again in this
environment — no reason to expect the same sandbox constraint
documented in `trash-restore-git-projection`'s T4 has changed
since then, and the real-git-subprocess method above already
exercises the actual fix end-to-end.