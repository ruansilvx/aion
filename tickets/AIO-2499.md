---
ticketId: AIO-2499
type: story
status: done
priority: none
parentId: acd2b438-557e-4a1f-9e2b-4fae6ca86a5f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.441422
updatedAt: 2026-09-02T21:17:20.441422
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T10. Create `test/core/utils/relative_time_format_test.dart`
      following `test/core/utils/duration_format_test.dart`'s style —
      one case per bucket (today, yesterday, N days, N weeks, N
      months), using the `now` parameter for determinism.

- [x] T11. Update
      `test/features/tickets/cubit/trash_cubit_test.dart`: add a third
      argument to every existing `TrashLoaded(...)` expectation/literal
      (`TrashLoaded([], {})` → `TrashLoaded([], {}, 0)`, etc., adjusting
      the `isA<TrashLoaded>().having(...)` cases too); add a case
      verifying `load()` computes a non-zero `purgeEligibleCount` from a
      mix of old/young trashed tickets; add a `purgeOldTrash` test pair
      (calls repository then reloads; emits `TrashError` on failure)
      mirroring the existing `emptyTrash` tests.

- [x] T12. In
      `test/features/tickets/data/repositories/drift_ticket_repository_test.dart`,
      add `group('purgeTrashOlderThan', ...)` per design.md: use the
      file's existing real in-memory `AppDatabase` setup; set specific
      past `deletedAt` values via
      `database.ticketDao.softDeleteByIds([id], customMs)` for
      deterministic ages; cover purging only tickets older than the
      cutoff (leaving younger/live tickets untouched), cascading to
      comments/`ticket_links`, and a no-op when nothing qualifies.