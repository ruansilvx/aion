---
ticketId: AIO-901
type: story
status: done
priority: none
parentId: 34f5d83d-805c-4bbe-9e27-6ebda07f31da
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.230479
updatedAt: 2026-09-02T21:17:20.230479
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Manual verification (desktop)

- [x] **T23.** Run the app on Windows desktop (per this project's
  desktop-only verification convention). Confirm: adding an estimate
  to a Task under a Story under an Epic immediately shows the rolled-up
  total on both ancestors' detail screens, list rows, and board cards,
  with no manual refresh; reparenting a Task to a different Story
  updates both the old and new Story's (and their Epics', if
  different) rollups immediately; trashing a Task removes its
  contribution from its Story/Epic's rollup immediately, and restoring
  it from the Trash screen adds it back; a leaf ticket with no children
  shows no rollup indicator anywhere; opening a project whose local DB
  predates this change (or deleting the local DB to force a fresh
  schema-9 migration against existing `tickets/*.md` git history —
  whichever is easier to set up) shows correct rollups immediately
  after the upgrade, with no per-subtree edit required to "wake up"
  the backfill. Confirm via the project's git log that one estimate
  edit on a deeply-nested Task produces exactly one rollup-related
  commit (not one per ancestor). No `setState`-after-dispose or
  overlay-related crashes in any touched widget.