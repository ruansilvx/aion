---
ticketId: AIO-1976
type: story
status: done
priority: none
parentId: 691cefc0-3a4f-451f-9dd4-bbfbdb237632
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.373554
updatedAt: 2026-09-02T21:17:20.373554
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] Unit tests for `evaluateTransitionGraph` — null root → `allowed`,
      no field name; single-node matched/unmatched termination with
      correct `blockingFieldDisplayName`; the 3-node `proposed` tree
      and 2-node `designSync` tree walk correctly; a dangling node
      reference resolves `allowed` defensively (not `unmatched` — a
      node id missing from `nodesById` has no field to resolve
      unmatched *from*; wording corrected 2026-09-01 post-`/verify`,
      no behavior change), never throws.
- [x] `DriftTransitionPreconditionRepository` tests — seeding
      idempotency (5 stages, correct tree shapes per `design.md` §3),
      CRUD round-trip, `onChanged` firing on write.
- [x] `TransitionPreconditionConfigCubit` tests — strict-tree
      invariant rejection (second reference to an existing node id;
      cycle creation).
- [x] `TicketsCubit` tests — for each of the 5 precondition-bearing
      stages: unedited baseline graph reproduces
      `_sddStageAdvanceCheck`'s exact pre-change `canAdvance` result
      across representative ticket states (chat with/without terminal
      reply; Story with/without design-needing Tasks; design page
      present/absent/empty; design-sync approved/not, tasks done/not).
      A configured graph resolving to `blocked` surfaces the correct
      auto-derived `blockReason` string. `null`/`archived` branches are
      provably untouched.
- [x] Regression test confirming `TicketDetailLoaded.canAdvanceSddStage`/
      `.sddStageBlockReason` and `_sddStageAdvanceCheck` never disagree
      (mirrors the existing dartdoc guarantee that the two share one
      check).
- [x] `getNodeCounts()` tests (2026-09-01 follow-up) — empty map before
      any graph exists, `0` for a graph row with a `null` root, and the
      correct per-stage count across multiple configured graphs in one
      call.