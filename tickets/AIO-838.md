---
ticketId: AIO-838
type: story
status: done
priority: none
parentId: de51291e-d1f0-4162-ae92-8785a5faf823
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.226735
updatedAt: 2026-09-02T21:17:20.226735
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Comment schema

- [x] T4. `features/tickets/domain/entities/ticket_comment.dart` —
      `TicketComment` gains `inputTokens`/`outputTokens` (`int?`,
      constructor + `props`) (design.md §4).
- [x] T5. `features/tickets/data/models/ticket_comment_model.dart` —
      `TicketCommentsTable` gains nullable `IntColumn`s `inputTokens`/
      `outputTokens` (design.md §4).
- [x] T6. `features/tickets/data/repositories/drift_comment_repository.dart`
      — `addComment`'s companion insert and `_toEntity`'s mapping both
      carry the two new fields (design.md §4).
- [x] T7. `core/database/app_database.dart` — `schemaVersion` 6 → 7;
      `onUpgrade` gains the `from < 7` `addColumn` step for both new
      columns (design.md §4). Update the class dartdoc's per-version
      history list. Run `dart run build_runner build
      --delete-conflicting-outputs` to regenerate `app_database.g.dart`
      as part of this task.