---
ticketId: AIO-2523
type: story
status: done
priority: none
parentId: da9b2da2-d14f-4f16-b657-f8b2ac0d5e8e
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.447101
updatedAt: 2026-09-02T21:17:20.447101
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data layer

- [x] 2. Modify `lib/features/tickets/data/repositories/drift_ticket_repository.dart`
      — rename the private `_resolveTrashCascade` helper to
      `_resolveDescendantCascade` and update its dartdoc to describe the
      broader (not trash-specific) reuse. Update its two existing call
      sites (`trashTickets`, `previewTrashCount`) to the new name. No
      behavior change. (design.md §3.1)
- [x] 3. Modify `lib/features/tickets/data/repositories/drift_ticket_repository.dart`
      — implement `permanentlyDeleteTickets(List<String> ids)`: resolve
      the cascade via `_resolveDescendantCascade(ids)`, return `0` early
      if empty, otherwise one `_db.transaction()` deleting comments
      (`commentDao.deleteCommentsForTickets`), ticket links
      (`ticketLinkDao.deleteLinksForTickets`), page wikilinks
      (`pageWikilinkDao.deleteLinksForTickets`), then ticket rows
      (`ticketDao.deleteTicketRows`) — identical shape to `emptyTrash`/
      `purgeTrashOlderThan`, scoped to the resolved cascade set. Returns
      the cascade set's size. Full dartdoc referencing the interface
      doc via `@override`. (design.md §3.2)