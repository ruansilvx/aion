---
ticketId: AIO-1574
type: story
status: done
priority: none
parentId: de935bb2-df1a-4878-b173-bb56983b4864
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.318264
updatedAt: 2026-09-02T21:17:20.318264
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T24. `test/core/agent/claude_agent_sdk_provider_test.dart` — ADD:
  unit tests for `availableModels` (three descriptors, correct
  `providerId`), `supportedToolAccessTiers` (`{noTools, full}`, not
  `readOnly`), `describeOverage` (wraps in `UsageWindowConsumption`),
  `normalizeErrorMessage` (known vendor-pattern input → cleaned output;
  unrecognized input → unchanged passthrough). Depends on T7.
- [x] T25. `test/core/agent/static_provider_registry_test.dart` — ADD:
  unit tests for `providerById` (found case, and the `StateError` for an
  unregistered id) and `availableProviders`. Depends on T8.
- [x] T26. `test/features/providers/data/repositories/shared_prefs_model_routing_repository_test.dart`
  — MODIFY existing tests: update for the new `ProviderRegistry`
  constructor dependency and two-key persistence; ADD a case covering
  the `..._provider_id`-missing migration fallback (existing
  `..._model_id`-only data still resolves to the right
  `AgentModelDescriptor` under `ProviderId.claudeAgentSdk`). Depends on
  T12.
- [x] T27. `test/features/providers/presentation/cubit/model_routing_cubit_test.dart`
  — MODIFY existing tests: update for `AgentModelDescriptor` and the new
  `availableModels` field; ADD a case verifying the tool-access-tier
  filter (a fake second provider lacking `full` support is excluded from
  `execution`'s `availableModels`) using a `mocktail`-mocked
  `AgentProvider`/`ProviderRegistry`. Depends on T14.
- [x] T28. `test/features/providers/presentation/cubit/provider_settings_cubit_test.dart`
  — MODIFY existing tests: update for `ProviderRegistry`-based
  resolution; ADD a case asserting `statusMessage` on a `disconnected`
  result is the `normalizeErrorMessage`-mapped text, not the raw
  message. Depends on T15.
- [x] T29. `test/features/tickets/presentation/cubit/chat_cubit_test.dart`
  — MODIFY existing tests: update `runChatTurn` call sites/mocks for
  the new `provider`/`AgentModelDescriptor`/`onConsumptionSignal`
  signature; ADD a case asserting an `AgentOverageDetectedEvent` reaches
  `onConsumptionSignal` as a `UsageWindowConsumption`. Depends on T19.
- [x] T30. `test/features/tickets/presentation/cubit/tickets_cubit_test.dart`
  and `test/features/tickets/presentation/cubit/inbox_cubit_test.dart`
  — MODIFY existing tests: update constructor mocks from
  `AgentModelClient` to `ProviderRegistry`/`AgentProvider` fakes across
  every affected test case. Depends on T20, T21.