---
ticketId: AIO-1640
type: story
status: done
priority: none
parentId: 81a1844d-9244-4154-863e-9f931f22703d
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.328996
updatedAt: 2026-09-02T21:17:20.328996
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tasks: Private Widget Retrofit

- [x] **T1** — `lib/design_system/atoms/app_button.dart`: convert
  `_buildDecoratedContent` → `_DecoratedContent` and `_label` → `_Label`
  into private `StatelessWidget` classes (Shape B + derived-value
  hoisting — see `design.md`). Hoist `_fillColor`/`_textColor`/`_border`/
  `_shadow` calls to the call site inside `_AppButtonState.build()`,
  passing their results in as constructor fields. No dartdoc needed
  (private classes); file header comment already exists and needs no
  change.
- [x] **T2** — `lib/design_system/molecules/app_confirm_dialog.dart`:
  convert `_leadingIcon` → `_LeadingIcon`, `_cancelButton` →
  `_CancelButton`, `_confirmButton` → `_ConfirmButton`, `_wideActions` →
  `_WideActions`, `_narrowActions` → `_NarrowActions` (Shape A —
  straightforward field mapping, `_WideActions`/`_NarrowActions` compose
  `_CancelButton`/`_ConfirmButton` directly).
- [x] **T3** — `lib/features/tickets/presentation/widgets/ticket_parent_picker.dart`:
  convert `_buildInlineTrigger` → `_InlineTrigger` and
  `_buildFormFieldTrigger` → `_FormFieldTrigger` (Shape B — read the full
  method bodies before finalizing each constructor's field list; confirm
  exactly which of `errorText`/`isDisabled`/`_isFocused`/`_isOpen`/
  `_isHovered` each one actually uses).
- [x] **T4** — `lib/features/projects/presentation/widgets/project_card.dart`:
  convert `_overflowRow` → `_OverflowRow`, `_titleRow` → `_TitleRow`,
  `_locationRow` → `_LocationRow`, `_metaRow` → `_MetaRow`, `_openButton`
  → `_OpenButton` (Shape A).
- [x] **T5** — `lib/features/projects/presentation/widgets/empty_hub_state.dart`:
  convert `_emblem` → `_Emblem` and `_newProjectCta` → `_NewProjectCta`
  (Shape A).
- [x] **T6** — `lib/features/projects/presentation/screens/new_project_screen.dart`:
  convert `_form` → `_Form`, `_directoryPicker` → `_DirectoryPicker`,
  `_noDirectoryNotice` → `_NoDirectoryNotice`, `_baselineVersionField` →
  `_BaselineVersionField`, `_footer` → `_Footer`, `_inlineError` →
  `_InlineError` (Shape A).
- [x] **T7** — `lib/features/projects/presentation/screens/hub_screen.dart`:
  convert `_header` → `_Header`, `_body` → `_Body`, `_grid` → `_Grid`,
  `_error` → `_Error` (Shape A).
- [x] **T8** — `lib/features/tickets/presentation/screens/tickets_list_screen.dart`:
  convert `_buildTicketsBody` → `_TicketsBody` (Shape A).
- [x] **T9** — `lib/features/tickets/presentation/screens/ticket_detail_screen.dart`:
  convert `_buildAvatar` → `_Avatar` (Shape A).
- [x] **T10** — Add the enforcement grep check to `flutter-conventions.md`'s
  Widget composition section (see `design.md`'s Enforcement block for the
  exact text), and remove the now-closed "private widget, not a
  widget-returning function" bullet from `aion-arch/specs/README.md`'s
  Known gaps once T1–T9 are done.
- [x] **T11** — Run `flutter analyze` (must stay clean) and the grep check
  from T10 (`grep -rn '^\s*Widget _[a-zA-Z0-9_]*(' lib/ --include=*.dart`
  → zero results). Then a single consolidated manual interactive pass in
  real Chrome (`flutter run -d web-server`, per `project.md`'s Testing
  section — not the sandboxed preview browser) covering: any screen
  rendering an `AppButton` (all of them) and an `AppConfirmDialog`
  (delete/destructive confirmations), the project hub screen, the new
  project screen (directory picker flow), the project card's overflow
  menu, the tickets list screen, the ticket detail screen's avatar, and
  the ticket parent picker's both trigger variants (inline on ticket
  detail, form-field on create-ticket). Confirm no visual regressions and
  no hover/focus/press state loss. Stop the dev server and close the
  Chrome tab when done.