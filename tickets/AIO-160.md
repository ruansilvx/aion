---
ticketId: AIO-160
type: story
status: done
priority: none
parentId: 240c0794-e9e6-4ce1-bf87-4ebdf249668b
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.146722
updatedAt: 2026-09-02T21:17:20.146722
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests — `ChatCubit`

- [x] T9. Edit `test/features/tickets/cubit/chat_cubit_test.dart` — add
      `ticketRepository: ticketRepository` (the file's existing
      `MockTicketRepository`) to all 4 existing direct
      `ChatCubit.runChatTurn(...)` calls in the `'runChatTurn usage
      capture'` group, stubbing
      `when(() => ticketRepository.getTicketById('chat-1')).thenAnswer(...)`
      to return a ticket with a non-null `parentId` (and
      `when(() => ticketRepository.addTimeSpent(any(), any())).thenAnswer((_) async {})`)
      so each existing test keeps passing unaffected by the new
      side-effecting call.
- [x] T10. Same file — add a new `'automatic time logging'` group
      covering: `addTimeSpent` is called with the chat's `parentId` and
      a `minutes` value `>= 1` on `ChatTurnSuccess`; the same on
      `ChatTurnFailure` (an `AgentErrorEvent` stream); the same on
      `ChatTurnCancelled` (an `AgentCancelledEvent` stream); and
      `addTimeSpent` is never called when `getTicketById` resolves a
      chat with `parentId: null` (or resolves `null` entirely) — per
      design.md §2.1/§3's "no `Clock` needed" approach, assert on the
      call happening and the `minutes` argument being `>= 1`, not an
      exact value.