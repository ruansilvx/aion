---
ticketId: AIO-138
type: story
status: done
priority: none
parentId: 7e9e2265-8f90-4599-9734-fba899b98bed
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.142116
updatedAt: 2026-09-02T21:17:20.142116
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T18. `test/core/agent/anthropic_messages_api_client_test.dart` —
  ADD: unit tests (mocking `Dio`) for: no-API-key → immediate
  `AgentErrorEvent`; `toolsEnabled: true` → immediate `AgentErrorEvent`;
  a successful SSE stream → the right sequence of `AgentTextEvent`s
  followed by one `AgentDoneEvent` carrying token counts; a `429`
  response → `AgentOverageDetectedEvent` not `AgentErrorEvent`; any
  other error status → `AgentErrorEvent`. Depends on T3.
- [x] T19. `test/core/agent/anthropic_messages_api_provider_test.dart`
  — ADD: unit tests for `availableModels` (three descriptors, correct
  `providerId`), `supportedToolAccessTiers` (`{noTools}` only),
  `describeOverage` (wraps in `CostConsumption` with `amountUsd: null`),
  `normalizeErrorMessage` (known-pattern input → cleaned; unrecognized
  input → unchanged passthrough). Depends on T4.
- [x] T20. `test/features/providers/data/repositories/secure_storage_anthropic_api_key_repository_test.dart`
  — ADD: unit tests for `getApiKey`/`setApiKey` round-trip and
  `setApiKey(null)`/`setApiKey('')` both deleting the stored key
  (mocking `FlutterSecureStorage`). Depends on T6.
- [x] T21. `test/features/providers/presentation/cubit/anthropic_provider_config_cubit_test.dart`
  — ADD: unit tests for `load()` never calling `provider.client.run`;
  `saveApiKey` trimming/clearing and resetting `status` to `unknown`;
  `testConnection()` no-op paths (`!hasApiKey`, already `checking`) and
  its success/error/overage-mapping paths, using `mocktail`-mocked
  `AnthropicApiKeyRepository`/`ProviderRegistry`/`AgentProvider`.
  Depends on T8.
- [x] T22. `test/features/providers/presentation/cubit/provider_settings_cubit_test.dart`
  — MODIFY existing tests: assert `providerDisplayName` on the emitted
  `ProviderSettingsReady` matches the resolved `AgentProvider.displayName`
  for both a Claude-Agent-SDK-resolved and an
  Anthropic-Messages-API-resolved Frontier model (fake/mocktail
  `AgentProvider`). Depends on T9, T10.