---
ticketId: AIO-1254
type: story
status: done
priority: none
parentId: 88f65bed-75f1-44af-a767-912a8a5c3fe3
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.273148
updatedAt: 2026-09-02T21:17:20.273148
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] 22. Update/add tests in
      `test/features/tickets/data/daos/ticket_dao_test.dart` (or
      equivalent existing DAO test file) covering: empty set == no
      constraint (regression vs. old `null` behavior), single-value set ==
      old single-value behavior (regression), multi-value set OR-matches
      within a field, multi-field sets AND together — both the no-query
      and FTS-query paths. (design.md §6)
- [x] 23. Update `test/features/tickets/data/repositories/drift_ticket_repository_test.dart`
      for the new `Set`-typed `searchTickets` params.
- [x] 24. Update `test/features/tickets/cubit/tickets_cubit_test.dart` —
      `Set`-typed `searchTickets`/`loadMoreTickets`/mutation-refresh
      tests, plus new tests for `toggleStatusFilter`/`toggleTypeFilter`/
      `togglePriorityFilter` (add/remove/persist/no-op-when-null) and
      `loadPersistedFilters()`. Also fixed a cascading test-corruption bug:
      the new `filterRepository.setFilters(any(), any())` stub needed a
      `registerFallbackValue(const TicketListFilters())` in `setUpAll` —
      without it, mocktail's internal matcher state broke for every test
      after the first filter test in the file.
- [x] 25. Create `test/features/tickets/data/repositories/shared_prefs_ticket_list_filter_repository_test.dart`
      — round-trip get/set, project-id key isolation between two
      different project ids, default-empty read when nothing saved yet.