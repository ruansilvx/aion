---
ticketId: AIO-369
type: story
status: done
priority: none
parentId: aec5e777-0cd8-45ff-b1d3-ffff29993d7f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.170179
updatedAt: 2026-09-02T21:17:20.170179
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Widget layer — SDD-stage advancing/failure states

- [x] T13. `features/tickets/presentation/widgets/ticket_metadata_section.dart`
      — `_SddStageSection` gains `isAdvancingStage`/
      `sddStageFailureReason`/`sddStageCanRetry`/`onRetryStageAdvance`
      params and the new priority-ordered branch (advancing → failure →
      existing gated/manual/auto → existing not-ready) per design.md
      §3.2 and Component Spec §0's "Action-slot state resolution" table
      (both new states render in the same action slot the §2.2-§2.4
      hint/banner/button already occupy, `gap: 12` below the
      current-stage line; no Advance button while `advancing`
      regardless of confidence; an `auto`-confidence failure re-enters
      `StageAdvancingHint` via a silent retry rather than showing the
      failure banner). Update the class dartdoc's state-priority list.
- [x] T14. Same file — new `_StageAdvancingHint` widget (mirrors
      `_ExecutionRunningHint`'s gear-glyph treatment, no live-tool-use
      sub-line) per Component Spec §2 "StageAdvancingHint" (§2.1 layout:
      no box/border, `Row` gap 8, 15px rotating `primary` gear at
      2400ms linear repeat static under reduced motion,
      `AionText.bodySm` `textSecondary` copy with the target stage name
      emphasized in `textPrimary` via a `TextSpan` — copy template
      `"Advancing to <stage> — running the stage chat."`; §2.2 no
      interactive states). Dartdoc it.
- [x] T15. Same file — new `_StageAdvanceFailureBanner` widget (mirrors
      `_ExecutionActionBanner(tone: .failure)`'s shape: reason well,
      retry button calling `onRetryStageAdvance`) per Component Spec §3
      "StageAdvanceFailureBanner" (§3.1 container: `AionRadius.lg`,
      `dangerTint`/`dangerBorderTint` fill/border, padding
      `EdgeInsets.fromLTRB(14,13,14,14)`; §3.2 header row: 17px `danger`
      warning glyph + `cardTitle` title "Stage advance failed" +
      `AionText.time` sub-line summarizing the failed transition; §3.3
      reason well: `ConstrainedBox(maxHeight: 96)` scrollable mono well,
      `background` fill, `border`, bottom fade overlay, identical
      geometry to the existing coding-execution failure well; §3.4-3.5
      full-width `secondary`-fill "Retry advance" button with all five
      interactive states — reuse the fixed `Colors.white`/`Colors.black`
      → `Color(0xFFFFFFFF)`/`Color(0xFF000000)` hover-shade correction
      made during `/design-sync`, not the export's original literal).
      Dartdoc it.
- [x] T16. `features/tickets/presentation/screens/ticket_detail_screen.dart`
      — the `_SddStageSection` call site passes the four new params from
      the current `TicketDetailLoaded`; `onRetryStageAdvance` calls
      `context.read<TicketsCubit>().advanceSddStage(ticket)` again
      (design.md §3.2 and Component Spec §0, no dedicated retry method).