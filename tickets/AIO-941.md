---
ticketId: AIO-941
type: task
status: done
priority: none
parentId: a4bfcc2e-7350-41f3-b939-db47b86c4dd8
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.240839
updatedAt: 2026-09-02T21:17:20.240839
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T6. In `lib/features/tickets/presentation/cubit/tickets_cubit.dart`:

- Rename `promoteSignal` → `promoteIdea` per `design.md` §3.1: same
signature/body, guard checks against `TicketType.idea`, dartdoc
updated to say "idea"/"Ideas" throughout.
- Add `createGapOrQuestion` per `design.md` §3.2, fully dartdoc'd.
- Add `reclassifyIdea` per `design.md` §3.3, fully dartdoc'd.
- Extend `loadDocumentRelations` per `design.md` §3.4: build
`subtreeIds` via the existing `_descendantIds` helper, query
`_linkRepository.getLinksByTypes([TicketLinkType.relatesTo])` once,
populate `gapsAndOpenQuestions`, and include it in the emitted
`TicketDetailLoaded`. Update the method's dartdoc to describe the
new aggregation.
- `runCodebaseSummarization`: both the run-record ticket and every
finding ticket now use `TicketType.idea`; update the method's
dartdoc's `signal` references to `idea` and its `promoteSignal`
reference to `promoteIdea`.