---
ticketId: AIO-376
type: story
status: done
priority: none
parentId: aec5e777-0cd8-45ff-b1d3-ffff29993d7f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.170179
updatedAt: 2026-09-02T21:17:20.170179
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Notification relocation

- [x] T18. `core/routing/workspace_nav_shell.dart` — wrap `build`'s
      returned `LayoutBuilder` in a `BlocListener<TicketsCubit,
      TicketsState>` (`listenWhen: current is TicketsError`) that calls
      `AppToast.show(context, ticketsErrorMessage(context, reason))` for
      every non-null classified `reason` (design.md §4). Import
      `AppToast`/`ticketsErrorMessage`/`TicketsCubit`/`TicketsState`.
      Update the class dartdoc to note it now also owns app-wide
      classified-error toasts, not just navigation chrome.
- [x] T19. `features/tickets/presentation/screens/tickets_board_view.dart`
      — `ticketsErrorMessage`'s `switch` gains the
      `TicketsErrorReason.sddStageAdvanceFailed` case (design.md §1.7).
- [x] T20. `features/tickets/presentation/screens/ticket_detail_screen.dart`
      — remove the `invalidParent`/`codingExecutionBlocked`/
      `executionBudgetOverageDetected` toast branches from the existing
      `BlocListener<TicketsCubit, TicketsState>`; `TicketTrashed`/
      `TicketDetailLoaded` handling is unchanged (design.md §4).
- [x] T21. `features/tickets/presentation/screens/create_ticket_screen.dart`
      — narrow the existing `state is TicketsError` toast branch to
      `state is TicketsError && state.reason == null` (design.md §4).