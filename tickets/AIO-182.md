---
ticketId: AIO-182
type: story
status: done
priority: none
parentId: c347dbac-8fab-46fc-bb96-b5281194ac7d
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.153335
updatedAt: 2026-09-02T21:17:20.153335
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Domain (`lib/core/automation/`)

- [x] `decision_outcome.dart` — `DecisionOutcome` enum
      (`proceed | gated | decline | modelJudgment`).
- [x] `decision_node.dart` — `DecisionBranch` sealed class
      (`.toNode(String)` / `.terminal(DecisionOutcome)`) and
      `DecisionNode` entity (`id`, `conditionId`, `conditionParams`,
      `matchedBranch`, `unmatchedBranch`).
- [x] `decision_graph.dart` — `DecisionGraph` entity (`context`,
      `rootNodeId`).
- [x] `decision_condition_catalog.dart` — `DecisionConditionSpec`,
      `decisionConditionsFor(AutomationContext)`, and the two shipped
      entries: `attemptExceedsMax` (`codingExecutionRetry`),
      `sessionOverageDetected` (`codingExecution`).
- [x] `decision_graph_repository.dart` — `DecisionGraphRepository`
      abstract interface (`getGraph`, `getNode`, `getAllNodes`,
      `upsertNode`, `deleteNode`, `setRoot`, `seedDefaultsIfEmpty`,
      `onChanged`).
- [x] `decision_graph_evaluator.dart` — `DecisionEvalContext`,
      `evaluateDecisionGraph(DecisionGraph, Map<String, DecisionNode>,
      DecisionEvalContext)`. Pure function, no repository/I/O
      dependency.