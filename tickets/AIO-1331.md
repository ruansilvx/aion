---
ticketId: AIO-1331
type: story
status: done
priority: none
parentId: f10d07c8-78ca-4146-996d-e58c802a1683
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.287759
updatedAt: 2026-09-02T21:17:20.287759
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 7. Final pass

- [x] 7.1 `flutter analyze` clean across every file touched above.
- [x] 7.2 Full `flutter test` run — every new/extended test from
      sections 1–6 passes.
- [x] 7.3 Manual interactive pass on a **native desktop build**
      (`flutter build windows --release`), not Chrome/web, per
      project.md's Testing conventions, driven via PowerShell/UIA system
      calls against the real `aion.exe`, against the pre-existing "Test"
      scratch project (`C:\Users\ruans\Desktop\test`) — not the
      "Aion Self-Iteration Dogfood" project, to avoid touching this
      repo's own tracked tickets. Confirmed the nav sidebar/bottom bar
      render all three destinations with Inbox navigable; `InboxScreen`
      renders the full 2×2 launcher and empty state; single-expanded-card
      invariant (§4.6) holds. Went further than the prior partial pass
      and exercised all four purposes with **real, live model calls**
      (no mocking):
      - **Brain dump** — live model call classified two raw notes into
        two `signal` tickets with `suggestedType: bug`, exactly per the
        SIGNAL:/TYPE:/BRAINDUMP: DONE contract (§5.1).
      - **What's next** — live model call returned real advisory prose
        (`toolsEnabled: false` confirmed — no tool narration was actually
        real tool use, correctly per §5.2).
      - **Plan a release** — live opening turn correctly assembled the
        real Epic/Story/Task/Bug ticket context and asked clarifying
        questions rather than concluding immediately, per §5's
        multi-turn framing.
      - **Ask a question** — live worktree-isolated, tool-enabled turn
        (confirmed via a real `node` subprocess and a real
        `aion/inbox-qa-<uuid>` git branch/worktree, both cleaned up
        afterward with no leftovers) produced a real, tool-grounded
        answer.
      Also exercised the widened promote-chooser live end-to-end: the
      brain-dump-classified signal's "Promote to Bug" row showed the
      "Suggested" pill/tint (§7.3), and completing "Create new bug"
      created a real `bug` ticket plus a real `relatesTo` link (verified
      directly against the project's sqlite file). Also verified the
      compact bottom bar's narrow (3-destination, 56px fixed trigger)
      and ultra-narrow (<360px, stepped-down icon/label size) layouts by
      resizing the real window.

      Two real, live-reproduced findings surfaced by this pass — both
      pre-existing behavior this change's own code does not touch, so
      left unfixed here and flagged separately for the user rather than
      expanded into this change's own scope:
      1. `TicketsCubit.promoteSignal`'s success path emits
         `TicketDetailLoaded` unconditionally (inherited unchanged from
         the original `promoteSignalToEpic`), which strands the shared
         `TicketsCubit`'s list-screen `BlocBuilder` on an unrenderable
         state when promotion is triggered from the Tickets list/board
         view (not the detail screen) — the list goes blank until the
         screen remounts. Data-layer effect is correct; this is a
         UI-state-shape bug only.
      2. `ChatCubit.sendMessage`/`runChatTurn`/`agent_bridge/index.mjs`
         send zero conversation history on every follow-up turn — each
         `AgentRequest` is a fully isolated one-shot prompt with no
         session/thread id and no replayed transcript. Reproduced live:
         a release-planning chat's second turn made the model say "I
         don't have context about the project" moments after correctly
         using that same context in turn one. This is app-wide,
         pre-existing infrastructure (not introduced by this change),
         but it directly undermines design.md §5's "genuinely
         multi-turn-conversational" framing for the release-planning
         purpose specifically.