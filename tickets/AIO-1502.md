---
ticketId: AIO-1502
type: story
status: done
priority: none
parentId: 899cd133-eb63-421a-a874-93812dd98f45
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.311179
updatedAt: 2026-09-02T21:17:20.311179
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tickets presentation layer

- [x] T9. In `lib/features/tickets/presentation/cubit/tickets_cubit.dart`:
  add the `ModelRoutingRepository? modelRoutingRepository` constructor
  parameter and `_modelRoutingRepository` field (following the existing
  `automationSettingsRepository` pattern exactly — see the constructor's
  existing dartdoc, which needs a one-clause addition describing this new
  optional dependency), and the new private `_resolveModel(ModelPhase)`
  helper, per `design.md` §3.1–3.2.

- [x] T10. In the same file: update `_spawnStageChat`'s `runChatTurn`
  call to `model: await _resolveModel(stage.modelPhase)`; update its
  dartdoc (currently says "A default model (`AgentModel.sonnet`) is used
  for this call; no per-phase model routing exists yet") to describe the
  phase-based resolution instead, per `design.md` §3.3.

- [x] T11. In the same file: update `retryDesignSync`'s `runChatTurn`
  call to `model: await _resolveModel(SddStage.designSync.modelPhase)`,
  per `design.md` §3.3.

- [x] T12. In the same file: update `_runCodingExecution`'s `runChatTurn`
  call to `model: await _resolveModel(ModelPhase.execution)`, deleting
  the `// Out of scope: per-phase-tier-based-model-routing.` comment
  above it; update the method's dartdoc accordingly, per `design.md` §3.3.

- [x] T13. In `lib/features/tickets/presentation/cubit/chat_cubit.dart`:
  add `TicketRepository`/`ModelRoutingRepository` as new required
  constructor parameters and fields; add the private `_phaseForChat`
  helper (per `design.md` §4.2); update `sendMessage` to drop its
  `required AgentModel model` parameter and resolve the model internally
  via `_phaseForChat` + `_modelRoutingRepository.getModelForPhase`, per
  `design.md` §4.3. Update the class-level and `sendMessage` dartdocs —
  `runChatTurn` itself and its dartoc are unchanged.

- [x] T14. In `lib/features/tickets/presentation/screens/ticket_detail_screen.dart`:
  in `_sendComment`, drop the `model: AgentModel.sonnet` argument to
  `ChatCubit.sendMessage`; remove the now-unused `AgentModel` import;
  update `_sendComment`'s dartoc per `design.md` §5.