---
ticketId: AIO-1347
type: task
status: done
priority: none
parentId: 2d783071-ccb0-4545-a624-b45a93368fb1
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.290783
updatedAt: 2026-09-02T21:17:20.290783
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T4. In `aion/lib/features/tickets/presentation/screens/tickets_board_view.dart`:

replace the per-status `GestureDetector` wrapper
(`tickets_board_view.dart:756-774`) with `OverlayMenuItem` per
`design.md` §4 — `onTap` calls
`context.read<TicketsCubit>().updateTicketStatus(widget.ticket.id,
status)` then `_removeOverlay()` exactly as today, `semanticsLabel:
ticketStatusLabel(context, status)`, `child` unchanged (existing
`Padding`/`Text`). Update `MoveToStatusMenu`'s class-level comment,
if any references the old mouse-only behavior, to reflect the fix
— otherwise no doc change needed if none exists.
Per design.md's Claude Design export §1 and §5 "State summary"
(the "Neutral" row) — a status row is a plain, non-accented row.