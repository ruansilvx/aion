---
ticketId: AIO-2075
type: task
status: done
priority: none
parentId: fc6318d5-9593-4388-bf53-16b3de63d304
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.383375
updatedAt: 2026-09-02T21:17:20.383375
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T41. **Corrected from the original task**: it assumed embedding

generation and the sync-status badge "should still be exercised
on web," but `_isSyncable` (T29) gates the badge/banner on
`ActiveTicketViewRegistry` presence, which `WorkspaceShell` (T30)
only provides on desktop — `resource`/`page` git sync, the
watcher, the badge, and the repair banner are **all**
desktop-only in this implementation, so a web pass was never
going to exercise any of it.
**Verified for real this time**: Visual Studio + the Windows
desktop toolchain turned out to be available in this environment
(`flutter doctor` — previously not checked). Ran
`flutter build windows`, launched the resulting `aion.exe`,
and drove it end-to-end via OS-level mouse/keyboard input
(screen coordinates, not a Flutter-aware driver): opened the
existing "Test" project, created a new `Resource`-type ticket
through the UI, and opened its detail screen. Confirmed live:
the header shows a "✓ SYNCED" `TicketSyncStatusBadge` (T27); on
disk, `tickets/AIO-1.md` was written by `TicketGitProjector`
(T19) with correct frontmatter (`ticketId`, `type: resource`,
`status: backlog`, timestamps) and a `# Sync Test Resource`
heading; `git log` in that project shows a real commit
("ticket: AIO-1 created") made via `GitRepositoryClient` (T17).
This confirms the full creation → embedding-trigger → git-projector
→ commit → badge-render pipeline (T19, T25, T27, T29, T30) works
on a native Windows build. Web remains unverified but is confirmed
out of scope for this feature per `_isSyncable`'s gating. Closed
`aion.exe` afterward per project convention.