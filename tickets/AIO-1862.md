---
ticketId: AIO-1862
type: story
status: done
priority: none
parentId: 1f953fb4-cab6-47a7-bdfd-8e129b5d1cb0
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.363289
updatedAt: 2026-09-02T21:17:20.363289
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data layer

- [x] T5. In `lib/features/tickets/data/models/ticket_model.dart`: add
  nullable `complexity` and `sdd_stage` (named) `TextColumn`s to
  `TicketsTable`, with dartdoc, per `design.md`.

- [x] T6. In `lib/core/database/app_database.dart`: bump `schemaVersion`
  to `5`, add the `if (from < 5) { ... }` migration block adding both new
  columns via `m.addColumn`, per `design.md`. Update the class-level
  dartdoc's schema-version history sentence to describe version 5.

- [x] T7. In `lib/features/tickets/domain/repositories/ticket_repository.dart`:
  widen `updateTicket`'s dartdoc to name `complexity` among the persisted
  fields; add the new `updateTicketSddStage(String id, SddStage stage)`
  method with dartdoc, per `design.md`.

- [x] T8. In `lib/features/tickets/data/repositories/drift_ticket_repository.dart`
  and `lib/features/tickets/data/daos/ticket_dao.dart`: implement
  `updateTicketSddStage` (single-column update + `updatedAt`, mirroring
  the existing `updateTicketStatus` implementation); extend the existing
  `updateTicket` implementation to also write `complexity`. Add/update
  dartdoc for any new/changed public methods.