---
ticketId: AIO-596
type: story
status: done
priority: none
parentId: 66b46201-2382-4fb6-9d92-afd31797bbd6
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.197089
updatedAt: 2026-09-02T21:17:20.197089
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tasks: Consistent max-width, centered content layout

Strict order — later tasks depend on the token/widget existing first.

- [x] **T1.** Create `lib/design_system/tokens/aion_content_width.dart`
      defining `AionContentWidth` (`form = 520`, `reading = 840`) per
      design.md's "New token" section. Include the file header comment
      and class-level dartdoc. per design.md §0.1 Content width variants
- [x] **T2.** Create `lib/design_system/atoms/content_max_width.dart`
      defining `ContentWidthVariant` (`form`, `reading`) and the
      `ContentMaxWidth` widget per design.md's "New widget" section.
      Include the file header comment and dartdoc on the enum, the
      enum values, the class, the constructor, and both fields. per
      design.md §1 ContentMaxWidth (§1.1 API, §1.2 Behavior spec, §1.3
      Implementation, §1.4 Visual spec, §1.5 States, §1.6 Usage rules)
- [x] **T3.** Add both new files to
      `lib/design_system/design_system.dart`'s exports, alongside the
      existing token/atom exports.
- [x] **T4.** Add a widget test at
      `test/design_system/atoms/content_max_width_test.dart` covering:
      (a) with available width greater than `AionContentWidth.form`,
      a `ContentWidthVariant.form`-wrapped child is constrained to
      520px width; (b) with available width greater than
      `AionContentWidth.reading`, a `ContentWidthVariant.reading`-
      wrapped child is constrained to 840px width; (c) the wrapped
      child is horizontally centered within a wider available width.
- [x] **T5.** Retrofit
      `lib/features/projects/presentation/screens/new_project_screen.dart`:
      replace its raw `ConstrainedBox(maxWidth: 520)` with
      `ContentMaxWidth(variant: ContentWidthVariant.form)` per
      design.md's "new_project_screen.dart" section. Update imports
      (add `design_system/atoms/content_max_width.dart` usage via the
      existing `design_system/design_system.dart` barrel import — no
      new import line needed since the barrel already covers it). per
      design.md §6.1 Structure (Create Ticket form) — migration note
- [x] **T6.** Update
      `lib/features/tickets/presentation/screens/ticket_detail_screen.dart`:
      wrap the scrollable body's inner `Column` and the pinned comment
      composer's content in `ContentMaxWidth(variant:
      ContentWidthVariant.reading)` per design.md's
      "ticket_detail_screen.dart" section. `AppHeader` stays untouched.
      per design.md §2 Ticket detail (retrofit)
- [x] **T7.** Update
      `lib/features/pages/presentation/screens/page_detail_screen.dart`:
      wrap `_PageDetailContent`'s inner `Column` in
      `ContentMaxWidth(variant: ContentWidthVariant.reading)` per
      design.md's "page_detail_screen.dart" section.
      `_PageDetailHeader` stays untouched. per design.md §3 Page
      detail / documentation content (retrofit)
- [x] **T8.** Update
      `lib/features/tickets/presentation/screens/documentation_screen.dart`:
      wrap the `ListView` in both `_TreeBody` and `_SearchResultsBody`
      in `ContentMaxWidth(variant: ContentWidthVariant.reading)` per
      design.md's "documentation_screen.dart" section. `_Header` stays
      untouched. per design.md §4 Documentation tree / search list
      (retrofit)
- [x] **T9.** Update
      `lib/features/tickets/presentation/screens/tickets_list_screen.dart`:
      wrap the list-mode body inside `_TicketsBody` in
      `ContentMaxWidth(variant: ContentWidthVariant.reading)` per
      design.md's "tickets_list_screen.dart" section. The board-mode
      branch (`TicketBoardView`) and every `Positioned` overlay (FAB,
      `TicketSelectionBar`, board "load more" button) stay unwrapped
      and viewport-anchored — do not modify their positioning. per
      design.md §5 Ticket list — list mode (retrofit), specifically
      §5.1 List body and §5.2 Exemptions
- [x] **T10.** Update
      `lib/features/tickets/presentation/screens/create_ticket_screen.dart`:
      wrap the scrollable field `Column` and the pinned submit
      `AppButton` in `ContentMaxWidth(variant: ContentWidthVariant.form)`
      per design.md's "create_ticket_screen.dart / page_create_screen.dart"
      section. per design.md §6 Create Ticket form (retrofit), §6.1
      Structure
- [x] **T11.** Update
      `lib/features/pages/presentation/screens/page_create_screen.dart`:
      same treatment as T10, `ContentMaxWidth(variant:
      ContentWidthVariant.form)` around the scrollable field column
      and the pinned submit button. per design.md §6 Create Ticket
      form (retrofit), §6.1 Structure — same pattern applied to
      Create Page
- [x] **T12.** Run `flutter analyze` — must be clean (including the
      T26 non-Material grep checks; `ContentMaxWidth` only uses
      `Align`/`ConstrainedBox`/`BoxConstraints`, so this should pass
      without special handling).
- [x] **T13.** Run `flutter test` — the new T4 widget test must pass,
      and no existing test may regress. If an existing test asserts on
      the exact padding/constraint structure of any screen touched in
      T5–T11, fix the test to match the new (still outer-padding-
      identical) structure rather than reverting the screen change —
      flag any such case explicitly when reporting completion.
- [x] **T14.** Manual interactive pass in real Chrome (per
      project.md's Testing section — not the sandboxed preview
      browser): verify at a wide viewport (e.g. 1600px) that ticket
      detail, page detail, the documentation tree, the ticket list
      (list mode), and both create-ticket/create-page forms all
      render as a centered, capped-width column, while the kanban
      board (switch to board mode) still spans full width and the FAB/
      selection bar stay anchored to the viewport edge. Verify at a
      narrow viewport (e.g. 375px) that nothing visually changed from
      before this change. Close the Chrome tab and stop the
      `web-server` process when done.