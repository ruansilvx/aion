---
ticketId: AIO-2442
type: story
status: done
priority: none
parentId: 60978800-50b4-47e4-a9fb-70c304ad0d3f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.432867
updatedAt: 2026-09-02T21:17:20.432867
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T31 — `test/features/tickets/data/repositories/drift_ticket_repository_test.dart`:
  remove `deleteTicket` cases. Add cases for `trashTicket`/
  `trashTickets` (multi-level subtree cascade: trashing a grandparent
  trashes parent and child too), `restoreTicket` (restoring a deep
  child restores its trashed ancestors, leaves unrelated trashed
  tickets untouched), `getAllTickets`/`searchTickets` never returning
  trashed rows, `permanentlyDeleteTicket` (cascades comments/links/
  subtree, rows actually gone), `emptyTrash` (removes every and only
  trashed ticket), and a non-existent id in `trashTickets` being
  silently skipped while the rest of the batch processes. Per
  design.md §11.
- [x] T32 — `test/features/tickets/cubit/tickets_cubit_test.dart`:
  remove `deleteTicket` cases. Add cases for `previewTrashCount`
  (selection-size-only vs. larger-with-descendants), `trashTicket`
  (context-aware branching, mirroring the removed `deleteTicket` tests
  minus the blocked-by-children case), `trashTickets` (success +
  generic failure). Per design.md §11.
- [x] T33 — Create `test/features/tickets/cubit/ticket_selection_cubit_test.dart`:
  `bloc_test` cases for `enter`, `toggle` (add/remove, no-op while
  inactive), `selectAll` (selects all when not all selected, deselects
  when all already selected, empty-`ids` edge case), `clear`. Per
  design.md §11.
- [x] T34 — Create `test/features/tickets/cubit/trash_cubit_test.dart`:
  `bloc_test` cases for `load` (success/failure), `restore`/
  `permanentlyDelete`/`emptyTrash` (each re-loads on success, emits
  `TrashError` on failure). Per design.md §11.