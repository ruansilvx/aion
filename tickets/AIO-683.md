---
ticketId: AIO-683
type: story
status: done
priority: none
parentId: 99ba9627-f06d-4c58-a59d-fee28572235d
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.203944
updatedAt: 2026-09-02T21:17:20.203944
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] `test/core/automation/decision_field_catalog_test.dart` (new) —
      `decisionFieldsFor`/`operatorsFor` filtering, `ruleBuilderConditionSpec`
      returning `null` for a context with no fields,
      `defaultRuleConditionParams`'s seeded shape,
      `isRecognizedConditionId`/`decisionNodeTitle`/`decisionNodeSummary`
      covering both a catalog node and a rule-builder node (including a
      rule-builder node with a malformed/missing `field` — must not
      throw).
- [x] `test/core/automation/decision_graph_evaluator_test.dart` — add
      cases for the new `ruleBuilder` registry entry: each
      `DecisionRuleOperator` against `attempt` (integer field), `equals`/
      `notEquals` against `sessionOverageDetected` (boolean field), an
      unrecognized `field`, an unrecognized `operator`, and a `null`
      underlying field value — all four failure modes must evaluate
      `false`, not throw.
- [x] `test/features/providers/presentation/screens/decision_graph_editor_screen_test.dart` —
      add coverage: the "Custom rule" entry appears in the picker for
      `codingExecutionRetry`/`codingExecution` (fields exist) and is
      absent for a context with no fields (e.g. `sddStage`); a
      rule-builder node renders its field-derived title/summary and
      does *not* show the "INCOMPLETE" error chrome.
- [x] `test/features/providers/presentation/screens/decision_graph_editor_screen_real_db_test.dart` —
      add a round-trip case: create a rule-builder node, reload from the
      real drift-backed repository, confirm `conditionId`/
      `conditionParams` survive the JSON encode/decode unchanged.
- [x] `flutter analyze` clean; run
      `flutter pub run build_runner build --delete-conflicting-outputs`
      after the l10n/ARB change, per project.md's Codegen step.