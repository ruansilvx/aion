---
ticketId: AIO-223
type: story
status: done
priority: none
parentId: c347dbac-8fab-46fc-bb96-b5281194ac7d
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.153335
updatedAt: 2026-09-02T21:17:20.153335
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `/verify` follow-up pass 2 — fixing WARNING/SUGGESTION findings

Domain tracking note: `lib/core/automation/default_decision_graphs.dart`
was created during the first `/apply` pass (shared baseline-data source
for `AutomationDecisionDao.seedDefaultsIfEmpty` and `TicketsCubit
._evaluateDecisionGraph`'s no-repository fallback) but was never itemized
above — recorded here for the record; no code change needed for this
line, the file itself is already correct and documented.

- [x] `lib/core/automation/decision_condition_catalog.dart` /
      `lib/features/providers/presentation/widgets/decision_node_form.dart`
      — add a shared `conditionParameterSummary(DecisionConditionSpec,
      Map<String, dynamic>)` helper (e.g. `> 3`) so a condition's actual
      threshold is visible without opening its form.
- [x] `lib/features/providers/presentation/screens/decision_graph_editor_screen.dart`
      — canvas condition-node chrome: render the new parameter-summary
      chip (design.md §1.2 "Parameter chip"), a root-marker chip on the
      graph's root node only (§1.5), and matched/unmatched anchor dots on
      the bottom edge (§1.2). Add hover/dragging/error node states (only
      default/selected existed) by threading a `hovered`/`dragging` flag
      through `GraphCanvas.nodeBuilder` and computing an `isError` flag
      (missing condition spec, or a dangling `ToNodeBranch` target).
- [x] `lib/design_system/molecules/graph_canvas.dart` — plane: add the
      24px dot-grid `CustomPainter` with its `scale < 0.6` fade (§1.1),
      change cursor between `grab`/`grabbing` while panning, snap a
      node's dropped position to the 24px lattice with an animated
      (120ms `Curves.easeOut`) settle rather than an immediate jump.
      Zoom cluster: add its shadow, a live zoom-percentage readout
      between fit and zoom-in, `Ctrl`/`⌘`+scroll continuous zoom,
      double-tap-empty-plane-to-fit, and disable the zoom-out/zoom-in
      buttons at `minScale`/`maxScale` (§1.6).
- [x] `lib/features/providers/presentation/widgets/decision_outline_list.dart`
      — row chrome: parameter-summary chip (reusing the new shared
      helper), a rotating caret/chevron driving expand/collapse (was
      whole-row-tap only, no visible affordance), a hover fill, and
      ancestor guide rails per depth (§2.1). Add a per-branch
      "+ Add condition" affordance (§2.3) beneath any branch that
      currently terminates — tapping it expands the owning node's form
      with that branch pre-set to "Continue to condition" (new
      `DecisionNodeForm.forceMatchedContinue`/`.forceUnmatchedContinue`
      params), rather than requiring the user to find and toggle the
      branch mode control themselves first.
- [x] `lib/features/providers/presentation/widgets/decision_node_form.dart`
      — `DecisionNodeForm.showAsPopover`'s `OverlayEntry`: dismiss on
      `Esc` (design.md §3 "Dismiss" row / §7 accessibility), not just
      click-outside/Cancel.
- [x] `lib/features/providers/presentation/cubit/decision_graph_config_cubit.dart`
      — `_validateTree`'s cycle check: replace the root-only reachability
      walk with a whole-node-set DFS (three-color/on-path marking) so a
      cycle among nodes not yet reachable from `graph.rootNodeId` (e.g.
      two freshly created, not-yet-attached nodes wired into a mutual
      loop) is rejected too, not just a cycle through the live tree.