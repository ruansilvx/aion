---
ticketId: AIO-2157
type: story
status: done
priority: none
parentId: b8a89dc2-a499-42ba-a708-12980030b115
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.394206
updatedAt: 2026-09-02T21:17:20.394206
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Manual verification

- [x] T9 — Live pass against the `flutter-web` dev server, driven from
  a real Chrome (per project.md, the sandboxed preview browser stalls
  before first frame on this app — unrelated to this change). Verified:
  the overflow trigger appears on each list row and each board card
  (compact 26×26 footprint, visually distinct from the board's
  move-status trigger after its glyph swap); tapping either opens the
  same "Delete ticket" menu as the detail screen; confirming deletes a
  childless ticket from the list and removes it from the list with no
  full-screen flash; confirming deletes a childless ticket from the
  board and removes the card from its column, board view mode staying
  active; tapping the overflow trigger on a list row or board card does
  NOT also navigate to the ticket's detail screen or start a drag
  (gesture-arena check, same class of risk the `ticket-deletion`
  change's manual-verification task flagged); the extracted
  `TicketOverflowMenu` still works unchanged from `TicketDetailScreen`'s
  header (37×37 footprint), including its navigate-to-`/tickets`
  on-delete behavior — no regression from the T1/T2 extraction; zero
  console errors throughout. The "blocked by children" toast path was
  **not** reachable live — same pre-existing gap the `ticket-deletion`
  change's own manual verification hit: the app has no UI anywhere to
  set a ticket's `parentId`, so no ticket can be given a child through
  the UI to trigger this path. Covered instead by the automated
  `TicketsCubit` tests added in T8 (both passing), which exercise the
  identical `TicketHasChildrenException` → `TicketsLoaded` code path.