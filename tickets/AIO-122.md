---
ticketId: AIO-122
type: story
status: done
priority: none
parentId: 7e9e2265-8f90-4599-9734-fba899b98bed
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.142116
updatedAt: 2026-09-02T21:17:20.142116
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `features/providers/presentation/cubit/` — new PROVIDERS cubit

- [x] T7. `lib/features/providers/presentation/cubit/anthropic_provider_config_state.dart`
  — ADD: sealed `AnthropicProviderConfigState`;
  `AnthropicProviderConfigLoading` (initial);
  `AnthropicProviderConfigReady` (`hasApiKey: bool`, `status:
  ProviderConnectionStatus`, `statusMessage: String?`). Per design.md
  §6. Depends on nothing new (reuses existing
  `ProviderConnectionStatus`). Full dartdoc, file header.
- [x] T8. `lib/features/providers/presentation/cubit/anthropic_provider_config_cubit.dart`
  — ADD: `AnthropicProviderConfigCubit`, constructed with an
  `AnthropicApiKeyRepository` and a `ProviderRegistry`. `load()` — reads
  whether a key is stored, emits `status: unknown`, never auto-pings.
  `saveApiKey(String rawKey)` — trims; empty clears via
  `setApiKey(null)`; re-emits with `hasApiKey` updated and `status`
  reset to `unknown`. `testConnection()` — no-ops if `!hasApiKey` or
  already `checking`; otherwise resolves
  `AnthropicMessagesApiProvider` via the registry and pings its
  cheapest (`availableModels.last`) model, mapping the result the same
  way `ProviderSettingsCubit._runConnectionTest` does (error →
  `disconnected` + `normalizeErrorMessage`; overage-on-success →
  `connected` + `describeOverage(...).message`). Per design.md §6.
  Depends on T4, T5, T7. Full dartdoc, file header.