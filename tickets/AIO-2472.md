---
ticketId: AIO-2472
type: story
status: done
priority: none
parentId: dc003a29-7b47-47fa-b112-813e28c0baf0
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.438116
updatedAt: 2026-09-02T21:17:20.438116
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# UI — gated behind the design gate (T13–T16)

- [x] T13. New file `presentation/widgets/token_count_label.dart` —
      `formatTokenCount(int tokens)` pure function ("~12.3K"/"~1.2M"
      compact abbreviation, exact integer below 1000) and
      `TokenCountLabel` stateless widget with `.range({low, high})`/
      `.total({total})` named constructors (design.md §4.1) per
      Component Spec §0.2, §0.3, §1, §5.1–§5.2. Full dartdoc + file
      header; exact spacing/typography/icon now specified — see
      Component Spec §1.5/§6.2 (dimensions/token recap) and §0.3 (icon:
      `PhosphorIcons.gauge`, fallback `speedometer` → `coins`).
- [x] T14. `presentation/screens/tickets_board_view.dart` — new private
      helper `_cardTokenLabel(TicketsState, Ticket)` (design.md §4.2,
      §5's precedence rule, including its queued-state carve-out) placed
      beside `_cardExecutionState`/`_cardIsBlocked`; `_CardVisual`'s
      meta-chip row renders its result (when non-`null`) after
      `PriorityBadge`, before `RollupBadge`/`_BoardCardStatusBadge`, per
      Component Spec §2 (§2.1 which-value-shows table, §2.2 ordering/gap,
      §2.3 crowding/wrap behavior, §2.4 type gate). Dartdoc the helper per
      `_cardExecutionState`'s existing style, cross-referencing design.md
      §5 and Component Spec §2.1.
- [x] T15. `presentation/widgets/ticket_metadata_section.dart` —
      `TicketMetadataSection` gains a required `executionTokenTotal`
      (`int?`) constructor param (threaded from the caller's
      `TicketDetailLoaded.executionTokenTotal`); a
      `TokenCountLabel.range(...)` renders beside the existing
      complexity/estimate row when `ticket.predictedExecutionTokensLow !=
      null && executionTokenTotal == null` (design.md §4.3, §5's
      queued-state carve-out) per Component Spec §3 (§3.1 own-line
      placement, §3.2 block structure, §3.3 gate/absence).
- [x] T16. Same file — `_CodingExecutionSection` gains a required
      `executionTokenTotal` (`int?`) parameter; when non-`null`, renders
      a `TokenCountLabel.total(...)` line inside the existing banner
      without altering the banner's own visibility condition (design.md
      §4.4). Update the section's call site (within the same file) to
      pass `ticket.executionTokenTotal`... i.e. the value already
      threaded into `TicketMetadataSection` via T15 — no new data
      plumbing beyond T15's parameter. Per Component Spec §4 (§4.1 line
      placement, §4.2 per-run-state value table — note `queued` shows no
      token line at all, §4.3 in-flight pulsing-dot treatment, §4.4
      settled/static treatment, §4.5 dimensions recap).