---
ticketId: AIO-1616
type: story
status: done
priority: none
parentId: 4fe449e0-2f8b-4802-a071-e65fb4468910
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.322679
updatedAt: 2026-09-02T21:17:20.322679
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# UI — notification bell + dropdown

- [x] T23. `lib/core/routing/workspace_nav_shell.dart` — new `_NotificationBellTrigger` widget: `ValueListenableBuilder<int>` over `context.read<TicketsCubit>().unreadNotificationCount`, bell glyph + numeric badge, `Semantics` label from `notificationBellSemantics`. Fully dartdoc'd. Visual spec per design.md Component Spec §0.2 (bell glyph), §0.3 (badge color — `primary`, not `danger`), §2 (sidebar variant: 38×38, geometry/states/theme values/badge shape), §3 (bottom-tab-bar variant: 56px cell, 22px glyph). New tokens `AionText.countBadge`/`AionColorsHubTokens.outcomeTileFill` added to `aion_colors.dart`/`aion_text.dart`. **Design gate: APPROVED — see proposal.md.** (Kept in `workspace_nav_shell.dart` rather than a sibling file — private symbols must share a file with `_Sidebar`/`_BottomTabBar` to be usable there.)
- [x] T24. New notification-dropdown widget file `lib/features/tickets/presentation/widgets/notification_dropdown.dart` — `Overlay`/`LayerLink`/`CompositedTransformFollower`/`mounted`-guard mechanics mirroring `TicketOverflowMenu`; header with `notificationCenterTitle`/`notificationCenterMarkAllRead`; rows from `TicketsCubit.getRecentNotifications()` (icon by `NotificationKind`, `ticketTitle`, `message`, relative timestamp via `formatRelativeTime`, unread dot); empty state via `notificationCenterEmptyState`/`notificationCenterEmptyStateSubtitle`; tap-to-navigate-then-mark-read. Fully dartdoc'd, plus file header. Visual spec per design.md Component Spec §4 (panel container/anchor/motion), §5 (header + "Mark all read" states), §6 (row layout, outcome-tile-to-`NotificationKind` mapping per §0.4, read/unread treatment), §7 (empty state). **Design gate: APPROVED — see proposal.md.**
- [x] T25. `workspace_nav_shell.dart` — wired `_NotificationBellTrigger` into `_Sidebar` (beside `_SecondaryActionsTrigger`) and `_BottomTabBar` (own fixed 56px cell, per design.md Component Spec §3.1's resolution of the open bottom-tab-bar-cell question), opening the T24 dropdown on tap. Anchor/offset per design.md Component Spec §4.2 (`_AnimatedDropdownEntrance` covers §4.3's fade+rise motion); keyboard focus order (Tab reaches bell before the avatar trigger, per source order) per §9 — full roving-focus-within-panel interaction (arrow-key row navigation) not implemented, noted as a gap below.