---
ticketId: AIO-2361
type: story
status: done
priority: none
parentId: cf463d53-a402-4803-af23-5ef7bb630573
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.424721
updatedAt: 2026-09-02T21:17:20.424721
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T12. Update `aion/test/features/tickets/cubit/tickets_cubit_test.dart`:
      update every existing `searchTickets`/`createTicket`/
      `updateTicketStatus`/`trashTicket`/`trashTickets` test for the new
      `TicketSearchPage` mock return shape and `hasMore`-carrying states.
      Add cases: `loadMoreTickets` appends and emits `TicketsLoaded` with
      the combined list; no-ops when `hasMore` is `false`; no-ops while
      already `TicketsLoadingMore`; emits `TicketsLoadMoreFailed`
      preserving the existing tickets on a repository throw; a
      `searchTickets` call issued while a `loadMoreTickets` is in flight
      causes the stale load-more's result to be discarded (generation
      race — see design.md §3/Testing).
- [x] T13. Update `aion/test/features/tickets/data/repositories/drift_ticket_repository_test.dart`:
      update mocked `TicketDao.searchTickets` call signatures/return
      values for the new `limit`/`offset` params and `TicketSearchPage`
      wrapping. Add cases: DAO returns `limit + 1` rows → repository
      returns exactly `limit` tickets with `hasMore: true`; DAO returns
      `<= limit` rows → `hasMore: false`.
- [x] T14. Create `aion/test/features/tickets/data/daos/ticket_dao_test.dart`
      — real in-memory drift instance (per flutter-conventions.md's
      stated exception: this is genuinely persistence behavior, not
      logic a mock can verify). Cover: typed-select branch respects
      `limit`/`offset`; FTS branch respects `limit`/`offset`;
      exact-boundary case (row count equals `limit` exactly, i.e.
      `hasMore` should end up `false` one layer up in the repository).
- [x] T15. Run `flutter test` — full suite green.
- [x] T16. Run `flutter analyze` — clean, including the T11 grep-equivalent
      lint pass.