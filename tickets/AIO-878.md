---
ticketId: AIO-878
type: story
status: done
priority: none
parentId: 34f5d83d-805c-4bbe-9e27-6ebda07f31da
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.230479
updatedAt: 2026-09-02T21:17:20.230479
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data layer

- [x] **T4.** Modify `lib/features/tickets/data/models/ticket_model.dart`
  — add `estimateRollup`/`timeSpentRollup` `IntColumn`s (named
  `estimate_rollup`/`time_spent_rollup`, nullable) to `TicketsTable`,
  appended after `inboxPurpose` per design.md §1.4. Dartdoc on both
  columns.
- [x] **T5.** Modify `lib/core/database/app_database.dart` —
  bump `schemaVersion` to `9`; add `if (from < 9) { ... }` to
  `onUpgrade` calling `m.addColumn` for both new columns, then a new
  private `_backfillRollups(Migrator m)` method (sibling of
  `_createSearchInfrastructure`) that selects every ticket's `(id,
  parent_id, estimate, time_spent)` filtered to `deleted_at IS NULL`,
  maps to `RollupNode`s (T1), calls `computeRollups`, and writes each
  non-empty result back via a raw `UPDATE tickets SET estimate_rollup =
  ?, time_spent_rollup = ? WHERE id = ?` per design.md §1.4. Dartdoc on
  `_backfillRollups` explaining it runs once, only on upgrade from a
  pre-9 schema, never on a fresh `onCreate`.
  **Run `flutter pub run build_runner build --delete-conflicting-outputs`
  after this task and T4** — do not hand-edit `app_database.g.dart`.
- [x] **T6.** Modify `lib/features/tickets/data/repositories/drift_ticket_repository.dart`
  — implement `updateRollup` via `_db.ticketDao.updateFields` with an
  `estimateRollup`/`timeSpentRollup`-only companion (no `updatedAt`),
  per design.md §1.3. Dartdoc matching `updateEmbedding`'s existing
  comment style ("no `updated_at` bump, since...").
- [x] **T7.** Modify `lib/core/markdown/ticket_markdown_template.dart` —
  add `estimateRollup`/`timeSpentRollup` keys, **appended to the end**
  of `fieldOrder` (never inserted next to `estimate`/`timeSpent` — see
  the file's own existing ordering rule). Dartdoc on both new constants
  referencing `Ticket.estimateRollup`/`.timeSpentRollup`.
- [x] **T8.** Modify `lib/core/markdown/ticket_markdown_serializer.dart`
  — add both new fields to `serialize`'s `values` map; extend
  `_convertField`'s `estimate`/`timeSpent` case list to also cover
  `estimateRollup`/`timeSpentRollup` (identical `int?` conversion, no
  new logic needed — just widen the existing `case` list). No dartdoc
  change needed beyond what already covers "every field this schema
  writes."
- [x] **T9.** Modify `lib/features/tickets/data/services/ticket_git_projector.dart`
  — add `projectBatch(List<Ticket> ancestors, String rootPath, String
  eventLabel)` per design.md §1.6: writes + stages every ticket in
  `ancestors`, then one commit for the whole batch (skipped if
  `hasChanges` is false), no-ops on an empty list. Leave `project`
  unchanged. Dartdoc per the design.md snippet.
- [x] **T10.** Modify `lib/features/tickets/data/services/ticket_markdown_reconciler.dart`
  — add a third class-level "Known limitation" doc block (alongside the
  existing `parentId`/`deletedAt` ones) noting `estimateRollup`/
  `timeSpentRollup` parse but are never applied by `_apply`, and why
  (`resource`/`page` tickets can never structurally carry a
  rollup-relevant value — see spec.md's ADDED section). No behavior
  change — doc-only task.