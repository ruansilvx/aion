---
ticketId: AIO-1289
type: story
status: done
priority: none
parentId: c8ac9fc6-324b-4f39-bc5e-3388cde40f53
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.284206
updatedAt: 2026-09-02T21:17:20.284206
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 6. `CodebaseAnalysisBanner` + `TicketsListScreen` wiring (design-gated)

- [x] 6.1 Create `aion/lib/features/tickets/presentation/widgets/codebase_analysis_banner.dart` —
      `CodebaseAnalysisBanner` widget rendering
      `TicketsCubit.codebaseAnalysisStatus`'s four states (offer / running
      / done / failed) per design.md §3 `CodebaseAnalysisBanner` (§3.1–3.4
      per state, shared geometry §3.0). Requires the four new derived tint
      helpers from design.md §1.2 (`noticeIconTint`, `pendingIconTint`,
      `dangerIconTint`, `signalFill`/`signalBorderTint`/`signalIconTint`/
      `signalChipTint`) to be added to `AionColorsHubTokens` in
      `aion/lib/design_system/tokens/aion_colors.dart` first (see this
      change's `/design-sync` output for the exact Dart to add — note
      design.md's spec text uses the deprecated `.withOpacity(x)`; write
      `.withValues(alpha: x)` instead, per project.md's Bootstrap notes).
      Non-Material; `AionColors`/`AionText`/`AionSpacing` only. Full
      class + method dartdocs.
- [x] 6.2 Modify `aion/lib/features/tickets/presentation/screens/tickets_list_screen.dart` —
      mounts `CodebaseAnalysisBanner` above the list/board when
      `ActiveProjectCubit`'s `offerCodebaseAnalysis` is true at first
      build (insertion point per design.md §4.2); calls
      `consumeCodebaseAnalysisOffer()` in `initState`. Update the screen's
      class dartdoc to mention the new banner.
- [x] 6.3 Update `aion/lib/l10n/app_en.arb` — add keys for every
      `CodebaseAnalysisBanner` state's copy (offer prompt, depth-option
      labels, running status, done count, failure message), following the
      existing naming convention.
- [x] 6.4 Extend `test/features/tickets/presentation/screens/tickets_list_screen_test.dart`
      (widget test) — banner shown only when the mocked
      `ActiveProjectCubit` state has `offerCodebaseAnalysis: true`; picking
      a depth calls `runCodebaseSummarization`; banner never reappears
      after `consumeCodebaseAnalysisOffer` fires.