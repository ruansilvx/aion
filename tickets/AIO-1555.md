---
ticketId: AIO-1555
type: story
status: done
priority: none
parentId: de935bb2-df1a-4878-b173-bb56983b4864
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.318264
updatedAt: 2026-09-02T21:17:20.318264
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `features/providers/domain/` — model routing generalization

- [x] T9. `lib/features/providers/domain/enums/agent_model.dart` —
  DELETE. Superseded by `AgentModelDescriptor` (T3).
- [x] T10. `lib/features/providers/domain/enums/model_phase.dart` —
  ADD: `ModelPhaseToolAccess` extension with `requiredToolAccessTier`
  getter (`frontier`/`capable` → `noTools`, `execution` → `full`). Per
  design.md §3. Full dartdoc on the new extension/getter.
- [x] T11. `lib/features/providers/domain/repositories/model_routing_repository.dart`
  — MODIFY: `getModelForPhase`/`setModelForPhase` now operate on
  `AgentModelDescriptor` instead of `AgentModel`. Depends on T3, T9.
  Update dartdoc to match.
- [x] T12. `lib/features/providers/data/repositories/shared_prefs_model_routing_repository.dart`
  — MODIFY: constructor takes a `ProviderRegistry`; persists
  `..._provider_id`/`..._model_id` key pairs per phase (was one
  `..._model_id` key); `getModelForPhase` resolves persisted ids back
  into a full `AgentModelDescriptor` via the registry, defaulting a
  missing `..._provider_id` to `ProviderId.claudeAgentSdk` (preserves an
  already-chosen model for existing installs, mirroring the existing
  `_legacySelectedModelKey` fallback already in this file). Per
  design.md §6. Depends on T3, T6, T8, T11. Update dartdoc to match.