---
ticketId: AIO-413
type: task
status: done
priority: none
parentId: 77b5b8f9-ea65-4ae8-971b-7d597acefae5
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.175274
updatedAt: 2026-09-02T21:17:20.175274
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T16. `features/tickets/presentation/screens/tickets_board_view.dart`

— new `_BlockedBadge` widget (design.md §5.1): static (no
animation), link-glyph icon + "Blocked" label, `AionText.chip`
styling, exact color/glyph per the Claude Design export (design
gate). `_CardVisual.build`'s meta-chip `Row` gains the
`isBlocked`-conditional insertion right after the existing
`PriorityBadge` conditional and before the execution-state
`Spacer()` block, resolved via a `context.select` scoped to
`ticket.id`'s membership in `TicketsLoaded.blockedTicketIds` (same
per-ticket-scoped-rebuild discipline as `_cardExecutionState`).
Dartdoc `_BlockedBadge`; update `_CardVisual`'s class dartdoc. Per
Component Spec §2 "BlockedBadge" (§2.1 container: `dangerTint`/
`dangerBorderTint` fill/border, `EdgeInsets.symmetric(horizontal:
7, vertical: 3)`, `AionRadius.pill`; §2.2 leading glyph — a
"link-break"/broken-dependency glyph, 11px, `danger`; §2.3 label —
`AionText.chip.copyWith(letterSpacing: 0.2, height: 1.0)`, mixed-
case "Blocked", not uppercased; §2.5 confirmed single, static,
non-interactive state) and §3 "Composed card meta row" (exact
insertion order: `TypeChip` → `PriorityBadge` → `BlockedBadge` →
`Spacer()` → `BoardCardStatusBadge`).