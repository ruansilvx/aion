---
ticketId: AIO-1229
type: story
status: done
priority: none
parentId: 88f65bed-75f1-44af-a767-912a8a5c3fe3
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.273148
updatedAt: 2026-09-02T21:17:20.273148
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data layer

- [x] 4. Create `lib/features/tickets/data/repositories/shared_prefs_ticket_list_filter_repository.dart`
      — `SharedPrefsTicketListFilterRepository`, project-id-prefixed
      `SharedPreferences` keys (`ticket_list_filters.<projectId>.statuses`/
      `.types`/`.priorities`), `setStringList`/`getStringList` of each
      enum's `.name`. Full dartdoc + file header. (design.md §1.4)
- [x] 5. Modify `lib/features/tickets/data/daos/ticket_dao.dart` —
      `searchTickets`'s no-query drift-`Query` path: replace each
      `if (x != null) q.where((t) => t.x.equals(x.name))` with
      `if (xs.isNotEmpty) q.where((t) => t.x.isIn(xs.map((v) => v.name)))`
      for status/type/priority. Update dartdoc. (design.md §1.1)
- [x] 6. Modify `lib/features/tickets/data/daos/ticket_dao.dart` —
      `searchTickets`'s FTS `customSelect` path: replace each single
      `column = ?` condition with a variable-length `column IN (?, ?,
      ...)` clause (one `?` per selected value) via a shared local
      helper, appending one `Variable` per value in order. (design.md §1.1)
- [x] 7. Modify `lib/features/tickets/data/repositories/drift_ticket_repository.dart`
      — `searchTickets` passthrough updated to the `Set`-typed params;
      update its dartdoc reference to the interface's new semantics.