---
ticketId: AIO-1743
type: task
status: done
priority: none
parentId: 9e2f724c-d814-4ab5-bd95-e0814fd3a21b
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.341753
updatedAt: 2026-09-02T21:17:20.341753
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T5. Edit `aion/lib/features/tickets/data/services/

ticket_markdown_reconciler.dart`: add the optional
`TicketParentTrashService? _parentTrashService` constructor
parameter (last positional-optional, after `_wikilinkIndexer`);
change `_apply`'s return type to `Future<({Ticket? ticket, bool
rejected})>` and call `_parentTrashService?.applyFromParsedFields`
per design.md, setting `rejected` on a `false` result; update
`reconcile` to read `applyResult.ticket`/`applyResult.rejected`
and set `syncStatus` to `needsRepair` (rejected) or `synced`
(not) accordingly, and to pass `applyResult.ticket` into the
existing wikilink-reindex block in place of the old `updated`
variable. Rewrite the class-level dartdoc's `parentId`/`deletedAt`
"Known limitation" paragraphs to describe the new applied
behavior and the `needsRepair` rejection path (the third
`estimateRollup`/`timeSpentRollup` paragraph is unchanged). Update
`_apply`'s own dartdoc for its new return shape.