---
ticketId: AIO-1653
type: spec
status: done
priority: none
parentId: null
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.328996
updatedAt: 2026-09-03T18:00:00
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Spec Delta: Private Widget Retrofit

**Reconciled 2026-09-03:** cross-checked against the current `aion-arch/specs/README.md` text — every distinctive symbol/behavior this delta names is still present and accurate (0/0 identifiers confirmed verbatim; the rest are prose/filename references, not drift). No content changes needed.

## ADDED

- A documented, canonical grep check in `flutter-conventions.md`'s Widget
  composition section: `grep -rn '^\s*Widget _[a-zA-Z0-9_]*(' lib/
  --include=*.dart` must return zero results. Mirrors the existing
  Non-Material constraint's `T26`-style enforcement — checked at the end
  of any `/apply` touching `presentation/` or `design_system/` files, and
  copied forward into future `tasks.md` files as their own final task.

## MODIFIED

- `lib/features/tickets/presentation/screens/tickets_list_screen.dart`,
  `lib/features/tickets/presentation/screens/ticket_detail_screen.dart`,
  `lib/features/tickets/presentation/widgets/ticket_parent_picker.dart`,
  `lib/features/projects/presentation/widgets/project_card.dart`,
  `lib/features/projects/presentation/widgets/empty_hub_state.dart`,
  `lib/features/projects/presentation/screens/new_project_screen.dart`,
  `lib/features/projects/presentation/screens/hub_screen.dart`,
  `lib/design_system/molecules/app_confirm_dialog.dart`,
  `lib/design_system/atoms/app_button.dart` — every `Widget _foo(...)`
  private helper-function build split (28 total) is replaced with a
  private widget class (`class _Foo extends StatelessWidget`), per
  `design.md`'s conversion pattern. No visual, layout, or behavioral
  change — every screen renders identically before and after; only the
  internal widget-tree construction changed.
- `aion-arch/specs/README.md`'s "Known gaps" section: the "No retrofit yet
  of the 'private widget, not a widget-returning function' rule" bullet is
  removed once every file above is converted and the grep check returns
  zero results.

## REMOVED

- None. No user-facing behavior changes — this is a structural refactor
  plus a new enforcement convention, not a feature change.