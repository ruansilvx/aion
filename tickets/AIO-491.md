---
ticketId: AIO-491
type: task
status: done
priority: none
parentId: b03f9039-998f-4e18-bea9-935d5427c24d
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.185459
updatedAt: 2026-09-02T21:17:20.185459
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T6. `features/tickets/presentation/screens/ticket_detail_screen.dart`

— in `_TicketDetailScreenState.build`, replace the single shared
`Expanded(child: SingleChildScrollView(...))` with a branch on
`ticket?.type == TicketType.chat` (design.md §8.6): the chat
branch renders `ChatTranscriptPane(ticketId: widget.ticketId,
ticket: ticket)` directly as the `Expanded` child — **no**
separate `SingleChildScrollView(TicketMetadataSection(...))`
sibling above it; the metadata now lives inside
`ChatTranscriptPane`'s collapsing header (T4). The non-chat
branch keeps the current single `SingleChildScrollView(
ContentMaxWidth(Column([TicketMetadataSection(ticket: ticket),
Container(border), Padding(BlocBuilder<CommentsCubit>(...
CommentTile...))])))` — i.e. today's behavior, now calling the
T1-extracted `TicketMetadataSection` instead of inline code.
Delete the old inline `BlocBuilder<ChatCubit, ChatState>`
chat-rendering block entirely (superseded by `ChatTranscriptPane`)
and the old `_StreamingBubble` class definition from this file
(moved to T4). Update this method's/class's dartdoc if it
describes the old single-scroll layout. The retry-validation
banner and compose row (T7) stay siblings below this `Expanded`,
unaffected by this task. **Deviation:** the screen-level
`AppHeader` at the top of `build` is also suppressed
(`SizedBox.shrink()`) for `chat` tickets — not called out in the
original task text, but necessary once `ChatMetaHeader` became
the header for chat tickets (design.md's navigation model shows
no separate screen header for chat); otherwise both would render
at once.