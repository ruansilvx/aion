---
ticketId: AIO-1471
type: story
status: done
priority: none
parentId: 2119e85e-5884-4c6b-848f-3075e40e9cb8
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.306054
updatedAt: 2026-09-02T21:17:20.306054
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation — Cubit & State

- [x] T3. Add `TicketsErrorReason.invalidParent` to
      `aion/lib/features/tickets/presentation/cubit/tickets_state.dart`,
      with a dartdoc explaining it classifies a rejected self/cycle
      reparent (mirror the `hasChildren` value's doc style).

- [x] T4. Add `TicketsCubit.getValidParentCandidates(Ticket ticket)` and
      the shared `_descendantIds(String rootId, List<Ticket> all)`
      helper to
      `aion/lib/features/tickets/presentation/cubit/tickets_cubit.dart`,
      exactly as specified in `design.md`. Full dartdoc per
      project.md's documentation standard (this is a public method on a
      public class).

- [x] T5. Add `TicketsCubit.updateTicketParent(Ticket ticket, String? newParentId)`
      and the private `_emitInvalidParent(String ticketId)` helper to
      the same file, per `design.md`. This method owns all cycle/self-
      reference validation — do not push any of that logic into
      `DriftTicketRepository` (see project.md's Cubit-level domain logic
      convention). Full dartdoc.

- [x] T6. Add a new switch arm for `TicketsErrorReason.invalidParent` to
      `ticketsErrorMessage` in
      `aion/lib/features/tickets/presentation/screens/tickets_board_view.dart`
      (returns `l10n.ticketInvalidParentError`).