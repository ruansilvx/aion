---
ticketId: AIO-2774
type: story
status: done
priority: none
parentId: d4780d82-4803-448b-8549-52d112db0edb
estimate: null
timeSpent: null
createdAt: 2026-09-09T13:51:33.977
updatedAt: 2026-09-09T13:51:33.977
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 7. Tests

- [x] `test/features/tickets/data/repositories/drift_ticket_repository_test.dart`:
      update every `trashTicket`/`trashTickets`/`restoreTicket`
      assertion for the new return type. Add: trashing an epic with a
      live child returns both ids; restoring only the child returns
      both ids too and the epic's `deletedAt` comes back `null`
      (the exact scenario `/explore` reproduced). Add: importing a
      `Ticket` with `deletedAt` set persists a trashed row (covers the
      `_buildInsertCompanion` fix).
- [x] `test/features/tickets/data/repositories/git_projecting_ticket_repository_test.dart`:
      update the existing `trashTicket`/`restoreTicket` cases for the
      new return type. Add a case where the fake inner repository's
      `trashTicket`/`restoreTicket` returns more than one id, asserting
      exactly one `projectBatch` call (not `project`) carrying every
      returned id's ticket, correctly labelled — and that a `null`
      result from `getTicketById` for one of the cascaded ids is
      dropped without failing the batch.
- [x] `test/features/tickets/data/services/ticket_git_projector_test.dart`:
      update the existing multi-item `projectBatch` commit-message
      assertion from `"N ancestors ..."` to `"N tickets ..."`.
- [x] `test/features/tickets/cubit/tickets_cubit_test.dart`:
      **Correction**: actual path is `test/features/tickets/cubit/`, not
      `test/features/tickets/presentation/cubit/`. Updated every
      `trashTicket`/`trashTickets` stub across the file for the new
      return type (13 call sites in total, well beyond the one bulk-trash
      group anticipated here — the live-refresh, rollup-recompute, and
      sort-passthrough groups each stub these methods too). Added the
      planned case (stub returns a cascaded id beyond what was passed
      in) directly in `git_projecting_ticket_repository_test.dart`
      instead of here, since `TicketsCubit`'s own bulk-trash git side
      effect (`_trashBatchGitSideEffects`) has no branching logic of its
      own worth a dedicated cascade test — it unconditionally batches
      whatever the repository returns; the interesting cascade-handling
      logic lives in the repository/decorator layer, already covered.
- [x] `test/features/tickets/cubit/trash_cubit_test.dart`: not in this
      task's original file list, found via the full-text search below —
      six `restoreTicket` stubs needed the same return-type fix.
- [x] `test/features/tickets/data/services/ticket_parent_trash_service_test.dart`:
      not in this task's original file list, found via the full-text
      search below — two `trashTicket`/`restoreTicket` stubs needed the
      same return-type fix.
- [x] `test/features/tickets/data/services/ticket_db_reconstruction_service_test.dart`:
      the two existing `deletedAt`-focused tests (`'reconstructs a new
      ticket with its deletedAt from the file'`,
      `'updating an existing row from a file with deletedAt overwrites
      the row's previous value'`) only assert against the mocked
      repository's captured argument — leave them, but add two new
      integration-style tests using a **real** `DriftTicketRepository`
      over an in-memory `NativeDatabase` (mirroring
      `drift_ticket_repository_test.dart`'s setup) instead of
      `MockTicketRepository`: (a) reconstruct against a project where
      a ticket is genuinely trashed in the DB and its `.md` file still
      exists — asserts no exception is thrown and the row's `deletedAt`
      is unchanged; (b) a live DB row whose `.md` file says trashed —
      asserts `reconstruct()` leaves the row's `deletedAt` non-null
      afterward (proving `_reconcileTrashState`'s `trashTicket` call
      actually persisted, not just that `updateTicket` was called with
      the right object). Also needed one small fix to an *existing* test
      (`'updating an existing row from a file with deletedAt overwrites
      the row's previous value'`): its scenario is exactly a
      null→non-null `deletedAt` transition, so `reconstruct()` now also
      calls `repository.trashTicket` on it — added that stub (plus a
      `verify(...).called(1)`) rather than leaving it uncovered.
- [x] Full-text search `test/` for `.trashTicket(`, `.restoreTicket(`,
      and `.trashTickets(` — found and fixed `trash_cubit_test.dart` and
      `ticket_parent_trash_service_test.dart` beyond the files
      enumerated above (see those two bullets). Also checked
      `page_ticket_provider_impl_test.dart`, the remaining file the
      search matched — its one `trashTicket` stub is on a mocked
      `TicketsCubit` (a different, unaffected method of that name), not
      `TicketRepository`; no change needed there.
- [x] **Additional fix found via `flutter analyze`, not originally
      listed**: `drift_ticket_repository_test.dart`'s pre-existing
      `'onUpgrade backfills existing rows into the FTS5 search index'`
      migration test manually drops `tickets.deleted_at` from a
      simulated v1 schema *before* calling `createTicket` — safe before
      this change (the insert companion never referenced that column),
      broken after (task 2's `_buildInsertCompanion` fix now always
      includes it). Reordered the test to drop `deleted_at` immediately
      after the `createTicket` call instead, alongside the other
      columns that were already dropped post-insert for the identical
      reason, and updated its comment to explain why.
- [x] **Additional fix found via `flutter analyze`, not originally
      listed**: `_projectOrSwallow` in `git_projecting_ticket_repository.dart`
      (task 3) had no remaining callers once `trashTicket`/`restoreTicket`
      moved to `_projectAllOrSwallow` — flagged `unused_element`. Removed
      it, folding its rationale dartdoc into `_projectAllOrSwallow`'s own
      (see task 3's note).
- [x] Ran `flutter analyze` (clean — only the same 2 pre-existing,
      unrelated `info` lints in `decision_graph_editor_screen.dart` that
      `fix-ticket-git-projection-gap` also saw) and the full
      `test/features/tickets/` suite: 1283/1283 passing.