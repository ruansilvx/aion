---
ticketId: AIO-2063
type: story
status: done
priority: none
parentId: 47a2b682-2c1b-45d1-a8f3-d29a6f0ff6a1
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.383375
updatedAt: 2026-09-02T21:17:20.383375
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Skill + baseline

- [x] T32. Create `aion-arch/.claude/skills/ticket-lint/SKILL.md` —
      new skill following the shape of the existing skills under
      `aion-arch/.claude/skills/`. Instructions: run `bin/ticket_lint.dart
      . --fix` in the current project, summarize the report, and for a
      `needs-repair-untouched` ticket whose issue is semantic (not just
      formatting), recommend the in-app "restore from last known good"
      action rather than attempting a content rewrite.
- [x] T33. Create `aion/assets/baseline/0.2.0/` — copied the `0.1.0`
      skill set forward plus the new `skills/ticket-lint.md` stub
      (matching the existing bundled-skill stub format: title, one-line
      description, pointer back to the `aion-arch` source — the bundled
      files are lightweight pointers, not the full skill content).
      `manifest.json` updated to list all assets including the new
      skill, version `"0.2.0"`, with `bundledPath`s repointed from
      `0.1.0/` to `0.2.0/`.
- [x] T34. Modify `lib/features/projects/data/repositories/bundled_baseline_repository.dart`
      — add `'0.2.0'` to `_bundledVersions`. **Also fixed**: a
      hardcoded `'v0.1.0'` label on `NewProjectScreen`
      (`lib/features/projects/presentation/screens/new_project_screen.dart`)
      would have silently shown the wrong version once new projects
      started pinning `0.2.0` (`CreateProjectCubit` resolves to
      `versions.last`) — updated to `'v0.2.0'`. Still a hardcoded
      string, not derived from `BaselineRepository`, so it'll go stale
      again at the next baseline bump — flagged, not fixed at the root,
      since deriving it properly wasn't in this task's scope.