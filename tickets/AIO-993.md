---
ticketId: AIO-993
type: story
status: done
priority: none
parentId: db91fdb7-f189-42f6-b315-f8c180b966a6
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.244038
updatedAt: 2026-09-02T21:17:20.244038
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] **T25** — Create `test/core/markdown/wikilink_extractor_test.dart`:
  `extractReferences` (bare title, bare ticketId, `Target|Alias` for
  both target kinds, multiple occurrences, duplicate-case-insensitive
  dedup, no-match, malformed-bracket cases), `looksLikeTicketId`
  (matches `AIO-42`-shaped strings, rejects plain titles/empty/partial
  patterns), and `rewriteTitle` (rewrites bare and aliased title-anchored
  occurrences, case-insensitive; leaves id-anchored occurrences
  untouched even when their alias text happens to contain the old
  title; no-match-returns-unchanged).
- [x] **T26** — Create `test/features/tickets/data/daos/
  page_wikilink_dao_test.dart`: real in-memory Drift instance —
  `getIncomingLinks`/`getOutgoingLinks` basic round-trip,
  `replaceOutgoingLinks` (clears prior rows, inserts new set, empty set
  clears all), `deleteLinksForTickets` (source match, target match, both
  in one call).
- [x] **T27** — Create `test/features/tickets/data/repositories/
  drift_page_wikilink_repository_test.dart`: mocked `PageWikilinkDao`
  (mocktail) — each method delegates and maps correctly.
- [x] **T28** — Extend `test/features/tickets/cubit/
  tickets_cubit_test.dart`: `updateTicket` cases for a `page` ticket —
  reindexes outgoing links on content change against a widened
  `page`/`resource` candidate set (mocked `PageWikilinkRepository`,
  verify `replaceOutgoingLinks` called with the correctly resolved id
  set for both a bare-title and a bare-ticketId reference in the same
  content), resolves a `ticketId` reference unambiguously even when its
  target's title collides with another live page's title (the
  duplicate-title case a bare-title reference would instead resolve via
  first-match), no-ops when `pageWikilinkRepository` is `null`, rename
  triggers `getIncomingLinks` + a recursive `updateTicket` call for each
  referrer whose content has a *title-anchored* occurrence rewritten
  (an id-anchored occurrence in a referrer's content is never touched by
  a rename), referrer whose content doesn't actually contain the old
  title is skipped (no redundant recursive call), a referrer whose id
  matches `activeTicketViewRegistry.activeTicketId` is deferred rather
  than rewritten immediately, then rewritten once a test-triggered
  `activeTicketId` change fires the deferred retry, a thrown error from
  the wikilink step doesn't affect the primary update's emitted state
  and is logged. Extend `loadDocumentRelations` cases to cover the
  merged explicit-link + wikilink `backlinks` list for both a `page` and
  a `resource` gated ticket, including the two-rows-for-one-ticket case.
- [x] **T29** — Extend `test/features/tickets/data/repositories/
  drift_ticket_repository_test.dart` or a new dedicated migration test:
  the schema `11 → 12` upgrade path — a real temp-file-backed database
  seeded at schema 11 with a live `page` ticket whose `description`
  already contains a resolvable bare-title `[[Title]]`, reopened at the
  current schema version, asserting the backfilled `page_wikilinks` row
  exists.
- [x] **T30** — Create/extend a `PageTicketProviderImpl` test covering
  `getWikilinkCandidates()` (returns every live page **and resource**,
  no exclusion) and the merged `loadPageRelations` backlinks shape.
- [x] **T31** — Extend `test/features/pages/cubit/pages_cubit_test.dart`
  (or create if it doesn't yet exist): `loadWikilinkCandidates` delegates
  to `PageTicketProvider.getWikilinkCandidates()` and returns its result
  unmodified.
- [x] **T32** — Create a `MarkdownView` wikilink-rendering test (widget
  test, following whatever existing `MarkdownView` test file/pattern
  already covers its other inline-node handling): a resolved `[[TicketId]]`
  reference renders the target's live title and is tappable; a resolved
  `[[Target|Alias]]` reference renders the literal alias verbatim, not
  the live title; an unresolved reference renders the chip-backed
  treatment and is tappable-to-create only when `onCreateWikilinkTarget`
  is supplied and the target doesn't look like a `ticketId`; every
  callback defaulting to `null` leaves `[[...]]` text unrecognized
  (existing non-page consumers unaffected).
- [x] **T33** — Create a `MarkdownEditor` autocomplete widget test:
  typing `[[` opens the caret-anchored overlay when `wikilinkSuggestions`
  is supplied (no-op when `null`); selecting a suggestion inserts
  `[[<ticketId>]]`, not the title; the no-matches state's Enter key
  calls `onCreatePage` and inserts its result when supplied, is a no-op
  otherwise; `Escape`/outside-tap dismisses without inserting.