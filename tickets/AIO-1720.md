---
ticketId: AIO-1720
type: story
status: done
priority: none
parentId: 9b27d880-86fa-4105-be02-07f20851da13
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.337236
updatedAt: 2026-09-02T21:17:20.337236
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Wiring

- [x] T16. `lib/main.dart` — MODIFY: `AionApp`'s `MultiRepositoryProvider`
  gains `RepositoryProvider<AgentBridgeLocator>`,
  `RepositoryProvider<AgentModelClient>` (constructs
  `ClaudeAgentSdkClient`), `RepositoryProvider<AgentSettingsRepository>`
  (constructs `SharedPrefsAgentSettingsRepository`) — app-level,
  alongside the existing `ProjectRepository`/`BaselineRepository`/
  `EmbeddingProvider` providers. Per design.md §5. Update `AionApp`'s
  class dartdoc to mention the new providers.
- [x] T17. `lib/core/routing/app_router.dart` — MODIFY: add
  `GoRoute(path: '/workspace/settings', ...)` inside the existing
  `ShellRoute`, wrapping `SettingsScreen` in
  `BlocProvider<ProviderSettingsCubit>(create: (context) =>
  ProviderSettingsCubit(context.read<AgentModelClient>(),
  context.read<AgentSettingsRepository>())..load())`. Registered
  alongside the other non-wildcard routes, per this file's existing
  declaration-order convention. Per design.md §5. Update the file's
  top-of-file route-table doc comment to list the new path.
- [x] T18. `lib/core/routing/workspace_nav_shell.dart` — MODIFY:
  `_SecondaryAction` enum gains a `settings` value; its `itemLabel`
  `switch` gains `_SecondaryAction.settings =>
  context.l10n.settingsScreenTitle`; its `onSelected` `switch` gains
  `case _SecondaryAction.settings: context.go('/workspace/settings');`.
  No other changes to this file. Per design.md §5 and, for the row's
  exact visual spec, the Settings Screen Component Spec §8
  ("The existing row style already fully covers this" — no new visual
  pattern, gear icon, last position in the popover).