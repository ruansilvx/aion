---
ticketId: AIO-485
type: story
status: done
priority: none
parentId: d815d033-0d7b-44a2-a811-3468718b5ff9
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.185459
updatedAt: 2026-09-02T21:17:20.185459
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# New widgets

- [x] T2. `features/tickets/presentation/widgets/chat_meta_header.dart`
      — new file, `ChatMetaHeader extends StatelessWidget` (design.md
      §0.1): the compact pinned header a `chat` ticket's
      `TicketMetadataSection` collapses into. Constructor takes
      `ticket` and `onBack`. Nav row: back button (`37×37`,
      `surfaceHover` fill, 1px `border`, radius `AionRadius.iconBtn`,
      `chevron-left` `20px` `textPrimary`) → `onBack`; ID badge
      (`ticket.ticketId`, `AionText.key`, `textSecondary`,
      `surfaceHover` fill, radius `5`); spacer; more button (`37×37`,
      transparent, `dots-three` `20px` `textSecondary` — wire to the
      existing `TicketOverflowMenu` used by the screen's current
      `AppHeader`, don't invent new overflow-menu content). Title
      block: title text (`AionText.h2` resized 19/w800,
      letter-spacing `-0.018em`, `textPrimary`, `maxLines: 2`,
      ellipsis); meta row with the chat type chip (`typeChat` accent,
      `"CHAT"` label, `AionText.chip`, per §0.1's exact padding/radius)
      and a status indicator (`7×7` dot + label, reuse the existing
      `StatusIndicator`/status-label pattern from `tickets_list_screen
      .dart` rather than inventing new status copy). File header + full
      class/method dartdocs.

- [x] T3. `features/tickets/presentation/widgets/chat_message_bubble.dart`
      — new file, `ChatMessageBubble extends StatelessWidget` (design.md
      §1 `ChatMessageBubble`): adapted from `CommentTile`'s existing
      author-label/timestamp/bubble/"via `<model>`" structure. Bubble
      max-width capped via `LayoutBuilder` + `ConstrainedBox` at **82%**
      of available width for human messages, **88%** for ai/system
      (§1.0) — never full-width. Bubble radius `16` on three corners,
      `5` on the tail corner: human's tail is bottom-right, ai/system's
      is top-left (§1.0). Human: right-aligned, `surface` fill + 1px
      `borderStrong`, plain `Text` (§1.1). AI: left-aligned,
      `primarySubtle` fill + 1px `aiBubbleBorder` (existing
      `AionColorsHubTokens` helper), `MarkdownView(source: comment
      .content)`, `"AI"` badge (§1.2). System: left-aligned,
      `surfaceHover` fill + 1px `border`, plain `Text`, no badge
      (§1.3). Reuses `_Avatar` from `ticket_detail_screen.dart` as-is
      (avatar-disc geometry per §1.0: `30×30`, sparkle/diamond/`"U"`
      per author type — confirm `_Avatar`'s existing sizing matches or
      adjust its call site, no changes to `_Avatar` itself needed
      beyond that). Markdown-in-bubble node treatments (headings, code
      blocks, lists, blockquote, table) per §1.5. File header + full
      class/method dartdocs.
      **Deviation:** `_Avatar` was private to `ticket_detail_screen
      .dart`, unreachable from this new file as-is — promoted to a
      public `CommentAuthorAvatar` in its own new file,
      `presentation/widgets/comment_author_avatar.dart` (same geometry,
      unchanged), used by both this widget and the existing
      `CommentTile`.

- [x] T4. `features/tickets/presentation/widgets/chat_transcript_pane.dart`
      — new file, `ChatTranscriptPane extends StatefulWidget` (design.md
      §2 `ChatTranscriptPane` + §8 `Collapsing header`). Constructor:
      `{ticketId, ticket}` — needs the full `Ticket`, not just its id,
      to render `TicketMetadataSection`/`ChatMetaHeader` in its
      collapsing header (§8.2). Owns a `ScrollController` (disposed in
      `dispose()`) — pure view-layer state, not business logic.
      `BlocConsumer<ChatCubit, ChatState>` (or `BlocListener`+
      `BlocBuilder`) exhaustively switching `ChatInitial`/`ChatError`/
      `ChatLoaded`. `ChatLoaded` renders a `CustomScrollView(controller:
      _scrollController)` with:
      1. `SliverPersistentHeader(pinned: true, delegate:
         _CollapsingChatHeaderDelegate(ticket: ticket, onBack: ...))`
         — a new private class in this file implementing
         `SliverPersistentHeaderDelegate` per design.md §8.3:
         `maxExtent`/`minExtent` measured from
         `TicketMetadataSection`'s/`ChatMetaHeader`'s intrinsic
         heights; `build` computes `progress = (shrinkOffset /
         (maxExtent - minExtent)).clamp(0.0, 1.0)` and renders a
         `Stack` cross-fading `Opacity(opacity: 1 - progress, child:
         TicketMetadataSection(ticket: ticket))` over `Opacity(opacity:
         progress, child: ChatMetaHeader(ticket: ticket, onBack:
         ...))`, each `IgnorePointer`-guarded at opacity `0`.
      2. `SliverPadding(padding: EdgeInsets.fromLTRB(18, 14, 18, 8),
         sliver: SliverList.builder(...))` rendering `"MESSAGES ·
         {n}"` as the first item (§2.1 — **not** `"COMMENTS ·
         <count>"`; needs a new l10n key, see T8) then one
         `ChatMessageBubble` per comment, plus a trailing synthetic
         item (only while `streamingText != null || currentToolUse !=
         null`) rendering the moved-and-adapted `_StreamingBubble`
         private class (moved from `ticket_detail_screen.dart`, now
         also rendering `currentToolUse` as a status line — using a
         **new `AionText.streamStatus` style**, add to `aion_text
         .dart`: mono 12.5/w400/height 1.35 — ahead of `streamingText`
         when present per §2.3, and rendering its accumulated text via
         `MarkdownView` instead of the current `RichText`/plain-span
         approach, keeping the existing blinking-caret indicator).
      Empty state (§2.4 — sparkle glyph + caption, centered, no
      messages yet), loading state (§2.5 — centered `AppSpinner`, no
      new spec), error state (§2.6 — `danger`-tinted row, tap-to-retry)
      render inside the list-sliver body. Auto-scroll: on every
      `ChatLoaded` emission, `WidgetsBinding.instance
      .addPostFrameCallback` calls `_scrollController.animateTo
      (_scrollController.position.maxScrollExtent, ...)` (§2.2/§8.7) —
      this naturally also collapses the header sliver to match the new
      scroll position, no separate logic needed. File header + full
      class/method dartdocs.
      **Deviations:** (1) `maxExtent`/`minExtent` are fixed estimates
      (560/140px), not dynamically measured — design.md §8.3 explicitly
      allowed either choice; documented at the delegate. Content taller
      than the estimate clips via `ClipRect`+`OverflowBox` rather than
      overflowing. (2) The constructor also grew
      `automationConfidence`/`onAdvanceSddStage`/`onMaybeAutoAdvance`
      (forwarded to the collapsing header's `TicketMetadataSection`),
      matching T1's deviation. (3) `_buildListItem`/`_buildEmptyState`
      were written first as `Widget`-returning methods, then converted
      to proper private widget classes (`_TranscriptListItem`,
      `_ChatEmptyState`) per flutter-conventions.md's ban on that
      pattern — caught by T10's grep check.

- [x] T5. `features/tickets/presentation/widgets/chat_compose_field.dart`
      — new file, `ChatComposeField extends StatefulWidget` (design.md
      §3 `ChatComposeField`): wraps `TextField` directly (transparent
      `Material` ancestor, same sanctioned pattern `AppTextField`/the
      current compose row already use). `maxLines: null`, `minLines:
      1`. Field container: radius `AionRadius.lg + 2` (14, **not**
      plain `AionRadius.lg`), min height **44**, max height **128**
      (**not** 160) via `AnimatedContainer` (120ms ease-out) — beyond
      that height the `TextField` scrolls internally with its own
      `scrollController` (§3.1). Row layout: leading avatar (`34×34`)
      and send button are **bottom-aligned** (`crossAxisAlignment
      .end`), not center-aligned, so they track the field's last line
      as it grows (§3.0). Field states: focused = 1.5px `primary`
      border + 3px focus-ring `BoxShadow` (same pattern as
      `AppTextField`'s existing focus treatment) (§3.2). Send button:
      idle (`surfaceHover` fill, `textMuted` glyph) / active
      (`primary` fill, glow) / hover (`primaryHover`) / pressed
      (`AnimatedScale` → 0.92) (§3.3). Field and compose row stay
      **always enabled** while a reply streams — only the send
      button's active/idle fill gates on non-empty input (§3.4); no
      `Focus`-disabling logic needed. Enter/Shift+Enter handling: wraps
      the `TextField` in a `Focus` widget with an `onKeyEvent` handler
      — Enter without Shift calls `widget.onSend()` and returns
      `KeyEventResult.handled`; Enter with `HardwareKeyboard.instance
      .isShiftPressed` returns `KeyEventResult.ignored` (lets the
      newline insert normally) — hardware-keyboard only, mobile's own
      return key inserts a newline and the send button remains the way
      to send there. Constructor takes `controller` and `onSend`. File
      header + full class/method dartdocs.