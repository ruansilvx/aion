---
ticketId: AIO-2510
type: story
status: done
priority: none
parentId: 9c47ade0-3ccb-4a4f-8334-874d7d93d7aa
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.443621
updatedAt: 2026-09-02T21:17:20.443621
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation layer — tickets feature

- [x] T1. Modify `lib/features/tickets/presentation/cubit/trash_cubit.dart`
      — add optional named constructor parameters `TicketGitProjector?
      gitProjector` and `String? projectRootPath` (default `null`,
      no-op), stored as `_gitProjector`/`_projectRootPath`. Add a
      private `_triggerGitProjection(Ticket ticket, String eventLabel)`
      helper mirroring `TicketsCubit`'s (no-ops when either field is
      null, otherwise calls `_gitProjector.project(ticket, rootPath,
      eventLabel)`), with a dartdoc explaining the no-op condition.
      Update `restore(String id)` to fetch the restored ticket via
      `_repository.getTicketById(id)` after `restoreTicket` succeeds
      and fire `unawaited(_triggerGitProjection(restored, 'restored'))`
      before calling `load()` — add the `dart:async` `unawaited`
      import. Update the class-level and constructor dartdoc to
      document the new parameters, following `TicketsCubit`'s existing
      phrasing for the same no-op-when-null desktop-only pattern.
      **Also fixed**: `tickets_cubit.dart`'s `_triggerGitProjection`
      dartdoc carried a "Known gap: the 'restored from trash' trigger
      ... is not wired anywhere" note — now stale and actively
      misleading since this task wires it. Updated that comment to
      point at `TrashCubit`'s own `_triggerGitProjection` instead of
      leaving a false claim in the codebase (not a separate task since
      it's a one-line documentation correction directly about the gap
      this change closes).