---
ticketId: AIO-523
type: story
status: done
priority: none
parentId: 2a157abe-81fc-4081-8a3c-341e6f32c4db
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.189498
updatedAt: 2026-09-02T21:17:20.189498
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Coding-execution mechanism

- [x] T13. `features/tickets/presentation/cubit/tickets_cubit.dart` —
      constructor gains `GitRepositoryClient`, `GitHubCliClient`,
      `FlutterVerifier` (all optional, same no-op-if-null pattern as
      `_agentClient`/`_commentRepository`). Update the class-level
      constructor dartdoc.
- [x] T14. Same file — `_assembleExecutionContext` updated per
      design.md §9 (drop PR instruction, end with `IMPLEMENTATION:
      DONE`). Update its dartdoc.
- [x] T15. Same file — new private helper assembling the corrective-turn
      prompt from a `FlutterVerifyResult` (design.md §9). Dartdoc it.
- [x] T16. Same file — `_runCodingExecution` fully restructured around
      the worktree lifecycle + two-turn implement→verify→PR loop, capped
      at `_maxAnalyzeRetries = 2` (design.md §8). Update its dartdoc
      completely — the existing one describes the old single-turn flow.
      Also added `_emitLiveExecutionActivity` (a new
      `TicketDetailLoaded.executionLiveActivity` field/helper, not
      literally in tasks.md but required to give the live tool-use line
      — design.md §2 — a real data source, since the spawned execution
      chat has no live per-screen `ChatCubit` instance of its own; see
      final apply report).
- [x] T17. Same file — new public `retryCodingExecution(Ticket task)`
      (design.md §8). Dartdoc it.
- [x] T18. Same file — `_executionSucceededWithPr` relaxed to match
      `ai`-or-`system` authorType (design.md §10). Update its dartdoc.
- [x] T19. Same file — `getTicketById`'s Task-only block gains
      `executionFailureReason`/`executionCanRetry` computation
      (design.md §11) via new `_computeExecutionFailure` helper. Update
      its dartdoc.
- [x] T20. Same file — `_effectiveCodingExecutionRetryConfidence` helper
      added for the new `codingExecutionRetry` context's
      retry-cap-then-escalate logic (design.md §8, §12). Dartdoc it.