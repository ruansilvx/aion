---
ticketId: AIO-1088
type: story
status: done
priority: none
parentId: ed155a5d-1b0b-4b14-8f8f-55295f49885a
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.254654
updatedAt: 2026-09-02T21:17:20.254654
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation — screen wiring

- [x] **T14.** Edit
      `lib/features/tickets/presentation/screens/tickets_list_screen.dart`:
      delete the private `_TicketViewMode` enum and the `_viewMode`
      field; replace every reference (the `_ViewModeToggle` construction,
      the board/list body switch, the `_narrowToVisible`-style helper)
      with `TicketListViewMode`/`context.read<TicketsCubit>()
      .currentViewMode` reads (design.md §5.1; visual spec: Component
      Spec §1.1 — behavior-only, no new visual for the toggle itself).
- [x] **T15.** Edit the same file: extend `_initializeAndSearch` to
      also await `loadPersistedViewMode()`/
      `loadPersistedBoardColumnVisibility()` before the first
      `_runSearch()`, followed by the forced `setState(() {})` (design.md
      §5.2; visual spec: Component Spec §1.1/§1.6 — persistence/scope,
      no new visual).
- [x] **T16.** Edit the same file: change `_ViewModeToggle`'s
      `onChanged` wiring from a bare `setState` to the new
      `_handleViewModeChanged` async handler (design.md §5.3; visual
      spec: Component Spec §1.1 — behavior-only).
- [x] **T17.** Edit the same file: add `_handleColumnVisibilityToggled`
      on `_TicketsListScreenState`; add `viewMode` and
      `onToggleColumnVisibility` required params to
      `_TicketFilterAndSortSection`; add the `Columns` trigger + two new
      `_isColumnsPopoverOpen`/`_isColumnsTriggerFocused` fields to
      `_TicketFilterAndSortSectionState`, gated on `viewMode ==
      TicketListViewMode.board` (design.md §5.4; visual spec: Component
      Spec §2 (`_ColumnsTriggerButton`), §2.4 (Board-only visibility
      gating), §3 (`TicketColumnsPopover`), §5 (header row
      composition/inter-trigger spacing)). Update the
      `_TicketFilterAndSortSection`/`_TicketFilterAndSortSectionState`
      construction call site (passing `viewMode`/
      `onToggleColumnVisibility` from the parent) and dartdoc every new
      field/param/method.
- [x] **T18.** Edit the same file: pass `hiddenStatuses:
      context.read<TicketsCubit>().hiddenBoardColumns` into the existing
      `TicketBoardView(...)` construction site (design.md §5.5; visual
      spec: Component Spec §1.2/§1.6 — model/scope, no new visual at
      this call site).