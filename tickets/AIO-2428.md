---
ticketId: AIO-2428
type: task
status: done
priority: none
parentId: 03d85c75-48be-4a82-b179-4b39e7fcedda
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.432867
updatedAt: 2026-09-02T21:17:20.432867
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T20 — Same file: replace the `Positioned` `AppFab` with

`TicketSelectionBar` (new widget, T21) when
`context.watch<TicketSelectionCubit>().state.isActive` is `true`.
Wire Cancel to `context.read<TicketSelectionCubit>().clear()`,
select-all to `context.read<TicketSelectionCubit>().selectAll(
currentTicketIds)` (the currently visible/filtered list), and Delete
to: `previewTrashCount(selectedIds.toList())` →
`showAppConfirmDialog` (title `ticketBulkTrashConfirmTitle` on the
original selection count, message `ticketTrashConfirmMessage` on the
preview total, `isDestructive: true`) → if confirmed,
`context.read<TicketsCubit>().trashTickets(selectedIds.toList())`.
Per design.md §7. Visual spec: design.md's Specification §4
TicketSelectionBar, §6.1 Confirm dialog — Reversible tone (use the
existing `AppConfirmDialog` shell's own width/shadow/scrim per that
section's design-sync note — do not introduce new dialog dimensions).