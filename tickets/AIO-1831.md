---
ticketId: AIO-1831
type: task
status: done
priority: none
parentId: c9ae856b-cd8d-4b15-ba50-0987b11d35db
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.357704
updatedAt: 2026-09-02T21:17:20.357704
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T10. Escape `.` characters in the version string before using it

in `release.yml`'s changelog-extraction `awk` regex, so a version
like `0.2.0` can't match unintended text via the unescaped-dot
wildcard. Addresses `/verify`'s SUGGESTION 2.
Note: `/verify`'s WARNING 2 (the pipeline is entirely untested — no tag
has ever been pushed) isn't a code fix — it's inherent to a CI workflow
that hasn't run yet. It stays an open risk until a real `vX.Y.Z` tag
push exercises it end to end; not something `/apply` can close out.
Note: `aion-arch/specs/release.md` (the new current-state spec file) is
**not** a task here — per this project's existing convention,
`/archive` creates/merges it from `specs/spec.md` automatically; no
change's `tasks.md` ever creates its own current-state spec file
directly.