---
ticketId: AIO-2251
type: task
status: done
priority: none
parentId: b378e1f1-6e98-432b-aab5-3f27a77c8919
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.409297
updatedAt: 2026-09-02T21:17:20.409297
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T7. Manual interactive pass (`flutter run -d web-server --web-port

8765`, real Chrome via the Claude in Chrome tools — not the
sandboxed preview browser).
**Blocker discovered, scope adjusted:** the exact script this task
specified ("link two tickets... trash one... confirm count drops")
cannot be performed through the app's UI. Confirmed by grepping
`aion/lib` for `TicketLinkRepository`/`createLink` call sites —
only `main.dart` (DI wiring), the interface, and the Drift
implementation reference it. No Cubit or screen calls `createLink`
anywhere; `TicketDetailScreen` has no "link ticket" affordance.
Creating a `ticket_links` row is only possible today via direct
repository/DAO calls (as the new T3 tests do), not through any UI
a user can drive — this is the pre-existing, already-tracked gap
this same change's `specs/spec.md` rewords ("no dedicated UI for
browsing, creating, or editing individual `ticket_links` rows").
Not something this change was scoped to build.
What was verified instead: created AIO-1 ("Ticket A") and AIO-2
("Ticket B") — both list rows correctly show no link-count label
(0 links, matching `LinkCountLabel`'s zero-count behavior, and
confirming the DAO's new joined query doesn't error or regress the
zero-link case). Trashed AIO-1 via the list's overflow menu →
"Delete ticket" → confirmed dialog; list correctly updated to show
only AIO-2, confirming the trash flow (which shares `AppDatabase`
with `TicketLinkDao`) is unaffected by adding `TicketsTable` to
`TicketLinkDao`'s `@DriftAccessor`. No console errors at any point
in the pass. The exact "excludes trashed" query behavior itself is
fully covered by the T3 automated tests (all 5 cases passing),
which is the only way to exercise it end-to-end today.