---
ticketId: AIO-2737
type: story
status: done
priority: none
parentId: a146dc27-1095-4674-97ef-1b109bf7ba89
estimate: null
timeSpent: null
createdAt: 2026-09-09T01:24:54.139
updatedAt: 2026-09-09T01:24:54.139
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 5. Tests

- [x] Add `test/features/tickets/data/repositories/git_projecting_ticket_repository_test.dart`:
      a fake `TicketRepository` + a mock/fake `TicketGitProjector`
      (or a real `TicketGitProjector` over a fake `GitRepositoryClient`
      + real `TicketMarkdownSerializer` writing to a temp directory —
      match whatever mocking convention `ticket_rollup_recomputer_test.dart`
      or `ticket_parent_trash_service_test.dart` already use for the
      same projector, for consistency). Assert: `createTicket`,
      `updateTicketStatus`, `updateStatusForIds` (one call per id),
      `updateTicketSddStage`, `updateTicketParent`, `trashTicket`,
      `restoreTicket` each trigger exactly one matching `project` call
      with the correct event label; every other method triggers zero
      `project`/`projectBatch` calls. Include a case where
      `_inner.getTicketById` returns `null` after the write (no
      projection attempted, no error).
- [x] Updated `test/core/git/git_repository_client_test.dart` (already
      existed): added an `add / commit / hasChanges` group covering the
      happy path (stage → hasChanges true, commit → hasChanges false)
      and the new checked-failure behavior (each throws
      `ProcessException` on a non-zero exit — a nonexistent pathspec for
      `add`, nothing staged for `commit`, no `.git` at all for
      `hasChanges`).
- [x] Updated existing `TicketsCubit`/`TrashCubit`/
      `TicketParentTrashService` tests that asserted a `TicketGitProjector`
      call directly. Found and fixed all of them via a search for
      `TicketGitProjector`/`gitProjector` across `test/features/tickets/`:
      `ticket_parent_trash_service_test.dart` (trash/restore — dropped
      the now-dead `gitProjector`/`MockTicketGitProjector` scaffolding
      entirely, nothing else in that file used it),
      `tickets_cubit_test.dart` (three separate spots — the bulk
      status-update group's own git-projection test removed, the
      "embedding + git-projection triggers" group's `createTicket`/
      `updateTicketStatus`/`trashTicket` assertions removed — the
      rollup-recompute group's `projectBatch` assertions are untouched,
      since that path is unaffected by this fix), and
      `trash_cubit_test.dart` (the whole "restore git-projection
      trigger" group, plus its now-dead `gitProjector` scaffolding).
      Coverage for what moved lives in the new
      `git_projecting_ticket_repository_test.dart` instead.
- [x] Ran `flutter analyze` (clean — only 2 pre-existing, unrelated
      `info` lints in `decision_graph_editor_screen.dart`) and the full
      `test/features/tickets/` suite (1270/1270 passing), plus
      `test/core/git/git_repository_client_test.dart` (13/13) on its
      own for good measure.