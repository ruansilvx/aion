---
ticketId: AIO-217
type: story
status: done
priority: none
parentId: c347dbac-8fab-46fc-bb96-b5281194ac7d
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.153335
updatedAt: 2026-09-02T21:17:20.153335
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] Unit tests for `evaluateDecisionGraph` — null root → `proceed`;
      single-node matched/unmatched termination; multi-level tree walk;
      `modelJudgment` resolves identically to `proceed`.
- [x] `DriftDecisionGraphRepository` tests — seeding idempotency, CRUD
      round-trip, `onChanged` firing on write.
- [x] `DecisionGraphConfigCubit` tests — strict-tree invariant rejection
      (second reference to an existing node id; cycle creation).
- [x] `TicketsCubit` tests — for each of the 8 contexts: `auto` +
      `null`-root graph behaves exactly as before this change;
      `auto` + a configured graph resolving to `gated`/`decline`
      surfaces through that context's existing mechanism;
      `gated`/`manual` paths are provably untouched. Fixed in the
      `/verify` follow-up pass: `tickets_cubit_test.dart` gained 3
      `restoreExecutionQueue` cases (`codingExecutionResume` auto +
      null-root/gated-via-graph/decline-via-graph) alongside its existing
      auto/gated/manual trio, and `ticket_detail_screen_test.dart` gained
      a 3-case group proving `sddStage`'s `initState` consultation
      (null-root short-circuits before `getAllNodes`; a configured graph
      is walked without throwing; `gated` confidence never reads the
      decision graph at all) — the full advance-fires assertion needs the
      screen's design-gate/validation subtree the widget-test harness
      doesn't build, so these target the exact wiring `/verify` found
      untested rather than the downstream `advanceSddStage` call.
- [x] Regression tests confirming
      `_effectiveCodingExecutionRetryConfidence`/
      `_effectiveCodingExecutionConfidence`'s pre-change behavior is
      reproduced exactly via the new baseline graphs (same attempt-count
      and overage-flag scenarios that covered the old hardcoded checks).