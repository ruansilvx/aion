---
ticketId: AIO-1448
type: story
status: done
priority: none
parentId: 16a56dcd-558b-4d27-9747-32e5764789f1
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.302519
updatedAt: 2026-09-02T21:17:20.302519
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 8. Settings screen

- [x] T39. Create `lib/features/providers/presentation/cubit/execution_scheduling_cubit.dart`
  (+ state file) — `ExecutionSchedulingCubit`/`ExecutionSchedulingState`
  (`Loading`/`Ready` with `mode`/`concurrencyCeiling`), `load()`/
  `selectMode()`/`setConcurrencyCeiling()`, file header + dartdoc
  (design.md §6).
- [x] T40. Edit `lib/features/providers/presentation/screens/settings_screen.dart`
  — add `_ExecutionSchedulingSection` (SelectionMenu<ExecutionSchedulingMode>
  + conditional concurrency-ceiling `AppTextField`) directly below
  `_ExecutionContextCapSection`; add the fifth "Resume After Restart"
  row to the existing AUTOMATION section list. Update screen-level
  dartdoc (design.md §8; per the Claude Design export's design.md §1
  `ExecutionSchedulingSection` — mode-dot tones, ceiling field
  show/hide, error-chip copy — and §4 "Resume After Restart" automation
  row, a content-only reuse of the existing automation-row component).
- [x] T41. Edit `lib/main.dart` — register
  `ExecutionSchedulingRepository` (as
  `SharedPrefsExecutionSchedulingRepository`) app-level in
  `MultiRepositoryProvider`, same tier as `ExecutionContextCapRepository`
  (design.md §8).
- [x] T42. Edit `lib/core/routing/app_router.dart` — add
  `BlocProvider<ExecutionSchedulingCubit>` to `/workspace/settings`'s
  provider stack; thread `ExecutionQueueRepository` into `TicketsCubit`'s
  project-scoped `create` call alongside its existing optional deps
  (design.md §8).
- [x] T43. Write/extend `bloc_test` coverage for T39 (design.md §11).