---
ticketId: AIO-1664
type: story
status: done
priority: none
parentId: 6906ad43-8260-4640-9991-36b0b552c062
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.332612
updatedAt: 2026-09-02T21:17:20.332612
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 4. `BaselineTailoringService`

- [x] Create `lib/features/projects/data/services/
      baseline_tailoring_service.dart` — `BaselineTailoringService` per
      design.md §3, full dartdoc + file header.
- [x] Create `test/features/projects/data/services/
      baseline_tailoring_service_test.dart` — covers: no-op when
      `ProjectStackDetector` detects nothing; writes the
      architecture-conventions override with the detected language and
      suggested command when a stack is detected.
- [x] Update `lib/features/projects/presentation/cubit/
      create_project_cubit.dart` — new constructor dependency
      `BaselineTailoringService`; after `_initializeDesktopProject`
      succeeds (desktop only), call
      `BaselineTailoringService.tailorForDetectedStack` per design.md
      §3. No change to `submit`'s parameter list.
- [x] Update `test/features/projects/cubit/create_project_cubit_test.dart`
      — cover `BaselineTailoringService.tailorForDetectedStack` being
      called (desktop path). The "not called (mobile/web path)" case is
      **not** separately testable here: `isDesktop`
      (`core/utils/platform_utils.dart`) reads `Platform.isX`/`kIsWeb`
      directly with no test seam to override it, and this whole test
      suite already runs host-desktop-only for the same pre-existing
      reason (`_initializeDesktopProject`'s own desktop-only branch has
      never had a "not called on mobile" test either) — not a gap this
      change introduces.
- [x] Update `lib/core/routing/app_router.dart`'s `CreateProjectCubit`
      construction site to supply the new `BaselineTailoringService`
      dependency (constructed from the same `BaselineRepository`
      instance already read there, plus a new `ProjectStackDetector()`).