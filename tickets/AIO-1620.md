---
ticketId: AIO-1620
type: story
status: done
priority: none
parentId: 4fe449e0-2f8b-4802-a071-e65fb4468910
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.322679
updatedAt: 2026-09-02T21:17:20.322679
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] T26. `test/core/git/github_cli_client_test.dart` (new) — URL/number parsing for a representative `gh pr create` stdout sample (and a multi-line-output sample). `GitHubCliClient.openPullRequest` itself shells out to the real `gh` binary with no injectable process runner, so these tests exercise the same parse formula the method applies rather than invoking the method through a live subprocess — no existing mocked-`Process.run` precedent exists in this codebase's git-client tests to follow instead.
- [x] T27. `test/core/git/git_repository_client_test.dart` (existing, extended) — `changedFileCount`/`defaultBranch` against real temp git repos (matching this file's existing real-subprocess test style).
- [x] T28. `test/features/tickets/data/daos/notification_dao_test.dart` (new) — `insert`/`getRecent`/`getUnreadCount`/`markRead`/`markAllRead`/`getMostRecentForTicket` against an in-memory `AppDatabase`.
- [x] T29. `test/features/tickets/data/repositories/drift_notification_repository_test.dart` (new) — entity/row mapping round-trip, mocktail-DAO style matching `drift_execution_queue_repository_test.dart`.
- [x] T30. `test/features/tickets/cubit/tickets_cubit_test.dart` (existing, extended) — new `group('notifications', ...)`: `unreadNotificationCount`/`getRecentNotifications`/`markNotificationRead`/`markAllNotificationsRead` no-op cleanly without a `NotificationRepository`; the same four methods delegate correctly with one; one full `_runCodingExecution` integration test (cloned from the existing PR-opened success test) asserting `_recordNotification` fires `NotificationKind.executionPrOpened` with the right ticketId/message. The remaining four call sites (verification-failed, hard-failure, stage-advance completed/failed) are exercised indirectly by `_recordNotification`'s own no-op-safe guard shape and by the four-method delegation tests above, but don't each have their own dedicated `_runCodingExecution`/`_runStageChatTurn` integration test — flagged as a coverage gap below rather than silently claimed as done, given the size/risk of extending this file's existing deep integration tests further.
- [x] T31. Widget test for `_ExecutionActionBanner`'s new `subLine` rendering (present for success tone + non-null subLine, absent otherwise) — `_ExecutionActionBanner` is private, so tested through the public `TicketMetadataSection`; extended the existing `test/features/tickets/presentation/widgets/ticket_metadata_section_test.dart` (its `_wrap` helper now accepts `executionAwaitingReview`/`executionPrSubLine`) rather than adding a new file.
- [x] T32. Widget test for the notification dropdown (empty state, row tap navigates + marks read, "Mark all read" clears every row's unread dot) — `test/features/tickets/presentation/widgets/notification_dropdown_test.dart`.