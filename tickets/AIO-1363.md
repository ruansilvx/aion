---
ticketId: AIO-1363
type: story
status: done
priority: none
parentId: 23efcc81-1953-4e50-82ac-8b5b26a27398
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.293989
updatedAt: 2026-09-02T21:17:20.293989
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `features/pages/` module (new)

- [x] T11. Create `features/pages/presentation/cubit/pages_state.dart`:
      the sealed `PagesState` hierarchy (`PagesInitial`, `PagesLoading`,
      `PageDetailLoaded(page, relations)`, `PageCreated(page)`,
      `PageTrashed`, `PagesError(message)`), all `Equatable`, per
      design.md. File header + dartdoc on every state class and field.

- [x] T12. Create `features/pages/presentation/cubit/pages_cubit.dart`:
      `PagesCubit extends Cubit<PagesState>`, constructed with
      `PageTicketProvider` (never `TicketsCubit`/`TicketRepository`
      directly). Methods: `loadPage(id)`, `createPage(...)`,
      `updatePage(page)`, `trashPage(id)`, each calling the corresponding
      `PageTicketProvider` method and emitting the matching `PagesState`,
      with a `PagesError` emission on any thrown exception. File header +
      dartdoc per design.md.

- [x] T13. Create `features/pages/presentation/cubit/pages_cubit_test.dart`:
      `bloc_test` + `mocktail`-mocked `PageTicketProvider` — success and
      provider-throws cases for `loadPage`, `createPage`, `updatePage`,
      `trashPage`.

- [x] T14. Locate the current sub-pages section widget (per design.md,
      likely a private `_SubPagesSection` inline inside
      `ticket_detail_screen.dart` — confirm via
      `grep -rn "_SubPagesSection" lib/`). Extract it into its own widget
      class if it isn't already one, generalize it (no `Ticket`-type-
      specific business logic beyond `List<Ticket>` + callbacks), and
      create `design_system/molecules/page_sub_pages_section.dart` with
      the promoted, generic version. Update `ticket_detail_screen.dart`'s
      existing usage to import the promoted widget instead of any inline
      version. File header + dartdoc. Per design.md §4 Sub-pages section
      (§4.1–§4.3: header/+Add, child-page row, nesting, empty state).

- [x] T15. Locate `DocumentationLinkedTicketsSection` and
      `DocumentationBacklinksSection` (`grep -rn "class
      DocumentationLinkedTicketsSection\|class DocumentationBacklinksSection"
      lib/`). If already generic (callback-based, no `page`/`resource`-
      specific coupling), promote both as-is to
      `design_system/molecules/linked_tickets_section.dart` and
      `design_system/molecules/backlinks_section.dart`; if not, generalize
      first per Pattern 2, then promote. Update existing `tickets`-feature
      usages to import from `design_system` instead. File header + dartdoc
      on both promoted classes. Per design.md §5 Linked-tickets & Backlinks
      sections (§5.1–§5.3).

- [x] T16. Locate `TicketDetailScreen`'s inline sync-status badge/repair-
      banner rendering (`grep -rn "TicketSyncStatus" lib/features/tickets/presentation/screens/ticket_detail_screen.dart`).
      Extract it into a standalone `SyncStatusBadge` widget (taking a
      `TicketSyncStatus` + repair callback, not a whole `Ticket`), create
      `design_system/molecules/sync_status_badge.dart`, and update
      `ticket_detail_screen.dart`'s existing usage to the promoted widget.
      File header + dartdoc. Per design.md §6 Sync-status badge & repair
      banner (§6.1–§6.4) — use the *existing* `AionColorsHubTokens`
      helpers (`needsRepairTint`/`needsRepairBorderTint`/
      `needsRepairIconTint`/`pendingTint`/`pendingSpinnerTrack`/
      `repairedTint`/`repairedBorderTint`/`repairedIconTint`) as already
      implemented in `aion_colors.dart`, not design.md §0.2's reimplemented
      alpha values (design-sync found these differ from the existing
      helpers of the same name — see this change's design-sync commit).

- [x] T17. Locate `TicketOverflowMenu`'s "Delete ticket" row
      (`features/tickets/presentation/widgets/ticket_overflow_menu.dart`
      per the existing spec). If the menu holds only delete-related
      actions and is already generic, promote it as-is to
      `design_system/molecules/`; otherwise extract a minimal, generic
      delete-action molecule (e.g. `DeleteActionButton`) instead and leave
      `TicketOverflowMenu` itself untouched in `tickets`. File header +
      dartdoc on the promoted/new widget. Per design.md §7 Delete action.

- [x] T18. Export every widget promoted in T14-T17 from
      `design_system/design_system.dart`.

- [x] T19. Create `features/pages/presentation/screens/page_detail_screen.dart`:
      `PageDetailScreen({required String pageId})`, wrapped in
      `BlocProvider<PagesCubit>`, calling `loadPage(pageId)` in `initState`.
      Renders `AppHeader` (title, back button, `SyncStatusBadge`, delete
      action), an `InlineEditableField<String>` for the title, the new
      `MarkdownEditor` for content (wired to `PagesCubit.updatePage`), and
      the promoted sub-pages/linked-tickets/backlinks sections — no
      priority/estimate/time-spent/status fields, no comment thread, per
      design.md. File header + dartdoc. Per design.md §3 PageDetailScreen
      header (§3.1–§3.2: back/type-chip/title/sync-badge/delete layout,
      screen body order).

- [x] T20. Create `features/pages/presentation/screens/page_create_screen.dart`:
      `PageCreateScreen({String? initialParentId})` — title field, optional
      parent picker (`PagesCubit`/`PageTicketProvider
      .getValidParentCandidatesForPage`), optional initial content field.
      On submit, calls `PagesCubit.createPage(...)`; on `PageCreated`,
      navigates to `/workspace/pages/${page.id}`. File header + dartdoc.
      Per design.md §8 PageCreateScreen (§8.1–§8.4: header, title/parent/
      content fields, footer, field states).