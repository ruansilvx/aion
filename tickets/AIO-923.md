---
ticketId: AIO-923
type: story
status: done
priority: none
parentId: 0745d23f-c178-4df7-a57b-fde9df1feaf2
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.236688
updatedAt: 2026-09-02T21:17:20.236688
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data layer — tickets feature services

- [x] T3. Modify
      `lib/features/tickets/data/services/ticket_db_reconstruction_service.dart`
      — in `_buildTicket`, add `deletedAt:
      fields.containsKey(TicketMarkdownTemplate.deletedAt) ?
      fields[TicketMarkdownTemplate.deletedAt] as DateTime? :
      existing?.deletedAt` to the constructed `Ticket`, following the
      exact `containsKey` pattern already used for `parentId`/
      `estimate`/`timeSpent`. Added a class dartdoc paragraph noting
      `deletedAt` now round-trips through reconstruction too, and what
      the previous (silent) behavior was.
- [x] T4. Modify
      `lib/features/tickets/data/services/ticket_markdown_reconciler.dart`
      — add a second "Known limitation" paragraph to the class dartdoc
      (immediately after the existing `parentId` one, same voice)
      explaining `deletedAt` is deliberately never applied by `_apply`
      either, and why (cascade logic in `trashTicket`/`restoreTicket` a
      bare field write would bypass). No code change to `_apply` itself
      — it already only reads specific named fields, so a field with no
      added `case`/read is already excluded by construction; this task
      is documentation-only.