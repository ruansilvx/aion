---
ticketId: AIO-1275
type: story
status: done
priority: none
parentId: c8ac9fc6-324b-4f39-bc5e-3388cde40f53
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.284206
updatedAt: 2026-09-02T21:17:20.284206
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 3. `NewProjectScreen` UI (design-gated — do not implement final visuals until `/design-sync` sets the gate to APPROVED)

- [x] 3.1 Modify `aion/lib/features/projects/presentation/screens/new_project_screen.dart` —
      `_browseDirectory` runs `GitRepositoryClient.isGitRepository` after
      `getDirectoryPath()` resolves, storing `_isExistingGitRepo` and a new
      `_appendGitignore` (default `true`) in `_NewProjectScreenState`. New
      `GitignoreConfirmationBanner` per design.md §2 `GitignoreConfirmationBanner`
      (insertion point per design.md §4.1) shown only when
      `_isExistingGitRepo` is true, placed between the directory picker and
      the baseline-version field. Requires the new `noticeIconTint`
      derived tint helper from design.md §1.2 (see this change's
      `/design-sync` output for the exact Dart to add to
      `AionColorsHubTokens`) — `noticeFill`/`noticeBorder` already exist.
      `onCreated` callback signature widens to
      `void Function(Project project, {required bool offerCodebaseAnalysis})`.
      `BlocListener` fires `AppToast.show` with the decline-warning message
      when `wasExistingGitRepo && !_appendGitignore`, before calling
      `onCreated`. Update the widget-level and new sub-widget dartdocs.
- [x] 3.2 Update `aion/lib/l10n/app_en.arb` — tweak
      `newProjectLocationHint` to mention an existing codebase is a valid
      choice; add new keys for the gitignore-gate banner copy, checkbox
      label, and decline-warning toast message, following the existing
      `newProject*` naming convention.
- [x] 3.3 Extend `test/features/projects/presentation/screens/new_project_screen_test.dart`
      (widget test) — banner appears only when the mocked directory-picker
      result is an existing-repo path; checkbox toggling flows through to
      `submit(appendGitignore: ...)`; decline path shows the toast.