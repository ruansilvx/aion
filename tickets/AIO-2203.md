---
ticketId: AIO-2203
type: story
status: done
priority: none
parentId: b0b4f362-a113-4f0b-b8cb-169366663b51
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.402977
updatedAt: 2026-09-02T21:17:20.402977
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 2. Data Layer

- [x] **T03** — Modify `lib/features/tickets/data/daos/ticket_dao.dart`
  Rename `updateStatus(String id, TicketsTableCompanion companion)` to
  `update(String id, TicketsTableCompanion companion)`. Update its
  dartdoc to describe it as generic (used for both status-only and
  general field updates), per `design.md`. **Deviation from design.md**:
  the method body must call `super.update(ticketsTable)`, not a bare
  `update(ticketsTable)` — the new method name now shadows
  `DatabaseAccessor.update`, which the body itself needs to call.

- [x] **T04** — Modify `lib/features/tickets/data/repositories/drift_ticket_repository.dart`
  - Update `updateTicketStatus`'s call site from `_db.ticketDao.updateStatus(...)`
    to `_db.ticketDao.update(...)` (rename only, no behavior change).
  - Implement `updateTicket(Ticket ticket)`: build a
    `TicketsTableCompanion` with `title`, `description`, `priority`
    (`.name`), `type` (`.name`), `estimate`, `timeSpent`, and
    `updatedAt: Value(DateTime.now().millisecondsSinceEpoch)` — no
    `status` in this companion. Call `_db.ticketDao.update(ticket.id,
    companion)`. Dartdoc the override per `design.md`.

- [x] **T05** — Run codegen (DAO mixin changed shape from the rename):
  ```
  flutter pub run build_runner build --delete-conflicting-outputs
  ```
  Verify `ticket_dao.g.dart` regenerates without errors.

---