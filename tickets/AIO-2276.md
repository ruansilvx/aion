---
ticketId: AIO-2276
type: task
status: done
priority: none
parentId: b938e6b3-21b2-49f5-9a01-0dc453e6e1ef
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.413857
updatedAt: 2026-09-02T21:17:20.413857
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T14. `presentation/widgets/ticket_metadata_section.dart` —

unwrap `LinkedTicketRef.ticket` before passing `state.backlinks`
to `BacklinksSection` (unchanged `List<Ticket>` contract); pass
`state.linkedTickets` directly to `LinkedTicketsSection.links`;
wire `onRemove`/`onChangeType` to
`context.read<TicketsCubit>().deleteTicketLink`/
`.updateTicketLinkType` (translating the editor's relative
selection via T3's `toCanonical` before calling
`updateTicketLinkType`); replace the existing
`TicketLinkPicker.onSelected` body's direct
`context.read<TicketLinkRepository>().createLink(...)` +
`loadDocumentRelations` + conditional `refreshBlockedBoardState`
calls with one call to the new `context.read<TicketsCubit>()
.createTicketLink(...)` (design.md §4's consistency note — all
three mutations now go through the Cubit). Per design.md §0
(piece placement) and §6 (full-row wiring/variants — resource's
single-option `relatesTo` rows are §6.1's edit-omitted variant;
the widened `epic`/`story`/`task`/`bug` set includes §6.2's
multi-option and §6.3's `duplicatedBy` no-edit variant). Update
this section's class doc comment accordingly.