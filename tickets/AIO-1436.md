---
ticketId: AIO-1436
type: story
status: done
priority: none
parentId: 16a56dcd-558b-4d27-9747-32e5764789f1
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.302519
updatedAt: 2026-09-02T21:17:20.302519
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 7. `TicketsCubit` — scheduling, cancellation, restart resume

- [x] T28. Create `lib/features/tickets/presentation/cubit/in_flight_execution_run.dart`
  (or private, same-file class if convention prefers — match existing
  `TicketsCubit` private-helper-class precedent) — `_InFlightExecutionRun`
  (`runId`, `provider`), dartdoc (design.md §5.1).
- [x] T29. Create `lib/features/tickets/domain/utils/sibling_cluster.dart`
  — `List<Ticket> clusterSiblingsAdjacently(List<Ticket> tickets)`, pure
  stable-regroup function, file header + dartdoc (design.md §9).
- [x] T30. Write unit tests for `clusterSiblingsAdjacently` (no-op case,
  adjacent-pair grouping, multiple sibling groups, stability check —
  design.md §11).
- [x] T31. Edit `lib/features/tickets/presentation/cubit/tickets_cubit.dart`
  — replace `_inFlightExecutionTaskId` with `_inFlightExecutionIds`
  (`Set<String>`) + `_inFlightRuns` (`Map<String, _InFlightExecutionRun>`)
  + `_preExecutionStatus` (`Map<String, TicketStatus>`); thread the new
  `ExecutionSchedulingRepository`/`ExecutionQueueRepository` through the
  constructor as additional optional named parameters, following the
  existing all-nullable-default-no-op pattern. Update class-level
  dartdoc.
- [x] T32. Continue editing `tickets_cubit.dart` — implement
  `_effectiveConcurrencyCeiling()` and `_tryStartNextQueuedExecutions()`
  (including `_nextEligibleForHybrid()`), replacing every existing
  single-slot "start next queued run" call site (design.md §5.2).
- [x] T33. Continue editing `tickets_cubit.dart` — capture
  `_preExecutionStatus[taskId]` at the trigger site
  (`_interceptTaskExecutionTrigger`'s allowed path), immediately before
  the `inProgress` status write (design.md §5.1).
- [x] T34. Continue editing `tickets_cubit.dart` — rewrite
  `_runCodingExecution` to generate a fresh `runId` per turn, populate/
  update `_inFlightRuns`, and switch on `ChatTurnResult` instead of a
  bool for every `runChatTurn` call (implement, verify, corrective
  retry), routing `ChatTurnCancelled` into the new in-flight cancellation
  sequence (design.md §5.4, §5.5). Update dartdoc.
- [x] T35. Continue editing `tickets_cubit.dart` — add public
  `Future<void> cancelCodingExecution(Ticket task)`, covering both the
  queued-removal path and the in-flight-cancel path per design.md §5.4;
  dartdoc.
- [x] T36. Continue editing `tickets_cubit.dart` — add private
  `_persistExecutionQueueSnapshot()` (`unawaited`, called from every
  mutation site of `_inFlightExecutionIds`/`_executionQueue`: trigger,
  `_tryStartNextQueuedExecutions`, `cancelCodingExecution`, normal
  completion) and public `Future<void> restoreExecutionQueue()`
  (design.md §5.3), covering the re-validation step and all three
  `AutomationContext.codingExecutionResume` confidence branches; dartdoc.
- [x] T37. Continue editing `tickets_cubit.dart` — update `getTicketById`'s
  `isExecuting` computation to `_inFlightExecutionIds.contains(ticket.id)`
  (design.md §5.1); no other `getTicketById` field changes.
- [x] T38. Write/extend `bloc_test` coverage for T32–T37: all three
  scheduling modes including the Hybrid skip-ahead case, both cancel
  paths reverting status correctly, and `restoreExecutionQueue` across
  all three resume confidences plus the stale-entry re-validation drop
  (design.md §11).