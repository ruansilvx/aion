---
ticketId: AIO-1401
type: story
status: done
priority: none
parentId: 16a56dcd-558b-4d27-9747-32e5764789f1
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.302519
updatedAt: 2026-09-02T21:17:20.302519
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 1. Domain foundations

- [x] T1. Create `lib/features/providers/domain/enums/execution_scheduling_mode.dart`
  — `ExecutionSchedulingMode` enum (`strictFifo | parallel | hybrid`),
  file header + dartdoc on the enum and each value (design.md §1).
- [x] T1a. **(added by /design-sync)** Edit
  `aion/lib/design_system/tokens/aion_colors.dart` (`AionColorsHubTokens`
  extension) — add `cancelFocusRing(bool isDark)` and `dangerText(bool
  isDark)` derived helpers per the Claude Design export's design.md §7.1
  (`cancelFocusRing` = `danger.withValues(alpha: isDark ? 0.30 : 0.16)`,
  a `danger`-keyed parallel of the existing `focusRing`; `dangerText` =
  `Color.lerp(danger, white/black, 0.22/0.35)`, derived exactly like the
  existing `warningText`). Dartdoc on each, following the existing
  `focusRing`/`warningText` pattern. No new `AionColors` fields, no
  per-theme `arctic`/`obsidian` const changes — both are pure formulas
  over existing base colors. Sequenced early since T26a/T27/T40/T45/T46
  below depend on them.
- [x] T2. Add `AutomationContext.codingExecutionResume` to
  `lib/core/automation/automation_context.dart` — dartdoc on the new
  value describing its three-confidence meaning (design.md §1).
- [x] T3. Create `lib/features/tickets/domain/entities/execution_queue_entry.dart`
  — `ExecutionQueueEntry` (`Equatable`: `taskId`, `inFlight`,
  `queuePosition`), file header + dartdoc (design.md §5.3).