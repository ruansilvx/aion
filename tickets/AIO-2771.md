---
ticketId: AIO-2771
type: task
status: done
priority: none
parentId: 4434734d-cbd6-424f-b354-74372b05043e
estimate: null
timeSpent: null
createdAt: 2026-09-09T13:51:33.977
updatedAt: 2026-09-09T13:51:33.977
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `lib/features/tickets/presentation/cubit/tickets_cubit.dart`:

- `trashTickets`: rename the local `trashedCount` variable to
`affectedIds` (now the returned `List<String>`), pass
`affectedIds` (not the original `ids`) to
`_trashBatchGitSideEffects`, and use `affectedIds.length` where
`trashedCount` was used in the `TicketsBatchTrashed(...)` emit.
Leave `_refreshDetailIfOpenAndAffected(ids.toSet(), ...)` using
the original explicitly-selected `ids` — per design.md, that's
deliberate and unrelated to this fix.
- `_trashBatchGitSideEffects`: change its first parameter from
`List<String> ids` (the explicitly-selected ids) to
`List<String> affectedIds` (the full cascade); replace its
per-id `_triggerGitProjection` loop with: re-fetch each id in
`affectedIds` via `_repository.getTicketById`, drop nulls, call
`_gitProjector?.projectBatch(tickets, _projectRootPath!,
'trashed')` guarded the same null-check shape
`_triggerGitProjection` already uses. The rollup-recompute call
below it is unchanged in logic, but now iterates
`parentIdOf.keys` (still built from the original explicitly-
selected `ids` in `trashTickets`, per design.md) rather than a
second `ids` parameter — pass `parentIdOf` alone, drop the now-
redundant separate `ids` argument if the signature simplifies
cleanly.
- Update `_trashBatchGitSideEffects`'s own dartdoc (batches the
full cascade now, not just the explicit ids) and
`_triggerGitProjection`'s dartdoc (note whether it retains any
callers after this edit — it does not; see next bullet).
- Search this file for every remaining reference to
`_triggerGitProjection` (its own definition plus doc-comment
cross-references — `/explore` found mentions near the
constructor dartdoc and a couple of method dartocs). Since this
task removes its one remaining call site, **delete**
`_triggerGitProjection` itself and fix every stale cross-
reference to it — re-run the search after deleting to confirm
none remain, the same "audit before deleting, don't assume the
first pass was exhaustive" caution `fix-ticket-git-projection-gap`
used for its own cleanup.