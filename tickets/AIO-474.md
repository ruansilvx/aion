---
ticketId: AIO-474
type: story
status: done
priority: none
parentId: 088b2ab7-494c-4459-a671-01ae0fb60e0a
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.181925
updatedAt: 2026-09-02T21:17:20.181925
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Tests

- [x] 18. Add tests to `test/features/tickets/data/daos/ticket_dao_test.dart`
      — `updateStatusByIds`/`updatePriorityByIds`: writes the given value
      to every id in the list, leaves other tickets untouched, bumps
      `updated_at` to the supplied value. (design.md §10)
- [x] 19. Update `test/features/tickets/cubit/tickets_cubit_test.dart` —
      `updateStatusForTickets`: target `inProgress` with a mixed selection
      (blocked / coding-execution-gated / clean) emits
      `TicketsBatchStatusUpdating` then `TicketsBatchStatusUpdated` with
      correct `updatedCount`/`skippedCount`, and only writable ids reach
      `updateStatusForIds`; target != `inProgress` skips gating entirely
      (`skippedCount` always 0); git-projection and coding-execution
      triggers fire only for successfully-written Task/Bug tickets moving
      to `inProgress`; repository-throws emits `TicketsError`.
      `updatePriorityForTickets`: unconditional write,
      `updatedCount == ids.length`, no git-projection call,
      repository-throws emits `TicketsError`. (design.md §10)
- [x] 20. No new widget tests for `TicketSelectionBar`'s two new triggers
      or the new `BulkStatusMenu`/`BulkPriorityMenu`/`BulkOverlayRow`
      widgets — same rationale already documented in `tickets.md`'s
      Testing section for this `Overlay`-family of widgets. Confirmed via
      the manual verification pass (task 21) instead. (design.md §10)