---
ticketId: AIO-321
type: story
status: done
priority: none
parentId: 751e56c8-0458-4136-85c1-9b3202d8a6da
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.163211
updatedAt: 2026-09-02T21:17:20.163211
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# 6. `BaselineUpgradeCubit` + Settings screen section (design-gated)

- [x] 6.1 Create
      `aion/lib/features/projects/presentation/cubit/baseline_upgrade_state.dart` —
      sealed `BaselineUpgradeState` (`BaselineUpgradeLoading`,
      `BaselineUpgradeReady` carrying `currentVersion`/`latestVersion`/
      `isUpgrading` with an `isUpToDate` getter) per design.md. File
      header + full dartdocs.
- [x] 6.2 Create
      `aion/lib/features/projects/presentation/cubit/baseline_upgrade_cubit.dart` —
      `BaselineUpgradeCubit(BaselineRepository, ActiveProjectProvider)`
      with `load()` and `upgrade()` per design.md's exact implementation
      (delegates the actual mutation to
      `ActiveProjectProvider.acceptBaselineUpgrade()`). Full dartdocs.
- [x] 6.3 Modify
      `aion/lib/features/providers/presentation/screens/settings_screen.dart` —
      new `_BaselineUpgradeSection` private widget per design.md §2
      `Settings "BASELINE" section` (§2.1 `Section layout`, §2.2 `Action —
      upgrade available`, §2.3 `Action — upgrading`, §2.4 `Action —
      already on latest`), inserted per §4.2 `SettingsScreen` (immediately
      after "OVERRIDES", same raw `22`-px inter-section gap the screen
      already uses between its existing sections — there is no
      `AionSpacing.sp22` token; design.md's own reference to one is a
      naming slip design-sync flagged, not a new token to add), mirroring
      `_OverridesSummarySection`'s shape (`BlocBuilder<
      BaselineUpgradeCubit, BaselineUpgradeState>`, current-version text,
      an `AppButton` calling `context.read<BaselineUpgradeCubit>()
      .upgrade()`, §2.4's quiet no-button message when `isUpToDate` rather
      than a disabled button). Update the screen-level dartdoc/comment
      enumerating its sections.
- [x] 6.4 Modify `aion/lib/core/routing/app_router.dart` —
      `/workspace/settings`'s `MultiBlocProvider` gains
      `BlocProvider<BaselineUpgradeCubit>` constructed from
      `context.read<BaselineRepository>()` and
      `context.read<ActiveProjectProvider>()`, calling `.load()` on
      create, per design.md's exact wiring.
- [x] 6.5 Update `aion/lib/l10n/app_en.arb` — add keys for the new
      "BASELINE" section (eyebrow, summary, current-version label,
      up-to-date message, upgrade button label with version
      interpolation), following the existing `settingsOverrides*` naming
      convention.
- [x] 6.6 Create
      `test/features/projects/presentation/cubit/baseline_upgrade_cubit_test.dart`
      (`bloc_test` + mocktail) — `load()` emits `BaselineUpgradeReady`
      with the correct current/latest versions; `upgrade()` emits
      `isUpgrading: true` then a final ready state reflecting the bumped
      version, calling `ActiveProjectProvider.acceptBaselineUpgrade()`
      exactly once; `upgrade()` is a no-op when already `isUpToDate`.