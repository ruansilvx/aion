---
ticketId: AIO-189
type: story
status: done
priority: none
parentId: c347dbac-8fab-46fc-bb96-b5281194ac7d
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.153335
updatedAt: 2026-09-02T21:17:20.153335
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Data (`lib/core/automation/data/`)

- [x] `automation_decision_graphs_table.dart` — drift
      `AutomationDecisionGraphsTable` (`context` unique, `rootNodeId`
      nullable), row type `AutomationDecisionGraphData`.
- [x] `automation_decision_nodes_table.dart` — drift
      `AutomationDecisionNodesTable` (`id` primary key, `conditionId`,
      `conditionParamsJson`, `matchedBranchKind`/`matchedBranchNodeId`,
      `unmatchedBranchKind`/`unmatchedBranchNodeId`), row type
      `AutomationDecisionNodeData`.
- [x] `automation_decision_dao.dart` — `AutomationDecisionDao`: CRUD for
      both tables plus `seedDefaultsIfEmpty` (idempotent — inserts all 8
      `DecisionGraph` rows; the two baseline nodes for
      `codingExecutionRetry`/`codingExecution`).
- [x] `drift_decision_graph_repository.dart` — `DriftDecisionGraphRepository
      implements DecisionGraphRepository`, mapping row data ↔ domain
      entities (including `DecisionBranch` reconstruction from the
      `*Kind`/`*NodeId` column pair and `conditionParamsJson` ↔
      `Map<String, dynamic>`).
- [x] `app_database.dart` — register `AutomationDecisionGraphsTable`/
      `AutomationDecisionNodesTable` in `@DriftDatabase(tables: [...])`,
      bump `schemaVersion` 17 → 18, add the additive migration step
      (two new empty tables, no existing-column changes), wire
      `DecisionGraphRepository.seedDefaultsIfEmpty()` into the
      schema-18 upgrade path and fresh-install seeding.