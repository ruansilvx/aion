---
ticketId: AIO-1080
type: story
status: done
priority: none
parentId: ed155a5d-1b0b-4b14-8f8f-55295f49885a
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.254654
updatedAt: 2026-09-02T21:17:20.254654
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Cubit

- [x] **T9.** Edit `lib/features/tickets/presentation/cubit/tickets_cubit.dart`:
      add `viewModeRepository`/`boardColumnVisibilityRepository`
      optional constructor params + backing `_viewModeRepository`/
      `_boardColumnVisibilityRepository` fields (following the existing
      `filterRepository`/`sortRepository` pattern exactly, including
      updating the constructor's param-naming comment block); add
      `_viewMode`/`_hiddenColumns` private fields with their stated
      defaults; add `currentViewMode`, `setViewMode`,
      `loadPersistedViewMode`, `hiddenBoardColumns`,
      `toggleBoardColumnVisibility`, `loadPersistedBoardColumnVisibility`
      (design.md §4). Dartdoc every new public getter/method per the
      class's existing style (states what it does/persists and — since
      none of these six emit — explicitly notes that no `TicketsState`
      is emitted and why).
- [x] **T10.** Test: add `bloc_test`-style cases to
      `test/features/tickets/cubit/tickets_cubit_test.dart` (the actual
      existing test file for `TicketsCubit` — this task's originally
      stated path,
      `test/features/tickets/presentation/cubit/tickets_cubit_test.dart`,
      doesn't match the real test tree; corrected here) covering
      `setViewMode`/`loadPersistedViewMode`/
      `toggleBoardColumnVisibility`/`loadPersistedBoardColumnVisibility`
      — repository-call assertions, `null`-repository no-op path, and
      an explicit assertion that none of the four emits a state
      (design.md §9).