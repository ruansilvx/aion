---
ticketId: AIO-1710
type: story
status: done
priority: none
parentId: 9b27d880-86fa-4105-be02-07f20851da13
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.337236
updatedAt: 2026-09-02T21:17:20.337236
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `features/providers/`

- [x] T7. `lib/features/providers/domain/enums/agent_model.dart` — ADD:
  `AgentModel` enum (`opus`, `sonnet`, `haiku`; each with `id`/`label`
  fields), per design.md §4.1. Confirm each `id` against the Agent
  SDK's actual accepted `model` values before finalizing this file — the
  source idea flagged this as unverified. One-line dartdoc on every enum
  value, per project.md's Documentation Standards.
- [x] T8. `lib/features/providers/domain/enums/provider_connection_status.dart`
  — ADD: `ProviderConnectionStatus` enum (`unknown`, `checking`,
  `connected`, `disconnected`), per design.md §4.1. One-line dartdoc on
  every value. **Naming note (design-sync):** design.md's Settings
  Screen Component Spec §1.1 independently defines the same four values
  as `ProviderConnectionState` — same enum, different name. Keep
  `ProviderConnectionStatus` as specified here; §1.1's name is not a
  second type to create.
- [x] T9. `lib/features/providers/domain/repositories/agent_settings_repository.dart`
  — ADD: `AgentSettingsRepository` abstract interface
  (`getSelectedModel()`/`setSelectedModel(AgentModel)`), per design.md
  §4.1. Full dartdoc, file header comment.
- [x] T10. `lib/features/providers/data/repositories/shared_prefs_agent_settings_repository.dart`
  — ADD: `SharedPrefsAgentSettingsRepository implements
  AgentSettingsRepository`, backed by `shared_preferences`, storing
  `AgentModel.id` under one string key; defaults to `AgentModel.sonnet`
  when unset. Full dartdoc, file header comment.
- [x] T11. `lib/features/providers/presentation/cubit/provider_settings_state.dart`
  — ADD: sealed `ProviderSettingsState`, `ProviderSettingsLoading`,
  `ProviderSettingsReady` (`selectedModel`, `status`, `statusMessage`),
  per design.md §4.2. Full dartdoc on the class and every field.
- [x] T12. `lib/features/providers/presentation/cubit/provider_settings_cubit.dart`
  — ADD: `ProviderSettingsCubit(AgentModelClient, AgentSettingsRepository)`
  with `load()`, `testConnection()`, `selectModel(AgentModel)`. `load()`
  reads the persisted model then immediately runs one connection test.
  `testConnection()`/`selectModel()` no-op while `status == checking`
  rather than racing two in-flight tests — this is the Cubit-level
  domain logic per project.md's Cubit-vs-repository split. A connection
  test sends a minimal `AgentRequest` (e.g. `prompt: 'ping'`) and maps
  the resulting event stream to `connected`/`disconnected` +
  `statusMessage`; an `AgentOverageDetectedEvent` on an otherwise
  successful test sets `status: connected` with that event's message as
  `statusMessage`. Per design.md §4.2. Full dartdoc, file header comment.
- [x] T13. `lib/features/providers/presentation/widgets/provider_connection_badge.dart`
  — ADD: `ProviderConnectionBadge` (takes a `ProviderConnectionStatus`),
  an icon+label pill matching `SyncStatusBadge`'s established visual
  pattern (`design_system/molecules/sync_status_badge.dart`) — see
  design.md §4.3 for why this stays feature-local instead of promoting
  to `design_system/molecules/`. Non-Material constraint applies. Full
  dartdoc, file header comment. **Design-gated** — see proposal.md's
  Design gate. Per design.md's Settings Screen Component Spec §4
  (`ProviderConnectionBadge` state table + §4.2 spinner spec) and §1.1–
  §1.2 (state enum + derived-tint tokens — see design-sync's Step 2 for
  the exact `AionColorsHubTokens` additions).
- [x] T14. `lib/features/providers/presentation/screens/settings_screen.dart`
  — ADD: `SettingsScreen`, per design.md §4.3's widget tree (header with
  back navigation, provider status card with `ProviderConnectionBadge` +
  "Test Connection" `AppButton`, `AppDropdown<AgentModel>` for model
  selection). Reads `ProviderSettingsCubit` via `BlocBuilder`. Non-Material
  constraint applies. Full dartdoc, file header comment. **Design-gated**
  — see proposal.md's Design gate. Per design.md's Settings Screen
  Component Spec §2 (`SettingsScreen` outer layout + header), §3
  (`ProviderStatusCard`), §5 (`TestConnectionButton`), §6
  (`ModelDropdown`), and §7 (status-message line).
- [x] T15. `lib/features/providers/providers.dart` — ADD: barrel file
  exporting every public symbol above, per project.md's "single barrel"
  convention. File header comment.