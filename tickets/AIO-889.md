---
ticketId: AIO-889
type: story
status: done
priority: none
parentId: 34f5d83d-805c-4bbe-9e27-6ebda07f31da
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.230479
updatedAt: 2026-09-02T21:17:20.230479
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation layer — widgets

- [x] **T13.** Modify `lib/features/tickets/presentation/widgets/ticket_metadata_section.dart`
  — render `ticket.estimateRollup`/`.timeSpentRollup` read-only
  alongside the existing estimate/time-spent `InlineEditableField`s
  when non-null, per design.md §3.1. Exact visual treatment from this
  change's `design.md` once the Claude Design export is pasted in —
  if `/apply` runs before that export exists, block on it per this
  project's design-gate rule rather than guessing a layout. Design
  reference: design.md §1 (`RollupIndicator`) and §0 (new tokens,
  `formatRollupMinutes`, `TicketRollupCounts` read model). **Added
  post-design-sync (amended 2026-08-11):** call `TicketsCubit.getRollupCounts`
  (T11) for the current ticket to back the "· N tickets" caption in
  design.md §1.1 — render the subline once that call resolves, not as
  a second late pop-in after the rest of the screen. Gate the subline
  itself on `ticket.estimateRollup`/`.timeSpentRollup != null` (already
  available synchronously, no need to wait on `getRollupCounts` for
  the gate — only the caption number waits).
- [x] **T14.** Modify `lib/features/tickets/presentation/screens/tickets_list_screen.dart`
  (`TicketListTile`) — add a compact, read-only rollup indicator shown
  only when `ticket.estimateRollup != null`, per design.md §3.2. Same
  design-gate dependency as T13. Design reference: design.md §2.4
  (`TicketListTile` integration) and §2 (`RollupBadge`). **No count
  caption** — per design.md §0.3 (amended 2026-08-11), `RollupBadge`
  reads `ticket.estimateRollup`/`.timeSpentRollup` directly and never
  calls `getRollupCounts` or constructs a `TicketRollupCounts`.
- [x] **T15.** Modify `lib/features/tickets/presentation/screens/tickets_board_view.dart`
  (`TicketBoardCard`) — same as T14, for the board card. Design
  reference: design.md §2.5 (`TicketBoardCard` integration) and §2
  (`RollupBadge`). **No count caption**, same reason as T14.