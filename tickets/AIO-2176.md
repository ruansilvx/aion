---
ticketId: AIO-2176
type: story
status: done
priority: none
parentId: 844bbf33-6b64-4c8c-804b-ecb50e358341
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.396743
updatedAt: 2026-09-02T21:17:20.396743
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Presentation — ticket detail screen

- [x] T11 — `lib/features/tickets/presentation/screens/ticket_detail_screen.dart`:
  add `TicketOverflowMenu` widget (feature-local) — `Overlay`/
  `LayerLink`/`CompositedTransformFollower`/`mounted`-guard mechanics
  matching `MoveToStatusMenu`; lists "Delete ticket" (danger-colored);
  selecting it calls `showAppConfirmDialog` then, if confirmed,
  `TicketsCubit.deleteTicket`. Wire the header's `⋮` trigger to this
  widget in place of the current static icon. Dartdoc on the new class.
  Per design.md §1 Overflow trigger (updated), §2 Overflow menu.
- [x] T12 — Same file: add a `BlocListener<TicketsCubit, TicketsState>`
  (combine with the existing `BlocBuilder` via `BlocConsumer` or nest a
  listener, whichever keeps the widget tree cleanest) that navigates to
  `/tickets` on `TicketDeleted`, and shows a count-aware `AppToast` on
  `TicketsError(reason: hasChildren)`. Per design.md §4 Blocked-by-
  children toast.