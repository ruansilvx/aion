---
ticketId: AIO-2651
type: story
status: done
priority: none
parentId: 097cbcf9-4910-46b7-acb8-673604e809d9
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.459835
updatedAt: 2026-09-02T21:17:20.459835
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Domain — new types

- [x] T1. New file `domain/enums/skill_attachment_kind.dart` —
      `SkillAttachmentKind` enum (`aionNativeTemplate | delegatedSkill`),
      dartdoc per design.md §1.1 explaining what each value causes to
      run and citing the 2026-08-19 spike that confirmed
      `delegatedSkill`'s mechanism.
- [x] T2. New file `domain/entities/skill_attachment.dart` —
      `SkillAttachment` entity (`Equatable`): `id`, `workflowStatusId`
      (`String?`), `sddStage` (`SddStage?`), `kind`
      (`SkillAttachmentKind`), `templateId` (`String?`), `skillName`
      (`String?`), `confidence` (`AutomationConfidence`). Const
      constructor, `props`, `copyWith`, full dartdoc per design.md §1.2
      stating both either/or invariants and that neither is enforced
      here.
- [x] T3. New file `domain/entities/workflow_prompt_template.dart` —
      `WorkflowPromptTemplate` entity (`Equatable`): `id`, `name`,
      `body`. Const constructor, `props`, `copyWith`, dartdoc per
      design.md §1.3 stating the project-wide name-uniqueness contract.
- [x] T4. New file `domain/utils/render_workflow_prompt_template.dart` —
      `renderWorkflowPromptTemplate(WorkflowPromptTemplate, Ticket)` pure
      function per design.md §1.4: substitutes `{{ticket.title}}`/
      `.description`/`.type`/`.status`/`.priority`; an unrecognized
      placeholder passes through unchanged. Full dartdoc listing every
      supported variable.
- [x] T5. New file
      `domain/repositories/workflow_skill_attachment_repository.dart` —
      `WorkflowSkillAttachmentRepository` interface exactly as
      design.md §1.5 specifies (`getAll`/`create`/`update`/`delete`/
      `onChanged`). Dartdoc explaining this is a dumb persistence layer
      — no validation, which lives in `WorkflowConfigCubit`.
- [x] T6. New file
      `domain/repositories/workflow_prompt_template_repository.dart` —
      `WorkflowPromptTemplateRepository` interface exactly as
      design.md §1.6 specifies (`getAll`/`create`/`update`/`delete`).
      Same dumb-persistence dartdoc convention as T5.