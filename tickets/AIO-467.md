---
ticketId: AIO-467
type: story
status: done
priority: none
parentId: 088b2ab7-494c-4459-a671-01ae0fb60e0a
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.181925
updatedAt: 2026-09-02T21:17:20.181925
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Screen wiring

- [x] 13. Modify `lib/features/tickets/presentation/screens/tickets_list_screen.dart`
      — add `_bulkChangeStatus(BuildContext, Set<String>, TicketStatus)`
      and `_bulkChangePriority(BuildContext, Set<String>, TicketPriority)`
      private handlers, calling `TicketsCubit.updateStatusForTickets`/
      `updatePriorityForTickets` directly (no confirmation dialog, unlike
      `_confirmAndTrashSelection`). (design.md §8)
- [x] 14. Modify `lib/features/tickets/presentation/screens/tickets_list_screen.dart`
      — wire `TicketSelectionBar`'s `onChangeStatus`/`onChangePriority`
      params to the two new handlers at its call site in `build()`.
      (design.md §8)
- [x] 15. Modify `lib/features/tickets/presentation/screens/tickets_list_screen.dart`
      — add two branches to the existing
      `BlocListener<TicketsCubit, TicketsState>`: `TicketsBatchStatusUpdated`
      (skipped-count-aware toast wording) and `TicketsBatchPriorityUpdated`,
      each calling `AppToast.show` then `TicketSelectionCubit.clear()`,
      mirroring the existing `TicketsBatchTrashed` branch. (design.md §8)
- [x] 16. Modify `lib/features/tickets/presentation/screens/tickets_list_screen.dart`
      — add `TicketsBatchStatusUpdating()`/`TicketsBatchPriorityUpdating()`
      to the loading-spinner state-switch arm, and
      `TicketsBatchStatusUpdated()`/`TicketsBatchPriorityUpdated()` to the
      `_TicketsBody`-rendering arm. Add matching `case` arms to
      `_currentTickets`/`_hasMore` (and any other exhaustive `switch` over
      `TicketsState` in this file). (design.md §6)