---
ticketId: AIO-1913
type: story
status: done
priority: none
parentId: 48acc8f2-b7f2-4931-88c4-3fe463cfdd30
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.370003
updatedAt: 2026-09-02T21:17:20.370003
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# `TicketsCubit`

- [x] 6. `lib/features/tickets/presentation/cubit/tickets_cubit.dart` —
      add `_mostRecentVerifyChat(String storyOrEpicId)` (factored lookup,
      mirrors `_mostRecentExecutionChat`) and `_verifyGateApproved(String
      storyOrEpicId)` built on it, per design.md §4.1. Doc-comment both.
      In the same pass, fix `_designSyncApproved`'s hardcoded
      `"Design Sync — "` chat-title prefix to resolve through
      `_stagePresentName(SddStage.designSync)` instead, per design.md
      §4.1 — a same-signature, same-caller-set change (the method is
      already `async`).
- [x] 7. Same file — split `_sddStageAdvanceCheck`'s shared
      `exploring`/`verifying` switch case: `verifying` becomes its own
      branch computing `verifyGateApproved` (short-circuited to `false`
      when there's no reply yet) alongside
      `mostRecentChatHasTerminalReply`, passed into
      `TransitionEvalContext` per design.md §4.2.
- [x] 8. Same file — extend `_assembleStageContext`'s `verifying`-stage
      request text: end the reply with `VERIFY GATE:
      APPROVED`/`PENDING`, and on `PENDING`, optionally a fenced `##
      Fixes Needed` block (`"- Task: <title>"`/`"- Bug: <title>"` lines,
      optional `" (blockedBy: <title>)"` suffix) per design.md §4.3.
      `archived`'s request text is unchanged.
- [x] 9. Same file — add `_materializeVerifyFixes(Ticket parent, String
      reply)`, reusing `_materializeDecomposition`'s line-parsing logic
      parameterized by block header/child-type keywords, per design.md
      §4.4. Wire it into `_runStageChatTurn`'s `verifying`-stage
      completion path, called when the reply contains `VERIFY GATE:
      PENDING` (mirrors the existing `proposed`-stage
      `_materializeDecomposition` call).
- [x] 10. Same file — add `retryVerify(Ticket verifyingChat)`, mirroring
      `retryDesignSync` exactly per design.md §4.5.
- [x] 11. Same file — add `_inFlightVerifyRetryIds` (`Set<String>`,
      mirrors `_inFlightStageAdvanceIds`'s purpose, scoped separately)
      and `_maybeRetryPendingVerify(Ticket task)` per design.md §4.6
      (steps 1-6). Call it, `unawaited`, from `updateTicketStatus`'s
      post-write side effects (alongside the existing
      `_maybeAutoLinkToSpec` call, guarded the same
      `updated.type.isExecutable && _roleOf(status) ==
      WorkflowStatusRole.done` way) and from `changeTicketStatus`'s
      equivalent status-write path.
- [x] 12. Same file — extend `TicketsCubit.getTicketById`'s existing
      `canAdvanceSddStage`/`sddStageBlockReason` computation to also
      derive `verifyRetryReady`/`verifyRetryConfidence` (reusing task
      11's steps 1-5 as a pure read, plus a
      `AutomationContext.verifyGateRetry` confidence read) per design.md
      §5.3, populating the two new `TicketDetailLoaded` fields from task
      13.