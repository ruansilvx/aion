---
ticketId: AIO-2135
type: task
status: done
priority: none
parentId: 590d61d7-c408-4a34-b582-70fc8dbe7741
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.391110
updatedAt: 2026-09-02T21:17:20.391110
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# T18. Create `test/features/tickets/cubit/tickets_cubit_ticket_crud_test.dart`

— a new, self-contained test file (own Mock classes/fixtures/
`registerFallbackValue` calls) rather than appending to the already
12,000+-line `tickets_cubit_test.dart`, mirroring that file's own
precedent of splitting a cohesive `TicketsCubit` coverage slice into
its own file (`tickets_cubit_codebase_analysis_test.dart`,
`tickets_cubit_workflow_status_test.dart`). Covers: `handleChatToolCall`
dispatching each of the five tool names to the correct handler; all
three `AutomationConfidence` branches for `_handleCreateTicketToolCall`
and `_handleAddLinkToolCall`; `_handleAddLinkToolCall`'s
target-not-found decline path; `_handleLogTimeToolCall`'s
unconditional-apply behavior (no confidence lookup at all);
`confirmPendingToolProposal`/`rejectPendingToolProposal` round trip
for `CreateTicketProposal`/`AddLinkProposal` (`bloc_test`). Also
edited `test/features/tickets/cubit/tickets_cubit_test.dart`'s
existing `_runCodingExecution resolves ModelPhase.execution` test
(per-phase-tier-based-model-routing) to assert `_toolsFor` now
returns the branch/close-branch tool plus all three new tools,
merged into that test's existing `agentClient.run` capture (a
second separate `verify()` on the same invocations found nothing
left to match, since mocktail's `verify()` only matches calls an
earlier `verify()` in the same test hasn't already consumed).