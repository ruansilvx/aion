---
ticketId: AIO-374
type: story
status: done
priority: none
parentId: aec5e777-0cd8-45ff-b1d3-ffff29993d7f
estimate: null
timeSpent: null
createdAt: 2026-09-02T21:17:20.170179
updatedAt: 2026-09-02T21:17:20.170179
deletedAt: null
estimateRollup: null
timeSpentRollup: null
---
# Widget layer — chat "Waiting for reply…" indicator

- [x] T17. Same file (`ticket_detail_screen.dart`) — in the
      `ticket.type == TicketType.chat` branch, add a "Waiting for
      reply…" row shown while this ticket's own `TicketDetailLoaded
      .isAdvancingStage` is `true` (design.md §3.3), per Component Spec
      §4 "WaitingForReplyIndicator" — reuse the Chat Transcript spec
      §2.3a pulsing-dot treatment verbatim (`7×7` `primary` circle,
      opacity 0.35↔1.0 + scale 0.8↔1.0 over 1100ms ease-in-out, static
      at full opacity under reduced motion), `AionText.streamStatus`
      `textSecondary` fixed text "Waiting for reply…" (no `{tool}`
      emphasis, unlike a real live-tool-use line), placed at the tail of
      the comment thread where the next reply bubble would render, no
      bubble frame/avatar/author label. Add a
      `BlocListener<TicketsCubit, TicketsState>` case that calls
      `context.read<ChatCubit>().loadMessages(ticket.id)` when this
      ticket's `isAdvancingStage` transitions `true` → `false`, so the
      real reply replaces the indicator once it lands. Update the
      relevant section of the screen's class dartdoc.