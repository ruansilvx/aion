# Architecture conventions

Detected stack: Flutter/Dart.

Suggested build/verification command: `flutter analyze` (after `flutter pub get`).

Add your own conventions, formatting rules, and architectural constraints below.